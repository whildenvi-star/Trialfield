---
phase: 26-portal-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/app/(protected)/dashboard/page.tsx
  - glomalin-portal/src/app/(protected)/app/[module]/page.tsx
autonomous: true
requirements: [UI-02, UI-03, UI-04]

must_haves:
  truths:
    - "Authenticated user visits /dashboard and sees module cards in a responsive grid"
    - "Cards for modules the user has access to show module name, sublabel, and 'Coming Soon' status and link to the module page"
    - "Cards for modules the user lacks access to are grayed out with a lock icon and are not clickable"
    - "User with access visits /app/macro-rollup (or any module route) and sees a shell page with the module name and coming soon placeholder"
    - "Admin visits /admin and sees the existing user table with role dropdowns and module toggle switches that persist changes (already built in Phase 25)"
  artifacts:
    - path: "glomalin-portal/src/app/(protected)/dashboard/page.tsx"
      provides: "Dashboard page with access-aware module cards"
      min_lines: 40
    - path: "glomalin-portal/src/app/(protected)/app/[module]/page.tsx"
      provides: "Dynamic module shell page with coming soon placeholder"
      min_lines: 20
  key_links:
    - from: "glomalin-portal/src/app/(protected)/dashboard/page.tsx"
      to: "Supabase module_access table"
      via: "server-side Supabase query"
      pattern: "from\\('module_access'\\)"
    - from: "glomalin-portal/src/app/(protected)/dashboard/page.tsx"
      to: "glomalin-portal/src/lib/modules.ts"
      via: "MODULES import for card rendering"
      pattern: "import.*MODULES.*from.*modules"
    - from: "glomalin-portal/src/app/(protected)/app/[module]/page.tsx"
      to: "glomalin-portal/src/lib/modules.ts"
      via: "MODULES import for label lookup"
      pattern: "import.*MODULES.*from.*modules"
---

<objective>
Build the dashboard with access-aware module cards and create dynamic module shell pages. The dashboard reads the user's module_access grants from Supabase and renders a responsive card grid where accessible modules link through and inaccessible modules are visually locked. Module shell pages at /app/[module] show a "coming soon" placeholder. UI-03 (admin panel) is already complete from Phase 25 and requires no additional work.

Purpose: Users see the full module ecosystem on their dashboard, understand what they can and cannot access, and can navigate to available modules. Module shells confirm routing works end-to-end.
Output: Dashboard with module cards, 5 module shell pages
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-portal-ui/26-CONTEXT.md
@glomalin-portal/src/app/(protected)/dashboard/page.tsx
@glomalin-portal/src/app/(protected)/layout.tsx
@glomalin-portal/src/app/(protected)/admin/page.tsx
@glomalin-portal/src/lib/modules.ts
@glomalin-portal/src/lib/supabase/server.ts
@glomalin-portal/src/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dashboard page with access-aware module cards</name>
  <files>
    glomalin-portal/src/app/(protected)/dashboard/page.tsx
  </files>
  <action>
    Rewrite `src/app/(protected)/dashboard/page.tsx` as a **server component** that:

    1. **Fetches user data from Supabase (server-side):**
       - Import `createClient` from `@/lib/supabase/server`
       - Get the authenticated user via `supabase.auth.getUser()`
       - Query `module_access` table: `supabase.from('module_access').select('module, granted').eq('user_id', user.id)`
       - Build a `Set<string>` of granted module IDs for O(1) lookup

    2. **Renders a responsive card grid:**
       - Page title: "Dashboard" (text-2xl, font-bold, font-mono, text-soil-text, tracking-wide)
       - Subtitle: "Your Modules" or "Farm Modules" (text-soil-muted, font-mono, text-sm, mt-2, mb-6)
       - Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4`
       - Map over `MODULES` array from `@/lib/modules` to render cards

    3. **Card design for ACCESSIBLE modules (granted = true):**
       - Wrapped in Next.js `<Link href={module.route}>` so click navigates to module page
       - Card container: `bg-soil-surface border border-soil-border rounded-lg p-6 hover:border-soil-accent transition-colors cursor-pointer group`
       - Module label: `text-lg font-bold font-mono text-soil-text group-hover:text-soil-accent transition-colors`
       - Sublabel: `text-sm text-soil-muted font-mono mt-1`
       - Status indicator: `text-xs font-mono text-soil-green mt-3 uppercase tracking-wider` — text "Coming Soon" (all modules are placeholders for now)
       - Subtle right-arrow icon (SVG, w-4 h-4, text-soil-muted, group-hover:text-soil-accent) positioned at top-right of the card

    4. **Card design for INACCESSIBLE modules (granted = false or not in module_access):**
       - Wrapped in a plain `<div>` (NOT a link — not clickable)
       - Card container: `bg-soil-surface border border-soil-border rounded-lg p-6 opacity-40 cursor-not-allowed`
       - Module label: `text-lg font-bold font-mono text-soil-text`
       - Sublabel: `text-sm text-soil-muted font-mono mt-1`
       - Lock icon: small SVG padlock icon (w-4 h-4, text-soil-muted) positioned at top-right of card
       - Status: `text-xs font-mono text-soil-muted mt-3 uppercase tracking-wider` — text "No Access"

    5. **Lock icon SVG** (inline, no external dependency):
       ```
       <svg className="w-4 h-4 text-soil-muted" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
         <rect x="3" y="11" width="18" height="11" rx="2" ry="2" />
         <path d="M7 11V7a5 5 0 0110 0v4" />
       </svg>
       ```

    6. **Arrow icon SVG** (for accessible cards):
       ```
       <svg className="w-4 h-4 text-soil-muted group-hover:text-soil-accent transition-colors" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
         <path strokeLinecap="round" strokeLinejoin="round" d="M9 5l7 7-7 7" />
       </svg>
       ```

    **Important:** This is a server component (no 'use client'). Data fetching happens at render time. The protected layout already handles auth checks and provides the Header. If `getUser()` returns no user, redirect to /login (defensive — middleware should catch this).
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx next build 2>&1 | grep -E "(dashboard|Error|error)" | head -10</automated>
    <manual>Log in and visit /dashboard — see 5 module cards. Cards for granted modules are clickable with hover effect. Cards for denied modules are grayed out with lock icon.</manual>
  </verify>
  <done>Dashboard renders 5 module cards in a responsive grid. Accessible modules show as clickable cards with hover effects and arrow icon. Inaccessible modules show as grayed-out cards with lock icon and "No Access" label. Data fetched server-side from Supabase module_access table.</done>
</task>

<task type="auto">
  <name>Task 2: Create dynamic module shell pages</name>
  <files>
    glomalin-portal/src/app/(protected)/app/[module]/page.tsx
  </files>
  <action>
    1. Create the directory structure: `src/app/(protected)/app/[module]/page.tsx`

       This uses Next.js dynamic route segments. The `[module]` param will be the module ID (e.g., "macro-rollup", "farm-registry", etc.).

    2. **Module shell page implementation:**
       - Server component (no 'use client')
       - Import `MODULES` from `@/lib/modules`
       - Extract `params.module` from the page props
       - Look up the module in MODULES array by matching `module.id === params.module` or by extracting the slug from `module.route`
       - If no matching module found, import `notFound` from `next/navigation` and call `notFound()` — this returns a 404 for invalid module slugs

    3. **Shell page design:**
       - Container: `flex flex-col items-center justify-center min-h-[60vh]`
       - Module icon placeholder: a simple hexagon or circuit-board SVG (w-16 h-16, text-soil-border) centered above the text. Use a generic placeholder icon:
         ```
         <svg className="w-16 h-16 text-soil-border mb-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1}>
           <path strokeLinecap="round" strokeLinejoin="round" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
         </svg>
         ```
       - Module label: `text-3xl font-bold font-mono text-soil-text tracking-wide`
       - Sublabel: `text-lg text-soil-muted font-mono mt-2`
       - Divider: `w-16 h-px bg-soil-border my-6`
       - "Coming Soon" badge: `text-sm font-mono text-soil-accent uppercase tracking-widest border border-soil-accent/30 rounded-full px-4 py-1.5`
       - Back link: `mt-8` — `<Link href="/dashboard">` styled as `text-sm font-mono text-soil-muted hover:text-soil-accent transition-colors` with text "Back to Dashboard"

    4. **Verify the module slug extraction works correctly.** The MODULES array uses route format `/app/macro-rollup`. The dynamic param will be `macro-rollup`. Match by comparing `params.module` against each module's `id` field (they already match: `macro-rollup`, `farm-registry`, `org-cert`, `inputs-seeds`, `fsa-reporting`).

    5. **Verify routing:** The middleware already handles module access checks at `/app/*` routes using `getModuleId(pathname)` which extracts `parts[2]` from the URL. The `(protected)` route group doesn't affect the URL path. Verify the file at `src/app/(protected)/app/[module]/page.tsx` maps to URL `/app/{module}` — the `(protected)` group is invisible in the URL.

    6. **Run build** to confirm all module routes resolve:
       ```
       cd glomalin-portal && npx next build 2>&1 | tail -20
       ```
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx next build 2>&1 | grep -E "(app/|Error|error)" | head -10</automated>
    <manual>Navigate to /app/macro-rollup (with access) — see "Macro Rollup" title, "Whole-farm P&L" sublabel, "Coming Soon" badge, and "Back to Dashboard" link. Try /app/invalid-module — get 404.</manual>
  </verify>
  <done>Dynamic module shell page exists at /app/[module]. All 5 valid module slugs render a centered shell with module name, sublabel, hexagon icon, "Coming Soon" badge, and back link. Invalid slugs return 404. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `cd glomalin-portal && npx next build` completes without errors
2. Dashboard (/dashboard) renders 5 module cards in responsive grid
3. Cards for granted modules are clickable links with hover effects
4. Cards for denied modules are grayed out (opacity-40) with lock icon, not clickable
5. Each of the 5 module routes (/app/macro-rollup, /app/farm-registry, /app/org-cert, /app/inputs-seeds, /app/fsa-reporting) renders a shell page
6. Invalid module slugs return 404
7. Admin panel (/admin) continues to function as built in Phase 25 — user table with role dropdowns and module toggles (UI-03 verified by existing implementation, no changes needed)
</verification>

<success_criteria>
- Authenticated user sees 5 module cards on /dashboard
- Accessible modules have clickable cards, inaccessible modules are grayed with lock icon
- All 5 module shell pages render with module name and "Coming Soon"
- Admin panel at /admin still functions (Phase 25 implementation unchanged)
- No build errors
</success_criteria>

<output>
After completion, create `.planning/phases/26-portal-ui/26-02-SUMMARY.md`
</output>
