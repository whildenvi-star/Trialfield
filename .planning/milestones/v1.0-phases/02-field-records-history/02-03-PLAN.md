---
phase: 02-field-records-history
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - organic-cert/src/app/(app)/fields/[id]/history/page.tsx
  - organic-cert/src/app/api/field-enterprises/[id]/operations/route.ts
  - organic-cert/src/app/api/field-enterprises/[id]/applications/route.ts
  - organic-cert/src/app/api/field-enterprises/[id]/harvest/route.ts
autonomous: false
requirements:
  - FIELD-05
  - FIELD-06

must_haves:
  truths:
    - "Farm manager can open a manual entry form from the timeline and create a tillage record"
    - "Farm manager can open a manual entry form from the timeline and create an application record with product, rate, and rate unit"
    - "Farm manager can open a manual entry form from the timeline and create a harvest record with auto-generated lot number"
    - "Manual entry forms pre-fill date to today and remember last-used field"
    - "After save, form clears (keeping field and date), shows success toast, and offers Add Another"
    - "Multiple products per application are supported by adding multiple MaterialUsage rows"
    - "Harvest form includes searchable equipment selector with inline Add Equipment option"
    - "Edit button on expanded timeline card opens the form pre-filled with that record's data"
  artifacts:
    - path: "organic-cert/src/app/(app)/fields/[id]/history/page.tsx"
      provides: "Sheet-based manual entry forms wired into timeline"
      contains: "Sheet"
    - path: "organic-cert/src/app/api/field-enterprises/[id]/operations/route.ts"
      provides: "Manual operation creation and update with dataSource MANUAL"
      exports: ["GET", "POST", "PUT"]
    - path: "organic-cert/src/app/api/field-enterprises/[id]/applications/route.ts"
      provides: "Manual application creation and update with dataSource MANUAL"
      exports: ["GET", "POST", "PUT"]
    - path: "organic-cert/src/app/api/field-enterprises/[id]/harvest/route.ts"
      provides: "Harvest creation and update with dataSource MANUAL"
      exports: ["GET", "POST", "PUT"]
  key_links:
    - from: "organic-cert/src/app/(app)/fields/[id]/history/page.tsx"
      to: "/api/field-enterprises/[id]/operations"
      via: "fetch POST/PUT from tillage form"
      pattern: "fetch.*field-enterprises.*operations"
    - from: "organic-cert/src/app/(app)/fields/[id]/history/page.tsx"
      to: "/api/field-enterprises/[id]/harvest"
      via: "fetch POST/PUT from harvest form"
      pattern: "fetch.*field-enterprises.*harvest"
    - from: "organic-cert/src/app/(app)/fields/[id]/history/page.tsx"
      to: "/api/field-enterprises/[id]/applications"
      via: "fetch POST/PUT from application form"
      pattern: "fetch.*field-enterprises.*applications"
---

<objective>
Add manual entry forms for all three operation types (tillage, application, harvest) as Sheet slide-over panels within the field history timeline page. Wire the "Add Records" buttons, "Edit" buttons, smart defaults, batch entry workflow, equipment selector, and lot number display. Update existing operation/application API routes to set dataSource MANUAL explicitly and add PUT handlers for edit mode.

Purpose: FIELD-05 (manual entry) and FIELD-06 (lot number auto-generation) require farm managers to enter records for pre-API historical data or non-synced operations. The forms use Sheet panels to maintain timeline context during data entry. Harvest form auto-creates CropLots via the API (built in Plan 01).
Output: 3 form panels integrated into history page, 3 API routes updated with dataSource and PUT handlers.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-field-records-history/02-CONTEXT.md
@.planning/phases/02-field-records-history/02-RESEARCH.md
@.planning/phases/02-field-records-history/02-01-SUMMARY.md
@.planning/phases/02-field-records-history/02-02-SUMMARY.md

@organic-cert/src/app/(app)/fields/[id]/history/page.tsx
@organic-cert/src/app/(app)/field-enterprises/[id]/page.tsx
@organic-cert/src/app/api/field-enterprises/[id]/operations/route.ts
@organic-cert/src/app/api/field-enterprises/[id]/applications/route.ts
@organic-cert/src/app/api/field-enterprises/[id]/harvest/route.ts
@organic-cert/src/components/ui/sheet.tsx
@organic-cert/src/components/ui/command.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manual entry Sheet forms to the history timeline page</name>
  <files>organic-cert/src/app/(app)/fields/[id]/history/page.tsx</files>
  <action>
Add three Sheet (slide-over panel) forms to the history timeline page for manual record entry. Use separate forms per operation type (per research recommendation -- field counts differ: tillage ~3 fields vs harvest 8+ fields).

**Sheet trigger points:**
- The "Add records" button on empty season cards (from Plan 02) opens a type selector, then the appropriate form
- A floating "+ Add Record" button at the top of the timeline (next to the filter bar) opens a type selector dialog, then the form
- The "Edit" button on expanded operation cards opens the appropriate form pre-filled with that record's data

**Type selector:**
- Small dialog or popover with 3 options: Tillage, Application, Harvest
- Each option has its badge color and icon (matching timeline badges)
- Selecting one opens the corresponding Sheet form
- When triggered from a season card, pre-fill the crop year context

**All forms share these patterns (per user decisions):**
- Field is pre-determined (we are on `/fields/[id]/history`, so the field is known)
- Enterprise selector: since operations belong to FieldEnterprise (not Field directly), the form needs to select which enterprise (year) to add the record to. Show a select with available enterprises for this field. If triggered from a season card, pre-select that year's enterprise. If no enterprise exists for the target year, show a message: "No crop year record exists for {year}. Create one first." with a link to `/field-enterprises`.
- Date input: default to today (`format(new Date(), "yyyy-MM-dd")` from date-fns). Persist last-used date in `localStorage` key `lastEntryDate`.
- After successful save: `toast.success("Record saved")` via sonner, clear form (but keep the selected enterprise and date per user decision), show "Add Another" button that keeps the Sheet open
- Validation: all required fields checked before submit. Show inline error messages on empty required fields.

**Tillage form (Sheet):**
- Title: "Add Tillage Operation"
- Fields: Operation type select (from FieldOpType enum: TILLAGE, CULTIVATION, or free-text input for other types), Date (date input), Notes (optional textarea)
- POST to `/api/field-enterprises/{enterpriseId}/operations` with body: `{ type: selectedType, operationDate: date, notes, dataSource: "MANUAL" }`
- Minimal form -- 3 fields as per user decision ("Operation type only plus date and field. No depth or additional details required")

**Application form (Sheet):**
- Title: "Add Input Application"
- Fields: Product selector (searchable -- use cmdk Command component to search materials from `/api/materials`), Application rate (number input), Rate unit selector (lbs/acre, gal/acre, oz/acre, units/acre), Application date (date input), Acres treated (number, default to enterprise acres), Notes (optional textarea)
- Support multiple products per application: "Add Product" button adds another product row (product + rate + unit). Each product row creates a separate `MaterialUsage` record with the same applicationDate.
- POST to `/api/field-enterprises/{enterpriseId}/applications` for each product row with body: `{ materialId, applicationRate, rateUnit, applicationDate, areaApplied, notes }`
- Use cmdk for product search since the materials list can be large. Fetch materials once on Sheet open and cache.
- "Suggest from recent" section: show last 3 products used in applications for this field (query from the loaded history data, not a separate API call)

**Harvest form (Sheet):**
- Title: "Add Harvest Record"
- Fields: Harvest date (date input), Yield per acre (number), Yield unit (select: bu, lbs, tons), Acres harvested (number, default enterprise acres), Equipment selector (searchable cmdk from `/api/equipment`), Net weight (optional, computed or manual), Notes (optional textarea)
- Equipment selector: cmdk Command component searching equipment from `/api/equipment`. Include an "Add New Equipment" button at the bottom of the cmdk list that opens a nested Dialog with: equipment name (required), equipment type (required, from EquipmentType enum select), then POSTs to `/api/equipment` and refreshes the equipment list.
- Lot number: display a read-only computed lot number preview below the form showing what the auto-generated lot number will be (based on enterprise cropYear, crop, field name). Include a "Lot number will be auto-generated" helper text. If a manual override is desired (per user decision), add a small "Override" link that reveals an editable text input for custom lot number.
- POST to `/api/field-enterprises/{enterpriseId}/harvest` with body: `{ harvestDate, yieldPerAcre, yieldUnit, acresHarvested, equipmentId, netWeight, notes }`. The API (updated in Plan 01) auto-creates the CropLot.
- Display the returned CropLot lot number in the success toast: `toast.success("Harvest saved - Lot: {lotNumber}")`

**Edit mode:**
- When "Edit" button is clicked on an expanded timeline card, open the appropriate Sheet form with all fields pre-filled from the existing record data
- Change the Sheet title to "Edit {Type} Operation"
- Change the save button text to "Update"
- On save, use PUT to the same API endpoint with the record ID: e.g., `PUT /api/field-enterprises/{enterpriseId}/operations` with `{ id: recordId, ...updatedFields }`
- After save, refresh the timeline data by re-fetching from the history API

**State management:**
- `useState` for: activeSheet ("tillage" | "application" | "harvest" | null), editingRecord (the record being edited, or null for new), selectedEnterpriseId
- Sheet open/close controlled by `activeSheet !== null`
- On Sheet close, reset editingRecord to null
- After any save, re-fetch the history data to show the new/updated record in the timeline

**Styling:**
- Sheet slides in from the right (shadcn Sheet with `side="right"`)
- Sheet width: `sm:max-w-lg` for tillage, `sm:max-w-xl` for application and harvest (more fields need more space)
- Use shadcn Label, Input, Textarea, Select, Button components inside the form
- Form sections separated with `Separator` component
- Submit button at bottom: brand green background, full width
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Open /fields/[id]/history. Click "+ Add Record" and verify: (1) type selector appears, (2) selecting Tillage opens Sheet with date/type/notes, (3) selecting Application opens Sheet with product search, rate, multi-product support, (4) selecting Harvest opens Sheet with equipment search, lot number preview, (5) save shows toast and clears form, (6) "Add Another" keeps Sheet open, (7) Edit button on expanded card opens pre-filled form</manual>
  </verify>
  <done>Three Sheet forms (tillage, application, harvest) open from the timeline. Forms have smart defaults, validation, batch entry support. Harvest shows lot number preview and equipment search with inline add. Application supports multiple products. Edit mode pre-fills forms from existing records. After save, timeline refreshes to show new data.</done>
</task>

<task type="auto">
  <name>Task 2: Update operation, application, and harvest API routes with dataSource MANUAL and PUT handlers</name>
  <files>
    organic-cert/src/app/api/field-enterprises/[id]/operations/route.ts
    organic-cert/src/app/api/field-enterprises/[id]/applications/route.ts
    organic-cert/src/app/api/field-enterprises/[id]/harvest/route.ts
  </files>
  <action>
Update the existing POST handlers and add PUT handlers to the operations, applications, and harvest API routes. The PUT handlers are required by the edit mode Sheet forms (Task 1), which issue PUT requests to update existing records.

**1. Operations route (`/api/field-enterprises/[id]/operations/route.ts`):**
- In the existing POST handler, find the `prisma.fieldOperation.create` call and add `dataSource: "MANUAL"` to the `data` object. This is technically redundant (the schema default is MANUAL) but makes intent explicit and consistent with the staged-ops approval route (which sets SYNCED).
- Add a new exported `PUT` handler that:
  - Accepts `{ id: string, ...updatedFields }` in the request body
  - Uses `prisma.fieldOperation.update({ where: { id }, data: { ...updatedFields } })` to update the record
  - Validates that the record belongs to the field enterprise in the URL (tenant isolation): query the record first and verify `fieldEnterpriseId === params.id`
  - Returns the updated record as JSON with 200 status
  - Returns 404 if record not found, 403 if record does not belong to this enterprise
  - Calls `logAudit()` after update

**2. Applications route (`/api/field-enterprises/[id]/applications/route.ts`):**
- In the existing POST handler, if it creates a FieldOperation (e.g., of type SPRAYING), add `dataSource: "MANUAL"` to that create call. If it only creates MaterialUsage rows, no dataSource change needed on POST (MaterialUsage does not have a dataSource field).
- Add a new exported `PUT` handler that:
  - Accepts `{ id: string, ...updatedFields }` in the request body
  - Updates the MaterialUsage record: `prisma.materialUsage.update({ where: { id }, data: { ...updatedFields } })`
  - Validates that the record belongs to the field enterprise in the URL
  - Returns the updated record as JSON with 200 status
  - Returns 404 if record not found, 403 if wrong enterprise
  - Calls `logAudit()` after update

**3. Harvest route (`/api/field-enterprises/[id]/harvest/route.ts`):**
- The POST handler was already updated in Plan 01 with `dataSource: "MANUAL"` and CropLot auto-creation. No POST changes needed.
- Add a new exported `PUT` handler that:
  - Accepts `{ id: string, ...updatedFields }` in the request body
  - Updates the HarvestEvent record: `prisma.harvestEvent.update({ where: { id }, data: { ...updatedFields } })`
  - Validates that the record belongs to the field enterprise in the URL
  - Does NOT re-create CropLot on update (the lot was created on initial harvest POST; editing yield/date should not generate a new lot)
  - Returns the updated record as JSON with 200 status
  - Returns 404 if record not found, 403 if wrong enterprise
  - Calls `logAudit()` after update

**All PUT handlers follow the same pattern:**
- Use `auth()` from `@/lib/auth` for session check (matching existing POST pattern)
- Use the session's `farmId` for tenant isolation
- Strip `id` from the update data to avoid Prisma errors: `const { id: recordId, ...updateData } = body`
- Wrap in try/catch with appropriate error responses

Also verify that all three routes continue to call `logAudit()` after creates and updates (existing pattern from Phase 1).
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Read all three route files and confirm: (1) dataSource: "MANUAL" is set on all FieldOperation.create calls, (2) PUT handler exists in each route with tenant isolation, (3) logAudit() is called after both create and update operations</manual>
  </verify>
  <done>Operations route sets dataSource MANUAL on creates and has PUT handler. Applications route updated with PUT handler. Harvest route has PUT handler (no CropLot re-creation on update). All routes have tenant isolation and audit logging on both POST and PUT. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete field records workflow end-to-end</name>
  <what-built>Complete field records and history system including field index with activity stats, 3-year timeline with season grouping and color-coded operation cards, manual entry forms for tillage/application/harvest, lot number auto-generation, equipment search with inline add, batch entry workflow, edit mode with PUT handlers, filter bar, and print support.</what-built>
  <how-to-verify>
1. Start the dev server: `cd organic-cert && npm run dev`
2. Navigate to `/fields` -- verify fields show name, acres, last activity date, record count. Test search and sort.
3. Click a field to open `/fields/[id]/history` -- verify 3 seasons shown with operations in chronological order within each season.
4. Check empty seasons show "No operations recorded" with "Add records" button.
5. Expand an operation card -- verify full details shown with source indicator (sync/pencil icon).
6. Click "+ Add Record" -- verify type selector appears (Tillage, Application, Harvest).
7. Add a tillage record: select tillage type, set date, save. Verify toast appears and record shows in timeline.
8. Add an application: search for a product, set rate, add a second product row, save. Verify both MaterialUsage rows appear.
9. Add a harvest: set yield, select equipment (try "Add New Equipment" inline), save. Verify lot number appears in success toast and harvest shows in timeline with lot number.
10. Click "Edit" on an expanded card -- verify form opens pre-filled. Make a change and save. Verify record updates in timeline.
11. Use the filter bar to filter by operation type, date range, product name, and data source.
12. Click "Print" -- verify clean printable output without sidebar/header.
13. Shift the year selector -- verify timeline shows different 3-year windows.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Field index navigates to history timeline
3. Timeline shows 3 seasons with all operation types
4. Manual entry works for all 3 types with smart defaults
5. Harvest auto-generates lot number via CropLot creation
6. Equipment selector includes inline add
7. Batch entry ("Add Another") works
8. Edit mode pre-fills and updates records via PUT endpoints
9. Filter bar filters correctly including product name
10. Print produces clean output
</verification>

<success_criteria>
1. Farm manager can manually enter tillage, application, and harvest records from the timeline
2. Forms have smart defaults (today's date, last-used field) and batch entry support
3. Harvest records auto-generate lot numbers in cropYear-crop-fieldName format
4. Equipment selector supports searching and adding new equipment inline
5. Edit button on expanded cards opens pre-filled forms that update via PUT
6. Multiple products per application supported via multiple MaterialUsage rows
7. All manually created records have dataSource MANUAL
8. PUT handlers exist for operations, applications, and harvest routes with tenant isolation
9. Human verification confirms the complete workflow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/02-field-records-history/02-03-SUMMARY.md`
</output>
