---
phase: 03-inspection-report-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - organic-cert/src/lib/pdf/inspection-report.tsx
  - organic-cert/src/lib/pdf/sections/cover-page.tsx
  - organic-cert/src/lib/pdf/sections/toc-page.tsx
  - organic-cert/src/lib/pdf/sections/operation-overview.tsx
  - organic-cert/src/lib/pdf/sections/field-list.tsx
  - organic-cert/src/lib/pdf/sections/field-history.tsx
  - organic-cert/src/lib/pdf/sections/application-log.tsx
  - organic-cert/src/lib/pdf/sections/harvest-log.tsx
  - organic-cert/src/lib/pdf/sections/mass-balance.tsx
autonomous: true
requirements:
  - RPT-01
  - RPT-02
  - RPT-03
  - RPT-04

must_haves:
  truths:
    - "InspectionReport Document component renders all 8 sections in NOP order"
    - "Cover page shows farm name, operator, address, cert number, crop year, generation date, and placeholder for inspector name"
    - "Table of contents lists all section names as a static section guide"
    - "Field history section renders in landscape orientation with full operation detail per 3-year period"
    - "Application log shows material name, NOP status, rate, field, date, and applicator for every application"
    - "Harvest log shows crop, field, lot number, yield, and equipment for every harvest"
    - "Mass balance groups by crop then by lot showing harvested vs sold side-by-side"
  artifacts:
    - path: "organic-cert/src/lib/pdf/inspection-report.tsx"
      provides: "Top-level InspectionReport Document component"
      exports: ["InspectionReport"]
    - path: "organic-cert/src/lib/pdf/sections/cover-page.tsx"
      provides: "Cover page with farm info, logo, and inspector placeholder"
      exports: ["CoverPage"]
    - path: "organic-cert/src/lib/pdf/sections/toc-page.tsx"
      provides: "Table of contents as static section listing"
      exports: ["TocPage"]
    - path: "organic-cert/src/lib/pdf/sections/operation-overview.tsx"
      provides: "Farm operation summary stats"
      exports: ["OperationOverview"]
    - path: "organic-cert/src/lib/pdf/sections/field-list.tsx"
      provides: "All fields with acres and organic status"
      exports: ["FieldList"]
    - path: "organic-cert/src/lib/pdf/sections/field-history.tsx"
      provides: "3-year per-field operation history table in landscape"
      exports: ["FieldHistory"]
    - path: "organic-cert/src/lib/pdf/sections/application-log.tsx"
      provides: "Input application records table in landscape"
      exports: ["ApplicationLog"]
    - path: "organic-cert/src/lib/pdf/sections/harvest-log.tsx"
      provides: "Harvest records with lot numbers table"
      exports: ["HarvestLog"]
    - path: "organic-cert/src/lib/pdf/sections/mass-balance.tsx"
      provides: "Mass balance by crop and lot"
      exports: ["MassBalance"]
  key_links:
    - from: "organic-cert/src/lib/pdf/inspection-report.tsx"
      to: "all section components"
      via: "imports and renders each section with ReportData"
      pattern: "import.*sections/"
    - from: "organic-cert/src/lib/pdf/sections/*.tsx"
      to: "organic-cert/src/lib/pdf/components/page-wrapper.tsx"
      via: "ReportPage wrapper for consistent header/footer"
      pattern: "import.*ReportPage"
    - from: "organic-cert/src/lib/pdf/sections/*.tsx"
      to: "organic-cert/src/lib/report-assembler.ts"
      via: "ReportData type for props"
      pattern: "import.*ReportData"
---

<objective>
Build all 8 PDF report section components and the top-level InspectionReport Document that assembles them. Each section is a pure react-pdf component receiving typed ReportData props from Plan 01's assembler.

Purpose: This is the core PDF rendering layer. Each section maps to a locked NOP report section from the user's decisions. The top-level Document component is what renderToBuffer will render in Plan 03's API route.
Output: 9 files -- 8 section components + 1 top-level InspectionReport Document.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-inspection-report-generation/03-CONTEXT.md
@.planning/phases/03-inspection-report-generation/03-RESEARCH.md
@.planning/phases/03-inspection-report-generation/03-01-SUMMARY.md

Key files from Plan 01:
@organic-cert/src/lib/report-assembler.ts
@organic-cert/src/lib/pdf/styles.ts
@organic-cert/src/lib/pdf/components/page-wrapper.tsx
@organic-cert/src/lib/pdf/components/table-row.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cover Page, TOC, Operation Overview, and Field List Sections</name>
  <files>
    organic-cert/src/lib/pdf/sections/cover-page.tsx
    organic-cert/src/lib/pdf/sections/toc-page.tsx
    organic-cert/src/lib/pdf/sections/operation-overview.tsx
    organic-cert/src/lib/pdf/sections/field-list.tsx
  </files>
  <action>
    All sections receive ReportData (or relevant subset) as props. All use ReportPage wrapper for consistent header/footer. All use styles from ../styles.ts.

    1. **cover-page.tsx** — CoverPage component (portrait):
       - Centered layout with vertical spacing.
       - If farm.logoPath exists, render `<Image src={path.join(process.cwd(), farm.logoPath)} />` at top. If not, render farm name in large bold text as header.
       - Title: "USDA National Organic Program" (large, centered) then "Inspection Report" below.
       - Info block (centered, medium text): Farm name, Operator name, Address (farm.address or farm.county + ", " + farm.state), Certification number (farm.certNumber or "N/A"), NOP ID (farm.nopId or "N/A").
       - Crop year in large text: "Crop Year: {cropYear}".
       - Report generation date: format(generatedAt, "MMMM d, yyyy") using date-fns.
       - Bottom section: "Certifier / Inspector: ___________________________" (placeholder line).
       - Do NOT use ReportPage wrapper for cover (no header/footer on cover). Use a bare `<Page size="LETTER">` with custom styling.

    2. **toc-page.tsx** — TocPage component (portrait):
       - Use ReportPage wrapper.
       - Title: "Table of Contents" using sectionTitle style.
       - Static list of section names (no page numbers per research recommendation — use PDF bookmarks instead):
         1. Operation Overview
         2. Field List
         3. 3-Year Field History
         4. Input Application Log
         5. Harvest Log
         6. Mass Balance Summary
       - Each item styled as a numbered list with consistent formatting. Add `<Bookmark title={sectionName} />` element on each entry for PDF navigation.
       - Note at bottom in small text: "Use PDF bookmarks for direct navigation to each section."

    3. **operation-overview.tsx** — OperationOverview component (portrait):
       - Use ReportPage wrapper.
       - Section title: "Operation Overview".
       - Summary stats in a clean grid layout (2 columns):
         - Total Fields: fields.length
         - Total Acres: sum of all field.totalAcres
         - Organic Acres: sum of fields where organicStatus is ORGANIC or TRANSITIONAL
         - Crop Year: cropYear
         - Crops Grown (current year): unique list of crops from current-year enterprises
         - Total Harvests: allHarvests.length
         - Total Applications: allApplications.length
       - Farm certifications section: cert number, cert status, cert expiry if available.

    4. **field-list.tsx** — FieldList component (portrait):
       - Use ReportPage wrapper.
       - Section title: "Field List".
       - Use TableHeader and TableRow components from components/table-row.tsx.
       - Columns: Field Name (25%), Total Acres (15%), Organic Status (15%), Current Crop (20%), Variety (25%).
       - One row per field. Current crop and variety from the current cropYear enterprise (if exists, otherwise "—").
       - Zebra striping via isAlt prop on alternating rows.
       - Summary row at bottom: total acres.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx tsc --noEmit src/lib/pdf/sections/cover-page.tsx src/lib/pdf/sections/toc-page.tsx src/lib/pdf/sections/operation-overview.tsx src/lib/pdf/sections/field-list.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Cover page renders farm info and inspector placeholder, TOC lists all sections with bookmarks, operation overview shows farm stats, field list shows all fields with organic status and current crop</done>
</task>

<task type="auto">
  <name>Task 2: Field History, Application Log, Harvest Log, and Mass Balance Sections</name>
  <files>
    organic-cert/src/lib/pdf/sections/field-history.tsx
    organic-cert/src/lib/pdf/sections/application-log.tsx
    organic-cert/src/lib/pdf/sections/harvest-log.tsx
    organic-cert/src/lib/pdf/sections/mass-balance.tsx
  </files>
  <action>
    All sections receive ReportData (or relevant subset) as props. All use ReportPage wrapper. All use styles from ../styles.ts.

    1. **field-history.tsx** — FieldHistory component (LANDSCAPE per locked decision — wide tables need landscape):
       - Use ReportPage wrapper with orientation="landscape".
       - Section title: "3-Year Field History".
       - For each field, render a subsection with field name as subsection title.
       - Within each field, group enterprises by cropYear (descending: current, prior, 2-years-ago).
       - For each enterprise/year, render all operations (FieldOperations + MaterialUsages + FertilityEvents + HarvestEvents) in a single chronological table.
       - Use MANUAL flex rows (NOT @ag-media/react-pdf-table) because this table will span multiple pages.
       - Table columns: Date (12%), Type (12%), Description/Product (25%), Rate (12%), Acres (8%), Equipment (15%), Source (8%), Status (8%).
       - Each row has wrap={false} to prevent mid-row page breaks.
       - Format dates with date-fns format(date, "MM/dd/yyyy").
       - Source column shows "Sync" for SYNCED, "Manual" for MANUAL dataSource.
       - If a field has no operations in a year, show "No operations recorded" text.

    2. **application-log.tsx** — ApplicationLog component (LANDSCAPE):
       - Use ReportPage wrapper with orientation="landscape".
       - Section title: "Input Application Log".
       - Uses allApplications array from ReportData (already flattened and sorted by date).
       - Manual flex table (multi-page safe). Columns: Date (12%), Field (15%), Material (18%), NOP Status (10%), Rate (10%), Rate Unit (8%), Acres (7%), Applicator (12%), Notes (8%).
       - NOP status column: show "Approved", "Restricted", "Prohibited", "Exempt" from material.nopStatus.
       - Zebra striping on alternating rows.
       - If no applications, show "No input applications recorded for crop year {cropYear}."

    3. **harvest-log.tsx** — HarvestLog component (portrait — fewer columns):
       - Use ReportPage wrapper with orientation="portrait".
       - Section title: "Harvest Log".
       - Uses allHarvests array from ReportData.
       - Manual flex table. Columns: Date (12%), Field (18%), Crop (12%), Lot Number (18%), Acres (10%), Yield/Ac (10%), Net Wt (lbs) (10%), Equipment (10%).
       - Zebra striping. wrap={false} on rows.
       - If no harvests, show "No harvest events recorded for crop year {cropYear}."

    4. **mass-balance.tsx** — MassBalance component (portrait):
       - Use ReportPage wrapper with orientation="portrait".
       - Section title: "Mass Balance Summary" then subtitle "Crop Year: {cropYear}".
       - Uses massBalance array from ReportData (grouped by crop).
       - For each crop: render crop name as subsection title, then a summary line: "Total Harvested: {totalHarvestedLbs} lbs | Total Sold: {totalSoldLbs} lbs".
       - Below summary, render per-lot detail table. Columns: Lot Number (30%), Harvested (lbs) (25%), Sold (lbs) (25%), Sales Status (20%).
       - Sales Status column: if lot.hasSales is true, show the sold amount; if false, show "No sales recorded" per locked decision (no warnings, just factual).
       - Per locked decision: NO reconciliation math, NO red/green pass/fail indicators, NO discrepancy calculations. Just show harvested and sold side-by-side cleanly.
       - If no mass balance data, show "No harvest data available for mass balance calculation."
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx tsc --noEmit src/lib/pdf/sections/field-history.tsx src/lib/pdf/sections/application-log.tsx src/lib/pdf/sections/harvest-log.tsx src/lib/pdf/sections/mass-balance.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Field history renders 3-year chronological operation tables per field in landscape, application log shows all inputs with NOP status, harvest log shows all harvests with lot numbers, mass balance shows harvested vs sold per crop/lot without reconciliation logic</done>
</task>

<task type="auto">
  <name>Task 3: Top-Level InspectionReport Document</name>
  <files>organic-cert/src/lib/pdf/inspection-report.tsx</files>
  <action>
    Create the top-level Document component that assembles all 8 sections.

    1. Import Document from @react-pdf/renderer.
    2. Import all 8 section components from ./sections/.
    3. Import ReportData type from ../report-assembler.
    4. Import format from date-fns for the generatedDate string.

    5. InspectionReport component:
       - Props: { data: ReportData }
       - Compute shared values: generatedDate = format(data.generatedAt, "MM/dd/yyyy"), reportTitle = `USDA NOP Inspection Report - ${data.cropYear}`
       - Render `<Document>` containing sections in locked NOP order:
         1. `<CoverPage data={data} />`
         2. `<TocPage farmName={data.farm.name} reportTitle={reportTitle} generatedDate={generatedDate} />`
         3. `<OperationOverview data={data} farmName={...} reportTitle={...} generatedDate={...} />`
         4. `<FieldList data={data} farmName={...} reportTitle={...} generatedDate={...} />`
         5. `<FieldHistory data={data} farmName={...} reportTitle={...} generatedDate={...} />`
         6. `<ApplicationLog data={data} farmName={...} reportTitle={...} generatedDate={...} />`
         7. `<HarvestLog data={data} farmName={...} reportTitle={...} generatedDate={...} />`
         8. `<MassBalance data={data} farmName={...} reportTitle={...} generatedDate={...} />`

    6. Export InspectionReport as named export.

    Props pattern: each section receives the full data (or relevant slice) plus farmName, reportTitle, generatedDate for the page wrapper. Alternatively, create a shared ReportPageProps type and spread common props.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx tsc --noEmit src/lib/pdf/inspection-report.tsx 2>&1 | head -20</automated>
  </verify>
  <done>InspectionReport Document component assembles all 8 sections in NOP order, passes typed ReportData to each section, and is ready for renderToBuffer in the API route</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all 9 new files (8 sections + 1 document)
- All sections use ReportPage wrapper (except cover page which uses bare Page)
- Field history and application log render in landscape orientation
- Mass balance shows harvested vs sold with no reconciliation/pass-fail logic
- All tables use manual flex rows with wrap={false} (not @ag-media/react-pdf-table for multi-page tables)
- InspectionReport renders sections in locked NOP order: Cover, TOC, Overview, Fields, History, Applications, Harvests, Mass Balance
</verification>

<success_criteria>
- Complete InspectionReport Document component ready for renderToBuffer
- All 8 NOP sections implemented with correct orientation and layout
- Consistent header/footer on all pages (except cover)
- Multi-page tables use manual flex rows with atomic row wrapping
- All components type-check successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-inspection-report-generation/03-02-SUMMARY.md`
</output>
