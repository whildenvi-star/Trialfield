---
phase: 42-design-token-alignment-palette-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/lib/tokens.ts
  - glomalin-portal/tailwind.config.ts
  - glomalin-portal/src/app/globals.css
  - glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx
autonomous: true
requirements: [TOKEN-01, TOKEN-02, TOKEN-03]

must_haves:
  truths:
    - "A canonical token file defines all design system colors, font stack, and spacing"
    - "Tailwind config uses glomalin-* namespace sourced from the token file"
    - "ASCIIBannerStrip has zero hardcoded hex strings — all colors from tokens"
    - "globals.css uses glomalin-* classes instead of soil-*"
  artifacts:
    - path: "glomalin-portal/src/lib/tokens.ts"
      provides: "Canonical design token definitions"
      exports: ["colors", "fonts", "spacing"]
      min_lines: 30
    - path: "glomalin-portal/tailwind.config.ts"
      provides: "Tailwind config consuming tokens"
      contains: "glomalin"
    - path: "glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx"
      provides: "Banner with token-sourced colors"
  key_links:
    - from: "glomalin-portal/tailwind.config.ts"
      to: "glomalin-portal/src/lib/tokens.ts"
      via: "import"
      pattern: "from.*tokens"
    - from: "glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx"
      to: "glomalin-portal/src/lib/tokens.ts"
      via: "import"
      pattern: "from.*tokens"
---

<objective>
Create the canonical design token file and wire it into Tailwind config and ASCIIBannerStrip.

Purpose: Establish the single source of truth for the navy/cyan design system so all subsequent component migration references tokens, not hardcoded values.
Output: tokens.ts, updated tailwind.config.ts (glomalin-* namespace), updated globals.css, token-consuming ASCIIBannerStrip.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@glomalin-portal/tailwind.config.ts
@glomalin-portal/src/app/globals.css
@glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx
@glomalin-portal/src/components/layout/ascii-noise.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tokens.ts and migrate Tailwind config to glomalin-* namespace</name>
  <files>
    glomalin-portal/src/lib/tokens.ts
    glomalin-portal/tailwind.config.ts
    glomalin-portal/src/app/globals.css
  </files>
  <action>
Create `src/lib/tokens.ts` at the path specified by user decision (NOT src/styles/). Export a `colors` object with the existing navy/cyan palette values currently in tailwind.config.ts:

```
bg: '#080a0f'
surface: '#0c1015'
border: '#1e293b'
accent: '#14b8a6'
accentDim: '#0d9488'
accentLight: '#2dd4bf'
text: '#cbd5e1'
muted: '#64748b'
green: '#7A9E7E'
highlight: '#0f172a'
```

Per user decision: rename namespace from `soil-*` to `glomalin-*`. Per Claude's discretion on gold: DROP gold (#C8860A) — it is a soil palette remnant and does not complement cyan. If any component currently uses soil-gold, plan 02 will handle replacement with accent or accentDim.

Per Claude's discretion on token scope: include colors and fonts (these vary across components). Skip spacing/radius/shadows — the existing Tailwind defaults suffice and nothing custom varies.

Per Claude's discretion on dual-consumer: YES — export raw values for direct React imports (canvas rendering) AND structure the colors object so tailwind.config.ts can spread it directly.

Export structure:
- `export const colors = { bg, surface, border, accent, ... }` (flat object, camelCase keys)
- `export const tailwindColors = { bg, surface, border, accent, 'accent-dim', 'accent-light', ... }` (kebab-case keys matching Tailwind convention for hyphenated class names like `glomalin-accent-dim`)
- `export const fonts = { mono: ['JetBrains Mono', 'monospace'] }`

Update `tailwind.config.ts`:
- Import `tailwindColors` and `fonts` from `../src/lib/tokens`
- Replace the hardcoded `soil: { ... }` color block with `glomalin: tailwindColors`
- Replace hardcoded fontFamily.mono with `fonts.mono`
- Remove ALL soil palette references

Update `globals.css`:
- Change `bg-soil-bg text-soil-text` to `bg-glomalin-bg text-glomalin-text`
- Update the comment to reference glomalin accent instead of soil accent
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit src/lib/tokens.ts 2>&1 | head -20 && grep -c "glomalin" tailwind.config.ts && grep -c "soil" tailwind.config.ts</automated>
    <manual>tailwind.config.ts should have 0 "soil" references and multiple "glomalin" references</manual>
  </verify>
  <done>tokens.ts exports colors/fonts, tailwind.config.ts uses glomalin-* namespace from tokens import, globals.css uses glomalin-* classes, zero soil references in these 3 files</done>
</task>

<task type="auto">
  <name>Task 2: Wire ASCIIBannerStrip to import colors from tokens.ts</name>
  <files>glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx</files>
  <action>
Read ASCIIBannerStrip.tsx. Find the hardcoded `DEFAULT_BG_COLOR = '#080a0f'` and any other hardcoded hex color values.

Import `{ colors }` from `@/lib/tokens`.

Replace:
- `DEFAULT_BG_COLOR = '#080a0f'` with `DEFAULT_BG_COLOR = colors.bg`
- Any other hardcoded hex colors with their token equivalents

The banner renders on a canvas element (not via Tailwind classes), so it needs the raw hex values from the `colors` export, not Tailwind classes.

Verify no hex color strings remain in the file (search for `#` followed by hex digits). The only acceptable hex references should be inside the tokens.ts file itself.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -cE "'#[0-9a-fA-F]{3,8}'" glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx && grep -c "from.*tokens" glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx</automated>
    <manual>First grep should return 0 (no hardcoded hex), second should return 1+ (has token import)</manual>
  </verify>
  <done>ASCIIBannerStrip imports all color values from tokens.ts, zero hardcoded hex strings remain in the component file</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in glomalin-portal (no type errors from token imports)
- `grep -r "soil" glomalin-portal/tailwind.config.ts glomalin-portal/src/lib/tokens.ts glomalin-portal/src/app/globals.css` returns nothing
- `grep "'#[0-9a-fA-F]'" glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx` returns nothing
</verification>

<success_criteria>
1. src/lib/tokens.ts exists and exports colors, tailwindColors, and fonts
2. tailwind.config.ts imports from tokens.ts and uses glomalin-* namespace
3. globals.css uses glomalin-bg and glomalin-text
4. ASCIIBannerStrip has zero hardcoded hex — all colors from tokens import
5. TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/42-design-token-alignment-palette-swap/42-01-SUMMARY.md`
</output>
