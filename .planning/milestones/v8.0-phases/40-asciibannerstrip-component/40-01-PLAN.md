---
phase: 40-asciibannerstrip-component
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/components/layout/ascii-noise.ts
  - glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx
autonomous: true
requirements:
  - BANNER-02
  - BANNER-04
  - BANNER-06
  - BANNER-07

must_haves:
  truths:
    - "noise2D, fbm, and generateMycelium are importable from a standalone module"
    - "ASCIIBannerStrip accepts height, className, and paused props (not nodeCount or bgColor)"
    - "Canvas auto-measures with 150ms debounced resize and devicePixelRatio support"
    - "Bottom gradient fades to page background seamlessly"
    - "Each instance gets a random time offset so multiple banners animate out of sync"
  artifacts:
    - path: "glomalin-portal/src/components/layout/ascii-noise.ts"
      provides: "Exported noise utility functions and types"
      exports: ["noise2D", "fbm", "generateMycelium", "MyceliumNode", "Pulse"]
    - path: "glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx"
      provides: "Refactored banner component with clean API"
      exports: ["default"]
  key_links:
    - from: "glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx"
      to: "glomalin-portal/src/components/layout/ascii-noise.ts"
      via: "named imports"
      pattern: "import.*from.*ascii-noise"
    - from: "glomalin-portal/src/app/(protected)/layout.tsx"
      to: "glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx"
      via: "component import"
      pattern: "ASCIIBannerStrip"
---

<objective>
Extract noise utilities into a standalone module and refactor the ASCIIBannerStrip component API to match the locked decisions from the discuss phase.

Purpose: Separating noise math from the React component enables Phase 43 scene reuse. Aligning the props API (height, className, paused) to the locked decision ensures a clean, minimal component surface.

Output: A standalone ascii-noise.ts module exporting noise2D, fbm, generateMycelium, and types; a refactored ASCIIBannerStrip.tsx with the correct props API.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-asciibannerstrip-component/40-CONTEXT.md

@glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx
@glomalin-portal/src/app/(protected)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract noise utilities and refactor component API</name>
  <files>
    glomalin-portal/src/components/layout/ascii-noise.ts
    glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx
    glomalin-portal/src/app/(protected)/layout.tsx
  </files>
  <action>
1. Create `glomalin-portal/src/components/layout/ascii-noise.ts`:
   - Move `noise2D`, `fbm`, `generateMycelium` functions from ASCIIBannerStrip.tsx
   - Move `MyceliumNode`, `Pulse` interfaces
   - Move `CHAR_RAMP`, `charColor`, `charOpacity` constants/functions
   - Export all of: `noise2D`, `fbm`, `generateMycelium`, `MyceliumNode`, `Pulse`, `CHAR_RAMP`, `charColor`, `charOpacity`
   - No `'use client'` directive needed — this is pure math, no React

2. Refactor `ASCIIBannerStrip.tsx` props interface:
   - Change props to: `{ height?: number; className?: string; paused?: boolean }` per user decision
   - Remove `nodeCount` prop — hardcode 10 nodes (desktop) internally. The layout.tsx already handles mobile/desktop with separate instances
   - Remove `bgColor` prop — use `'#080a0f'` (the current soil-bg) internally as the gradient target color. Per Claude's discretion, matching the current page bg is correct
   - Add `className` prop support on the outer container div (merge with existing classes)
   - Add `paused` prop: when true, stop the RAF loop (cancelAnimationFrame). When transitioning from paused=true to paused=false, restart the loop
   - Import noise utilities from `./ascii-noise`

3. Update `glomalin-portal/src/app/(protected)/layout.tsx`:
   - Remove `nodeCount` prop from both ASCIIBannerStrip usages
   - The two instances (desktop 72px, mobile 48px) stay as-is since height is still a prop
   - Remove any `bgColor` prop if present

Note: Do NOT change the visual rendering logic, animation behavior, or noise algorithms in this task. That is Plan 02's scope.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Run `npm run dev` and confirm the banner still renders and animates on the dashboard page</manual>
  </verify>
  <done>
    - ascii-noise.ts exists and exports noise2D, fbm, generateMycelium, MyceliumNode, Pulse, CHAR_RAMP, charColor, charOpacity
    - ASCIIBannerStrip.tsx imports from ./ascii-noise and accepts only height, className, paused props
    - layout.tsx renders ASCIIBannerStrip without nodeCount or bgColor props
    - TypeScript compiles without errors
    - Banner still renders visually (no regression)
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in glomalin-portal
- `grep -r "from.*ascii-noise" glomalin-portal/src/` shows the import link
- `grep "nodeCount\|bgColor" glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx` returns no matches (props removed)
- `grep "export.*noise2D\|export.*fbm\|export.*generateMycelium" glomalin-portal/src/components/layout/ascii-noise.ts` confirms exports
</verification>

<success_criteria>
Noise utilities are standalone and importable. Component API matches locked decision (height, className, paused). No visual regression — banner renders identically to before.
</success_criteria>

<output>
After completion, create `.planning/phases/40-asciibannerstrip-component/40-01-SUMMARY.md`
</output>
