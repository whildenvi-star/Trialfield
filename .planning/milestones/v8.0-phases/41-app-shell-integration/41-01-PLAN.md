---
phase: 41-app-shell-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx
  - glomalin-portal/src/app/(protected)/layout.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-05]

must_haves:
  truths:
    - "Banner appears between header and content on every protected route"
    - "On mobile (<768px), the banner renders at 48px with 6 mycelium nodes"
    - "With prefers-reduced-motion enabled, banner shows a static frame with no RAF loop"
    - "Banner fades in from opacity 0 to 1 over 400ms on mount"
  artifacts:
    - path: "glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx"
      provides: "Animated ASCII banner with nodeCount prop and aria attributes"
      contains: "nodeCount"
    - path: "glomalin-portal/src/app/(protected)/layout.tsx"
      provides: "Protected layout with banner between header and content"
      contains: "ASCIIBannerStrip"
  key_links:
    - from: "glomalin-portal/src/app/(protected)/layout.tsx"
      to: "glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx"
      via: "nodeCount prop passed as 6 for mobile instance"
      pattern: "nodeCount.*6"
---

<objective>
Add mobile-responsive node count and accessibility attributes to the ASCIIBannerStrip component already wired into the protected layout.

Purpose: The banner is already rendering at correct heights (72px desktop / 48px mobile) with fade-in and reduced-motion support from Phase 40. This plan re-exposes nodeCount as a prop so the mobile instance uses 6 nodes (less dense for small screens), and adds proper ARIA attributes for accessibility compliance.

Output: ASCIIBannerStrip with nodeCount prop and aria-hidden/role attributes, layout passing 6 nodes to mobile instance.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-asciibannerstrip-component/40-01-SUMMARY.md
@.planning/phases/40-asciibannerstrip-component/40-02-SUMMARY.md
@glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx
@glomalin-portal/src/app/(protected)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-expose nodeCount prop and add accessibility attributes</name>
  <files>glomalin-portal/src/components/layout/ASCIIBannerStrip.tsx</files>
  <action>
Modify ASCIIBannerStrip to accept an optional `nodeCount` prop (default 10, matching current DEFAULT_NODE_COUNT):

1. Add `nodeCount?: number` to ASCIIBannerStripProps interface.
2. Destructure `nodeCount = 10` in the component function signature (keep DEFAULT_NODE_COUNT as fallback pattern).
3. Pass `nodeCount` instead of `DEFAULT_NODE_COUNT` to the `initNodes(nodeCount)` call in the mount useEffect.
4. Add `nodeCount` to the useEffect dependency array for the mount effect so re-init happens if prop changes.

Add accessibility attributes to the container div:
- `role="img"` on the container div (decorative visual element).
- `aria-hidden="true"` since the banner is purely decorative and should not be read by screen readers.

Verify the prefers-reduced-motion path still works correctly: the existing code renders a single static frame and skips the RAF loop — confirm the `else` branch containing `requestAnimationFrame(loop)` is NOT entered when `prefersReducedMotion` is true. The current implementation already handles this correctly; just verify no regression.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Component compiles without type errors, nodeCount prop accepted</manual>
  </verify>
  <done>ASCIIBannerStrip accepts nodeCount prop (default 10), has aria-hidden="true" and role="img" on container div</done>
</task>

<task type="auto">
  <name>Task 2: Pass nodeCount=6 to mobile banner instance in protected layout</name>
  <files>glomalin-portal/src/app/(protected)/layout.tsx</files>
  <action>
Update the mobile ASCIIBannerStrip instance in the protected layout to pass `nodeCount={6}`:

1. Find the mobile instance (inside `className="block md:hidden"`).
2. Change `<ASCIIBannerStrip height={48} />` to `<ASCIIBannerStrip height={48} nodeCount={6} />`.
3. Leave the desktop instance unchanged — it uses the default of 10 nodes.

This ensures mobile viewports get a less dense mycelium network (6 nodes vs 10) that doesn't feel cluttered at 48px height.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Layout passes nodeCount=6 to mobile banner, nodeCount absent (default 10) on desktop banner</manual>
  </verify>
  <done>Mobile banner renders with 6 nodes, desktop banner renders with 10 nodes. No horizontal scroll or layout break at any viewport width.</done>
</task>

</tasks>

<verification>
1. `cd glomalin-portal && npx tsc --noEmit` passes with zero errors
2. `cd glomalin-portal && npx next build` completes successfully
3. Grep confirms: `nodeCount` appears in ASCIIBannerStripProps interface
4. Grep confirms: `aria-hidden="true"` appears in ASCIIBannerStrip.tsx
5. Grep confirms: `nodeCount={6}` appears in layout.tsx
</verification>

<success_criteria>
- ASCIIBannerStrip accepts nodeCount prop with default of 10
- Mobile instance in protected layout passes nodeCount={6}
- Container div has role="img" and aria-hidden="true"
- TypeScript compilation succeeds
- Existing fade-in (400ms) and reduced-motion behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/41-app-shell-integration/41-01-SUMMARY.md`
</output>
