---
phase: 30-insurance-decision-tool-ui
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - glomalin-portal/src/components/insurance/payout-simulator.tsx
  - glomalin-portal/src/components/insurance/insurance-pdf.tsx
  - glomalin-portal/src/components/insurance/insurance-pdf-button.tsx
  - glomalin-portal/src/components/insurance/insurance-workspace.tsx
autonomous: true
requirements: [INS-04, INS-08]

must_haves:
  truths:
    - "User can move yield and price sliders and see payout recalculate instantly (<100ms) with a disclaimer that results are illustrative only"
    - "User can generate and download an insurance summary report as a PDF"
  artifacts:
    - path: "glomalin-portal/src/components/insurance/payout-simulator.tsx"
      provides: "Interactive yield + price sliders with instant indemnity calculation"
      contains: "computeInsurancePolicy"
    - path: "glomalin-portal/src/components/insurance/insurance-pdf.tsx"
      provides: "react-pdf Document with policy table and coverage matrix snapshot"
      contains: "Document"
    - path: "glomalin-portal/src/components/insurance/insurance-pdf-button.tsx"
      provides: "PDFDownloadLink wrapper loaded via dynamic({ ssr: false })"
      contains: "PDFDownloadLink"
  key_links:
    - from: "glomalin-portal/src/components/insurance/payout-simulator.tsx"
      to: "glomalin-portal/src/lib/fsa/calc.ts"
      via: "computeInsurancePolicy() called on every slider change via useMemo"
      pattern: "computeInsurancePolicy"
    - from: "glomalin-portal/src/components/insurance/insurance-workspace.tsx"
      to: "glomalin-portal/src/components/insurance/insurance-pdf-button.tsx"
      via: "dynamic({ ssr: false }) import with named export pattern"
      pattern: "dynamic.*insurance-pdf-button"
    - from: "glomalin-portal/src/components/insurance/insurance-pdf-button.tsx"
      to: "glomalin-portal/src/components/insurance/insurance-pdf.tsx"
      via: "InsurancePdfDocument rendered inside PDFDownloadLink"
      pattern: "InsurancePdfDocument"
---

<objective>
Build the payout simulator with interactive sliders and the insurance summary PDF report, then wire both into InsuranceWorkspace. Completes the interactive insurance decision tool with real-time "what-if" analysis and a downloadable summary document.

Purpose: Enables INS-04 (payout simulation with yield/price sliders) and INS-08 (insurance summary PDF report) — the analytical and export features of the insurance module.
Output: PayoutSimulator component, InsurancePdfDocument + InsurancePdfButton (SSR-guarded), updated InsuranceWorkspace with simulator section and PDF export button.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-insurance-decision-tool-ui/30-RESEARCH.md
@.planning/phases/30-insurance-decision-tool-ui/30-01-SUMMARY.md
@.planning/phases/28-fsa-planting-workflow-ui/28-02-SUMMARY.md
@glomalin-portal/src/components/insurance/insurance-workspace.tsx
@glomalin-portal/src/components/insurance/coverage-matrix.tsx
@glomalin-portal/src/lib/fsa/calc.ts
@glomalin-portal/src/components/fsa/acreage-pdf.tsx
@glomalin-portal/src/components/fsa/acreage-pdf-button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PayoutSimulator component with yield and price sliders</name>
  <files>
    glomalin-portal/src/components/insurance/payout-simulator.tsx
    glomalin-portal/src/components/insurance/insurance-workspace.tsx
  </files>
  <action>
    1. **Create `payout-simulator.tsx`** ('use client'):
       - Props: `policy: InsurancePolicy`, `pricing: PricingEntry[]`
       - Find the matching pricing entry for the policy's crop (case-insensitive match)
       - Initialize state:
         - `simYield`: default to policy.actual if > 0, else policy.guarantee (represents "what if my yield is X")
         - `simPrice`: default to max(spring_price, fall_price) from matching pricing entry, or 5.00 fallback
       - Calculate slider ranges dynamically:
         - Yield: min=0, max=ceil(policy.guarantee * 1.5), step=1
         - Price: min=0, max=ceil(defaultPrice * 2 * 20) / 20 (round to $0.05), step=0.05
       - `result` via useMemo (keyed on [policy, pricing, simYield, simPrice]):
         - Create adjusted pricing array: for the matching crop, override both spring_price and fall_price to simPrice (simulating a uniform market price scenario)
         - Call `computeInsurancePolicy({ ...policy, actual: simYield }, adjustedPricing)`
       - Layout:
         a. **Disclaimer banner** (above results): "Results are illustrative only. Verify all figures with your insurance agent." — italic, text-xs, text-soil-muted
         b. **Yield slider**:
            - Label: "Simulated Yield: {simYield.toFixed(1)} bu/ac" — text-xs text-soil-muted font-mono
            - Native `<input type="range">` with `className="w-full accent-soil-accent"`
         c. **Price slider**:
            - Label: "Price: ${simPrice.toFixed(2)}/bu" — text-xs text-soil-muted font-mono
            - Native `<input type="range">` with `className="w-full accent-soil-accent"`
         d. **Results grid** (grid-cols-3 gap-3):
            - Card 1: "Effective Guarantee" — `result.effectiveGuarantee.toFixed(1) bu/ac`
            - Card 2: "Est. Indemnity" — `$X,XXX` if > 0 (yellow-400 text, yellow-700 border), "$0" if zero (soil-muted)
            - Card 3: "Projected Revenue" — `$X,XXX` from result.projectedRevenue
            - Each card: bg-soil-surface border border-soil-border rounded px-4 py-3, label in text-xs text-soil-muted font-mono, value in text-lg font-mono font-bold
       - Handle edge cases:
         - If no pricing data exists for the crop: show a message "No pricing data available for {crop}. Add pricing data to enable simulation." instead of sliders
         - If guarantee is 0: yield slider max defaults to 200 (reasonable fallback)

    2. **Wire PayoutSimulator into InsuranceWorkspace**:
       - Import PayoutSimulator
       - Add a "Payout Simulator" section below the Coverage Matrix section (same conditional: shown only when a policy is selected)
       - Section header: "Payout Simulator" with soil-accent text
       - Render `<PayoutSimulator policy={selectedPolicy} pricing={initialPricing} />`
  </action>
  <verify>
    <automated>cd glomalin-portal && npx tsc --noEmit 2>&1 | tail -5</automated>
    <manual>Select a policy, move yield slider down below guarantee — indemnity value appears in yellow. Move price slider — results update instantly.</manual>
  </verify>
  <done>PayoutSimulator renders two sliders (yield, price) that recalculate via computeInsurancePolicy on every change. Results show effective guarantee, estimated indemnity, and projected revenue. Disclaimer banner is visible above results. Wired into InsuranceWorkspace below the coverage matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Insurance summary PDF report with SSR-guarded dynamic import</name>
  <files>
    glomalin-portal/src/components/insurance/insurance-pdf.tsx
    glomalin-portal/src/components/insurance/insurance-pdf-button.tsx
    glomalin-portal/src/components/insurance/insurance-workspace.tsx
  </files>
  <action>
    1. **Create `insurance-pdf.tsx`** (NO 'use client' directive — loaded via dynamic):
       - Import from `@react-pdf/renderer`: Document, Page, Text, View, StyleSheet, Font
       - Props: `policies: InsurancePolicy[]`, `pricing: PricingEntry[]`
       - Register JetBrains Mono font if available, or use Helvetica (follow acreage-pdf.tsx pattern)
       - Styles (StyleSheet.create):
         - page: padding 30, backgroundColor '#ffffff', fontFamily 'Helvetica', fontSize 9
         - header: fontSize 14, fontWeight 'bold', marginBottom 10, color '#333'
         - subheader: fontSize 10, fontWeight 'bold', marginBottom 6, color '#555'
         - disclaimer: fontSize 7, color '#888', fontStyle 'italic', marginTop 10
         - table: flexDirection 'column', borderWidth 1, borderColor '#ccc'
         - tableRow: flexDirection 'row', borderBottomWidth 1, borderBottomColor '#eee'
         - tableHeader: flexDirection 'row', backgroundColor '#f5f5f5', borderBottomWidth 1, borderBottomColor '#ccc'
         - cell: padding 4, flex 1
         - cellRight: padding 4, flex 1, textAlign 'right'
       - Document structure:
         a. **Page 1: Policy Summary**
            - Header: "Insurance Summary — 2026 Crop Year"
            - Generation date: "Generated: {new Date().toLocaleDateString()}"
            - Policy table with columns: Farm, Crop, Plan Type, Coverage %, Planted Ac, Guarantee, Actual, Alert
            - Each policy as a row
         b. **Page 2: Coverage Matrix Snapshot** (if pricing data available)
            - Subheader: "Coverage Level Comparison"
            - For each policy: policy label (farm/crop), then a mini-table with 8 rows (coverage levels) × 3 columns (RP, RP-HPE, YP)
            - Each cell shows indemnity value from computeInsurancePolicy (same logic as CoverageMatrix component)
            - Note: react-pdf cannot do CSS grid — use flexDirection 'row' for column layout
         c. **Disclaimer footer on every page**:
            - "This is a decision-support summary for producer records. It is not an official insurance document. Verify all figures with your insurance agent before making coverage decisions."
       - Import and call `computeInsurancePolicy` from `@/lib/fsa/calc` for matrix cells
       - Export as named export: `export function InsurancePdfDocument({ policies, pricing }: Props)`

    2. **Create `insurance-pdf-button.tsx`** (NO 'use client' directive — loaded via dynamic):
       - Import `PDFDownloadLink` from `@react-pdf/renderer`
       - Import `InsurancePdfDocument` from `./insurance-pdf`
       - Props: `policies: InsurancePolicy[]`, `pricing: PricingEntry[]`
       - Export named: `export function InsurancePdfButton({ policies, pricing }: Props)`
       - Render PDFDownloadLink:
         - document: `<InsurancePdfDocument policies={policies} pricing={pricing} />`
         - fileName: `insurance-summary-2026.pdf`
         - Children: render function `({ loading }) =>` button with soil-accent styling
         - Button text: "Generating..." when loading, "Export PDF" when ready
         - Button className: `bg-soil-accent text-soil-bg px-4 py-2 rounded font-mono text-sm font-bold hover:opacity-90 disabled:opacity-50`

    3. **Wire InsurancePdfButton into InsuranceWorkspace** via dynamic import:
       - Add dynamic import at top of insurance-workspace.tsx (OUTSIDE component):
         ```
         const InsurancePdfButton = dynamic(
           () => import('./insurance-pdf-button').then(m => ({ default: m.InsurancePdfButton })),
           { ssr: false, loading: () => <button disabled className="...">Loading...</button> }
         )
         ```
       - Import `dynamic` from `next/dynamic`
       - Place the `<InsurancePdfButton policies={policies} pricing={initialPricing} />` in the page header area (top-right, next to Add Policy button)
       - Passes current `policies` state (not initialPolicies) so PDF reflects any CRUD changes made during the session

    **CRITICAL: @react-pdf/renderer isolation rule** — only insurance-pdf.tsx and insurance-pdf-button.tsx import from @react-pdf/renderer. No other file in the insurance/ directory or anywhere else touches react-pdf. This prevents SSR crashes.
  </action>
  <verify>
    <automated>cd glomalin-portal && npx tsc --noEmit 2>&1 | tail -5 && npx next build 2>&1 | grep -E "(error|Error|✓|Route)" | tail -20</automated>
    <manual>Visit /app/insurance — see "Export PDF" button in header. Click it — PDF downloads with policy table and coverage matrix. No SSR crash on page load.</manual>
  </verify>
  <done>InsurancePdfDocument renders policy summary table and coverage matrix snapshot with disclaimer. InsurancePdfButton wraps PDFDownloadLink with loading state. Dynamic import in InsuranceWorkspace prevents SSR crash. Export PDF button appears in page header and generates correct PDF on click. TypeScript compiles, Next.js builds with no errors.</done>
</task>

</tasks>

<verification>
1. `cd glomalin-portal && npx tsc --noEmit` — zero TypeScript errors
2. `cd glomalin-portal && npx next build` — build succeeds, no "Component is not a constructor" SSR errors
3. Selecting a policy shows Payout Simulator below Coverage Matrix
4. Moving yield slider below guarantee triggers indemnity > $0 display in yellow
5. Moving price slider recalculates all results instantly (no network requests)
6. Disclaimer "Results are illustrative only" visible above simulator results
7. "Export PDF" button visible in page header
8. Clicking "Export PDF" downloads insurance-summary-2026.pdf
9. PDF contains policy summary table, coverage matrix snapshot, and disclaimer on every page
10. No @react-pdf/renderer imports outside insurance-pdf.tsx and insurance-pdf-button.tsx
</verification>

<success_criteria>
- PayoutSimulator recalculates in <100ms on slider change (no API calls, pure client-side)
- Yield and price slider ranges are contextual to the selected policy
- Disclaimer is prominent and visible before any simulation results
- Insurance PDF contains policy table, coverage matrix, and disclaimer
- PDF downloads without SSR crash (dynamic import with ssr: false)
- InsurancePdfButton uses named export dynamic import pattern (.then(m => ({ default: m.NamedExport })))
- All insurance components use soil design tokens consistently
</success_criteria>

<output>
After completion, create `.planning/phases/30-insurance-decision-tool-ui/30-02-SUMMARY.md`
</output>
