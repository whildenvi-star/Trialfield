---
phase: 33-cross-module-integration-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/lib/fsa/calc.ts
  - glomalin-portal/src/app/api/fsa/clu-records/[id]/route.ts
  - glomalin-portal/src/app/api/insurance/policies/route.ts
  - glomalin-portal/src/components/fsa/clu-card.tsx
  - glomalin-portal/src/components/fsa/clu-workspace.tsx
  - glomalin-portal/src/components/insurance/insurance-workspace.tsx
  - glomalin-portal/src/app/(protected)/app/insurance/page.tsx
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03

must_haves:
  truths:
    - "User can click a CLU card and navigate to the related insurance policy (or be offered policy creation)"
    - "User can click a File Claim button on an insurance policy and be navigated to the claims module with a new claim created"
    - "User sees a prevented planting prompt when a CLU is marked as prevented planting, offering to create a claim"
  artifacts:
    - path: "glomalin-portal/src/components/fsa/clu-card.tsx"
      provides: "View Policy link, prevented planting checkbox, inline claim creation banner"
      contains: "View Insurance Policy"
    - path: "glomalin-portal/src/components/insurance/insurance-workspace.tsx"
      provides: "File Claim button per policy row"
      contains: "handleCreateClaim"
    - path: "glomalin-portal/src/app/api/insurance/policies/route.ts"
      provides: "farm_number + crop filter support for CLU-to-Policy lookup"
      contains: "farm_number"
    - path: "glomalin-portal/src/app/api/fsa/clu-records/[id]/route.ts"
      provides: "prevented_planting in EDITABLE_FIELDS"
      contains: "prevented_planting"
  key_links:
    - from: "glomalin-portal/src/components/fsa/clu-card.tsx"
      to: "/api/insurance/policies"
      via: "fetch with farm_number+crop filter"
      pattern: "fetch.*api/insurance/policies.*farm_number"
    - from: "glomalin-portal/src/components/fsa/clu-card.tsx"
      to: "/app/insurance"
      via: "router.push with highlight or action param"
      pattern: "router\\.push.*insurance"
    - from: "glomalin-portal/src/components/insurance/insurance-workspace.tsx"
      to: "/api/claims"
      via: "POST fetch with policy_id"
      pattern: "fetch.*api/claims.*POST"
    - from: "glomalin-portal/src/components/fsa/clu-card.tsx"
      to: "/api/claims"
      via: "POST for prevented planting claim"
      pattern: "handleCreatePreventedPlantingClaim"
---

<objective>
Wire cross-module navigation links between FSA, Insurance, and Claims modules, and implement the prevented planting trigger.

Purpose: Connect the three separate modules into a coherent workflow so users can navigate from CLU to policy to claim in one path, and automatically prompt for claim creation when prevented planting is detected.
Output: CluCard with "View Policy" link + prevented planting prompt, InsuranceWorkspace with "File Claim" button, extended insurance GET API with filter support.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-cross-module-integration-dashboard/33-RESEARCH.md
@glomalin-portal/src/components/fsa/clu-card.tsx
@glomalin-portal/src/components/fsa/clu-workspace.tsx
@glomalin-portal/src/components/insurance/insurance-workspace.tsx
@glomalin-portal/src/app/api/fsa/clu-records/[id]/route.ts
@glomalin-portal/src/app/api/insurance/policies/route.ts
@glomalin-portal/src/app/api/claims/route.ts
@glomalin-portal/src/lib/fsa/calc.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add insurance policy filter API + prevented_planting to CluRecord and EDITABLE_FIELDS</name>
  <files>
    glomalin-portal/src/lib/fsa/calc.ts
    glomalin-portal/src/app/api/fsa/clu-records/[id]/route.ts
    glomalin-portal/src/app/api/insurance/policies/route.ts
  </files>
  <action>
1. In `glomalin-portal/src/lib/fsa/calc.ts`, add `prevented_planting: boolean` field to the `CluRecord` interface after the `reported` field. The DB column may not exist yet — check first via Supabase query or reading migration files. If it does not exist, create a small migration script at `glomalin-portal/scripts/migrate-33.ts` that runs `ALTER TABLE clu_records ADD COLUMN IF NOT EXISTS prevented_planting BOOLEAN NOT NULL DEFAULT false;` and execute it. Pattern: see `scripts/migrate-29.ts` for prior ALTER TABLE migration pattern.

2. In `glomalin-portal/src/app/api/fsa/clu-records/[id]/route.ts`, add `'prevented_planting'` to the `EDITABLE_FIELDS` Set (currently: `['crop', 'irrigated', 'organic', 'grain_plant_date', 'use']`). This allows the PATCH handler to accept prevented_planting boolean updates from the CluCard UI.

3. In `glomalin-portal/src/app/api/insurance/policies/route.ts` GET handler, add optional `farm_number` and `crop` query param filters needed by the CLU-to-Policy lookup. After the existing `year` filter logic, add:
   - Read `farm_number` from searchParams. If present, chain `.eq('farm_number', farmNumber)` on the query.
   - Read `crop` from searchParams. If present, chain `.ilike('crop', crop)` on the query (case-insensitive to handle casing differences between FSA data and insurance data, per Research pitfall 3).
   This enables CluCard to fetch the matching policy for a given CLU by farm_number + crop + year.
  </action>
  <verify>
    <automated>cd glomalin-portal && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify CluRecord interface includes prevented_planting, EDITABLE_FIELDS includes it, and insurance GET supports farm_number/crop params</manual>
  </verify>
  <done>CluRecord type includes prevented_planting boolean, PATCH route accepts it, insurance GET supports farm_number+crop filtering</done>
</task>

<task type="auto">
  <name>Task 2: Wire CluCard cross-nav + prevented planting prompt + InsuranceWorkspace File Claim button</name>
  <files>
    glomalin-portal/src/components/fsa/clu-card.tsx
    glomalin-portal/src/components/fsa/clu-workspace.tsx
    glomalin-portal/src/components/insurance/insurance-workspace.tsx
    glomalin-portal/src/app/(protected)/app/insurance/page.tsx
  </files>
  <action>
**CluCard (clu-card.tsx):**
1. Add `useRouter` from `next/navigation` import.
2. Add `prevented_planting: boolean` to the `DraftFields` type.
3. In the expanded section, below the Organic toggle, add a "Prevented Planting" checkbox bound to `draft.prevented_planting`. Include it in the PATCH body sent in `handleSave`.
4. After the expanded section fields but before the Save/Cancel buttons, add a linked policy lookup:
   - Add state: `const [linkedPolicy, setLinkedPolicy] = useState<{id: string} | null>(null)` and `const [ppPromptDismissed, setPpPromptDismissed] = useState(false)`.
   - In a `useEffect` that runs when `isExpanded` becomes true, fetch `/api/insurance/policies?farm_number=${record.farm_number}&crop=${encodeURIComponent(record.crop ?? '')}&year=${record.crop_year}` and set `linkedPolicy` to the first result (or null).
   - Render a "View Insurance Policy" link that calls `router.push('/app/insurance?highlight=<linkedPolicy.id>')`. If no policy found, render "No policy -- Add one" linking to `/app/insurance?action=create&farm=${record.farm_number}&crop=${encodeURIComponent(record.crop ?? '')}`.
5. Add prevented planting prompt: When `draft.prevented_planting` is true (or `record.prevented_planting` is true) AND `!ppPromptDismissed`, render an amber inline banner below the policy link. If linkedPolicy exists, show "Create Claim" button that calls `POST /api/claims` with `{ policy_id: linkedPolicy.id, date_of_loss: new Date().toISOString().slice(0,10), description: 'Prevented Planting - ${record.crop} - Farm ${record.farm_number}' }` and on success calls `router.push('/app/claims')`. Include a "Dismiss" button that sets `ppPromptDismissed(true)`. If no linkedPolicy, show "Add Policy First" linking to insurance module.
6. Style all additions using existing dark soil tokens (bg-amber-950/30, border-amber-700/50, text-amber-300 for the prevented planting prompt, matching the existing warning patterns in the codebase).

**CluWorkspace (clu-workspace.tsx):**
- Track dismissed prevented planting prompt IDs in the workspace via `useState<Set<string>>` so dismissal persists across card expand/collapse cycles within a session (per Research pitfall 4). Pass `isPpPromptDismissed` and `onDismissPpPrompt` props to CluCard. Update CluCard props interface accordingly.

**InsuranceWorkspace (insurance-workspace.tsx):**
1. Add `useRouter` from `next/navigation` import.
2. Add `useSearchParams` from `next/navigation` import.
3. Add a `useEffect` that reads `searchParams.get('highlight')` and `searchParams.get('action')` on mount:
   - If `highlight` param exists, call `setSelectedPolicyId(highlight)`.
   - If `action === 'create'`, open the create drawer. Optionally read `crop` and `farm` params to pre-populate (add pre-fill support to PolicyDrawer open if straightforward, otherwise just open the blank drawer).
4. In the policy table Actions cell, add a "+ Claim" button next to Edit and Delete. The handler `handleCreateClaim(policy)` calls `POST /api/claims` with `{ policy_id: policy.id, date_of_loss: new Date().toISOString().slice(0,10), description: 'Claim for ${policy.crop ?? "unknown crop"} - ${policy.farm_name ?? ""}' }`. On success (res.ok), call `router.push('/app/claims')`. Use `e.stopPropagation()` on the button click to avoid triggering the row click handler.

**Insurance page.tsx:**
- Wrap `<InsuranceWorkspace>` in a `<Suspense fallback={null}>` to satisfy Next.js 14's requirement for components using `useSearchParams()` (per Research pitfall 1). Import `Suspense` from 'react'.
  </action>
  <verify>
    <automated>cd glomalin-portal && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify: CluCard shows policy link in expanded view, prevented planting checkbox + prompt renders, InsuranceWorkspace has File Claim button, insurance page has Suspense wrapper</manual>
  </verify>
  <done>CluCard shows View Policy link with Create fallback, prevented planting prompt creates claim via existing API, InsuranceWorkspace has File Claim button per policy row, searchParams-based highlighting works with Suspense boundary</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd glomalin-portal && npx tsc --noEmit`
2. Dev server starts without error: `cd glomalin-portal && npm run dev` (check for build errors)
3. Spot-check: CluCard expanded section shows policy link or "no policy" message
4. Spot-check: Insurance workspace table has "+ Claim" action button
5. Spot-check: Navigating to /app/insurance?highlight=<id> selects that policy row
</verification>

<success_criteria>
- CluCard expanded view shows "View Insurance Policy" or "No policy -- Add one" link based on farm_number+crop match
- Prevented planting checkbox saves to DB and triggers inline claim creation prompt
- InsuranceWorkspace "+ Claim" button creates a claim via POST /api/claims and navigates to /app/claims
- Insurance page accepts ?highlight= and ?action=create query params
- All TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/33-cross-module-integration-dashboard/33-01-SUMMARY.md`
</output>
