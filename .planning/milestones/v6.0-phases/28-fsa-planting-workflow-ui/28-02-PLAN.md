---
phase: 28-fsa-planting-workflow-ui
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - glomalin-portal/package.json
  - glomalin-portal/src/components/fsa/clu-workspace.tsx
  - glomalin-portal/src/components/fsa/acreage-pdf.tsx
  - glomalin-portal/src/components/fsa/acreage-pdf-button.tsx
autonomous: true
requirements: [FSA-07, FSA-08]

must_haves:
  truths:
    - "User can click 'Export PDF' and download a print-ready Acreage Reporting Summary PDF grouped by Farm/Tract with per-farm subtotals, per-crop subtotals, organic/conventional split, and grand total"
    - "User can click 'Export CSV' and download a full data dump CSV of all CLU records with all fields including IDs, timestamps, and validation flags"
    - "PDF is labeled 'Acreage Reporting Summary' and NOT 'FSA-578' — it is explicitly a summary, not a government form replica"
  artifacts:
    - path: "glomalin-portal/src/components/fsa/acreage-pdf.tsx"
      provides: "react-pdf Document component with Farm/Tract grouped table layout, subtotals, and grand total"
      min_lines: 80
    - path: "glomalin-portal/src/components/fsa/acreage-pdf-button.tsx"
      provides: "PDFDownloadLink wrapper — only file that imports from @react-pdf/renderer directly"
      min_lines: 15
    - path: "glomalin-portal/src/components/fsa/clu-workspace.tsx"
      provides: "Updated with Export PDF (dynamic import) and Export CSV buttons in page header"
      contains: "dynamic.*ssr.*false"
  key_links:
    - from: "glomalin-portal/src/components/fsa/clu-workspace.tsx"
      to: "glomalin-portal/src/components/fsa/acreage-pdf-button.tsx"
      via: "dynamic(() => import(...), { ssr: false })"
      pattern: "dynamic.*acreage-pdf-button.*ssr.*false"
    - from: "glomalin-portal/src/components/fsa/acreage-pdf-button.tsx"
      to: "glomalin-portal/src/components/fsa/acreage-pdf.tsx"
      via: "import and render AcreagePdfDocument inside PDFDownloadLink"
      pattern: "PDFDownloadLink.*AcreagePdfDocument"
---

<objective>
Add PDF and CSV export capabilities to the FSA Planting Workflow. Install @react-pdf/renderer, build a table-based Acreage Reporting Summary PDF with Farm/Tract grouping and comprehensive totals, add a client-side CSV export function, and wire both export buttons into the CluWorkspace page header.

Purpose: Deliver FSA-07 (print-ready acreage summary PDF) and FSA-08 (CSV export) so users can generate reports for FSA office appointments and power-user spreadsheet analysis.

Output: 3 new files + 1 modified file + 1 package update
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-fsa-planting-workflow-ui/28-RESEARCH.md
@.planning/phases/28-fsa-planting-workflow-ui/28-01-SUMMARY.md
@glomalin-portal/src/lib/fsa/calc.ts
@glomalin-portal/src/components/fsa/clu-workspace.tsx
@glomalin-portal/tailwind.config.ts
@glomalin-portal/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @react-pdf/renderer + build AcreagePdfDocument + AcreagePdfButton</name>
  <files>
    glomalin-portal/package.json
    glomalin-portal/src/components/fsa/acreage-pdf.tsx
    glomalin-portal/src/components/fsa/acreage-pdf-button.tsx
  </files>
  <action>
    **1. Install @react-pdf/renderer:**
    ```bash
    cd glomalin-portal && npm install @react-pdf/renderer
    ```
    This is the only new package for Phase 28. No other npm install needed.

    **2. Create src/components/fsa/acreage-pdf.tsx:**
    - DO NOT add 'use client' directive — this file is imported by acreage-pdf-button.tsx which is loaded via dynamic({ ssr: false })
    - Import: `Document, Page, View, Text, StyleSheet` from `@react-pdf/renderer`
    - Import: `CluRecord` type from `@/lib/fsa/calc`
    - Props: `{ records: CluRecord[] }`
    - StyleSheet: page padding 30pt, fontFamily 'Helvetica', landscape LETTER orientation
    - Table styles: row as flexDirection 'row', cell with padding 4pt 6pt, fontSize 9, header row bold with bottom border
    - **Document structure:**
      - Title: "Acreage Reporting Summary — Crop Year 2026" (fontSize 14, marginBottom 8)
      - Subtitle: "Generated: {current date}" (fontSize 8, color '#666', marginBottom 12)
      - Disclaimer: "This is a reporting summary for producer records. It is not an official FSA-578 government form." (fontSize 7, italic, marginBottom 12)
      - **Table columns:** CLU, Field Name, Crop, Practice, Acres, Plant Date, Organic, Status (8 columns)
      - **Group by Farm → Tract:** For each farm, render a farm header row ("Farm {farm_number} — {farm_name}"), then for each tract within, render tract subheader ("Tract {tract_number}"), then CLU rows
      - **Per-farm subtotals:** After all tracts in a farm, render a subtotal row with total acres for the farm, count of reported/unreported
      - Use `<View break>` between farm sections (react-pdf `break` prop forces page break — use this to prevent mid-farm page overflow for large farms)
    - **After all farms, render summary section:**
      - Per-crop subtotals: group all records by crop, show crop name + total acres for each
      - Organic/Conventional split: total organic acres vs total conventional acres
      - Grand total: total CLU records, total acres, total reported, total unreported
    - IMPORTANT: react-pdf does NOT auto-paginate table rows — use `wrap` prop on `<View>` elements to allow cross-page wrapping. Use `<View break>` between farm groups for clean page breaks.
    - Test with full 444-record dataset mentally — ensure page breaks don't split mid-row

    **3. Create src/components/fsa/acreage-pdf-button.tsx:**
    - DO NOT add 'use client' directive — loaded via dynamic({ ssr: false }) from clu-workspace
    - Import: `PDFDownloadLink` from `@react-pdf/renderer`
    - Import: `AcreagePdfDocument` from `./acreage-pdf`
    - Import: `CluRecord` type from `@/lib/fsa/calc`
    - Props: `{ records: CluRecord[] }`
    - Render PDFDownloadLink with `document={<AcreagePdfDocument records={records} />}` and `fileName="acreage-reporting-summary-2026.pdf"`
    - Button inside PDFDownloadLink: shows "Generating..." while loading, "Export PDF" when ready
    - Button styled: `bg-soil-accent text-soil-bg px-4 py-2 rounded font-mono text-sm font-bold hover:opacity-90`
    - CRITICAL: This file and acreage-pdf.tsx are the ONLY files that import from @react-pdf/renderer. No other file should import react-pdf symbols.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify @react-pdf/renderer is in package.json dependencies, both PDF files exist, neither has 'use client' directive</manual>
  </verify>
  <done>@react-pdf/renderer installed. AcreagePdfDocument renders a landscape LETTER PDF with Farm/Tract grouped table, per-farm subtotals, per-crop subtotals, organic/conventional split, and grand total. AcreagePdfButton wraps PDFDownloadLink with correct fileName. Neither file uses 'use client' — both are loaded exclusively via dynamic({ ssr: false }).</done>
</task>

<task type="auto">
  <name>Task 2: Wire Export PDF (dynamic import) and Export CSV into CluWorkspace header</name>
  <files>
    glomalin-portal/src/components/fsa/clu-workspace.tsx
  </files>
  <action>
    **1. Add dynamic import for AcreagePdfButton at top of clu-workspace.tsx:**
    ```typescript
    import dynamic from 'next/dynamic'

    const AcreagePdfButton = dynamic(
      () => import('@/components/fsa/acreage-pdf-button'),
      { ssr: false, loading: () => <span className="text-soil-muted font-mono text-sm">Loading PDF...</span> }
    )
    ```
    - IMPORTANT: ssr: false is REQUIRED — importing @react-pdf/renderer in SSR crashes Next.js App Router with "Component is not a constructor"

    **2. Add exportCsv function inside CluWorkspace (or as a module-level helper):**
    - Define headers array: id, legacy_id, farm_number, tract_number, clu, field_name, farm_name, fsa_acres, crop, irrigated, organic, double_crop, cover_crop, grain_plant_date, use, reported, aph, tillage_2024, tillage_2025, share_percent, crop_year
    - Per CONTEXT.md decision: "CSV: full data dump — all CLU record fields including IDs, timestamps, validation flags, share %"
    - Map each record to a CSV row: for each header, get value from record, handle null/undefined as empty string, escape values containing commas or quotes (wrap in double quotes, double any internal quotes)
    - Join rows with newline, prepend header row
    - Create Blob with type 'text/csv;charset=utf-8;'
    - Create temporary anchor element, set href to URL.createObjectURL(blob), set download to `clu-records-2026-${new Date().toISOString().slice(0,10)}.csv`
    - Click the anchor, then revoke the object URL

    **3. Add export buttons to the CluWorkspace page header:**
    - In the header section (where title "FSA Acreage Reporting" is rendered), add a flex row with the title on the left and export buttons on the right
    - Right side contains: `<AcreagePdfButton records={records} />` and a "Export CSV" button
    - Export CSV button: `<button onClick={() => exportCsv(records)} className="bg-soil-surface border border-soil-border text-soil-text px-4 py-2 rounded font-mono text-sm hover:border-soil-accent">Export CSV</button>`
    - Per CONTEXT.md: "Export buttons: top-right of page header, always visible"
    - Buttons should be side by side with a small gap (gap-2 or gap-3)

    **4. Verify no other file imports @react-pdf/renderer:**
    - Only acreage-pdf.tsx and acreage-pdf-button.tsx should have `from '@react-pdf/renderer'` imports
    - clu-workspace.tsx accesses the PDF button ONLY through `dynamic(() => import(...))`
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npm run build 2>&1 | tail -30</automated>
    <manual>Check that clu-workspace.tsx uses dynamic import with ssr:false for PDF button, and has exportCsv function with Blob download pattern</manual>
  </verify>
  <done>CluWorkspace header shows "Export PDF" and "Export CSV" buttons always visible top-right. PDF button uses dynamic({ ssr: false }) import — no SSR crash. CSV export generates full data dump with all fields, proper escaping, and auto-download. Next.js build passes cleanly.</done>
</task>

</tasks>

<verification>
1. `cd glomalin-portal && npm run build` completes with no errors
2. `npx tsc --noEmit` passes (no TypeScript errors)
3. @react-pdf/renderer is listed in package.json dependencies
4. `grep -r "from '@react-pdf/renderer'" glomalin-portal/src/` returns ONLY acreage-pdf.tsx and acreage-pdf-button.tsx
5. clu-workspace.tsx contains `dynamic(` with `ssr: false`
6. AcreagePdfDocument contains "Acreage Reporting Summary" title (not "FSA-578")
7. AcreagePdfDocument contains disclaimer text about not being an official form
8. exportCsv function produces proper CSV with comma/quote escaping
9. Page orientation is landscape LETTER
10. PDF contains per-farm subtotals and grand total
</verification>

<success_criteria>
- "Export PDF" button is always visible in page header and triggers PDF download
- PDF document is landscape, grouped by Farm/Tract, with per-farm subtotals, per-crop subtotals, organic/conventional split, and grand total
- PDF is explicitly labeled "Acreage Reporting Summary" with disclaimer it is not an official form
- "Export CSV" button triggers download of full CLU data dump with all fields
- CSV includes IDs, timestamps, validation flags, and share %
- No SSR-related crashes from @react-pdf/renderer
- Next.js build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/28-fsa-planting-workflow-ui/28-02-SUMMARY.md`
</output>
