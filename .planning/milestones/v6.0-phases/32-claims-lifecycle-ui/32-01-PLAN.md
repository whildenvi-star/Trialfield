---
phase: 32-claims-lifecycle-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/package.json
  - glomalin-portal/src/lib/claims/calc.ts
  - glomalin-portal/src/app/(protected)/app/claims/page.tsx
  - glomalin-portal/src/components/claims/claims-workspace.tsx
  - glomalin-portal/src/components/claims/claims-kanban.tsx
  - glomalin-portal/src/components/claims/claim-column.tsx
  - glomalin-portal/src/components/claims/claim-card.tsx
  - glomalin-portal/src/components/claims/deadline-alert-banner.tsx
autonomous: true
requirements: [CLM-01, CLM-02, CLM-05]

must_haves:
  truths:
    - "User can view all claims as a Kanban board with 6 pipeline stage columns"
    - "User can drag a claim card to a different stage and the stage change persists without hydration error"
    - "User sees a deadline alert banner when any claim has a deadline within 7 days"
    - "Overdue claims are pinned to the top of their stage column with distinct red styling"
    - "Claim cards show crop, policy reference, deadline countdown badge, and claim amount"
  artifacts:
    - path: "glomalin-portal/src/components/claims/claims-workspace.tsx"
      provides: "Client-side claims state management + dynamic import of Kanban"
      contains: "dynamic"
    - path: "glomalin-portal/src/components/claims/claims-kanban.tsx"
      provides: "DndContext + 6-column Kanban layout"
      contains: "DndContext"
    - path: "glomalin-portal/src/components/claims/claim-column.tsx"
      provides: "Droppable column with SortableContext"
      contains: "SortableContext"
    - path: "glomalin-portal/src/components/claims/claim-card.tsx"
      provides: "Draggable claim card with useSortable"
      contains: "useSortable"
    - path: "glomalin-portal/src/components/claims/deadline-alert-banner.tsx"
      provides: "Persistent deadline warning banner"
      contains: "approaching"
    - path: "glomalin-portal/src/lib/claims/calc.ts"
      provides: "Deadline UI helpers"
      contains: "getDeadlineDaysRemaining"
  key_links:
    - from: "glomalin-portal/src/app/(protected)/app/claims/page.tsx"
      to: "glomalin-portal/src/components/claims/claims-workspace.tsx"
      via: "server fetches claims, passes as initialClaims prop"
      pattern: "initialClaims"
    - from: "glomalin-portal/src/components/claims/claims-workspace.tsx"
      to: "glomalin-portal/src/components/claims/claims-kanban.tsx"
      via: "dynamic({ssr:false}) import"
      pattern: "dynamic.*ssr.*false"
    - from: "glomalin-portal/src/components/claims/claims-kanban.tsx"
      to: "/api/claims/[id]"
      via: "PATCH fetch on drag-and-drop stage change"
      pattern: "PATCH.*api/claims"
---

<objective>
Install dnd-kit and react-dropzone, extend calc.ts with deadline UI helpers, build the ClaimsKanban board with drag-and-drop stage management, replace the shell page with a server-to-client handoff pattern, and add a deadline alert banner.

Purpose: Delivers the core visual pipeline (CLM-01), drag-and-drop stage changes (CLM-02), and deadline alerts (CLM-05) — the three most interactive requirements in Phase 32.
Output: Working Kanban board with 6 columns, draggable claim cards, optimistic PATCH on drop, overdue pinning, and persistent deadline alert banner.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-claims-lifecycle-ui/32-CONTEXT.md
@.planning/phases/32-claims-lifecycle-ui/32-RESEARCH.md
@.planning/phases/31-claims-tables-api/31-01-SUMMARY.md
@.planning/phases/31-claims-tables-api/31-02-SUMMARY.md
@glomalin-portal/src/app/(protected)/app/claims/page.tsx
@glomalin-portal/src/lib/claims/calc.ts
@glomalin-portal/src/components/insurance/insurance-workspace.tsx
@glomalin-portal/src/components/insurance/policy-drawer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages + extend calc.ts with deadline UI helpers + STAGE_ORDER constant</name>
  <files>
    glomalin-portal/package.json
    glomalin-portal/src/lib/claims/calc.ts
  </files>
  <action>
    1. Install new packages from glomalin-portal/:
       ```bash
       cd glomalin-portal && npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities react-dropzone
       ```

    2. Extend `src/lib/claims/calc.ts` — add these exports BELOW existing code (do NOT modify existing addDays, INITIAL_DEADLINE_DAYS, STAGE_DEADLINE_DAYS, computeDeadline):

       **STAGE_ORDER** — visual column order per CONTEXT.md user decision (NOT the DB enum order):
       ```typescript
       export const STAGE_ORDER = [
         'notice_of_loss',
         'filed',
         'under_review',
         'adjuster_assigned',
         'settled',
         'closed',
       ] as const
       ```

       **STAGE_LABELS** — display names. Map `settled` to "Settled / Approved" (CONTEXT.md says "Approved/Denied" but DB has `settled` not `approved_denied` — see RESEARCH.md open question 1):
       ```typescript
       export const STAGE_LABELS: Record<string, string> = {
         notice_of_loss: 'Notice of Loss',
         filed: 'Filed',
         under_review: 'Under Review',
         adjuster_assigned: 'Adjuster Assigned',
         settled: 'Settled / Approved',
         closed: 'Closed',
       }
       ```

       **getDeadlineDaysRemaining(deadlineAt, stage)** — returns number of days remaining (negative = overdue), null for closed or no deadline.

       **getDeadlineBorderClass(deadlineAt, stage)** — returns Tailwind classes for card left border: green >30d, amber 7-30d, red <7d, pulsing red overdue. Default border for null.

       **getDeadlineCountdown(deadlineAt, stage)** — returns "14d left", "Due today", "3d overdue", or null.

       **isOverdue(claim)** — returns true if days < 0.

       See exact implementations in 32-RESEARCH.md Pattern 3 (Deadline Color Logic).
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities, react-dropzone appear in package.json dependencies</manual>
  </verify>
  <done>
    - @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities, react-dropzone in package.json
    - calc.ts exports STAGE_ORDER, STAGE_LABELS, getDeadlineDaysRemaining, getDeadlineBorderClass, getDeadlineCountdown, isOverdue
    - Existing calc.ts exports (addDays, INITIAL_DEADLINE_DAYS, STAGE_DEADLINE_DAYS, computeDeadline) unchanged
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: ClaimsWorkspace + ClaimsKanban + ClaimColumn + ClaimCard + DeadlineAlertBanner + page.tsx rewrite</name>
  <files>
    glomalin-portal/src/app/(protected)/app/claims/page.tsx
    glomalin-portal/src/components/claims/claims-workspace.tsx
    glomalin-portal/src/components/claims/claims-kanban.tsx
    glomalin-portal/src/components/claims/claim-column.tsx
    glomalin-portal/src/components/claims/claim-card.tsx
    glomalin-portal/src/components/claims/deadline-alert-banner.tsx
  </files>
  <action>
    **page.tsx** — Rewrite the existing shell page to follow the InsuranceWorkspace server-to-client handoff pattern:
    - Server component fetches claims from Supabase `.from('claims').select('*').order('created_at', { ascending: false })`
    - Renders `<ClaimsWorkspace initialClaims={claimsData ?? []} />`
    - Keep the tracking disclaimer div above the workspace
    - Keep the page header (h1 "Claims", subtitle "Crop Insurance Claims Tracking")
    - Remove the stat cards and table (replaced by Kanban)

    **claims-workspace.tsx** (`'use client'`):
    - Manages `claims` state via `useState<Claim[]>(initialClaims)` where `Claim` is typed inline or as a Record type with known fields (id, stage, crop, deadline_at, coverage_type, coverage_level, effective_guarantee, estimated_loss_bu, appraised_value, indemnity_amount, deductible_amount, policy_id, date_of_loss, cause_of_loss, created_at)
    - Dynamically imports ClaimsKanban: `dynamic(() => import('./claims-kanban').then(m => ({ default: m.ClaimsKanban })), { ssr: false, loading: () => <div className="text-[#6a5a4a] font-mono text-sm py-12 text-center">Loading board...</div> })`
    - `selectedClaimId` state + `drawerOpen` state (prepare for Plan 32-02 ClaimDrawer)
    - `handleStageChange(claimId, newStage)`:
      - Captures `previousClaims` before update
      - Optimistic update: `setClaims(prev => prev.map(c => c.id === claimId ? { ...c, stage: newStage } : c))`
      - `fetch(`/api/claims/${claimId}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ stage: newStage }) })`
      - On non-ok response: revert to `previousClaims`
    - `pendingNoteClaimId` state for stage-change note prompt (small floating prompt bottom-right, per CONTEXT.md: "skippable note prompt" and RESEARCH.md: "10-second auto-dismiss"):
      - After successful PATCH in handleStageChange, set `pendingNoteClaimId` to the moved claim's ID
      - Render a small fixed div bottom-right (z-50) with textarea + "Add Note" button + "Skip" button
      - Auto-dismiss after 10 seconds via setTimeout (clear on unmount or interaction)
      - On submit: POST to `/api/claims/${pendingNoteClaimId}/timeline` with `{ event_type: 'note', note: text }`
      - On skip or auto-dismiss: clear pendingNoteClaimId, no API call
    - Renders: `<DeadlineAlertBanner claims={claims} />` + `<ClaimsKanban>` + note prompt
    - `onCardClick` handler sets selectedClaimId and drawerOpen=true (drawer rendering added in Plan 32-02)

    **claims-kanban.tsx** (`'use client'`, named export `ClaimsKanban`):
    - Props: `{ claims, onStageChange, onCardClick }`
    - `activeClaimId` state for DragOverlay
    - Sensors: `useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }), useSensor(KeyboardSensor, { coordinateGetter: sortableKeyboardCoordinates }))`
    - `DndContext` with `closestCorners` collision detection, `onDragStart` (sets activeClaimId), `onDragEnd` (cross-container detection)
    - `onDragEnd` logic — detect target stage:
      - If `over.id` is in `STAGE_ORDER` array: target = over.id (dropped on column)
      - Else: target = `over.data.current?.stage` (dropped on another card)
      - If source stage === target stage or no target: no-op
      - Otherwise call `onStageChange(active.id, targetStage)`
    - Render 6 `<ClaimColumn>` elements in `STAGE_ORDER` order inside a flex container with `overflow-x-auto`
    - `DragOverlay` renders a `<ClaimCard>` clone of the active claim with `isDragOverlay` flag

    **claim-column.tsx** (`'use client'`):
    - Props: `{ stage, claims, onCardClick }`
    - `useDroppable({ id: stage })` to make column a drop target
    - Build sorted array: overdue claims first, then non-overdue — use `isOverdue` from calc.ts
    - `SortableContext items={sortedClaims.map(c => c.id)} strategy={verticalListSortingStrategy}`
    - Render stage header with `STAGE_LABELS[stage]` + claim count badge
    - Map `sortedClaims` to `<ClaimCard>` elements inside the droppable ref div
    - `min-h-[120px]` on droppable area so empty columns are still drop targets
    - Empty state: subtle "No claims" text if column has 0 claims

    **claim-card.tsx** (`'use client'`):
    - Props: `{ claim, onCardClick?, isDragOverlay? }`
    - `useSortable({ id: claim.id, data: { stage: claim.stage, claim } })` — pass stage in data for cross-container detection
    - Style: `CSS.Transform.toString(transform)`, opacity 0.4 when isDragging
    - Card content (per CONTEXT.md "standard card density"):
      - Left border color via `getDeadlineBorderClass(claim.deadline_at, claim.stage)`
      - Crop name (bold): `claim.crop ?? '—'`
      - Coverage: `claim.coverage_type` + `claim.coverage_level%`
      - Deadline countdown badge: `getDeadlineCountdown(claim.deadline_at, claim.stage)` — render as small pill, color-matched to border
      - Claim amount: format `claim.effective_guarantee` or `claim.indemnity_amount` as currency, whichever is present
    - Overdue cards: if `isOverdue(claim)`, add `bg-red-900/10` background
    - `onClick` fires `onCardClick(claim.id)` but NOT when isDragOverlay
    - `cursor-grab active:cursor-grabbing` on the card div

    **deadline-alert-banner.tsx** (`'use client'`):
    - Props: `{ claims }`
    - Filter claims where `getDeadlineDaysRemaining(c.deadline_at, c.stage)` is not null AND <= 7
    - If 0 approaching: return null
    - `expanded` state for click-to-expand list
    - Banner styling per CONTEXT.md:
      - If any overdue: red border + bg-red-900/10 + animate-pulse
      - Else: amber border + bg-amber-900/10
    - Persistent: no dismiss button (per CONTEXT.md: "not dismissible")
    - Click toggles expanded list showing each approaching claim: crop, deadline date, countdown text
    - Message: "{N} claim(s) have deadlines within 7 days" or "overdue or approaching" if any are overdue

    **Soil palette** — use the project's established color tokens throughout:
    - bg: #080604, surface: #0e0c0b, border: #2a2218, accent: #C8860A, text: #e8d8c0, muted: #6a5a4a, green: #7A9E7E
    - font-mono on all text
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>
      1. Open http://localhost:3000/app/claims (portal must be running with Supabase)
      2. Verify 6 Kanban columns appear in order: Notice of Loss, Filed, Under Review, Adjuster Assigned, Settled / Approved, Closed
      3. Drag a claim card to a different column — card should move instantly (optimistic) and persist on page reload
      4. If any claims have deadlines within 7 days, verify the alert banner appears at the top
    </manual>
  </verify>
  <done>
    - Shell page.tsx replaced with server-to-client handoff (server fetch + ClaimsWorkspace client component)
    - 6 Kanban columns render in STAGE_ORDER (CONTEXT.md visual order)
    - Cards show crop, coverage, deadline countdown badge, claim amount, color-coded left border
    - Drag-and-drop moves cards between stages, PATCH persists, optimistic update reverts on failure
    - Overdue claims pinned to top of their column with red styling
    - DeadlineAlertBanner persistent at page top, click-to-expand, not dismissible
    - Note prompt appears after drag (bottom-right floating, auto-dismiss 10s, skippable)
    - ClaimsKanban wrapped in dynamic({ssr:false}) — no hydration errors
    - TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in glomalin-portal
2. Claims page loads with Kanban board (no hydration errors in browser console)
3. All 6 stage columns visible with correct labels
4. Drag-and-drop moves a card to a new column and PATCH /api/claims/[id] is called
5. Optimistic state reverts if PATCH fails (can test by stopping server mid-drag)
6. Deadline alert banner renders when claims have deadlines <= 7 days away
7. Overdue claims appear at top of their column with red styling
8. Note prompt appears after successful drag, can submit or skip
</verification>

<success_criteria>
- CLM-01: Kanban board with 6 pipeline stages visible, claim cards show crop/deadline/amount
- CLM-02: Drag-and-drop stage change persists, no hydration error
- CLM-05: Deadline alert banner with approaching/overdue claims, not dismissible, click-to-expand
</success_criteria>

<output>
After completion, create `.planning/phases/32-claims-lifecycle-ui/32-01-SUMMARY.md`
</output>
