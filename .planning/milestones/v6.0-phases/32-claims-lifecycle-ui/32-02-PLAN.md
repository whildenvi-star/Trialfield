---
phase: 32-claims-lifecycle-ui
plan: "02"
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - glomalin-portal/src/components/claims/claims-workspace.tsx
  - glomalin-portal/src/components/claims/claim-drawer.tsx
  - glomalin-portal/src/components/claims/timeline-feed.tsx
  - glomalin-portal/src/components/claims/document-upload.tsx
autonomous: true
requirements: [CLM-03, CLM-06]

must_haves:
  truths:
    - "User can open a claim detail drawer and see timeline, documents, and financial totals"
    - "User can add a timestamped note to a claim timeline without refreshing the page"
    - "User can upload a document via drag-and-drop using the three-step signed URL pattern"
    - "User can change a claim stage from the drawer dropdown (same effect as drag-and-drop)"
    - "System events and user notes appear in a unified chronological feed with distinct styling"
  artifacts:
    - path: "glomalin-portal/src/components/claims/claim-drawer.tsx"
      provides: "Slide-over drawer with header + stage dropdown + 3 tabs"
      contains: "ClaimDrawer"
    - path: "glomalin-portal/src/components/claims/timeline-feed.tsx"
      provides: "Unified timeline feed with inline note input"
      contains: "TimelineFeed"
    - path: "glomalin-portal/src/components/claims/document-upload.tsx"
      provides: "react-dropzone upload with three-step signed URL flow"
      contains: "useDropzone"
  key_links:
    - from: "glomalin-portal/src/components/claims/claims-workspace.tsx"
      to: "glomalin-portal/src/components/claims/claim-drawer.tsx"
      via: "selectedClaim prop + drawerOpen state"
      pattern: "ClaimDrawer.*open.*claim"
    - from: "glomalin-portal/src/components/claims/claim-drawer.tsx"
      to: "/api/claims/[id]/timeline"
      via: "GET for timeline data, POST for notes"
      pattern: "api/claims.*timeline"
    - from: "glomalin-portal/src/components/claims/document-upload.tsx"
      to: "/api/claims/[id]/upload-url"
      via: "POST to get signed URL token"
      pattern: "upload-url"
    - from: "glomalin-portal/src/components/claims/document-upload.tsx"
      to: "supabase.storage.uploadToSignedUrl"
      via: "Client-side PUT to Storage"
      pattern: "uploadToSignedUrl"
---

<objective>
Build the ClaimDrawer slide-over with three tabbed sections (Timeline, Documents, Financials), implement the timeline feed with inline note submission, and wire the three-step signed URL document upload UI.

Purpose: Delivers the claim detail view (CLM-03) and timeline notes (CLM-06) — completing the full claims lifecycle UI.
Output: Working slide-over drawer accessible by clicking a claim card, with live timeline, document upload, and financial summary.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-claims-lifecycle-ui/32-CONTEXT.md
@.planning/phases/32-claims-lifecycle-ui/32-RESEARCH.md
@.planning/phases/31-claims-tables-api/31-01-SUMMARY.md
@.planning/phases/31-claims-tables-api/31-02-SUMMARY.md
@.planning/phases/32-claims-lifecycle-ui/32-01-SUMMARY.md
@glomalin-portal/src/components/insurance/policy-drawer.tsx
@glomalin-portal/src/lib/supabase/browser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClaimDrawer slide-over with tabs + stage dropdown + Financials tab</name>
  <files>
    glomalin-portal/src/components/claims/claim-drawer.tsx
    glomalin-portal/src/components/claims/claims-workspace.tsx
  </files>
  <action>
    **claim-drawer.tsx** (`'use client'`):

    Props: `{ open, claim, onClose, onClaimUpdated }` — same shape as PolicyDrawer pattern.

    **Structure:**
    - Backdrop: `fixed inset-0 z-40 bg-black/40` when open, onClick=onClose
    - Drawer panel: `fixed inset-y-0 right-0 z-50 w-[520px] bg-[#0e0c0b] border-l border-[#2a2218] flex flex-col transition-transform duration-200` with `translate-x-0` when open, `translate-x-full` when closed
    - On smaller screens, use `w-full sm:w-[520px]` for responsive behavior (Claude's discretion)

    **Header section** (always visible at top):
    - Claim crop name: bold, large text
    - Stage dropdown (select element) with all stages from STAGE_ORDER/STAGE_LABELS
      - On change: same PATCH flow as drag — `fetch(`/api/claims/${claim.id}`, { method: 'PATCH', body: JSON.stringify({ stage: newStage }) })`
      - On success: call `onClaimUpdated(updatedClaim)` AND optimistically add a stage_change entry to local timeline state
    - Close button (X) top-right
    - Date of loss and cause of loss displayed below crop name in muted text

    **Tabs** — 3 tabs below header: Timeline | Documents | Financials
    - `activeTab` state: `'timeline' | 'documents' | 'financials'`, default `'timeline'`
    - Tab bar: horizontal buttons, active tab has accent (#C8860A) underline, others muted

    **Financials tab** (simplest — build first):
    - Read-only summary from the claim object (no API call needed):
      - Effective Guarantee: `claim.effective_guarantee` formatted as currency
      - Estimated Loss (bu): `claim.estimated_loss_bu`
      - Appraised Value: `claim.appraised_value` formatted as currency
      - Indemnity Payment: `claim.indemnity_amount` formatted as currency (or "Pending" if null)
      - Deductible: `claim.deductible_amount` formatted as currency (or "—" if null)
    - Each as a labeled row: `<div className="flex justify-between py-2 border-b border-[#2a2218]">` label left, value right
    - All font-mono, soil palette colors

    **Data fetching** on open:
    - When `open` transitions to true and `claim` exists, fetch timeline and documents in parallel:
      ```
      Promise.all([
        fetch(`/api/claims/${claim.id}/timeline`).then(r => r.json()),
        fetch(`/api/claims/${claim.id}/documents`).then(r => r.json()),
      ])
      ```
    - Store in local state: `timeline` and `documents`
    - Reset both to empty arrays when claim changes (new claim selected)
    - Loading state while fetching

    **Wire into claims-workspace.tsx:**
    - Import ClaimDrawer (direct import — NOT dynamic, it does not use browser-only DnD APIs)
    - Render `<ClaimDrawer open={drawerOpen} claim={selectedClaim} onClose={() => setDrawerOpen(false)} onClaimUpdated={handleClaimUpdated} />`
    - `handleClaimUpdated(updated)`: update the claim in `claims` state array — `setClaims(prev => prev.map(c => c.id === updated.id ? updated : c))`
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>
      1. Click a claim card — drawer should slide in from the right
      2. Verify 3 tabs visible: Timeline, Documents, Financials
      3. Click Financials tab — see claim financial summary
      4. Change stage from dropdown — claim card should move to new column on the Kanban board
      5. Click X or backdrop — drawer closes
    </manual>
  </verify>
  <done>
    - ClaimDrawer renders as slide-over with backdrop overlay
    - Header shows crop, stage dropdown, date of loss, cause of loss, close button
    - 3 tabs render with active tab highlighting
    - Financials tab shows guarantee, loss, appraised value, indemnity, deductible
    - Stage dropdown triggers PATCH and updates parent claims state
    - Drawer wired into ClaimsWorkspace with open/close state
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: TimelineFeed with inline notes + DocumentUpload with signed URL flow</name>
  <files>
    glomalin-portal/src/components/claims/timeline-feed.tsx
    glomalin-portal/src/components/claims/document-upload.tsx
    glomalin-portal/src/components/claims/claim-drawer.tsx
  </files>
  <action>
    **timeline-feed.tsx** (`'use client'`):

    Props: `{ claimId, timeline, setTimeline }` — timeline state owned by ClaimDrawer, passed down.

    **Timeline rendering** — unified chronological feed (CONTEXT.md locked decision):
    - Map over `timeline` array (already sorted ascending by created_at from API)
    - Each entry renders differently based on `event_type`:
      - `stage_change`: gray text, "Stage changed: {from} -> {to}" using STAGE_LABELS, muted (#6a5a4a) styling, small dot icon
      - `note`: accent (#C8860A) left border (border-l-2 border-l-[#C8860A] pl-3), note text in #e8d8c0, user attribution if available
      - `doc_upload`: gray text, "Document uploaded: {filename}" — if CONTEXT.md says "with link to Documents tab", add a clickable span that would switch to Documents tab (pass an `onSwitchTab` callback prop)
      - `financial_update`: gray text, "Financial details updated"
      - `adjuster_assigned`: gray text, "Adjuster assigned: {name}"
      - `created`: gray text, "Claim created"
      - `deadline_change`: gray text, "Deadline updated"
    - Each entry shows timestamp: `formatDate(event.created_at)` — use a relative format like "2h ago" or "Mar 5" depending on recency
    - Scroll behavior: timeline container should be `overflow-y-auto flex-1` with newest at bottom (natural chat order)

    **Inline note input** (CONTEXT.md locked decision: "always-visible inline textarea at bottom of Timeline tab"):
    - `<textarea>` with placeholder "Add a note..." — always visible below the timeline feed
    - Submit on Enter (without Shift) or click "Add Note" button
    - `handleAddNote(text)`:
      - Optimistic append: push `{ event_type: 'note', note: text, created_at: new Date().toISOString() }` to timeline
      - POST `/api/claims/${claimId}/timeline` with `{ event_type: 'note', note: text }`
      - On success: replace last optimistic entry with server-returned event (has real id + actor_id)
      - On failure: remove optimistic entry, show inline error
      - Clear textarea after successful submit
    - Append-only: no edit or delete buttons (CONTEXT.md: "audit integrity")

    **document-upload.tsx** (`'use client'`):

    Props: `{ claimId, documents, onUploadComplete }` — documents state owned by ClaimDrawer.

    **Document list:**
    - Render each document as a row: filename (linked to signed download URL from the document object), file size formatted (KB/MB), upload date
    - Links open in new tab (`target="_blank"`)
    - If no documents: "No documents uploaded yet." placeholder

    **Upload zone** (react-dropzone + three-step signed URL per Phase 31-02):
    - Import `useDropzone` from `react-dropzone`
    - Import `createClient` from `@/lib/supabase/browser`
    - Constants: `MAX_SIZE_BYTES = 25 * 1024 * 1024`, `ALLOWED_TYPES` object (PDF, JPG, PNG, WebP, XLSX, CSV — see RESEARCH.md Pattern 6)
    - `useDropzone({ onDrop, accept: ALLOWED_TYPES, maxSize: MAX_SIZE_BYTES, maxFiles: 1 })`
    - `onDrop` flow:
      1. Client-side size check (redundant with maxSize but explicit UX message)
      2. Set uploading=true, clear error
      3. **Step 1:** POST `/api/claims/${claimId}/upload-url` with `{ filename: file.name, mimeType: file.type }` → get `{ path, token }`
      4. **Step 2:** `supabase.storage.from('claim-documents').uploadToSignedUrl(path, token, file, { contentType: file.type })`
      5. **Step 3:** POST `/api/claims/${claimId}/documents` with `{ path, filename: file.name, fileSize: file.size, mimeType: file.type }`
      6. Call `onUploadComplete()` which triggers re-fetch of documents in ClaimDrawer
    - Error handling: catch at each step, display error message in the drop zone area
    - CRITICAL: Never cache/reuse the signed token — on retry, start from Step 1 (fresh token)
    - Upload zone UI: dashed border, "Drop a file here or click to select" text, active state with accent border, uploading spinner state
    - Drop zone below the document list

    **Wire into claim-drawer.tsx:**
    - Timeline tab: render `<TimelineFeed claimId={claim.id} timeline={timeline} setTimeline={setTimeline} onSwitchTab={() => setActiveTab('documents')} />`
    - Documents tab: render `<DocumentUpload claimId={claim.id} documents={documents} onUploadComplete={refetchDocuments} />` where `refetchDocuments` re-calls `GET /api/claims/[id]/documents` and updates local state
    - Pass `onSwitchTab` to TimelineFeed so doc_upload events can link to Documents tab
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>
      1. Open a claim drawer, verify Timeline tab shows chronological entries
      2. Type a note in the textarea and press Enter — note should appear immediately in the feed
      3. Switch to Documents tab — see document list (may be empty) and upload drop zone
      4. Drag a file onto the drop zone — verify upload completes and document appears in list
      5. Verify no page refresh needed for any of these actions
    </manual>
  </verify>
  <done>
    - TimelineFeed renders system events (gray) and user notes (accent border) in unified feed
    - Inline textarea always visible, note submission is optimistic, append-only
    - DocumentUpload shows file list with signed download URLs
    - Drop zone accepts PDF/JPG/PNG/WebP/XLSX/CSV up to 25MB
    - Three-step upload works: upload-url → Storage PUT → metadata POST
    - Document upload triggers timeline doc_upload event (server-side, visible on re-fetch)
    - TypeScript compiles cleanly
    - No page refresh required for any interaction
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in glomalin-portal
2. Clicking a claim card opens the slide-over drawer
3. Drawer header shows crop, stage dropdown, date of loss
4. Timeline tab shows chronological system events and user notes with distinct styling
5. Adding a note via textarea appears immediately without page refresh (CLM-06)
6. Documents tab shows uploaded files with download links
7. Drag-and-drop file upload works via react-dropzone + signed URL
8. Financials tab shows read-only claim financial summary
9. Stage change from drawer dropdown moves the card on the Kanban board
10. Drawer closes on X click or backdrop click
</verification>

<success_criteria>
- CLM-03: Claim detail drawer shows full timeline, documents, and financial totals
- CLM-06: User can add timestamped note via inline textarea, appears immediately in feed
</success_criteria>

<output>
After completion, create `.planning/phases/32-claims-lifecycle-ui/32-02-SUMMARY.md`
</output>
