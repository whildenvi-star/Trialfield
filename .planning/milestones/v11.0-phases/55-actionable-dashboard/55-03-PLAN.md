---
phase: 55-actionable-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/lib/action-items.ts
  - glomalin-portal/src/app/api/dashboard/action-items/route.ts
autonomous: true
gap_closure: true
requirements: [DASH-02]

must_haves:
  truths:
    - "FSA group header navigates to /app/fsa-578 (not '#')"
    - "Farm Budget group header navigates to /app/farm-budget (not '#')"
  artifacts:
    - path: "glomalin-portal/src/lib/action-items.ts"
      provides: "MODULE_SOURCES with keys matching MODULES ids"
      contains: "'fsa-578'"
    - path: "glomalin-portal/src/app/api/dashboard/action-items/route.ts"
      provides: "API route using corrected module keys"
      contains: "MODULE_SOURCES['fsa-578']"
  key_links:
    - from: "glomalin-portal/src/app/api/dashboard/action-items/route.ts"
      to: "glomalin-portal/src/lib/action-items.ts"
      via: "MODULE_SOURCES keys"
      pattern: "MODULE_SOURCES\\['fsa-578'\\]"
    - from: "glomalin-portal/src/components/dashboard/action-items-list.tsx"
      to: "MODULES.find(m => m.id === group.module)"
      via: "group.module value matching MODULES id"
      pattern: "module: 'fsa-578'"
---

<objective>
Fix module ID mismatch between MODULE_SOURCES keys and MODULES array ids so that FSA and Budget group headers navigate correctly.

Purpose: The verification found that clicking FSA 578 and Farm Budget group headers on the dashboard navigates nowhere (falls back to '#') because the API returns module: 'fsa' and module: 'budget' but the UI looks up MODULES by id: 'fsa-578' and id: 'farm-budget'.

Output: Corrected MODULE_SOURCES keys and route.ts references so group.module values match MODULES ids exactly.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/55-actionable-dashboard/55-VERIFICATION.md
@glomalin-portal/src/lib/action-items.ts
@glomalin-portal/src/app/api/dashboard/action-items/route.ts
@glomalin-portal/src/components/dashboard/action-items-list.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename MODULE_SOURCES keys and update route.ts references</name>
  <files>
    glomalin-portal/src/lib/action-items.ts
    glomalin-portal/src/app/api/dashboard/action-items/route.ts
  </files>
  <action>
In glomalin-portal/src/lib/action-items.ts:
- Rename MODULE_SOURCES key 'fsa' to 'fsa-578' (keep same label/badge values)
- Rename MODULE_SOURCES key 'budget' to 'farm-budget' (keep same label/badge values)
- Keys 'insurance', 'claims', 'grain-tickets' are already correct — do not change them

In glomalin-portal/src/app/api/dashboard/action-items/route.ts:
- Line 93: Change module: 'fsa' to module: 'fsa-578'
- Line 94: Change MODULE_SOURCES.fsa to MODULE_SOURCES['fsa-578']
- Line 95: Change MODULE_SOURCES.fsa to MODULE_SOURCES['fsa-578']
- Line 275: Change module: 'budget' to module: 'farm-budget'
- Line 276: Change MODULE_SOURCES.budget to MODULE_SOURCES['farm-budget']
- Line 277: Change MODULE_SOURCES.budget to MODULE_SOURCES['farm-budget']

Do NOT modify action-items-list.tsx — it already uses MODULES.find(m => m.id === group.module) which will work correctly once the API returns the right module values.

Do NOT modify dashboard/page.tsx — it already uses 'fsa-578' in its SSR pre-fetch.
  </action>
  <verify>
    <automated>cd glomalin-portal && grep -n "'fsa'" src/lib/action-items.ts src/app/api/dashboard/action-items/route.ts | grep -v 'fsa-578' | grep -v node_modules; echo "EXIT:$?"; grep -n "'budget'" src/lib/action-items.ts src/app/api/dashboard/action-items/route.ts | grep -v 'farm-budget' | grep -v node_modules; echo "EXIT:$?"; grep -c "fsa-578" src/lib/action-items.ts src/app/api/dashboard/action-items/route.ts; grep -c "farm-budget" src/lib/action-items.ts src/app/api/dashboard/action-items/route.ts</automated>
    <manual>Click FSA 578 and Farm Budget group headers on dashboard — should navigate to /app/fsa-578 and /app/farm-budget respectively</manual>
  </verify>
  <done>
    - MODULE_SOURCES has keys 'fsa-578' and 'farm-budget' (no bare 'fsa' or 'budget' keys)
    - route.ts references MODULE_SOURCES['fsa-578'] and MODULE_SOURCES['farm-budget']
    - route.ts sets module: 'fsa-578' and module: 'farm-budget' on group objects
    - No other files modified (action-items-list.tsx and dashboard/page.tsx unchanged)
    - TypeScript compilation passes (npx tsc --noEmit)
  </done>
</task>

</tasks>

<verification>
1. grep confirms no bare 'fsa' or 'budget' keys remain in action-items.ts or route.ts
2. grep confirms 'fsa-578' appears in both MODULE_SOURCES definition and route.ts usage
3. grep confirms 'farm-budget' appears in both MODULE_SOURCES definition and route.ts usage
4. npx tsc --noEmit passes in glomalin-portal/
</verification>

<success_criteria>
- MODULE_SOURCES keys match MODULES array ids exactly: 'fsa-578', 'insurance', 'claims', 'grain-tickets', 'farm-budget'
- API response group.module values resolve correctly in MODULES.find(m => m.id === group.module) for all 5 modules
- DASH-02 gap closed: all group headers navigate to correct module routes
</success_criteria>

<output>
After completion, create `.planning/phases/55-actionable-dashboard/55-03-SUMMARY.md`
</output>
