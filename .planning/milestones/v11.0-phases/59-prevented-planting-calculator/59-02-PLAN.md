---
phase: 59-prevented-planting-calculator
plan: 02
type: execute
wave: 2
depends_on: [59-01]
files_modified:
  - glomalin-portal/src/components/insurance/insurance-workspace.tsx
  - glomalin-portal/src/components/insurance/insurance-pdf.tsx
autonomous: true
requirements: [PP-02]

must_haves:
  truths:
    - "Policy table shows a PP badge and PP indemnity column when any policy has prevented_planting=true"
    - "Insurance PDF report includes a Prevented Planting section with PP indemnity per policy when PP is on"
    - "Insurance PDF omits the PP section entirely when no policies have prevented_planting=true"
    - "PP indemnity in the PDF matches the formula: guarantee × 60% × spring_price × pp_acres"
  artifacts:
    - path: "glomalin-portal/src/components/insurance/insurance-workspace.tsx"
      provides: "PP badge and PP indemnity in policy table rows"
      contains: "ppIndemnity"
    - path: "glomalin-portal/src/components/insurance/insurance-pdf.tsx"
      provides: "Conditional Prevented Planting page/section in PDF document"
      contains: "Prevented Planting"
  key_links:
    - from: "glomalin-portal/src/components/insurance/insurance-pdf.tsx"
      to: "glomalin-portal/src/lib/insurance/calc.ts"
      via: "import computePpIndemnity for PDF indemnity calculation"
      pattern: "computePpIndemnity"
    - from: "glomalin-portal/src/components/insurance/insurance-workspace.tsx"
      to: "glomalin-portal/src/lib/insurance/calc.ts"
      via: "import computePpIndemnity for table display"
      pattern: "computePpIndemnity"
---

<objective>
Display PP indemnity in the insurance policy table and add a conditional Prevented Planting section to the insurance PDF report.

Purpose: Farm manager sees PP indemnity at a glance in the policy table and can generate a PDF report that includes PP figures for sharing with their insurance agent.
Output: PP badge + indemnity in policy table, conditional PP section in insurance PDF.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-prevented-planting-calculator/59-01-SUMMARY.md
@glomalin-portal/src/components/insurance/insurance-workspace.tsx
@glomalin-portal/src/components/insurance/insurance-pdf.tsx
@glomalin-portal/src/lib/insurance/calc.ts
@glomalin-portal/src/lib/fsa/calc.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PP badge and PP indemnity to the insurance policy table</name>
  <files>glomalin-portal/src/components/insurance/insurance-workspace.tsx</files>
  <action>
Import `computePpIndemnity` from `@/lib/insurance/calc` in insurance-workspace.tsx.

**Policy table enhancements:**

1. Add a "PP" column header after the "Alert" column in the table header row:
   ```
   <th className="px-4 py-3 text-center text-glomalin-accent font-semibold">PP</th>
   ```

2. In each policy row, add a PP cell after the Alert cell:
   - If `policy.prevented_planting` is true:
     - Show an amber "PP" badge (matching the existing pattern from clu-card.tsx: `text-amber-300 bg-amber-950/30 border border-amber-700/50 rounded px-1.5 py-0.5`)
     - Compute PP indemnity using `computePpIndemnity({ guarantee: policy.guarantee, prevented_planting_acres: policy.prevented_planting_acres }, initialPricing, policy.crop)`
     - Show PP indemnity below the badge in small text: `$XXX,XXX` formatted with `toLocaleString()`
     - Show PP acres in muted text: `(XXX ac)`
   - If `policy.prevented_planting` is false: show muted dash `—`

3. Add a stat card or update the existing stat cards section to show total PP indemnity across all PP policies. Add a fourth stat card to the grid (change `grid-cols-3` to `grid-cols-4` on sm+):
   - Label: "PP Indemnity"
   - Value: sum of all PP indemnities, formatted as dollars
   - Only show this card if at least one policy has `prevented_planting=true`. If none have PP, keep the 3-column grid as-is.

Style consistency: use existing glomalin design tokens throughout. The PP badge style matches what's already used in clu-card.tsx.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -c "computePpIndemnity" glomalin-portal/src/components/insurance/insurance-workspace.tsx && grep "PP Indemnity" glomalin-portal/src/components/insurance/insurance-workspace.tsx | head -3</automated>
    <manual>Open insurance workspace — policies with PP toggled on should show amber PP badge and dollar indemnity in the PP column</manual>
  </verify>
  <done>Policy table has PP column with amber badge, PP indemnity dollar amount, and PP acres for policies with prevented_planting=true. Stat cards include PP Indemnity total when any PP policies exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add conditional Prevented Planting section to insurance PDF</name>
  <files>glomalin-portal/src/components/insurance/insurance-pdf.tsx</files>
  <action>
Import `computePpIndemnity, PP_COVERAGE_FACTOR` from `@/lib/insurance/calc` in insurance-pdf.tsx.

**PDF changes:**

1. **Policy Summary table (Page 1):** Add a "PP" column to the existing policy table. Column width: `colPp: { flex: 0.6 }`.
   - Header cell: "PP"
   - Row cell: If `policy.prevented_planting` is true, show "PP" in bold. Otherwise show "—".
   - This is a compact indicator — the detail goes in the dedicated PP section.

2. **Prevented Planting section (new conditional page):** Add a new `<Page>` after the Coverage Matrix page (Page 2), rendered ONLY when at least one policy has `prevented_planting === true`.

   Page structure:
   ```
   Header: "Prevented Planting Summary"
   Generated date (same pattern as other pages)
   Disclaimer: "PP indemnity estimates use RMA standard 60% coverage factor. Verify with your agent."

   Table with columns:
   - Farm (flex 1.5)
   - Crop (flex 1)
   - Guarantee (bu/ac) (flex 0.8, right-aligned)
   - PP Factor (flex 0.6, right-aligned) — show "60%"
   - PP Guarantee (bu/ac) (flex 0.8, right-aligned) — guarantee × 0.60
   - Spring Price (flex 0.7, right-aligned) — from pricing lookup
   - PP Acres (flex 0.7, right-aligned)
   - PP Indemnity (flex 1, right-aligned, bold) — formatted as dollars

   Only include rows for policies where prevented_planting === true.

   Totals row at bottom:
   - "Total" label spanning Farm+Crop columns
   - Total PP Acres (sum)
   - Total PP Indemnity (sum, formatted as dollars, bold)
   ```

   Use the existing table/row styles (styles.table, styles.tableHeader, styles.tableRow, styles.tableRowAlt, styles.cell, styles.cellRight, etc.).

   Add new column width styles:
   ```typescript
   colPpFactor: { flex: 0.6 },
   colPpGuarantee: { flex: 0.8 },
   colSpringPrice: { flex: 0.7 },
   colPpAcres: { flex: 0.7 },
   colPpIndemnity: { flex: 1 },
   colPp: { flex: 0.6 },
   ```

   Use `computePpIndemnity` to calculate each row's values. Format dollar amounts with `formatDollars` helper (already exists in the file).

   Include `<PageDisclaimer />` fixed footer on the PP page (same pattern as other pages).

3. **Conditional rendering:** Wrap the PP page in:
   ```tsx
   {ppPolicies.length > 0 && (
     <Page ...>...</Page>
   )}
   ```
   Where `ppPolicies = policies.filter(p => p.prevented_planting)` is computed at the top of the component.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -c "Prevented Planting" glomalin-portal/src/components/insurance/insurance-pdf.tsx && grep -c "computePpIndemnity" glomalin-portal/src/components/insurance/insurance-pdf.tsx && grep -c "PP_COVERAGE_FACTOR" glomalin-portal/src/components/insurance/insurance-pdf.tsx</automated>
    <manual>Generate insurance PDF — if any policy has PP on, a Prevented Planting Summary page should appear with per-policy PP indemnity table and totals row. If no PP policies, this page should not appear.</manual>
  </verify>
  <done>Insurance PDF has PP indicator in policy summary table and a dedicated Prevented Planting Summary page (conditional) with per-policy PP indemnity breakdown and totals. Page omitted when no policies have PP.</done>
</task>

</tasks>

<verification>
1. Policy table PP column shows amber badge + dollar indemnity for PP policies, dash for non-PP policies
2. Stat cards include PP Indemnity total when at least one PP policy exists
3. Insurance PDF policy summary table has PP column indicator
4. Insurance PDF has Prevented Planting Summary page when PP policies exist
5. Insurance PDF omits PP page when no PP policies exist
6. PP indemnity values in PDF match the calc engine formula
7. TypeScript compiles without errors: `cd glomalin-portal && npx tsc --noEmit`
</verification>

<success_criteria>
- PP column visible in policy table with badge and indemnity for PP-flagged policies
- PP Indemnity stat card appears when PP policies exist
- Insurance PDF conditionally renders Prevented Planting Summary page
- PP page has per-policy breakdown table with farm, crop, guarantee, factor, PP guarantee, spring price, PP acres, PP indemnity
- PP page has totals row with summed acres and indemnity
- PP page omitted entirely when no policies have prevented_planting=true
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/59-prevented-planting-calculator/59-02-SUMMARY.md`
</output>
