---
phase: 06.1-phase-6-defect-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/import-plan/confirm/route.ts
  - src/components/budget/BudgetTab.tsx
autonomous: false
requirements:
  - ACT-02
  - VIEW-01
  - VIEW-02

must_haves:
  truths:
    - "POST /api/import-plan/confirm returns 401 for unauthenticated requests"
    - "POST /api/import-plan/confirm returns 403 for users without budget:write permission"
    - "logAudit in confirm route logs actual userId and userName from auth context"
    - "Seed rows on Budget tab show actual cost per acre (actualPricePerUnit * rate), not raw price per unit"
    - "Seed variance compares $/ac projected vs $/ac actual — units are consistent"
    - "Unplanned-expense category dropdown lists exactly the 11 categories accepted by the API"
    - "Selecting any category from the dropdown does not produce a silent API failure"
  artifacts:
    - path: "src/app/api/import-plan/confirm/route.ts"
      provides: "Auth-guarded confirm endpoint with budget:write permission check"
      contains: "getAuthContext"
    - path: "src/components/budget/BudgetTab.tsx"
      provides: "Fixed seed actual formula and aligned category list"
      contains: "Crop Insurance"
  key_links:
    - from: "src/app/api/import-plan/confirm/route.ts"
      to: "@/lib/auth"
      via: "getAuthContext() import"
      pattern: "getAuthContext"
    - from: "src/app/api/import-plan/confirm/route.ts"
      to: "@/lib/rbac"
      via: "hasPermission() import"
      pattern: "hasPermission.*budget:write"
    - from: "src/components/budget/BudgetTab.tsx"
      to: "src/app/api/field-enterprises/[id]/unplanned-cost/route.ts"
      via: "BUDGET_CATEGORIES matching ALLOWED_CATEGORIES"
      pattern: "Crop Insurance.*Land Rent.*Overhead.*Labor.*Fuel.*Repair"
---

<objective>
Fix three high-severity defects from the v2.0 milestone audit: (1) add missing auth guard to POST /api/import-plan/confirm, (2) fix seed actual display from price-per-unit to cost-per-acre, (3) align unplanned-expense category dropdown with API's ALLOWED_CATEGORIES.

Purpose: Close security gap on the only unprotected write route, make seed variance meaningful by fixing unit mismatch, and prevent silent API failures from mismatched category lists.
Output: Two patched files — confirm/route.ts with auth guard, BudgetTab.tsx with corrected seed formula and category list.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-phase-6-defect-fixes/06.1-RESEARCH.md

# Auth guard reference pattern (copy this exact structure)
@src/app/api/field-enterprises/[id]/applications/[recordId]/actual/route.ts

# Target files to fix
@src/app/api/import-plan/confirm/route.ts
@src/components/budget/BudgetTab.tsx

# API category source of truth
@src/app/api/field-enterprises/[id]/unplanned-cost/route.ts

# Auth context shape (read to confirm field names for logAudit)
@src/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth guard to confirm route and fix BudgetTab defects</name>
  <files>
    src/app/api/import-plan/confirm/route.ts
    src/components/budget/BudgetTab.tsx
    src/lib/auth.ts
  </files>
  <action>
**Read `src/lib/auth.ts` first** to confirm the exact field names returned by `getAuthContext()` — specifically what fields map to userId and userName for the `logAudit` call. This is essential before writing code.

**Defect 1 — Auth guard on confirm route** (`src/app/api/import-plan/confirm/route.ts`):

1. Add imports at top of file:
   ```typescript
   import { getAuthContext } from "@/lib/auth";
   import { hasPermission } from "@/lib/rbac";
   import { Role } from "@prisma/client";
   ```

2. Inside the `POST` handler, immediately after the try { opening, add the auth guard block (before any body parsing):
   ```typescript
   const user = await getAuthContext();
   if (!user) {
     return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
   }
   const role = user.role as Role;
   if (!hasPermission(role, "budget:write")) {
     return NextResponse.json({ error: "Forbidden" }, { status: 403 });
   }
   ```
   This mirrors the exact pattern from `applications/[recordId]/actual/route.ts`.

3. Find the `logAudit` call at the bottom of the handler. Replace `userId: undefined` and `userName: undefined` with the actual user fields from `getAuthContext()`. Use the field names confirmed from reading `src/lib/auth.ts` (likely `user.id` and `user.email` or similar — DO NOT GUESS, read the file).

**Defect 2 — Seed actual per-acre formula** (`src/components/budget/BudgetTab.tsx`):

4. Find the seed section map (around line 598-601) where `actualPerAcre` is computed. Change from:
   ```typescript
   const actualPerAcre =
     sc.actualPricePerUnit !== null
       ? sc.actualPricePerUnit
       : null;
   ```
   To:
   ```typescript
   const actualPerAcre =
     sc.actualPricePerUnit !== null
       ? sc.actualPricePerUnit * sc.rate
       : null;
   ```
   Math: `$/unit * units/ac = $/ac`. This matches how projected is derived (`totalCost / acres` = `pricePerUnit * rate * acres / acres` = `pricePerUnit * rate`).

5. Find the sub-label below the seed ActualCell that currently shows `{sc.rateUnit}`. Change it to `/ac` since the cell now displays cost per acre, not price per unit.

**Defect 3 — Category list alignment** (`src/components/budget/BudgetTab.tsx`):

6. Find `BUDGET_CATEGORIES` constant (around line 147-154). Replace the entire array with exactly the 11 categories from `ALLOWED_CATEGORIES` in `unplanned-cost/route.ts`:
   ```typescript
   const BUDGET_CATEGORIES = [
     "Seed", "Fertilizer", "Chemical", "Custom Application",
     "Crop Insurance", "Land Rent", "Overhead", "Labor",
     "Fuel", "Repair", "Other",
   ] as const;
   ```
   This removes "Equipment" (not in API) and adds 8 missing categories.

**Anti-patterns to avoid:**
- Do NOT add client-side validation for removed categories — just remove "Equipment" from the list
- Do NOT change what `actualPricePerUnit` stores or how the API computes anything — this is display-only
- Do NOT add a separate DB call for user info — `getAuthContext()` already returns it
- Do NOT change the `logAudit` function signature — just populate existing parameters
  </action>
  <verify>
    <automated>cd /Users/glomalinguild && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Verify: (1) confirm/route.ts has getAuthContext + hasPermission guard at top of POST handler, (2) logAudit has populated userId/userName, (3) BudgetTab seed actualPerAcre formula multiplies by sc.rate, (4) BUDGET_CATEGORIES has exactly 11 entries matching API</manual>
  </verify>
  <done>
    - confirm/route.ts: POST handler has auth guard returning 401/403, logAudit uses real user fields
    - BudgetTab.tsx: seed actualPerAcre = actualPricePerUnit * rate ($/ac), sub-label shows /ac
    - BudgetTab.tsx: BUDGET_CATEGORIES matches ALLOWED_CATEGORIES exactly (11 items, no Equipment)
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all three defect fixes</name>
  <files>
    src/app/api/import-plan/confirm/route.ts
    src/components/budget/BudgetTab.tsx
  </files>
  <action>
    Human verifies the three defect fixes applied in Task 1. No automated action — this is a code review checkpoint.

    **Defect 1 — Auth guard:**
    1. Open `src/app/api/import-plan/confirm/route.ts`
    2. Confirm: imports for getAuthContext, hasPermission, Role are present
    3. Confirm: auth guard block is the first thing inside try { in the POST handler
    4. Confirm: logAudit call at the bottom has populated userId and userName (not undefined)

    **Defect 2 — Seed formula:**
    1. Open `src/components/budget/BudgetTab.tsx`
    2. Find the seed section — search for `actualPricePerUnit`
    3. Confirm: `actualPerAcre = sc.actualPricePerUnit * sc.rate` (not just `sc.actualPricePerUnit`)
    4. Confirm: sub-label below seed ActualCell shows `/ac` (not `{sc.rateUnit}`)

    **Defect 3 — Category list:**
    1. In BudgetTab.tsx, search for `BUDGET_CATEGORIES`
    2. Confirm: array has exactly 11 entries: Seed, Fertilizer, Chemical, Custom Application, Crop Insurance, Land Rent, Overhead, Labor, Fuel, Repair, Other
    3. Confirm: "Equipment" is NOT in the list
  </action>
  <verify>Human reviews both files and confirms all three defect fixes are correct</verify>
  <done>All three defect fixes approved by human reviewer</done>
</task>

</tasks>

<verification>
- TypeScript compiles cleanly (`npx tsc --noEmit`)
- `src/app/api/import-plan/confirm/route.ts` contains `getAuthContext`, `hasPermission`, `budget:write`
- `src/components/budget/BudgetTab.tsx` seed `actualPerAcre` formula includes `* sc.rate`
- `BUDGET_CATEGORIES` array has 11 entries matching `ALLOWED_CATEGORIES` in unplanned-cost route
- No "Equipment" in BUDGET_CATEGORIES
</verification>

<success_criteria>
1. POST /api/import-plan/confirm is auth-guarded with the same pattern as all other Phase 6 write routes
2. Seed Actual column displays cost per acre (actualPricePerUnit * rate), making variance meaningful
3. Category dropdown contains exactly the 11 API-accepted categories — no silent failures possible
4. All changes compile without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-phase-6-defect-fixes/06.1-01-SUMMARY.md`
</output>
