---
phase: 06-actuals-entry-and-enterprise-budget-view
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - src/components/budget/BudgetTab.tsx
  - src/components/budget/ActualCell.tsx
  - src/components/budget/DataSourceBadge.tsx
  - src/components/budget/VarianceCell.tsx
autonomous: false
requirements: [VIEW-01, VIEW-02, VIEW-03]

must_haves:
  truths:
    - "Sandy can click an Actual cell, type a value, and it saves on blur or Enter without page navigation"
    - "Enter advances focus to the next editable cell below; Esc cancels; Tab moves right"
    - "Projected, Actual, and Variance columns display side by side"
    - "Variance shows green for favorable (under budget), red for unfavorable (over budget)"
    - "PROJ badge (blue), ACTUAL badge (green), and UNPLANNED badge (amber) appear on correct rows"
    - "Collapsible cost category sections match farm-budget spreadsheet groupings"
    - "Summary row shows Total Projected/ac, Total Actual/ac, Total Variance/ac"
    - "Field operations show checkbox + date picker for confirmation"
    - "Sandy can add unplanned expense rows via category dropdown + amount"
    - "Toast shows 'Saved' on success, 'Couldn't save — try again' on error"
    - "Financial columns visible to ADMIN only, absent for OFFICE"
  artifacts:
    - path: "src/components/budget/ActualCell.tsx"
      provides: "Inline click-to-edit cell with keyboard navigation"
      min_lines: 50
    - path: "src/components/budget/DataSourceBadge.tsx"
      provides: "PROJ/ACTUAL/UNPLANNED pill badges"
      min_lines: 15
    - path: "src/components/budget/VarianceCell.tsx"
      provides: "Variance display with green/red color coding"
      min_lines: 15
    - path: "src/components/budget/BudgetTab.tsx"
      provides: "Full dual-column budget view with inline editing, collapsible sections, summary row"
      min_lines: 300
  key_links:
    - from: "ActualCell.tsx"
      to: "PATCH /api/field-enterprises/[id]/applications/[recordId]/actual"
      via: "onSave callback passed from BudgetTab"
      pattern: "fetch.*actual"
    - from: "BudgetTab.tsx"
      to: "budget-summary API response"
      via: "budgetSummary prop with projected+actual+variance+unplanned data"
      pattern: "budgetSummary\\.(actual|variance|unplanned)"
    - from: "BudgetTab.tsx"
      to: "sonner toast"
      via: "toast.success and toast.error in save handlers"
      pattern: "toast\\.(success|error)"
---

<objective>
Build the dual-column Budget tab UI with inline editing, operation confirmation, data source badges, variance display, and collapsible sections.

Purpose: This is the user-facing feature -- Sandy sees Projected, Actual, and Variance columns, clicks to edit actual values, confirms operations with checkboxes, and adds unplanned expenses. All CONTEXT.md locked decisions about spreadsheet-like behavior are implemented here.

Output: Complete interactive Budget tab with all VIEW requirements satisfied
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-actuals-entry-and-enterprise-budget-view/06-CONTEXT.md
@.planning/phases/06-actuals-entry-and-enterprise-budget-view/06-RESEARCH.md
@.planning/phases/06-actuals-entry-and-enterprise-budget-view/06-01-SUMMARY.md
@.planning/phases/06-actuals-entry-and-enterprise-budget-view/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ActualCell, DataSourceBadge, and VarianceCell components</name>
  <files>
    src/components/budget/ActualCell.tsx
    src/components/budget/DataSourceBadge.tsx
    src/components/budget/VarianceCell.tsx
  </files>
  <action>
    **App root:** `/Users/glomalinguild/Desktop/my-project-one/organic-cert/`

    **ActualCell.tsx** — Inline editable cell component ("use client"):

    Props: `{ value: number | null; onSave: (value: number | null) => Promise<void>; placeholder?: string; cellRef?: React.Ref<HTMLElement> }`

    Behavior (per CONTEXT.md locked decisions):
    - Display mode: shows formatted dollar value (`$XX.XX`) or placeholder ("—") when null. Clickable with hover:bg-stone-100 to indicate editability.
    - Edit mode: autoFocus numeric input appears on click. Save on blur OR Enter key. Esc cancels and reverts to display mode.
    - **Enter key:** commits value, then moves focus to the NEXT ActualCell below. Implement by dispatching a custom event or using a ref registry pattern -- BudgetTab manages a list of cell refs and ActualCell calls an `onAdvance` callback that focuses the next ref.
    - **Tab key:** let browser handle natural tab order (moves right).
    - **On save failure:** cell stays in edit mode with Sandy's typed value preserved (do NOT revert). The BudgetTab save handler will show error toast.
    - Input type="number" with step="0.01". Right-aligned text. Compact: w-24 with border rounded.

    **DataSourceBadge.tsx** — Badge pill component:

    Props: `{ source: "PROJ" | "ACTUAL" | "UNPLANNED" }`

    - PROJ: muted blue pill (text-blue-600 border-blue-200)
    - ACTUAL: muted green pill (text-green-600 border-green-200)
    - UNPLANNED: amber/orange pill (text-amber-600 border-amber-200)
    - Use shadcn Badge with variant="outline" and custom classes. Text size: text-[9px] h-4 px-1.5.

    **VarianceCell.tsx** — Variance display with color coding:

    Props: `{ projected: number | null; actual: number | null }`

    - If either is null: render "—" in muted text (text-stone-300)
    - variance = projected - actual
    - favorable (variance >= 0, under budget): green (text-green-700), display as negative number (savings)
    - unfavorable (variance < 0, over budget): red (text-red-700), display as positive number (overage)
    - Format with 2 decimal places, right-aligned, font-medium
    - Show as $/ac values (the BudgetTab passes pre-computed per-acre values)
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    Three components exist, compile cleanly, and implement the exact interaction patterns from CONTEXT.md. ActualCell handles click-to-edit with Enter/Esc/Tab/blur. DataSourceBadge renders three badge variants. VarianceCell shows green/red color-coded variance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build dual-column BudgetTab with inline editing, collapsible sections, and operation confirmation</name>
  <files>
    src/components/budget/BudgetTab.tsx
  </files>
  <action>
    **App root:** `/Users/glomalinguild/Desktop/my-project-one/organic-cert/`

    Rewrite BudgetTab.tsx (extracted in Plan 01) to implement the full dual-column layout. This is the largest task in Phase 6.

    **Layout structure (per CONTEXT.md):**

    1. **Summary row at top:** Card showing Total Projected/ac | Total Actual/ac | Total Variance/ac. Use budgetSummary.costPerAcre, actualCostPerAcre, varianceCostPerAcre from the API response.

    2. **Collapsible cost category sections** grouped to match farm-budget spreadsheet: Seed, Fertilizer, Chemical, Custom Application (Operations), Other/Misc. Use shadcn Collapsible component (install via `npx shadcn@latest add collapsible` if not already available). All sections expanded by default. ChevronDown icon rotates on collapse.

    3. **Table header per section:** Item | Projected (with PROJ badge) | Actual (with ACT badge) | Variance

    4. **Seed section:**
       - Each SeedUsage row: seed name | projected cost (from SeedLot.purchasePrice) | ActualCell (saves via PATCH /seed-usage/[id]/actual) | VarianceCell
       - DataSourceBadge on projected value: "PROJ"
       - DataSourceBadge on actual value: "ACTUAL" (only when value is set)
       - Show unit label next to actual input derived from seed data (per RESEARCH.md pitfall 4)

    5. **Material sections (Fertilizer, Chemical, etc.):**
       - Each MaterialUsage row: product name | projected totalCost/ac | ActualCell (saves via PATCH /applications/[id]/actual) | VarianceCell
       - Group by category to match farm-budget sections

    6. **Operations section:**
       - Each FieldOperation row: operation description | projected cost/ac | status indicator | actual date
       - **Checkbox + date picker** for confirmation (per CONTEXT.md): shadcn Checkbox. When checked, date picker appears defaulting to today (use native input type="date" or date-fns formatted). Save calls POST /api/import-plan/confirm. Un-check calls PUT /operations to revert to PLANNED.
       - CONFIRMED operations show green check icon and actual date. PLANNED operations show empty checkbox.

    7. **Harvest yield row:**
       - Projected yield (from budgetSummary targetYieldPerAcre) | ActualCell for yield entry (saves via PATCH /actual-yield) | VarianceCell
       - Unit label from projected plan (bu/ac, tons/ac, etc.) -- NOT hardcoded

    8. **Unplanned expenses section:**
       - Rows from budgetSummary.unplannedCosts: category | dash in Projected | actual amount | no variance
       - "UNPLANNED" badge (amber) on each row
       - "Add unplanned expense" button at bottom: opens inline row with category dropdown (populated from existing budget categories) + amount input. Save calls POST /unplanned-cost. No dialog -- inline add row pattern.

    **Save handlers:**
    Each ActualCell's onSave callback:
    1. Calls the appropriate PATCH endpoint
    2. On success: `toast.success("Saved", { duration: 2000 })` then calls `onDataChanged()` (the refreshBudget callback from Plan 01)
    3. On failure: `toast.error("Couldn't save — try again")` and throws to keep cell editable

    **CRITICAL:** `onDataChanged` calls `refreshBudget()` which ONLY re-fetches budget-summary. It must NOT call the full `load()` function. This prevents scroll-jump (RESEARCH.md pitfall 1).

    **Financial columns (VIEW-06):** The `canSeeFinancial` prop controls visibility of revenue projection, gross margin, and profit/acre sections. These must be wrapped in `{canSeeFinancial && (...)}` -- same silent omission pattern from Phase 5. OFFICE sees cost data only.

    **All values in $/ac:** Divide totals by `budgetSummary.acres` for display. The summary row and all line items show per-acre values.

    **Keyboard navigation for Enter-advances-down:** Maintain an ordered array of ActualCell refs. When ActualCell fires onAdvance (after Enter commit), BudgetTab focuses the next ref in the array. This gives Sandy the spreadsheet feel of Enter-to-advance.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Open an enterprise Budget tab. Verify: (1) Projected/Actual/Variance columns visible, (2) click an Actual cell and type a value -- toast shows "Saved", (3) variance updates with green/red color, (4) PROJ/ACTUAL badges appear, (5) collapse a section, (6) check an operation checkbox -- date picker appears</manual>
  </verify>
  <done>
    BudgetTab renders dual-column Projected/Actual/Variance layout with collapsible sections. Sandy can click-to-edit actual values with Enter/Esc/blur behavior. Operations have checkbox+date confirmation. Badges show PROJ/ACTUAL/UNPLANNED. Variance is green for favorable, red for unfavorable. Summary row shows totals per acre. Financial sections are ADMIN-only. Unplanned expenses can be added.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of complete actuals entry and budget view</name>
  <action>
    Human verifies the full Phase 6 feature set across all three plans. This checkpoint covers the complete actuals entry workflow and dual-column budget view.

    What was built:
    - Schema migration with actuals fields (Plan 01)
    - Budget-summary API returning projected + actual + variance (Plan 02)
    - Four PATCH/POST routes for saving actuals (Plan 02)
    - Inline editable Budget tab with spreadsheet-like keyboard navigation (Plan 03)
    - Operation confirmation via checkbox + date picker (Plan 03)
    - Data source badges PROJ/ACTUAL/UNPLANNED (Plan 03)
    - Variance color coding green favorable / red unfavorable (Plan 03)
    - Collapsible cost category sections (Plan 03)
    - Unplanned expense entry (Plan 03)
    - Financial columns ADMIN-only (Plan 03)
  </action>
  <verify>
    Test as OFFICE user (Sandy's role):

    1. Navigate to any enterprise detail page, click the Budget tab
    2. Verify: Projected, Actual, and Variance columns are visible
    3. Verify: Cost sections are collapsible (Seed, Fertilizer, Chemical, Operations)
    4. Click an Actual cell for any material cost -- type a value, press Enter
       - Expected: toast "Saved" appears, variance updates with color, cursor moves to next cell below
    5. Click an Actual cell, type a value, press Escape
       - Expected: value reverts, cell returns to display mode
    6. Enter a seed cost actual -- verify unit label matches projected
    7. Check the "Completed" checkbox on a PLANNED field operation
       - Expected: date picker appears defaulting to today, status changes to CONFIRMED
    8. Uncheck a CONFIRMED operation -- verify it reverts to PLANNED
    9. Enter an actual harvest yield -- verify unit label is not hardcoded
    10. Click "Add unplanned expense" -- select a category, enter amount, save
        - Expected: new row appears with UNPLANNED (amber) badge
    11. Verify summary row at top shows per-acre totals
    12. Verify: NO revenue, margin, or profit columns visible as OFFICE

    Test as ADMIN:
    13. Navigate to same Budget tab as ADMIN
    14. Verify: financial columns (revenue, margin, profit) ARE visible
    15. Verify: all actuals editing features work identically

    Page behavior:
    16. Scroll to middle of budget table, enter an actual, verify page does NOT scroll to top
    17. Verify the page never navigates away during any save operation
  </verify>
  <done>
    Human confirms all 17 verification steps pass. Sandy can enter actuals inline with spreadsheet-like behavior, see projected/actual/variance side by side, confirm operations, add unplanned expenses. Financial data is ADMIN-only. No scroll-jump on save.
  </done>
</task>

</tasks>

<verification>
- All three budget sub-components compile (ActualCell, DataSourceBadge, VarianceCell)
- BudgetTab renders with dual-column layout
- Inline editing saves via PATCH endpoints with toast feedback
- Keyboard navigation: Enter saves+advances, Esc cancels, Tab moves right
- Operation confirmation works with checkbox + date picker
- Variance shows correct green/red coloring
- PROJ/ACTUAL/UNPLANNED badges render on correct rows
- Financial columns hidden for OFFICE, visible for ADMIN
- No scroll-jump on save
</verification>

<success_criteria>
Sandy can open an enterprise Budget tab, see projected and actual columns side by side, click to enter actual values with immediate save, confirm operations with checkboxes, add unplanned expenses, and see variance color-coded. The page never scrolls or navigates away. Financial data is ADMIN-only. All 9 Phase 6 requirements (ACT-01 through ACT-05, VIEW-01 through VIEW-03, VIEW-06) are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/06-actuals-entry-and-enterprise-budget-view/06-03-SUMMARY.md`
</output>
