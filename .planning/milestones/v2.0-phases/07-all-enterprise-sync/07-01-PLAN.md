---
phase: 07-all-enterprise-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/app/api/fields/sync-macro/route.ts
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02

must_haves:
  truths:
    - "After sync, conventional enterprises exist in the database as FieldEnterprise records with enterpriseType=CONVENTIONAL"
    - "After sync, existing organic FieldEnterprise records retain their actuals data (actualYieldPerAcre, SeedUsage.actualPricePerUnit, MaterialUsage.actualTotalCost) unchanged"
    - "No duplicate FieldEnterprise records exist for the same field+year+crop+label when organic and conventional enterprises coexist"
    - "enterpriseType is derived from the local Field.organicStatus (registry source of truth), not from the farm-budget enterprise category"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "EnterpriseType enum and updated @@unique constraint"
      contains: "enum EnterpriseType"
    - path: "src/app/api/fields/sync-macro/route.ts"
      provides: "All-enterprise sync with type-aware upsert"
  key_links:
    - from: "src/app/api/fields/sync-macro/route.ts"
      to: "prisma.fieldEnterprise"
      via: "findFirst with enterpriseType in where clause"
      pattern: "enterpriseType"
    - from: "src/app/api/fields/sync-macro/route.ts"
      to: "matchedField.organicStatus"
      via: "registry-derived type flag"
      pattern: "organicStatus.*ORGANIC"
---

<objective>
Add enterpriseType enum to FieldEnterprise schema and expand the sync-macro endpoint to pull ALL enterprises (organic + conventional) from the farm-budget service, using registry-derived organic status for type classification.

Purpose: Satisfies SYNC-01 (all enterprises sync) and SYNC-02 (existing organic data preserved) — the database-level foundation before any UI changes.
Output: Updated schema with EnterpriseType enum and @@unique constraint; sync-macro route that processes all 56 fields across 7 enterprises.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-all-enterprise-sync/07-RESEARCH.md
@.planning/phases/06-actuals-entry-and-enterprise-budget-view/06-01-SUMMARY.md

@prisma/schema.prisma
@src/app/api/fields/sync-macro/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EnterpriseType enum and update FieldEnterprise unique constraint</name>
  <files>prisma/schema.prisma</files>
  <action>
1. Add a new enum to schema.prisma:

```prisma
enum EnterpriseType {
  ORGANIC
  CONVENTIONAL
}
```

2. Add `enterpriseType` field to the FieldEnterprise model:

```prisma
enterpriseType EnterpriseType @default(ORGANIC)
```

The `@default(ORGANIC)` is critical — all existing FieldEnterprise records are organic (from previous sync), so the default preserves them without a backfill migration.

3. Update the existing `@@unique` constraint on FieldEnterprise. Replace:
```prisma
@@unique([fieldId, cropYear, crop, label])
```
With:
```prisma
@@unique([fieldId, cropYear, crop, label, enterpriseType])
```

This prevents match key collision (flagged in STATE.md) where a conventional and organic crop of the same type on the same field would create duplicates.

4. Run `npx prisma db push` from the organic-cert project root (`/Users/glomalinguild/Desktop/my-project-one/organic-cert`) to apply the schema change.

5. Run `npx prisma generate` to regenerate the Prisma client with the new enum and field.

IMPORTANT: Do NOT modify any other models or fields. This is an additive-only change.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx prisma validate && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm EnterpriseType enum and enterpriseType field exist in schema.prisma; confirm @@unique includes enterpriseType</manual>
  </verify>
  <done>EnterpriseType enum (ORGANIC, CONVENTIONAL) exists; FieldEnterprise has enterpriseType field with @default(ORGANIC); @@unique constraint includes enterpriseType; prisma db push succeeds; TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Expand sync-macro to process all enterprises with type-aware upsert</name>
  <files>src/app/api/fields/sync-macro/route.ts</files>
  <action>
Modify the POST handler in sync-macro/route.ts to sync ALL enterprises instead of organic-only.

**Step 1: Remove the organic-only filter (lines ~165-169)**

Remove these lines:
```typescript
const organicEnterprises = enterprises.filter(
  (e) => e.category === "organic" || e.systemCodes.some((c) => c.includes("ORG"))
);
const organicEntIds = new Set(organicEnterprises.map((e) => e.id));
const organicFields = fields.filter((f) => organicEntIds.has(f.enterpriseId));
```

Replace the subsequent iteration variable (likely `organicFields`) with `fields` — iterate ALL fields from farm-budget, not just organic-filtered ones.

**Step 2: Derive enterpriseType from registry in the upsert loop**

In the sync loop where each budget field (`bf`) is matched to a local Field (`matchedField`), add:
```typescript
const enterpriseType = matchedField.organicStatus === "ORGANIC" ? "ORGANIC" : "CONVENTIONAL";
```

CRITICAL: Use `matchedField.organicStatus` (from farm-registry sync), NOT the budget enterprise's `category` field. The Conv/Org Canning enterprise has `category: "conventional"` but may contain organic-certified fields. Per user decision: "Type flag is derived from the farm-registry service's explicit organic flag on fields."

**Step 3: Add enterpriseType to the findFirst where clause**

Update the existing `findFirst` call:
```typescript
let enterprise = await prisma.fieldEnterprise.findFirst({
  where: { fieldId: matchedField.id, cropYear, crop: bf.crop, label: null, enterpriseType },
});
```

**Step 4: Add enterpriseType to the create data**

When creating a new FieldEnterprise record, include:
```typescript
enterpriseType,
```
in the data object.

**Step 5: Update results reporting**

The response JSON should still report `matched`, `created`, `updated`, `unmatched` counts. No structural change needed — just the numbers will be larger (56 fields instead of ~12).

Do NOT change:
- The actuals fields (actualYieldPerAcre, SeedUsage.actualPricePerUnit, MaterialUsage.actualTotalCost) — these must NEVER be touched by the sync
- The seed, material, and operation upsert logic within each enterprise — only the enterprise-level filter and match key change
- The farm-budget fetch URLs or authentication
- The field name matching logic (bf.name.toLowerCase() against fieldNameMap)

Also update the `budget-client.ts` if there is a function like `getBudgetOrganicFields()` that filters to organic only — rename or add a `getBudgetAllFields()` variant. Check the actual implementation before deciding. If the client already returns all fields and the filter is only in sync-macro, then only sync-macro needs changing.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Read sync-macro/route.ts and confirm: (1) no organic filter exists, (2) enterpriseType derived from matchedField.organicStatus, (3) enterpriseType in findFirst where clause, (4) enterpriseType in create data</manual>
  </verify>
  <done>sync-macro processes all enterprises from farm-budget; enterpriseType derived from registry organicStatus; findFirst uses enterpriseType in match key; create includes enterpriseType; TypeScript compiles; existing actuals fields untouched in upsert</done>
</task>

</tasks>

<verification>
1. `npx prisma validate` passes — schema is valid
2. `npx tsc --noEmit` passes — no TypeScript errors
3. `grep -n "organicEnterprises\|organicEntIds\|organicFields" src/app/api/fields/sync-macro/route.ts` returns no results — organic filter removed
4. `grep -n "enterpriseType" src/app/api/fields/sync-macro/route.ts` returns matches in findFirst where clause AND create data
5. `grep -n "EnterpriseType" prisma/schema.prisma` shows enum definition and field usage
</verification>

<success_criteria>
- Schema has EnterpriseType enum with ORGANIC and CONVENTIONAL values
- FieldEnterprise @@unique includes enterpriseType
- sync-macro route iterates all fields (no organic filter)
- enterpriseType derived from matchedField.organicStatus, not budget category
- TypeScript compiles cleanly
- No actuals fields modified in sync upsert logic
</success_criteria>

<output>
After completion, create `.planning/phases/07-all-enterprise-sync/07-01-SUMMARY.md`
</output>
