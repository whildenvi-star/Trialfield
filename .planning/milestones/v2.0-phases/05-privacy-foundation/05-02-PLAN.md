---
phase: 05-privacy-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/app/(app)/field-enterprises/[id]/page.tsx
  - src/types/next-auth.d.ts
autonomous: false
requirements:
  - PRIV-01
  - PRIV-03

must_haves:
  truths:
    - "CREW user viewing an enterprise detail page does not see the Budget tab trigger in the DOM"
    - "OFFICE user viewing the Budget tab sees seed, material, and operation cost tables but no revenue projection section, no margin cards, no profit/acre"
    - "ADMIN user viewing the Budget tab sees everything including revenue projection and financial summary cards"
    - "The BudgetSummary TypeScript interface marks revenueProjection as optional so the client handles its absence without type errors"
  artifacts:
    - path: "src/app/(app)/field-enterprises/[id]/page.tsx"
      provides: "Role-conditional Budget tab and revenue section rendering"
      contains: "useSession"
    - path: "src/types/next-auth.d.ts"
      provides: "Session type with role field"
      contains: "role"
  key_links:
    - from: "src/app/(app)/field-enterprises/[id]/page.tsx"
      to: "next-auth/react"
      via: "useSession() hook for client-side role access"
      pattern: "useSession"
    - from: "src/app/(app)/field-enterprises/[id]/page.tsx"
      to: "src/app/api/field-enterprises/[id]/budget-summary/route.ts"
      via: "fetch call to /api/field-enterprises/[id]/budget-summary"
      pattern: "budget-summary"
---

<objective>
Add defense-in-depth UI filtering: hide the Budget tab from CREW, hide revenue/financial sections from OFFICE, and update TypeScript types for the conditional API response shape.

Purpose: The API (Plan 01) is the authoritative enforcement layer. This plan adds the UI layer so CREW never sees a Budget tab and OFFICE never sees revenue/margin cards -- even if they inspect the page source. Per user decision: "No indication to OFFICE that data was withheld -- it looks like the data doesn't exist."

Output: Modified enterprise detail page with role-conditional rendering, updated TypeScript interface for optional revenueProjection.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-privacy-foundation/05-CONTEXT.md
@.planning/phases/05-privacy-foundation/05-RESEARCH.md
@.planning/phases/05-privacy-foundation/05-01-SUMMARY.md

# Source files to modify
@/Users/glomalinguild/Desktop/my-project-one/organic-cert/src/app/(app)/field-enterprises/[id]/page.tsx
@/Users/glomalinguild/Desktop/my-project-one/organic-cert/src/types/next-auth.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role-conditional rendering to enterprise detail page</name>
  <files>
    /Users/glomalinguild/Desktop/my-project-one/organic-cert/src/app/(app)/field-enterprises/[id]/page.tsx
    /Users/glomalinguild/Desktop/my-project-one/organic-cert/src/types/next-auth.d.ts
  </files>
  <action>
**Update next-auth.d.ts type declaration:**
Ensure the Session User type includes `role: string` (or the Role enum type) so `session.user.role` is typed. Check if the file already extends the Session type -- if so, verify `role` is included. If not, add the augmentation:
```typescript
declare module "next-auth" {
  interface Session {
    user: {
      role: string;
      // ...other existing fields
    } & DefaultSession["user"];
  }
}
```

**Update BudgetSummary interface in page.tsx:**
Find the TypeScript interface or type alias for the budget summary response (likely named `BudgetSummary` or similar). Mark `revenueProjection` as optional: `revenueProjection?: { ... } | null`. This prevents type errors when the API omits the field for OFFICE users. Also mark `fallowCost.category` as optional if it exists in the type.

**Add useSession() for client-side role access:**
The enterprise detail page is a `"use client"` component. Import `useSession` from `next-auth/react`. At the top of the component, add:
```typescript
const { data: session } = useSession();
const role = (session?.user as any)?.role ?? null;
const canSeeBudget = role === "ADMIN" || role === "OFFICE";
const canSeeFinancial = role === "ADMIN";
```

**Hide Budget tab trigger for CREW:**
Find the `TabsTrigger` for the Budget tab (likely `<TabsTrigger value="budget">Budget</TabsTrigger>`). Wrap it in a conditional: `{canSeeBudget && (<TabsTrigger value="budget">Budget</TabsTrigger>)}`. Also wrap the corresponding `TabsContent` for budget in the same conditional.

**Hide revenue/financial sections for OFFICE:**
Inside the Budget tab content, find the revenue projection section (likely a card or section rendering `revenueProjection` data -- may include revenue, gross margin, profit/acre, sale price). Wrap it: `{canSeeFinancial && budgetSummary?.revenueProjection && (<RevenueProjectionSection ... />)}` or equivalent.

Also find and hide:
- Revenue/margin summary cards (Total Revenue, Gross Margin, Profit/Acre) -- wrap with `canSeeFinancial`
- Any display of: land rent, overhead, drying costs, interest, crop insurance, sale prices -- wrap with `canSeeFinancial`
- Keep visible for all roles: Total Cost card, Cost/Acre card, seed/material/operation cost tables

Per user decision: "Revenue/margin summary cards simply don't render -- only Total Cost and Cost/Acre cards appear" for OFFICE. Same "Budget" tab title. No "restricted" badges, lock icons, or "admin only" labels anywhere.

**IMPORTANT:** This is a ~1300-line file. Make surgical changes -- do not refactor or restructure. Only add the useSession import, role variables, and conditional wrappers around the specific elements listed above.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Search page.tsx for: (1) useSession import present, (2) canSeeBudget wraps Budget TabsTrigger, (3) canSeeFinancial wraps revenue/margin sections, (4) no "restricted" or "admin only" text added</manual>
  </verify>
  <done>CREW users cannot see the Budget tab in the DOM. OFFICE users see the Budget tab with cost tables but no revenue/margin/profit sections. ADMIN sees everything unchanged. No UI indicators of filtering exist. TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify privacy enforcement end-to-end</name>
  <files>no files modified -- verification only</files>
  <action>
Human verifies the complete privacy foundation end-to-end across all access vectors.

**What was built:** Complete privacy foundation -- auth fallback removed (Plan 01), RBAC permissions enforced (Plan 01), API field stripping (Plan 01), UI role filtering (Task 1 above). All four PRIV requirements implemented across API and UI layers.

**How to verify:**

1. Start the organic-cert dev server: `cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npm run dev`
2. Open browser to http://localhost:3004 (or the correct port)

**Test PRIV-02 -- Unauthenticated returns 401:**
3. In a terminal, run: `curl -s http://localhost:3004/api/field-enterprises/[SOME-ID]/budget-summary | jq .`
4. Expected: `{"error": "Unauthorized"}` with 401 status (NOT a full data response)

**Test PRIV-03 + PRIV-01 -- OFFICE sees costs but not financial data:**
5. Log in as Sandy (OFFICE role)
6. Navigate to any enterprise detail page, click the Budget tab
7. Verify you see: seed costs, material costs, operation costs, Total Cost, Cost/Acre
8. Verify you do NOT see: revenue, gross margin, profit/acre, sale prices, land rent, overhead, drying, interest, insurance
9. Open DevTools Network tab, find the budget-summary API response
10. Verify the JSON response has NO keys for: revenueProjection, margin, profit, salePrice, revenue

**Test PRIV-03 -- CREW gets 403:**
11. Log in as a CREW user (if one exists)
12. Navigate to an enterprise detail page
13. Verify the Budget tab is NOT visible in the tab bar
14. In DevTools console, run: `fetch('/api/field-enterprises/[SOME-ID]/budget-summary').then(r => r.json()).then(console.log)`
15. Expected: `{"error": "Forbidden"}` with 403 status

**Test ADMIN sees everything:**
16. Log in as ADMIN
17. Navigate to enterprise Budget tab
18. Verify all data visible including revenue projection, margin, profit/acre

**NOTE:** If the iframe deployment breaks (all requests return 401 even when logged in), this is the known iframe cookie issue from RESEARCH.md. Verify using direct login at localhost:3004 first. The portal iframe auth (X-Auth-Token) is a separate concern.

**Resume signal:** Type "approved" if all checks pass, or describe which checks failed and what you observed.
  </action>
  <verify>Human confirms all four test scenarios pass (unauthenticated 401, OFFICE filtered, CREW 403, ADMIN full access)</verify>
  <done>All four PRIV requirements verified working end-to-end by human testing across API and UI layers.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. CREW user: Budget tab absent from DOM, API returns 403
3. OFFICE user: Budget tab visible with cost data, no financial fields in API response or UI
4. ADMIN user: Full Budget tab with all data
5. Unauthenticated curl: 401 response with no data leak
</verification>

<success_criteria>
- Budget tab hidden from CREW in the enterprise detail page DOM
- Revenue/margin/profit sections hidden from OFFICE -- "it looks like the data doesn't exist"
- ADMIN experience unchanged -- all data visible
- TypeScript compiles with optional revenueProjection type
- Human verification confirms all four PRIV requirements work end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/05-privacy-foundation/05-02-SUMMARY.md`
</output>
