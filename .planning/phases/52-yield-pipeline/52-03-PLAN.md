---
phase: 52-yield-pipeline
plan: 03
type: execute
wave: 1
depends_on: ["52-02"]
files_modified:
  - glomalin-portal/src/components/insurance/insurance-workspace.tsx
  - farm-budget/public/dashboard.js
autonomous: true
gap_closure: true
requirements: [PIPE-04]

must_haves:
  truths:
    - "Insurance policy with no synced yield shows 'No yield data yet' text"
    - "Farm-budget crop row with no grain-ticket data shows '(no GT data)' next to projected yield"
  artifacts:
    - path: "glomalin-portal/src/components/insurance/insurance-workspace.tsx"
      provides: "Updated empty-state text for unsynced yield"
      contains: "No yield data yet"
    - path: "farm-budget/public/dashboard.js"
      provides: "Subtle indicator when no grain-ticket yield available"
      contains: "no GT data"
  key_links:
    - from: "insurance-workspace.tsx"
      to: "insurance_policies.actual_synced_from_grain"
      via: "conditional render when flag is false/null"
      pattern: "No yield data yet"
    - from: "dashboard.js"
      to: "grainYields cache"
      via: "findGrainYieldForCrop returns null"
      pattern: "no GT data"
---

<objective>
Close SC-4 verification gap: update empty-state wording in both insurance and farm-budget UIs when no grain-ticket yield has been synced.

Purpose: The verifier flagged that insurance shows "No grain tickets recorded for this field/crop yet" (functionally correct but doesn't match SC-4's "No yield data yet" phrasing), and farm-budget shows no indicator at all when grain yield isn't synced.

Output: Two small UI text changes that satisfy SC-4 exactly.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/52-yield-pipeline/52-02-SUMMARY.md
@glomalin-portal/src/components/insurance/insurance-workspace.tsx
@farm-budget/public/dashboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix empty-state wording in insurance and budget UIs</name>
  <files>glomalin-portal/src/components/insurance/insurance-workspace.tsx, farm-budget/public/dashboard.js</files>
  <action>
Two targeted changes:

1. **insurance-workspace.tsx** (~line 432): Change the title attribute on the muted dash span from `"No grain tickets recorded for this field/crop yet"` to `"No yield data yet"`. Keep the muted dash `—` and the `text-glomalin-muted` class unchanged — only the tooltip text changes.

2. **farm-budget/public/dashboard.js** (~line 247-248): After the existing `actualYieldText` block (the `if (grainMatch && grainMatch.yieldPerAcre > 0)` block ending around line 247), add an `else` clause that sets `actualYieldText` to a subtle indicator when no grain data exists. Use:
```javascript
} else {
  actualYieldText = ' <span style="font-size:0.7em;opacity:0.5;font-style:italic;" title="No yield data yet">(no GT data)</span>';
}
```
This renders "(no GT data)" in small, muted italic text next to the projected yield, with a "No yield data yet" tooltip on hover. It does not replace the projected yield — it supplements it as a visual cue that grain-ticket actuals haven't arrived.
  </action>
  <verify>
    <automated>grep -n "No yield data yet" glomalin-portal/src/components/insurance/insurance-workspace.tsx && grep -n "no GT data" farm-budget/public/dashboard.js</automated>
    <manual>In the insurance workspace, hover the muted dash for an unsynced policy — tooltip reads "No yield data yet". In the farm-budget dashboard, crop rows without grain data show "(no GT data)" in small italic text next to the projected yield.</manual>
  </verify>
  <done>Insurance empty state shows "No yield data yet" tooltip; farm-budget shows "(no GT data)" indicator next to projected yield when no grain-ticket data exists. SC-4 fully satisfied.</done>
</task>

</tasks>

<verification>
- `grep "No yield data yet" glomalin-portal/src/components/insurance/insurance-workspace.tsx` returns a match
- `grep "no GT data" farm-budget/public/dashboard.js` returns a match
- No other functionality changed — GT badge, variance display, push pipeline all untouched
</verification>

<success_criteria>
SC-4 fully closed: both insurance and farm-budget UIs show appropriate empty-state indicators when no grain-ticket yield has been synced.
</success_criteria>

<output>
After completion, create `.planning/phases/52-yield-pipeline/52-03-SUMMARY.md`
</output>
