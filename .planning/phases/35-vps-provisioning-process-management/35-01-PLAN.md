---
phase: 35-vps-provisioning-process-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ecosystem.config.js
  - glomalin-portal/package.json
  - organic-cert/package.json
autonomous: true
requirements:
  - INFRA-01
  - INFRA-06

must_haves:
  truths:
    - "Running pm2 start ecosystem.config.js launches all 8 apps and they stay up"
    - "Both Next.js apps run via next start in production mode with correct ports"
  artifacts:
    - path: "ecosystem.config.js"
      provides: "PM2 process management config for all 8 apps"
      min_lines: 40
  key_links:
    - from: "ecosystem.config.js"
      to: "glomalin-portal/package.json"
      via: "script reference to next start"
      pattern: "next start"
    - from: "ecosystem.config.js"
      to: "organic-cert/package.json"
      via: "script reference to next start with port"
      pattern: "next start.*3004"
---

<objective>
Create PM2 ecosystem config for all 8 apps and configure Next.js production builds.

Purpose: Enable single-command startup of all 8 apps with crash recovery, proper port isolation, and production mode for Next.js apps.
Output: ecosystem.config.js at repo root, production-ready Next.js start scripts.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-vps-provisioning-process-management/35-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PM2 ecosystem config for all 8 apps</name>
  <files>ecosystem.config.js</files>
  <action>
Create `ecosystem.config.js` at repository root with all 8 apps configured:

Port assignments (document in comments):
- glomalin-portal: 3000 (Next.js default)
- farm-budget: 3001
- fsa-acres: 3002
- meristem-malt: 3003
- organic-cert: 3004
- farm-registry: 3005
- seed-inventory: 3006
- grain-tickets: 3007 (changed from 3000 to avoid portal conflict)

For each Express app (6 total: grain-tickets, farm-budget, fsa-acres, meristem-malt, farm-registry, seed-inventory):
- name: app directory name (e.g., "grain-tickets")
- script: "server.js"
- cwd: relative path to app directory (e.g., "./grain-tickets")
- env: { NODE_ENV: "production", PORT: assigned_port }
- instances: 1 (JSON-backed apps cannot multi-instance)
- autorestart: true
- max_restarts: 10
- restart_delay: 5000
- watch: false (manual git-pull deploy, not file-watch)
- max_memory_restart: "256M"

For glomalin-portal (Next.js 14):
- name: "glomalin-portal"
- script: "node_modules/.bin/next"
- args: "start"
- cwd: "./glomalin-portal"
- env: { NODE_ENV: "production", PORT: 3000 }
- autorestart: true, max_restarts: 10, restart_delay: 5000
- max_memory_restart: "512M"

For organic-cert (Next.js 16):
- name: "organic-cert"
- script: "node_modules/.bin/next"
- args: "start -p 3004"
- cwd: "./organic-cert"
- env: { NODE_ENV: "production", PORT: 3004 }
- autorestart: true, max_restarts: 10, restart_delay: 5000
- max_memory_restart: "512M"

Add header comment explaining: port map, placeholder domain (farm.example.com per user decision), deploy workflow (git pull, npm install in each app, pm2 restart ecosystem.config.js).
  </action>
  <verify>
    <automated>node -e "const cfg = require('./ecosystem.config.js'); const apps = cfg.apps; if (apps.length !== 8) throw new Error('Expected 8 apps, got ' + apps.length); const ports = apps.map(a => a.env.PORT); if (new Set(ports).size !== 8) throw new Error('Duplicate ports: ' + ports); const gt = apps.find(a => a.name === 'grain-tickets'); if (gt.env.PORT !== 3007) throw new Error('grain-tickets port should be 3007'); console.log('OK: 8 apps, unique ports, grain-tickets on 3007');"</automated>
    <manual>Review ecosystem.config.js for correct cwd paths and script references</manual>
  </verify>
  <done>ecosystem.config.js exists with 8 apps, unique ports, auto-restart, production NODE_ENV</done>
</task>

<task type="auto">
  <name>Task 2: Configure Next.js production build and start scripts</name>
  <files>glomalin-portal/package.json, organic-cert/package.json</files>
  <action>
Verify both Next.js apps have correct production scripts in package.json:

glomalin-portal/package.json:
- Confirm "build": "next build" exists (it does)
- Confirm "start": "next start" exists (it does)
- No changes needed if both are present

organic-cert/package.json:
- Confirm "build": "next build" exists (it does)
- Update "start" script to: "next start -p 3004" (currently just "next start" which would default to 3000, conflicting with portal)
- This ensures organic-cert always starts on 3004 whether run via PM2 or standalone

Add a "prod" convenience script to each if not present:
- glomalin-portal: "prod": "next build && next start"
- organic-cert: "prod": "next build && next start -p 3004"

These are convenience scripts for manual testing; PM2 runs build separately from start.
  </action>
  <verify>
    <automated>node -e "const gp = require('./glomalin-portal/package.json'); const oc = require('./organic-cert/package.json'); if (!gp.scripts.build.includes('next build')) throw new Error('portal missing build'); if (!gp.scripts.start.includes('next start')) throw new Error('portal missing start'); if (!oc.scripts.start.includes('3004')) throw new Error('organic-cert start should use port 3004'); console.log('OK: both Next.js apps have production scripts');"</automated>
  </verify>
  <done>Both Next.js apps have build and start scripts configured for production mode with correct ports. organic-cert explicitly starts on port 3004.</done>
</task>

</tasks>

<verification>
- ecosystem.config.js exists at repo root and is valid JS (node -e "require('./ecosystem.config.js')")
- All 8 apps listed with unique ports
- organic-cert start script includes -p 3004
- Both Next.js apps have build + start scripts
</verification>

<success_criteria>
- PM2 ecosystem config defines all 8 apps with auto-restart, unique ports, production NODE_ENV
- Both Next.js apps can build and start in production mode
</success_criteria>

<output>
After completion, create `.planning/phases/35-vps-provisioning-process-management/35-01-SUMMARY.md`
</output>
