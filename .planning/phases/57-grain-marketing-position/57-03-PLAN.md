---
phase: 57-grain-marketing-position
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/components/marketing/marketing-workspace.tsx
  - glomalin-portal/src/app/(protected)/app/marketing/page.tsx
  - glomalin-portal/src/lib/modules.ts
autonomous: true
requirements: [MKT-01, MKT-02, MKT-03]
gap_closure: true

must_haves:
  truths:
    - "After any contract CRUD operation (add/edit/delete), Est. Production column retains its real value — does not drop to 0 or dash"
    - "Marketing module appears in portal dashboard cards and is navigable without a direct URL"
  artifacts:
    - path: "glomalin-portal/src/components/marketing/marketing-workspace.tsx"
      provides: "yieldSummaries prop wired from page.tsx, used in client-side recomputePositions"
      contains: "yieldSummaries: YieldSummary[]"
    - path: "glomalin-portal/src/lib/modules.ts"
      provides: "marketing module entry in MODULES array"
      contains: "id: 'marketing'"
  key_links:
    - from: "glomalin-portal/src/app/(protected)/app/marketing/page.tsx"
      to: "glomalin-portal/src/components/marketing/marketing-workspace.tsx"
      via: "yieldSummaries prop passed to MarketingWorkspace"
      pattern: "yieldSummaries=\\{yieldSummaries\\}"
    - from: "glomalin-portal/src/lib/modules.ts"
      to: "dashboard card rendering"
      via: "MODULES array includes marketing entry"
      pattern: "id: 'marketing'"
---

<objective>
Close two verification gaps from Phase 57: (1) fix the yieldSummaries prop bug that causes Est. Production to zero out after any contract CRUD operation, and (2) register the marketing module in the MODULES array so it appears in portal navigation.

Purpose: Phase 57 verification found 2/7 truths failing — one blocker (position data regression after CRUD) and one warning (module not discoverable via navigation). Both are wiring gaps, not missing features.
Output: Two patched files restoring full Phase 57 goal achievement.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-grain-marketing-position/57-02-SUMMARY.md
@.planning/phases/57-grain-marketing-position/57-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire yieldSummaries prop from page.tsx to MarketingWorkspace</name>
  <files>
    glomalin-portal/src/components/marketing/marketing-workspace.tsx
    glomalin-portal/src/app/(protected)/app/marketing/page.tsx
  </files>
  <action>
Fix the yieldSummaries prop gap that causes Est. Production to zero out after any contract CRUD operation.

In marketing-workspace.tsx:
1. Add `yieldSummaries: YieldSummary[]` to the `MarketingWorkspaceProps` interface (after `yieldAvailable: boolean`, around line 16)
2. Destructure `yieldSummaries: initialYieldSummaries` from props in the component function signature
3. Change line 169 from `const [yieldSummaries] = useState<YieldSummary[]>([])` to `const [yieldSummaries] = useState<YieldSummary[]>(initialYieldSummaries)` — this initializes the client-side state from the SSR-loaded yield data instead of an empty array

In page.tsx:
1. Add `yieldSummaries={yieldSummaries}` prop to the `<MarketingWorkspace>` JSX (around line 192, alongside the existing props like `initialContracts`, `cbotPrices`, etc.)

The yieldSummaries variable is already computed in page.tsx (lines 165-168) from the Promise.allSettled yield result — it just is not passed down. This is a one-line addition to page.tsx and a three-line edit to marketing-workspace.tsx.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit /app/marketing with grain-tickets running. Add a cash corn contract for 5000 bu. After save, confirm Est. Production column retains its value (does not drop to 0 or dash).</manual>
  </verify>
  <done>MarketingWorkspaceProps includes yieldSummaries, page.tsx passes it, useState initializes from prop. After any CRUD operation, recomputePositions uses real yield data instead of empty array — Est. Production remains correct.</done>
</task>

<task type="auto">
  <name>Task 2: Register marketing module in MODULES array</name>
  <files>glomalin-portal/src/lib/modules.ts</files>
  <action>
Add a marketing module entry to the MODULES array in src/lib/modules.ts so the module appears in dashboard cards and portal navigation.

Add this entry after the 'claims' entry (line 67, before 'macro-rollup') to group native portal modules together:

```typescript
{
  id: 'marketing',
  label: 'Grain Marketing',
  sublabel: 'Position & Contracts',
  route: '/app/marketing',
  status: 'live',
  type: 'native',
},
```

The marketing page is a native Next.js route (not an embed), uses the same `(protected)/app/` layout as fsa-578/insurance/claims. Place it after claims in the array since it is the next native module in the domain workflow (insurance -> marketing position).
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -A5 "id: 'marketing'" glomalin-portal/src/lib/modules.ts</automated>
    <manual>Log in to portal dashboard. Confirm "Grain Marketing" card is visible. Click it to navigate to /app/marketing.</manual>
  </verify>
  <done>MODULES array includes marketing entry with id 'marketing', label 'Grain Marketing', route '/app/marketing', status 'live', type 'native'. Module card appears on dashboard.</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with no errors: `cd glomalin-portal && npx tsc --noEmit`
2. grep confirms yieldSummaries in MarketingWorkspaceProps: `grep 'yieldSummaries' glomalin-portal/src/components/marketing/marketing-workspace.tsx`
3. grep confirms marketing in MODULES: `grep "'marketing'" glomalin-portal/src/lib/modules.ts`
4. grep confirms prop passed in page.tsx: `grep 'yieldSummaries=' glomalin-portal/src/app/\(protected\)/app/marketing/page.tsx`
</verification>

<success_criteria>
- Est. Production column retains real yield values after contract add/edit/delete (yieldSummaries wired from SSR to client state)
- Marketing module appears in portal dashboard navigation cards (MODULES array includes 'marketing' entry)
- TypeScript compiles cleanly
- All 7/7 verification truths pass (upgrading the 2 partial/failed to verified)
</success_criteria>

<output>
After completion, create `.planning/phases/57-grain-marketing-position/57-03-SUMMARY.md`
</output>
