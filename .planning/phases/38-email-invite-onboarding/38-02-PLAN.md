---
phase: 38-email-invite-onboarding
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified: []
autonomous: false
requirements: [ONB-01, ONB-02, ONB-03]

must_haves:
  truths:
    - "Admin invites a real coworker email and they receive the signup email"
    - "Invited user completes signup and sees their granted modules on dashboard"
    - "Password reset email arrives and user can set a new password"
  artifacts: []
  key_links: []
---

<objective>
Verify the full onboarding flow works end-to-end: admin invite, user signup, module access, and password reset.

Purpose: These flows involve Supabase email delivery and browser redirects that cannot be verified by automated tests alone. A human must confirm emails arrive and links work.
Output: Confirmed working onboarding path for production deployment.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/38-email-invite-onboarding/38-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify invite, signup, and password reset flows</name>
  <files></files>
  <action>
Human verification checkpoint -- no code changes. The executor should present the verification steps below to the user and wait for confirmation.

Before presenting to the user, the executor should ensure the Next.js dev server is running (`cd glomalin-portal && npm run dev`) so the user can test the flows.
  </action>
  <verify>
User confirms all three flows work by typing "approved".

Test locally (or in production if deployed):

1. INVITE FLOW (ONB-01):
   - Log in as admin, go to /admin
   - Enter a test email address in the invite form
   - Select a role and check some module access boxes
   - Click "Invite User"
   - Verify the invite success message appears
   - Check the test email inbox for a Supabase invite email
   - Verify the email link points to your domain (not localhost)

2. SIGNUP FLOW (ONB-02):
   - Click the invite link from the email
   - Verify you land on the "Set New Password" page
   - Enter and confirm a password
   - Click "Update Password"
   - Verify you land on /dashboard
   - Verify the dashboard shows only the modules that were granted during invite

3. PASSWORD RESET (ONB-03):
   - Log out
   - Click "Forgot your password?" on the login page
   - Enter the same email address
   - Click "Send Reset Link"
   - Verify success message appears
   - Check email for reset link
   - Click reset link, verify you land on "Set New Password" page
   - Set a new password, verify redirect to dashboard

Note: If testing locally without NEXT_PUBLIC_SITE_URL set, email links will use localhost origin which is correct for local dev. For production, ensure NEXT_PUBLIC_SITE_URL is set in .env and that Supabase project has the production domain in its redirect allow-list (Authentication > URL Configuration > Redirect URLs).
  </verify>
  <done>User has confirmed all three onboarding flows (invite, signup, password reset) work end-to-end. ONB-01, ONB-02, ONB-03 verified.</done>
</task>

</tasks>

<verification>
All three onboarding success criteria from the roadmap are verified by human testing:
1. Admin invite sends email with production domain link
2. Invited user sets password and sees granted modules
3. Password reset flow completes successfully
</verification>

<success_criteria>
- User confirms all three onboarding flows work end-to-end
- Email links point to correct domain (not localhost in production)
- Invited users see only their granted modules on dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/38-email-invite-onboarding/38-02-SUMMARY.md`
</output>
