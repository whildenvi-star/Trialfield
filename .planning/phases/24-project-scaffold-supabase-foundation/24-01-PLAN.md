---
phase: 24-project-scaffold-supabase-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/package.json
  - glomalin-portal/tailwind.config.ts
  - glomalin-portal/src/app/layout.tsx
  - glomalin-portal/src/app/page.tsx
  - glomalin-portal/src/app/globals.css
  - glomalin-portal/next.config.mjs
  - glomalin-portal/tsconfig.json
autonomous: true
requirements:
  - SCF-01
  - SCF-02

must_haves:
  truths:
    - "npm run dev in glomalin-portal/ starts a Next.js 14 App Router dev server with no errors"
    - "The root page renders with the dark soil background (#080604), accent color (#C8860A), and JetBrains Mono font"
    - "Tailwind config contains all dark soil palette tokens (bg, surface, border, accent, text, muted, green)"
  artifacts:
    - path: "glomalin-portal/package.json"
      provides: "Next.js 14 project manifest with dependencies"
      contains: "next"
    - path: "glomalin-portal/tailwind.config.ts"
      provides: "Dark soil color palette and JetBrains Mono font config"
      contains: "#080604"
    - path: "glomalin-portal/src/app/layout.tsx"
      provides: "Root layout with JetBrains Mono Google Font and dark body"
      contains: "JetBrains"
    - path: "glomalin-portal/src/app/page.tsx"
      provides: "Root page confirming scaffold renders"
      min_lines: 10
  key_links:
    - from: "glomalin-portal/tailwind.config.ts"
      to: "glomalin-portal/src/app/globals.css"
      via: "Tailwind directives import config colors"
      pattern: "@tailwind"
    - from: "glomalin-portal/src/app/layout.tsx"
      to: "Google Fonts"
      via: "next/font/google JetBrains_Mono import"
      pattern: "JetBrains_Mono"
---

<objective>
Scaffold the glomalin-portal/ Next.js 14 App Router project with Tailwind CSS configured for the dark soil aesthetic and JetBrains Mono typography.

Purpose: Create the foundational project structure that all subsequent phases build on. The portal must start clean and render the dark soil visual identity from day one.
Output: A working Next.js 14 dev server with dark soil palette visible on the root page.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-project-scaffold-supabase-foundation/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 14 App Router project</name>
  <files>
    glomalin-portal/package.json
    glomalin-portal/tsconfig.json
    glomalin-portal/next.config.mjs
    glomalin-portal/src/app/layout.tsx
    glomalin-portal/src/app/page.tsx
    glomalin-portal/src/app/globals.css
  </files>
  <action>
    Run `npx create-next-app@14 glomalin-portal` with these options:
    - TypeScript: yes
    - ESLint: yes
    - Tailwind CSS: yes
    - src/ directory: yes
    - App Router: yes
    - Import alias: @/* (default)

    IMPORTANT: Use Next.js 14 specifically (not 15 or 16). The command should pin to v14: `npx create-next-app@14`.
    Use npm as the package manager (consistent with existing farm-budget, grain-tickets modules).

    After scaffolding, verify the generated structure has src/app/layout.tsx and src/app/page.tsx.
    Run `npm run dev` briefly to confirm it starts without errors, then stop it.
  </action>
  <verify>
    <automated>cd glomalin-portal && cat package.json | grep '"next"' && ls src/app/layout.tsx src/app/page.tsx</automated>
    <manual>package.json shows next@14.x, layout.tsx and page.tsx exist</manual>
  </verify>
  <done>glomalin-portal/ directory exists with Next.js 14 App Router scaffold, npm dependencies installed, dev server starts without errors</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind dark soil palette and JetBrains Mono typography</name>
  <files>
    glomalin-portal/tailwind.config.ts
    glomalin-portal/src/app/globals.css
    glomalin-portal/src/app/layout.tsx
    glomalin-portal/src/app/page.tsx
  </files>
  <action>
    1. **tailwind.config.ts** — Extend the theme with the dark soil color palette:
       ```
       colors: {
         soil: {
           bg: '#080604',
           surface: '#0e0c0b',
           border: '#2a2218',
           accent: '#C8860A',
           text: '#e8d8c0',
           muted: '#6a5a4a',
           green: '#7A9E7E',
         }
       }
       ```
       Add fontFamily override: `mono: ['JetBrains Mono', 'monospace']`
       Ensure content paths include `./src/**/*.{js,ts,jsx,tsx,mdx}`.

    2. **globals.css** — Set base styles:
       - `body` background: `bg-soil-bg`, text: `text-soil-text`
       - Ensure Tailwind base/components/utilities directives are present
       - Add a base layer setting `body { @apply bg-soil-bg text-soil-text font-mono; }`

    3. **layout.tsx** — Import JetBrains Mono via `next/font/google`:
       ```tsx
       import { JetBrains_Mono } from 'next/font/google'
       const jetbrains = JetBrains_Mono({ subsets: ['latin'], variable: '--font-mono' })
       ```
       Apply the font variable class to `<html>` element.
       Set metadata: title "GLOMALIN", description "Farm Operations Portal".

    4. **page.tsx** — Replace default content with a simple dark soil confirmation page:
       - Full-height flex column, centered
       - "GLOMALIN" wordmark in large text using `text-soil-accent font-mono font-bold tracking-widest`
       - Subtitle "Farm Operations Portal" in `text-soil-muted`
       - A surface-colored card with border to confirm palette tokens work:
         `bg-soil-surface border border-soil-border rounded-md p-6`
       - Inside the card: "Portal scaffold active" text

    This creates the visual identity from the first render. All-mono aesthetic — the portal should feel like a farm operations terminal per user's vision.
  </action>
  <verify>
    <automated>cd glomalin-portal && npx next build 2>&1 | tail -5</automated>
    <manual>Run npm run dev, open browser — dark background, amber GLOMALIN wordmark, monospace font visible</manual>
  </verify>
  <done>Tailwind config has all 7 dark soil tokens, JetBrains Mono loads via Google Fonts, root page renders dark soil aesthetic with GLOMALIN wordmark, build succeeds with no errors</done>
</task>

</tasks>

<verification>
1. `cd glomalin-portal && npm run dev` starts on localhost with no errors
2. Root page shows dark background (#080604), GLOMALIN in accent gold (#C8860A), JetBrains Mono font
3. `grep -c '#080604' glomalin-portal/tailwind.config.ts` returns 1 (palette configured)
4. `npx next build` in glomalin-portal/ completes with no errors
</verification>

<success_criteria>
- Next.js 14 App Router project exists in glomalin-portal/ with TypeScript, Tailwind, and ESLint
- Dark soil palette (7 colors) configured in Tailwind theme extension
- JetBrains Mono loaded via next/font/google and applied as default mono font
- Root page renders GLOMALIN wordmark with dark soil aesthetic
- Dev server starts and build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/24-project-scaffold-supabase-foundation/24-01-SUMMARY.md`
</output>
