---
phase: 28-fsa-planting-workflow-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/app/(protected)/app/fsa-578/page.tsx
  - glomalin-portal/src/app/api/fsa/clu-records/[id]/route.ts
  - glomalin-portal/src/app/api/fsa/clu-records/bulk-update/route.ts
  - glomalin-portal/src/components/fsa/clu-workspace.tsx
  - glomalin-portal/src/components/fsa/farm-accordion.tsx
  - glomalin-portal/src/components/fsa/tract-accordion.tsx
  - glomalin-portal/src/components/fsa/clu-card.tsx
  - glomalin-portal/src/components/fsa/bulk-action-bar.tsx
  - glomalin-portal/src/components/fsa/crop-typeahead.tsx
  - glomalin-portal/src/components/fsa/confirm-dialog.tsx
  - glomalin-portal/src/lib/fsa/fsa-crop-list.ts
autonomous: true
requirements: [FSA-02, FSA-03, FSA-04]

must_haves:
  truths:
    - "User can see CLU records as cards grouped by Farm/Tract with Reported/Unreported status badges and warning badges"
    - "User can click a CLU card to expand it inline and edit crop, practice, planting date, and organic flag, then save"
    - "User can select multiple CLU cards via checkboxes and bulk-mark them as Reported or Unreported via a sticky bottom action bar"
    - "User can bulk-assign a crop to multiple selected CLUs"
    - "Accordion sections with unreported CLUs or warnings start expanded by default"
  artifacts:
    - path: "glomalin-portal/src/components/fsa/clu-workspace.tsx"
      provides: "Top-level 'use client' container managing records, selectedIds, expandedId, warnings state"
      min_lines: 80
    - path: "glomalin-portal/src/components/fsa/clu-card.tsx"
      provides: "Individual CLU card with collapsed/expanded view, inline editing, save button"
      min_lines: 60
    - path: "glomalin-portal/src/components/fsa/bulk-action-bar.tsx"
      provides: "Sticky bottom bar with selection count and bulk action buttons"
      min_lines: 30
    - path: "glomalin-portal/src/app/api/fsa/clu-records/[id]/route.ts"
      provides: "PATCH endpoint for single CLU record update"
      exports: ["PATCH"]
    - path: "glomalin-portal/src/app/api/fsa/clu-records/bulk-update/route.ts"
      provides: "POST endpoint for bulk CLU operations"
      exports: ["POST"]
  key_links:
    - from: "glomalin-portal/src/components/fsa/clu-card.tsx"
      to: "/api/fsa/clu-records/[id]"
      via: "fetch PATCH on save"
      pattern: "fetch.*api/fsa/clu-records.*PATCH"
    - from: "glomalin-portal/src/components/fsa/bulk-action-bar.tsx"
      to: "/api/fsa/clu-records/bulk-update"
      via: "fetch POST on bulk action"
      pattern: "fetch.*api/fsa/clu-records/bulk-update.*POST"
    - from: "glomalin-portal/src/app/(protected)/app/fsa-578/page.tsx"
      to: "glomalin-portal/src/components/fsa/clu-workspace.tsx"
      via: "Server Component passes initialRecords prop to client component"
      pattern: "CluWorkspace.*initialRecords"
---

<objective>
Build the card-based CLU management workflow with API routes for editing and bulk actions. Replace the existing fsa-578 stub page with a Server Component shell that passes initial records to a CluWorkspace client component containing Farm/Tract accordions, inline-editable CLU cards, checkbox selection, and a sticky bulk action bar.

Purpose: Deliver the core FSA-02 (card view with grouping/badges), FSA-03 (inline editing), and FSA-04 (bulk actions) requirements so users can manage all 444 CLU records through an interactive card-based workflow.

Output: 11 files — 2 API routes, 7 client components, 1 static data file, 1 modified page
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-fsa-planting-workflow-ui/28-RESEARCH.md
@.planning/phases/27-fsa-data-foundation-migration/27-01-SUMMARY.md
@.planning/phases/27-fsa-data-foundation-migration/27-02-SUMMARY.md
@glomalin-portal/src/lib/fsa/calc.ts
@glomalin-portal/src/app/api/fsa/clu-records/route.ts
@glomalin-portal/src/app/(protected)/app/fsa-578/page.tsx
@glomalin-portal/tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PATCH and bulk-update API routes + FSA crop list + CluWorkspace shell</name>
  <files>
    glomalin-portal/src/app/api/fsa/clu-records/[id]/route.ts
    glomalin-portal/src/app/api/fsa/clu-records/bulk-update/route.ts
    glomalin-portal/src/lib/fsa/fsa-crop-list.ts
    glomalin-portal/src/app/(protected)/app/fsa-578/page.tsx
    glomalin-portal/src/components/fsa/clu-workspace.tsx
  </files>
  <action>
    **1. Create PATCH /api/fsa/clu-records/[id]/route.ts:**
    - Auth check with `createClient()` from `@/lib/supabase/server`, `getUser()`, 401 on failure
    - Parse JSON body, whitelist only editable fields: `crop`, `irrigated`, `organic`, `grain_plant_date`, `use` (strip anything else)
    - Call `supabase.from('clu_records').update(updates).eq('id', params.id).select().single()`
    - Return `{ record: data }` on success, `{ error: error.message }` with 500 on failure
    - Handle empty updates (no valid fields in body) with 400 response
    - IMPORTANT: Always chain `.select().single()` after `.update()` — Supabase returns null data without it

    **2. Create POST /api/fsa/clu-records/bulk-update/route.ts:**
    - Auth check same pattern as PATCH route
    - Accept body: `{ ids: string[], action: 'mark-reported' | 'mark-unreported' | 'assign-crop', crop?: string }`
    - Validate: ids must be non-empty array, action must be one of the three valid values, crop required when action is 'assign-crop'
    - Build updatePayload: `mark-reported` → `{ reported: true }`, `mark-unreported` → `{ reported: false }`, `assign-crop` → `{ crop: body.crop }`
    - Call `supabase.from('clu_records').update(updatePayload).in('id', ids).select()`
    - Return `{ records: data, count: data.length }` on success

    **3. Create src/lib/fsa/fsa-crop-list.ts:**
    - Export `FSA_CROP_LIST: string[]` — static array of common FSA crop names
    - Include: Corn, Soybeans, Winter Wheat, Spring Wheat, Oats, Barley, Sorghum, Sunflowers, Canola, Flax, Cotton, Rice, Peanuts, Alfalfa, Clover, Timothy, Fescue, Orchardgrass, Rye, Triticale, Millet, Buckwheat, Field Peas, Dry Beans, Lentils, Chickpeas, Potatoes, Sugarbeets, Sweet Corn, Popcorn, Cover Crop, CRP, Idle, Fallow, Organic Corn, Organic Soybeans, Organic Winter Wheat, Hybrid Rye, Organic Rye
    - Sort alphabetically for consistent display

    **4. Rewrite src/app/(protected)/app/fsa-578/page.tsx:**
    - Keep as Server Component (no 'use client')
    - Import `createClient` from `@/lib/supabase/server`
    - Import `CluWorkspace` from `@/components/fsa/clu-workspace`
    - Import `CluRecord` type from `@/lib/fsa/calc`
    - Fetch all clu_records for crop_year 2026 ordered by farm_number, tract_number, clu
    - Pass `initialRecords={records}` and `loadError={error?.message ?? null}` to CluWorkspace
    - Wrap in `<div className="max-w-7xl mx-auto px-6 py-8">`

    **5. Create src/components/fsa/clu-workspace.tsx:**
    - 'use client' directive at top
    - Props: `{ initialRecords: CluRecord[], loadError: string | null }`
    - State: `records` (CluRecord[]), `selectedIds` (Set<string>), `expandedId` (string | null), `warnings` (ValidationWarning[])
    - State: `expandedFarms` (Set<string>), `expandedTracts` (Set<string>) — initialized via lazy useState initializer to compute smart defaults from initialRecords (farms/tracts with any unreported CLU start expanded)
    - useEffect on mount: fetch `/api/fsa/validation`, extract warnings array, catch silently (warnings are non-blocking)
    - useMemo: `grouped` — group records by farm_number → tract_number → CluRecord[] using the groupByFarmTract helper function (defined in this file)
    - useMemo: `warningsByRecordId` — Map<string, ValidationWarning[]> keyed by record id, built from warnings array matching on legacy_id or record field
    - Callback: `handleSaveRecord(updated: CluRecord)` — replaces record in `records` array by id using `setRecords(prev => prev.map(...))`
    - Callback: `handleBulkAction(action, crop?)` — fetches POST /api/fsa/clu-records/bulk-update with selectedIds + action, on success replaces matching records in state, clears selectedIds
    - Callback: `handleToggleSelect(id)` — add/remove from selectedIds Set
    - Callback: `handleSelectAllInTract(tractRecords)` / `handleSelectAllInFarm(farmRecords)` — select all CLUs in a tract or farm
    - Render: page header with title "FSA Acreage Reporting" and subtitle showing record count; export buttons placeholder div (top-right, Plan 02 will add PDF/CSV buttons here)
    - Render: if loadError, show error banner
    - Render: iterate grouped entries, render FarmAccordion for each farm, passing tracts data
    - Render: BulkActionBar at bottom when selectedIds.size > 0
    - Add `pb-20` to main content wrapper when selectedIds.size > 0 to prevent content hidden behind sticky bar
    - IMPORTANT: Use `Array.from()` for all Set/Map iterations (TypeScript target compatibility — established Phase 27 pattern)
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check that page.tsx imports CluWorkspace, both API routes exist with correct exports</manual>
  </verify>
  <done>PATCH route updates single CLU and returns updated record. Bulk-update route handles mark-reported/unreported/assign-crop for arrays of IDs. CluWorkspace renders page header, groups records by farm/tract, manages selection/expansion/warning state. FSA crop list exists as static module.</done>
</task>

<task type="auto">
  <name>Task 2: Farm/Tract accordions + CluCard with inline editing + BulkActionBar + CropTypeahead + ConfirmDialog</name>
  <files>
    glomalin-portal/src/components/fsa/farm-accordion.tsx
    glomalin-portal/src/components/fsa/tract-accordion.tsx
    glomalin-portal/src/components/fsa/clu-card.tsx
    glomalin-portal/src/components/fsa/bulk-action-bar.tsx
    glomalin-portal/src/components/fsa/crop-typeahead.tsx
    glomalin-portal/src/components/fsa/confirm-dialog.tsx
  </files>
  <action>
    **1. Create src/components/fsa/farm-accordion.tsx:**
    - 'use client' — receives `farmNumber: string`, `farmName: string | null`, `tracts: Record<string, CluRecord[]>`, `isExpanded: boolean`, `onToggleExpand: () => void`, plus pass-through props for selection/expansion/save callbacks, `selectedIds`, `expandedId`, `warningsByRecordId`
    - Also receives `expandedTracts: Set<string>`, `onToggleExpandTract: (key: string) => void`
    - Render: clickable header div with farm number + farm name (from first record's farm_name), chevron icon (rotated when expanded), total acres badge, Select All checkbox for the farm
    - When expanded: render TractAccordion for each tract
    - Farm-level Select All: toggles all CLU IDs in the farm (add all or remove all from selectedIds)
    - Use soil design tokens: `bg-soil-surface`, `border-soil-border`, `text-soil-text`, `text-soil-accent` for farm header

    **2. Create src/components/fsa/tract-accordion.tsx:**
    - 'use client' — receives `tractNumber: string`, `records: CluRecord[]`, `isExpanded: boolean`, `onToggleExpand: () => void`, plus pass-through props for selection/expansion/save callbacks, `selectedIds`, `expandedId`, `warningsByRecordId`
    - Render: clickable header with tract number, record count badge, Select All checkbox for the tract
    - When expanded: render CluCard for each record in the tract
    - Tract-level Select All: toggles all CLU IDs in the tract
    - Slightly indented from farm level (ml-4)

    **3. Create src/components/fsa/clu-card.tsx:**
    - 'use client' — receives `record: CluRecord`, `isExpanded: boolean`, `isSelected: boolean`, `warnings: ValidationWarning[]`, `onToggleExpand: () => void`, `onToggleSelect: () => void`, `onSave: (updated: CluRecord) => void`
    - **Collapsed view:** Single div showing: checkbox (top-left corner), field name (or "CLU {clu}"), crop name, practice, planting date, fsa_acres, organic badge (green "O" if organic), status badge (green "Reported" / amber "Unreported"), warning badge (red dot with count if warnings exist). 2-3 lines per card per user decision.
    - Click anywhere on the card (except checkbox) calls onToggleExpand
    - **Expanded view:** Card expands in-place below the collapsed summary to show editable fields:
      - Crop: CropTypeahead component
      - Practice/Use: text input (or dropdown of common values: "Non-Irrigated", "Irrigated")
      - Planting date: date input (`<input type="date">`)
      - Organic: checkbox toggle
    - Local `draft` state (`useState<Partial<CluRecord>>({})`) for edit values; initialized from record when expanded
    - Local `fieldErrors` state for inline validation (red text below field): crop required, planting date must be valid date format
    - Local `saving` state (boolean) for save button loading state
    - Save button: calls `fetch PATCH /api/fsa/clu-records/${record.id}` with draft fields, on success calls `onSave(updated)`, on error shows field-level or general error
    - Cancel button: clears draft and collapses card (onToggleExpand)
    - Use `bg-soil-surface` for card background, `border-soil-border` for border, `ring-soil-accent` for expanded state ring/outline

    **4. Create src/components/fsa/bulk-action-bar.tsx:**
    - 'use client' — receives `selectedCount: number`, `onAction: (action: 'mark-reported' | 'mark-unreported' | 'assign-crop', crop?: string) => void`, `onClear: () => void`
    - Fixed position: `fixed bottom-0 left-0 right-0 z-50`
    - Styled: `bg-soil-surface border-t border-soil-accent px-6 py-3 flex items-center gap-4`
    - Shows: "{N} selected" in accent color, "Mark Reported" button, "Mark Unreported" button, "Assign Crop" button, "Clear" button
    - "Assign Crop" opens a small inline CropTypeahead + confirm flow — when user selects a crop and clicks confirm, calls onAction('assign-crop', selectedCrop)
    - "Mark Reported" and "Mark Unreported" show a ConfirmDialog before executing — e.g., "Mark 12 CLUs as reported to FSA?" per user decision
    - `assignMode` local state: when true, shows CropTypeahead + Confirm/Cancel instead of the action buttons

    **5. Create src/components/fsa/crop-typeahead.tsx:**
    - 'use client' — receives `value: string`, `onChange: (value: string) => void`, optional `className`
    - Fetches farm-budget crop list once on mount from `/api/fsa/auto-populate-preview` response (extract unique crop names from proposals) with timeout, catches errors silently
    - Merges farm-budget crops with FSA_CROP_LIST (imported from `@/lib/fsa/fsa-crop-list`), deduplicates, sorts alphabetically
    - Renders a controlled `<input>` with `type="text"` and a filtered `<ul>` dropdown below (not a `<datalist>` — need more styling control)
    - Filters on keystroke: case-insensitive startsWith match, show top 10 results
    - Click on a dropdown item sets the value; clicking outside or pressing Escape closes the dropdown
    - Styled with soil tokens: `bg-soil-bg`, `border-soil-border`, dropdown items `hover:bg-soil-surface`

    **6. Create src/components/fsa/confirm-dialog.tsx:**
    - 'use client' — receives `open: boolean`, `title: string`, `message: string`, `onConfirm: () => void`, `onCancel: () => void`
    - Simple inline conditional render (not a `<dialog>` element — just a backdrop div + centered card)
    - Backdrop: `fixed inset-0 z-[60] bg-black/50` (above the bulk action bar z-50)
    - Card: centered, `bg-soil-surface border border-soil-border rounded-lg p-6 max-w-sm mx-auto mt-[30vh]`
    - Title in `text-soil-text font-mono font-bold`, message in `text-soil-muted`, Cancel + Confirm buttons
    - Confirm button in `bg-soil-accent text-soil-bg font-bold` — the "do it" button stands out
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -20</automated>
    <manual>Verify all 6 component files exist, each has 'use client' directive, card shows editable fields when expanded</manual>
  </verify>
  <done>Farm/Tract accordions render grouped CLU data with smart default expansion. CluCard shows collapsed summary with status/warning badges and expands inline for editing with save/cancel. BulkActionBar appears as sticky bottom bar when selection > 0 with confirm dialog. CropTypeahead merges farm-budget crops with FSA crop list. Full build passes with no TypeScript errors.</done>
</task>

</tasks>

<verification>
1. `cd glomalin-portal && npm run build` completes with no errors
2. `npx tsc --noEmit` passes (no TypeScript compilation errors)
3. All 11 files exist in the expected paths
4. PATCH route at `/api/fsa/clu-records/[id]` exports PATCH function
5. Bulk-update route at `/api/fsa/clu-records/bulk-update` exports POST function
6. CluWorkspace is imported by the fsa-578 page and receives initialRecords
7. CluCard renders both collapsed (badges visible) and expanded (editable fields) states
8. BulkActionBar uses `fixed bottom-0` positioning with z-50
9. No `for...of` on Set/Map without Array.from() wrapper
10. No imports from @react-pdf/renderer in any file in this plan
</verification>

<success_criteria>
- CLU records display as cards grouped by Farm → Tract accordion with Reported/Unreported badges
- Clicking a card expands it inline to show editable crop, practice, date, organic fields with Save/Cancel
- PATCH API saves single record changes to Supabase and returns updated record
- Checkboxes enable multi-select with Select All at farm and tract levels
- Sticky bottom bar shows selected count and offers Mark Reported/Unreported and Assign Crop actions
- Bulk API processes arrays of IDs for reported status or crop assignment
- Confirmation dialog appears before bulk actions
- TypeScript and Next.js build pass cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/28-fsa-planting-workflow-ui/28-01-SUMMARY.md`
</output>
