---
phase: 42-design-token-alignment-palette-swap
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/components/farm-node-map.tsx
  - glomalin-portal/src/components/layout/ascii-noise.ts
  - glomalin-portal/src/components/fsa/farm-accordion.tsx
  - glomalin-portal/src/components/fsa/tract-accordion.tsx
  - glomalin-portal/src/lib/claims/calc.ts
autonomous: true
gap_closure: true
requirements: [TOKEN-04]

must_haves:
  truths:
    - "All portal components render with no hardcoded hex colors -- tokens are the single source of truth"
    - "farm-node-map.tsx uses colors imported from tokens.ts for all inline styles"
    - "ascii-noise.ts canvas color ramp derives from tokens.ts"
    - "FSA accordion components use glomalin-* token classes instead of arbitrary hex values"
  artifacts:
    - path: "glomalin-portal/src/components/farm-node-map.tsx"
      provides: "React Flow node map with token-sourced inline styles"
      contains: "import.*colors.*from.*@/lib/tokens"
    - path: "glomalin-portal/src/components/layout/ascii-noise.ts"
      provides: "Canvas color ramp from tokens"
      contains: "import.*colors.*from.*@/lib/tokens"
    - path: "glomalin-portal/src/components/fsa/farm-accordion.tsx"
      provides: "FSA accordion with glomalin-* classes"
    - path: "glomalin-portal/src/components/fsa/tract-accordion.tsx"
      provides: "Tract accordion with glomalin-* classes"
    - path: "glomalin-portal/src/lib/claims/calc.ts"
      provides: "Claims calc using glomalin-green token class"
  key_links:
    - from: "glomalin-portal/src/components/farm-node-map.tsx"
      to: "glomalin-portal/src/lib/tokens.ts"
      via: "import colors"
      pattern: "import.*colors.*from.*@/lib/tokens"
    - from: "glomalin-portal/src/components/layout/ascii-noise.ts"
      to: "glomalin-portal/src/lib/tokens.ts"
      via: "import colors"
      pattern: "import.*colors.*from.*@/lib/tokens"
---

<objective>
Close the single verification gap from Phase 42: eliminate all remaining hardcoded hex values in 5 portal files by wiring them to the canonical tokens.ts.

Purpose: Truth #5 ("all portal components render with no hardcoded colors") failed verification because 5 files bypass the token system with inline hex values. This plan replaces every instance with token imports or glomalin-* Tailwind classes.

Output: Zero hardcoded hex values in portal component/lib files (excluding intentional PDF print stylesheets).
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-design-token-alignment-palette-swap/42-VERIFICATION.md
@.planning/phases/42-design-token-alignment-palette-swap/42-01-SUMMARY.md
@glomalin-portal/src/lib/tokens.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire farm-node-map.tsx and ascii-noise.ts to tokens</name>
  <files>
    glomalin-portal/src/components/farm-node-map.tsx
    glomalin-portal/src/components/layout/ascii-noise.ts
  </files>
  <action>
**farm-node-map.tsx:**
Add `import { colors } from '@/lib/tokens'` at the top.
Replace every hardcoded hex value in inline styles with the corresponding `colors.*` reference:
- `#080a0f` or `#0c1015` -> `colors.bg` or `colors.surface` (context-dependent)
- `#14b8a6` -> `colors.accent`
- `#1e293b` -> `colors.border`
- `#cbd5e1` -> `colors.text`
- `#64748b` -> `colors.muted`
- `#334155` -> `colors.highlight` (or add to tokens if no match -- check if it maps to an existing token)
- Any other hex values: map to nearest token, or if genuinely new, add to tokens.ts with a semantic name.

Do NOT change any Tailwind class strings (those already use glomalin-* from plan 02). Only fix inline `style={{}}` hex values.

**ascii-noise.ts:**
Add `import { colors } from '@/lib/tokens'` at the top.
Replace the 5 hardcoded color constants (COLOR_WHITE, COLOR_BRIGHTEST, COLOR_MID, COLOR_DIM, COLOR_FAINT) with values derived from tokens. These form a brightness ramp for the canvas character rendering. The approach:
- If the current hex values map directly to token colors, use them (e.g., COLOR_BRIGHTEST might be colors.text, COLOR_DIM might be colors.muted).
- If they form a gradient not in tokens, export a `bannerGradient` array from tokens.ts (add to the file) and import it. Keep the gradient values aligned with the navy/cyan palette.
- Preserve the variable names (COLOR_WHITE etc.) for readability -- just source their values from the import.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -n '#[0-9a-fA-F]\{6\}' glomalin-portal/src/components/farm-node-map.tsx glomalin-portal/src/components/layout/ascii-noise.ts | grep -v '^\s*//' | grep -v 'import' | wc -l | tr -d ' '</automated>
    <manual>Output should be 0 -- no hardcoded hex values remain in either file</manual>
  </verify>
  <done>farm-node-map.tsx and ascii-noise.ts have zero hardcoded hex values; all colors sourced from tokens.ts imports</done>
</task>

<task type="auto">
  <name>Task 2: Fix FSA accordions and claims calc token references</name>
  <files>
    glomalin-portal/src/components/fsa/farm-accordion.tsx
    glomalin-portal/src/components/fsa/tract-accordion.tsx
    glomalin-portal/src/lib/claims/calc.ts
  </files>
  <action>
**farm-accordion.tsx:**
Find all Tailwind arbitrary value classes containing `#141210` and replace with `glomalin-highlight` (the token for dark highlight surfaces). Find all classes containing `#1a1714` and replace with `glomalin-surface` (the token for elevated surfaces). These are old soil palette remnants. Example: `bg-[#141210]` becomes `bg-glomalin-highlight`, `bg-[#1a1714]` becomes `bg-glomalin-surface`.

**tract-accordion.tsx:**
Same replacements as farm-accordion.tsx: `#141210` -> `glomalin-highlight`, `#1a1714` -> `glomalin-surface`.

**calc.ts:**
Find `border-l-[#7A9E7E]` and replace with `border-l-glomalin-green`. The value is identical to the glomalin-green token -- this just switches from arbitrary syntax to the proper token class.

Do NOT touch any other logic in these files. Only replace the hardcoded hex class references.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -rn '\[#[0-9a-fA-F]' glomalin-portal/src/components/fsa/farm-accordion.tsx glomalin-portal/src/components/fsa/tract-accordion.tsx glomalin-portal/src/lib/claims/calc.ts | wc -l | tr -d ' '</automated>
    <manual>Output should be 0 -- no arbitrary Tailwind hex values remain in these 3 files</manual>
  </verify>
  <done>FSA accordions use glomalin-highlight and glomalin-surface tokens; calc.ts uses glomalin-green token class; zero arbitrary hex values in all 3 files</done>
</task>

</tasks>

<verification>
Run a comprehensive hex scan across all portal source files (excluding acceptable PDF exceptions):

```bash
grep -rn '#[0-9a-fA-F]\{6\}' glomalin-portal/src/ \
  --include='*.tsx' --include='*.ts' \
  | grep -v 'acreage-pdf' \
  | grep -v 'insurance-pdf' \
  | grep -v 'tokens.ts' \
  | grep -v 'tailwind.config' \
  | grep -v '^\s*//'
```

Expected: Zero matches (all hex values live in tokens.ts, Tailwind config, or the two PDF exceptions).
</verification>

<success_criteria>
1. grep for hardcoded hex across glomalin-portal/src/ (excluding tokens.ts, tailwind.config.ts, and PDF files) returns zero results
2. farm-node-map.tsx imports colors from @/lib/tokens
3. ascii-noise.ts imports colors from @/lib/tokens
4. Zero `[#` arbitrary Tailwind values in farm-accordion, tract-accordion, and calc.ts
5. `npx next build` succeeds (no import errors)
</success_criteria>

<output>
After completion, create `.planning/phases/42-design-token-alignment-palette-swap/42-03-SUMMARY.md`
</output>
