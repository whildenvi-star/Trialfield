---
phase: 51-fsa-insurance-data-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/scripts/migrate-fsa-final.ts
  - fsa-acres/server.js
  - fsa-acres/public/index.html
  - fsa-acres/public/gcs.js
autonomous: true
requirements: [CONS-05]

must_haves:
  truths:
    - "Migration script reads fsa-acres/data/data.json and upserts all records into Supabase"
    - "Duplicate detection runs before any writes and reports duplicates found"
    - "Verification report prints row counts comparing source JSON vs Supabase after migration"
    - "Spot-check of 10-20 random records confirms field-by-field accuracy"
    - "GCS enrollment feature is completely removed from fsa-acres UI and server"
    - "Original data.json renamed to data.json.migrated after successful migration"
  artifacts:
    - path: "glomalin-portal/scripts/migrate-fsa-final.ts"
      provides: "One-time migration script with dedup + verification"
      min_lines: 150
  key_links:
    - from: "glomalin-portal/scripts/migrate-fsa-final.ts"
      to: "fsa-acres/data/data.json"
      via: "fs.readFileSync"
      pattern: "readFileSync.*data\\.json"
    - from: "glomalin-portal/scripts/migrate-fsa-final.ts"
      to: "Supabase clu_records + insurance_policies + insurance_pricing"
      via: "supabase.from().upsert()"
      pattern: "supabase.*from.*upsert"
---

<objective>
Create the final one-time migration script that moves all fsa-acres JSON data to Supabase with duplicate detection, verification reporting, and spot-check validation. Also remove the discontinued GCS enrollment feature entirely from fsa-acres.

Purpose: CONS-05 requires a migration script with duplicate detection and verification. GCS removal per user decision ("program isn't happening next year, completely remove").
Output: migrate-fsa-final.ts script, GCS-free fsa-acres app
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-fsa-insurance-data-consolidation/51-CONTEXT.md
@glomalin-portal/scripts/migrate-fsa.ts
@fsa-acres/server.js
@fsa-acres/public/index.html
@fsa-acres/public/gcs.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create enhanced migration script with duplicate detection and verification</name>
  <files>glomalin-portal/scripts/migrate-fsa-final.ts</files>
  <action>
Create a new migration script `migrate-fsa-final.ts` (separate from the existing `migrate-fsa.ts` which was used in phase 27) that serves as the definitive one-time consolidation migration. Use the existing `migrate-fsa.ts` as a template for the env loading and Supabase client setup patterns.

The script must:

1. **Load source data** from `../../fsa-acres/data/data.json` (same path pattern as existing script)
2. **Duplicate detection phase** (runs BEFORE any writes):
   - For CLU records: deduplicate on composite key (farmNumber + tractNumber + clu + crop). If duplicates found, log them with details and ask for confirmation to proceed (or use `--force` flag to skip prompt)
   - For insurance policies: deduplicate on (policyNumber + farmNumber + crop)
   - For pricing: deduplicate on (crop) — one price per crop
   - Print summary: "Found X duplicate CLU records, Y duplicate policies, Z duplicate pricing entries"
3. **Migration phase** — upsert all records to Supabase using service_role key:
   - CLU records (444) → `clu_records` table. Map camelCase JSON keys to snake_case Supabase columns (farmNumber→farm_number, tractNumber→tract_number, fsaAcres→fsa_acres, fieldName→field_name, grainPlantDate→grain_plant_date, etc.). Set `crop_year` to the settings.year value. Preserve `registryFieldId` as `registry_field_id` if present. Use `legacy_id` for upsert conflict resolution (same pattern as existing migrate-fsa.ts).
   - Insurance policies (3) → `insurance_policies` table
   - Pricing (22) → `insurance_pricing` table
   - Skip GCS enrollments entirely (program discontinued, per user decision)
4. **Verification phase** (runs AFTER writes):
   - Query each Supabase table for row counts
   - Print comparison: "Source: X CLU records → Supabase: Y CLU records"
   - Spot-check 10-20 random CLU records: select by legacy_id, compare field_name, farm_number, fsa_acres, crop against source JSON. Print pass/fail for each.
   - Print overall: "MIGRATION VERIFIED: all spot checks passed" or list failures
5. **Rename source file**: After successful migration + verification, rename `data.json` to `data.json.migrated` (per user decision: keep as read-only backup, don't delete)

Batch upserts in chunks of 500 (same as existing script pattern).

Run with: `cd glomalin-portal && npx tsx scripts/migrate-fsa-final.ts`
Add `--force` flag to skip duplicate confirmation prompt.
Add `--dry-run` flag to show what would be migrated without writing.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsx scripts/migrate-fsa-final.ts --dry-run 2>&1 | grep -E "Source|duplicate|CLU|insurance|pricing"</automated>
    <manual>Dry-run output shows correct counts: 444 CLU, 22 pricing, 3 insurance, 0 GCS</manual>
  </verify>
  <done>Migration script loads JSON, detects duplicates, upserts to Supabase, prints verification report with spot checks, and renames data.json on success</done>
</task>

<task type="auto">
  <name>Task 2: Remove GCS enrollment feature from fsa-acres</name>
  <files>fsa-acres/server.js, fsa-acres/public/index.html, fsa-acres/public/gcs.js</files>
  <action>
Remove the discontinued GCS enrollment feature completely:

1. **fsa-acres/server.js**:
   - Remove the `gcsEnrollments: []` from the default store object (line 45)
   - Remove all GCS API routes: GET /api/gcs, GET /api/gcs/summary, POST /api/gcs, PUT /api/gcs/:id, DELETE /api/gcs/:id (lines 609-647)
   - Remove gcsEnrollments from the loadData console.log line (line 1325)
   - Do NOT remove gcsEnrollments from the in-memory store loading — it may still exist in data.json and Object.assign would silently carry it. Just remove the API routes so it's inaccessible.

2. **fsa-acres/public/index.html**:
   - Remove the GCS tab button: `<button class="tab-btn" data-tab="gcs">GCS</button>` (line 35)
   - Remove the entire GCS tab content section: `<section class="tab-content" id="tab-gcs">...</section>` (lines 335-367)
   - Remove the `<script src="gcs.js"></script>` tag (line 598)

3. **fsa-acres/public/gcs.js**: Delete this file entirely (151 lines).

4. **fsa-acres/server.js season dashboard**: In the `buildSeasonalStatus` function, remove any GCS-related logic if present. The seasonal status should work without GCS data.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -rn "gcs\|GCS" fsa-acres/public/index.html fsa-acres/public/gcs.js 2>&1; grep -c "gcs" fsa-acres/server.js</automated>
    <manual>GCS tab gone from index.html, gcs.js deleted, server.js GCS routes removed</manual>
  </verify>
  <done>GCS enrollment feature completely removed from UI (tab, table, script tag), server API routes deleted, gcs.js file deleted</done>
</task>

</tasks>

<verification>
- Migration script runs in dry-run mode showing correct source counts
- No GCS references remain in fsa-acres/public/index.html
- gcs.js file no longer exists
- GCS API routes removed from server.js
</verification>

<success_criteria>
- migrate-fsa-final.ts exists and runs in dry-run mode showing 444 CLU, 22 pricing, 3 insurance
- GCS feature fully removed from fsa-acres (UI + API)
- No regression in other fsa-acres tabs (FSA Entry, Insurance, Reports, Dashboard, Season)
</success_criteria>

<output>
After completion, create `.planning/phases/51-fsa-insurance-data-consolidation/51-01-SUMMARY.md`
</output>
