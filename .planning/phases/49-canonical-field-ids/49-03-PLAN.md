---
phase: 49-canonical-field-ids
plan: 03
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - farm-budget/server.js
  - grain-tickets/server.js
  - grain-tickets/public/farms.js
  - glomalin-portal/src/components/fsa/clu-card.tsx
  - glomalin-portal/src/app/api/fsa/auto-populate-preview/route.ts
  - fsa-acres/public/app.js
autonomous: true
requirements:
  - CONS-07

must_haves:
  truths:
    - "Cross-module data requests between apps use registry field ID instead of string name matching"
    - "Field selection UI in each app uses a dropdown populated from farm-registry autocomplete"
    - "New field records created after this phase carry a registry_field_id from the dropdown selection"
    - "Old field name columns are preserved for display and historical reference"
  artifacts:
    - path: "farm-budget/server.js"
      provides: "Registry-based farm-registry sync using registryFieldId"
      contains: "registryFieldId"
    - path: "grain-tickets/server.js"
      provides: "Registry sync using registryId on Farm model"
      contains: "registryId"
    - path: "grain-tickets/public/farms.js"
      provides: "Farm form with registry field dropdown"
      contains: "autocomplete"
    - path: "glomalin-portal/src/components/fsa/clu-card.tsx"
      provides: "CLU card field selector using registry dropdown"
      contains: "registry_field_id"
  key_links:
    - from: "farm-budget sync-from-registry"
      to: "farm-registry /api/fields/:id"
      via: "registryFieldId lookup instead of name match"
      pattern: "registryFieldId.*api/fields"
    - from: "grain-tickets farm sync"
      to: "farm-registry /api/fields/:id"
      via: "registryId lookup instead of name match"
      pattern: "registryId.*api/fields"
    - from: "field selection dropdowns"
      to: "farm-registry /api/fields/autocomplete"
      via: "HTTP fetch for dropdown options"
      pattern: "fetch.*autocomplete"
---

<objective>
Update cross-module joins to use registry field ID instead of string name matching, and switch field selection UIs to use registry-backed dropdowns.

Purpose: With schema in place (Plan 01) and IDs populated (Plan 02), the actual behavior change happens here -- apps now use the canonical ID for data exchange and require dropdown selection for new records, preventing future name mismatches.

Output: All cross-module joins use registry_field_id; all field selection UIs use dropdowns; free-text field name entry eliminated for new records.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-canonical-field-ids/49-CONTEXT.md
@.planning/phases/49-canonical-field-ids/49-01-SUMMARY.md
@farm-budget/server.js
@grain-tickets/server.js
@grain-tickets/public/farms.js
@glomalin-portal/src/components/fsa/clu-card.tsx
@fsa-acres/public/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update cross-module sync endpoints to use registry field ID</name>
  <files>farm-budget/server.js, grain-tickets/server.js</files>
  <action>
**farm-budget "Sync from Registry" endpoint:**
- Find the existing sync-from-registry handler in farm-budget/server.js (search for "sync" or "registry" -- there should be a route that pulls field acres from farm-registry).
- Currently this likely matches fields by name string. Update to:
  1. If the field has a `registryFieldId`, fetch that specific field by ID: `GET http://localhost:3005/api/fields/${registryFieldId}`
  2. If no registryFieldId (legacy fallback during transition), fall back to existing name-based matching
  3. When syncing, also store the registryFieldId on the field record if it came from the registry lookup
- This ensures future syncs use the canonical ID, not name matching.

**grain-tickets "Sync from Registry" endpoint:**
- Find the existing sync-from-registry handler in grain-tickets/server.js (search for "registry" or "sync").
- Currently matches Farm records to registry by name. Update to:
  1. If Farm has a `registryId`, use it to fetch by ID: `GET http://localhost:3005/api/fields/${registryId}`
  2. If no registryId, fall back to name matching
  3. Store registryId on the Farm record after successful sync

**Key rule per user decision:** Hard switch to ID-based joins once backfill hits 100%. The fallback is ONLY for the transition period. After backfill, all records will have IDs and the name fallback will be dead code.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -n "registryFieldId\|registryId" farm-budget/server.js grain-tickets/server.js | grep -i "sync\|registry\|fetch" | head -15</automated>
    <manual>Both sync endpoints prefer ID-based lookup with name-based fallback</manual>
  </verify>
  <done>farm-budget and grain-tickets sync-from-registry endpoints use registryFieldId/registryId for lookups with name-based fallback</done>
</task>

<task type="auto">
  <name>Task 2: Add registry field dropdown to field selection UIs</name>
  <files>grain-tickets/public/farms.js, glomalin-portal/src/components/fsa/clu-card.tsx, fsa-acres/public/app.js</files>
  <action>
Per user decision: "Field selection in all apps uses dropdown/autocomplete from registry -- no free-text field names."

**grain-tickets/public/farms.js:**
- Find the Farm create/edit form (where users enter farm name).
- Replace the free-text name input with a `<select>` dropdown or autocomplete input populated from `http://localhost:3005/api/fields/autocomplete`.
- On selection: set the farm name from the selected field's name, AND set registryId from the selected field's id.
- For existing farms being edited, show the current name and allow re-linking to a registry field.
- Keep the name field visible (read-only display from selection) for context.

**glomalin-portal/src/components/fsa/clu-card.tsx:**
- Find where `field_name` is entered/edited on CLU cards.
- Add a registry field selector (could be a simple `<select>` or a searchable dropdown using existing shadcn/ui patterns).
- Fetch options from `http://localhost:3005/api/fields/autocomplete` (through portal proxy or direct if CORS allows -- check existing CORS config; farm-registry CORS is set to PORTAL_ORIGIN).
- On selection: set both `field_name` (display) and `registry_field_id` (canonical ID) on the CLU record.
- For existing CLU records that already have a field_name, show it but allow linking to registry via the dropdown.

**fsa-acres/public/app.js:**
- Find where `fieldName` is entered on CLU records in the fsa-acres UI.
- Replace with a dropdown fetching from `http://localhost:3005/api/fields/autocomplete` (fsa-acres runs on same server, CORS should allow cross-port or use relative URL).
- On selection: set fieldName from selection and registryFieldId from field ID.

All dropdowns should:
- Show field name as the display text
- Include aliases in search/filter (if implementing autocomplete)
- Store the registry field ID alongside the display name
- Allow the user to see current field name for existing records
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -l "autocomplete\|registry.*field.*select\|fields/autocomplete" grain-tickets/public/farms.js glomalin-portal/src/components/fsa/clu-card.tsx fsa-acres/public/app.js 2>/dev/null | wc -l | xargs test 3 -eq && echo "All 3 files have registry field selection" || echo "MISSING: some files lack registry field selection"</automated>
    <manual>Each app's field entry UI shows a dropdown populated from farm-registry instead of free-text input</manual>
  </verify>
  <done>All 3 app UIs (grain-tickets, portal CLU, fsa-acres) use registry-backed field dropdowns that set both display name and canonical registry field ID</done>
</task>

</tasks>

<verification>
- Cross-module sync endpoints in farm-budget and grain-tickets prefer registry field ID over name matching
- Field selection in grain-tickets, portal, and fsa-acres uses dropdown from farm-registry autocomplete
- New records always carry a registry_field_id from dropdown selection
- Existing field name columns preserved for display
- No existing functionality broken -- name-based fallback exists during transition
</verification>

<success_criteria>
- farm-budget sync uses registryFieldId for registry lookup
- grain-tickets sync uses registryId for registry lookup
- grain-tickets farm form has registry dropdown
- portal CLU card has registry field selector
- fsa-acres CLU entry has registry dropdown
- All dropdowns fetch from /api/fields/autocomplete
</success_criteria>

<output>
After completion, create `.planning/phases/49-canonical-field-ids/49-03-SUMMARY.md`
</output>
