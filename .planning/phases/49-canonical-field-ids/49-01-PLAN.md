---
phase: 49-canonical-field-ids
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - farm-budget/server.js
  - grain-tickets/prisma/schema.prisma
  - glomalin-portal/supabase/schema.sql
  - fsa-acres/server.js
  - farm-registry/server.js
autonomous: true
requirements:
  - CONS-06

must_haves:
  truths:
    - "farm-budget fields and rent records accept a registryFieldId property"
    - "portal clu_records table has a registry_field_id column"
    - "fsa-acres CLU records accept a registryFieldId property"
    - "grain-tickets Farm model already has registryId (confirm it exists and is usable)"
    - "farm-registry exposes a /api/fields/autocomplete endpoint returning id+name+aliases for dropdown population"
  artifacts:
    - path: "farm-budget/server.js"
      provides: "registryFieldId in field and rent updatable arrays"
      contains: "registryFieldId"
    - path: "glomalin-portal/supabase/migrations/003-add-registry-field-id.sql"
      provides: "registry_field_id column on clu_records"
      contains: "registry_field_id"
    - path: "fsa-acres/server.js"
      provides: "registryFieldId in CLU record data model"
      contains: "registryFieldId"
    - path: "farm-registry/server.js"
      provides: "autocomplete endpoint for field dropdown"
      contains: "/api/fields/autocomplete"
  key_links:
    - from: "all apps"
      to: "farm-registry"
      via: "registryFieldId / registry_field_id column stores farm-registry field ID"
      pattern: "registryFieldId|registry_field_id"
---

<objective>
Add the registry_field_id schema to every app that stores field records, and expose a farm-registry autocomplete endpoint for field selection dropdowns.

Purpose: This is the schema foundation -- before backfill scripts or join updates can work, every app needs to accept and store a registry field ID. The autocomplete endpoint enables dropdown-based field selection (replacing free-text) after backfill.

Output: All 4 apps accept registry_field_id on field records; farm-registry has autocomplete API.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-canonical-field-ids/49-CONTEXT.md
@farm-registry/server.js
@farm-budget/server.js
@grain-tickets/prisma/schema.prisma
@glomalin-portal/src/app/api/fsa/clu-records/route.ts
@fsa-acres/server.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add registry_field_id to farm-budget and fsa-acres data models</name>
  <files>farm-budget/server.js, fsa-acres/server.js</files>
  <action>
**farm-budget/server.js:**
- Add `registryFieldId` to the updatable fields array in the PUT /api/fields/:id handler (the `updatable` array around line 454). This allows field records to store a registry field ID.
- Also add `registryFieldId` to the enrichField() function passthrough if one exists, so it appears in API responses.
- In the POST /api/fields handler, ensure `registryFieldId` is preserved from req.body (it already uses Object.assign, so this should work by default -- verify).
- Add `registryFieldId` to the updatable array in PUT /api/rent routes if rent records have their own field reference (rent records use `fieldName` string -- add `registryFieldId` as an additional property alongside fieldName, do NOT remove fieldName).

**fsa-acres/server.js:**
- Find where CLU records are created/updated (POST and PUT handlers for CLU data).
- Add `registryFieldId` to the allowed/updatable fields for CLU records.
- Ensure the property is preserved on save and returned in GET responses (JSON-backed store, so this should be automatic once the property is set on the record object).

Both apps are JSON-backed, so no migration needed -- the new property will be null/undefined on existing records until backfill runs.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -n "registryFieldId" farm-budget/server.js fsa-acres/server.js | head -20</automated>
    <manual>Confirm registryFieldId appears in updatable field lists in both files</manual>
  </verify>
  <done>farm-budget and fsa-acres server code accepts registryFieldId on field/CLU record create and update operations</done>
</task>

<task type="auto">
  <name>Task 2: Add registry_field_id to portal clu_records and confirm grain-tickets schema</name>
  <files>glomalin-portal/supabase/migrations/003-add-registry-field-id.sql, glomalin-portal/src/app/api/fsa/clu-records/route.ts, grain-tickets/prisma/schema.prisma</files>
  <action>
**Portal Supabase migration:**
- Create `glomalin-portal/supabase/migrations/003-add-registry-field-id.sql` with:
  ```sql
  ALTER TABLE clu_records ADD COLUMN IF NOT EXISTS registry_field_id text;
  CREATE INDEX IF NOT EXISTS idx_clu_records_registry_field_id ON clu_records(registry_field_id);
  ```
- The column is nullable (null until backfill runs). No FK constraint -- farm-registry is a separate Express app, not in Supabase.

**Portal API route update:**
- In `glomalin-portal/src/app/api/fsa/clu-records/route.ts`, add `'registry_field_id'` to the `ALLOWED_FIELDS` set so the POST endpoint accepts it.
- The GET already does `select('*')` so the new column will be returned automatically.

**Grain-tickets confirmation:**
- The Farm model in `grain-tickets/prisma/schema.prisma` already has `registryId String?` on line 44. This is the equivalent of registry_field_id. No schema change needed -- just confirm and document that `registryId` on the Farm model is the canonical field ID linkage. The Ticket model references farms by `farm String` (string name) -- the join point is Farm.name -> Ticket.farm, and Farm.registryId links to farm-registry. This is correct for grain-tickets' architecture.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && cat glomalin-portal/supabase/migrations/003-add-registry-field-id.sql && grep "registry_field_id" glomalin-portal/src/app/api/fsa/clu-records/route.ts</automated>
    <manual>Migration SQL exists and ALLOWED_FIELDS includes registry_field_id</manual>
  </verify>
  <done>Portal clu_records has registry_field_id column via migration; API accepts it on create; grain-tickets Farm.registryId confirmed as the linkage field</done>
</task>

<task type="auto">
  <name>Task 3: Add farm-registry autocomplete endpoint for field selection</name>
  <files>farm-registry/server.js</files>
  <action>
Add a new GET `/api/fields/autocomplete` endpoint to farm-registry/server.js. This endpoint powers the dropdown/autocomplete field selectors that will replace free-text field name entry across all apps.

Endpoint behavior:
- Returns all active fields as `[{ id, name, aliases, reportingAcres, organicAcres, ownership }]`
- Optional `?q=` query parameter for filtering (case-insensitive match on name + aliases, same logic as existing `/api/fields/search` but returning ALL matches without the 10-result limit)
- If no `?q=` param, return all active fields sorted alphabetically by name
- Response format: `{ fields: [...] }` -- keep it simple, no pagination needed (56 fields)
- Set Cache-Control to `public, max-age=300` (5 min) since field list rarely changes

Place this route BEFORE the existing `/api/fields/:id` route (Express matches routes in order, and `/api/fields/autocomplete` must not be caught by the `:id` param route). The existing `/api/fields/search` route is already before `:id`, so place autocomplete near it.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -A 15 "autocomplete" farm-registry/server.js | head -20</automated>
    <manual>Endpoint returns field list with id, name, aliases</manual>
  </verify>
  <done>farm-registry exposes /api/fields/autocomplete returning all active fields with id+name+aliases for dropdown population</done>
</task>

</tasks>

<verification>
- All 4 apps have a field/column for storing registry field IDs (farm-budget: registryFieldId, grain-tickets: registryId, portal: registry_field_id, fsa-acres: registryFieldId)
- farm-registry has /api/fields/autocomplete endpoint
- No existing functionality is broken (all changes are additive -- new nullable fields, new endpoint)
</verification>

<success_criteria>
- grep for registryFieldId in farm-budget/server.js shows it in updatable arrays
- grep for registryFieldId in fsa-acres/server.js shows it in allowed fields
- glomalin-portal/supabase/migrations/003-add-registry-field-id.sql exists with ALTER TABLE
- registry_field_id in ALLOWED_FIELDS in portal clu-records route
- /api/fields/autocomplete endpoint exists in farm-registry/server.js
</success_criteria>

<output>
After completion, create `.planning/phases/49-canonical-field-ids/49-01-SUMMARY.md`
</output>
