---
phase: 54-iframe-embed-navigation-design-tokens
plan: 04
type: execute
wave: 2
depends_on:
  - 54-01
  - 54-02
  - 54-03
files_modified:
  - glomalin-portal/src/app/globals.css
  - fsa-acres/public/style.css
  - fsa-acres/public/index.html
autonomous: false
requirements:
  - UXN-04
  - UXN-09

must_haves:
  truths:
    - "All Express apps hide their header when embedded — verified across all 7 embeddable apps"
    - "No visual jarring when switching between portal native pages and any embedded app"
    - "Breadcrumb bar, theme cascade, and color tokens all work together end-to-end"
  artifacts:
    - path: "glomalin-portal/src/app/globals.css"
      provides: "Any final CSS adjustments for embed visual consistency"
  key_links:
    - from: "glomalin-portal/src/app/(protected)/app/[module]/page.tsx"
      to: "glomalin-portal/src/components/embed-breadcrumb.tsx"
      via: "renders breadcrumb for embed modules"
      pattern: "EmbedBreadcrumb"
---

<objective>
Audit header-hiding completeness across all embedded apps, fix any gaps in fsa-acres iframe detection, and verify end-to-end visual consistency with a human checkpoint.

Purpose: Plans 01-03 unified color tokens, added breadcrumbs, and wired theme cascade. This plan audits the header-hiding mechanism across all apps (some may have inconsistent patterns) and provides a final human verification that everything looks clean together.

Output: All apps verified for header hiding; any CSS fixes applied; human-verified visual consistency.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-iframe-embed-navigation-design-tokens/54-01-SUMMARY.md
@.planning/phases/54-iframe-embed-navigation-design-tokens/54-02-SUMMARY.md
@.planning/phases/54-iframe-embed-navigation-design-tokens/54-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix header-hiding across all embeddable apps</name>
  <files>
    fsa-acres/public/style.css
    fsa-acres/public/index.html
    glomalin-portal/src/app/globals.css
  </files>
  <action>
Systematically verify that every app that can be embedded in the portal correctly hides its header bar when `html.in-iframe` is set. Check each app:

**Already confirmed working (from codebase grep):**
- farm-budget: `html.in-iframe .header-top { display: none !important; }` + inline script adds class --- OK
- grain-tickets: `html.in-iframe .header-top { display: none !important; }` + inline script --- OK
- seed-inventory: `html.in-iframe .header-row { display: none !important; }` + inline script --- OK
- meristem-malt: `html.in-iframe .sticky-header { display: none !important; }` + inline script --- OK
- farm-registry: `html.in-iframe header { display: none !important; }` + inline script --- OK
- organic-cert: `html.in-iframe header { display: none !important; }` + inline script --- OK

**Needs verification:**
- fsa-acres: Has `html.in-iframe .header-top { display: none !important; }` in style.css. Check that index.html has the inline iframe detection script (`if(window!==window.top)document.documentElement.classList.add('in-iframe')`). If missing, add it.

**Also check:** After header hiding, is there extra top padding or margin that creates a gap? Each app should have `html.in-iframe header { padding-top: 0; }` or equivalent to collapse the space.

**For fsa-acres specifically:** Verify the `<header>` element structure matches what the CSS rule targets. fsa-acres style.css hides `.header-top` but the HTML has a bare `<header>` tag — check if `.header-top` is a child div or if the CSS should target `header` directly. Fix the CSS selector if it doesn't match the actual HTML structure.

**Portal globals.css:** Add a CSS custom property `--embed-breadcrumb-h: 36px` to `:root` if Plan 02 didn't already add it. This ensures the breadcrumb height is configurable.

Also hide the settings-panel tab trigger in the portal when viewing an embed page (the embedded app's own settings panel is already hidden, but verify the portal's settings panel trigger doesn't overlap with the breadcrumb bar).
  </action>
  <verify>
    <automated>for app in farm-budget grain-tickets seed-inventory meristem-malt farm-registry fsa-acres; do echo "=== $app ==="; grep "in-iframe" "$app/public/style.css" | head -3; grep "in-iframe\|window.top" "$app/public/index.html" | head -2; echo; done</automated>
    <manual>Load each embed in portal — no double headers visible</manual>
  </verify>
  <done>All 7 embeddable apps have working header-hiding when loaded in portal iframe; fsa-acres verified/fixed; no stale padding/margins creating gaps</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 54 integration</name>
  <action>
Human verifies that all Phase 54 changes work together: unified color tokens, breadcrumb navigation, theme cascade, and header hiding produce a seamless portal experience with zero visual jarring.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — human verification required"</automated>
    <manual>
1. Open portal at https://app.whughesfarms.com (or localhost:3010)
2. Navigate to Dashboard — portal header and module cards visible
3. Click into "Macro Rollup" (farm-budget embed):
   - Breadcrumb bar shows "Dashboard > Macro Rollup" below portal header
   - "Back to Dashboard" link visible on right side of breadcrumb
   - Farm-budget's own header bar is HIDDEN (no duplicate title)
   - Colors match portal exactly (same background, borders, text color)
4. Toggle day/night in the settings panel:
   - Portal header switches theme
   - Breadcrumb bar switches theme
   - Embedded farm-budget switches theme simultaneously
5. Click "Back to Dashboard" — returns to dashboard
6. Click into "Grain Tickets" — breadcrumb shows "Dashboard > Grain Tickets"
7. Click into "Seed Inventory", "Farm Registry", "Meristem Malt" — same breadcrumb pattern, same colors
8. Click into "Organic Cert" (cross-origin embed):
   - Breadcrumb shows "Dashboard > Organic Cert"
   - Header hidden
   - Toggle day/night — organic-cert switches theme
9. Compare any two apps side by side: background color, border color, text color should be indistinguishable
    </manual>
  </verify>
  <done>Human confirms zero visual jarring, working breadcrumbs, consistent theme cascade, and no duplicate headers across all embedded apps</done>
</task>

</tasks>

<verification>
1. All 7 embeddable apps hide headers when in portal iframe
2. Breadcrumb bar visible on all embed pages with correct module name
3. "Back to Dashboard" always accessible
4. Day/night toggle cascades to every embed
5. No color jarring between portal and any embedded app
6. Human verified visual consistency
</verification>

<success_criteria>
- Human confirms zero visual jarring switching between portal and embeds
- Human confirms breadcrumb bar works for all embed modules
- Human confirms theme toggle cascades consistently
- Human confirms no duplicate headers in any embed
</success_criteria>

<output>
After completion, create `.planning/phases/54-iframe-embed-navigation-design-tokens/54-04-SUMMARY.md`
</output>
