---
phase: 06.1-phase-6-defect-fixes
plan: 01
subsystem: api, ui
tags: [auth, rbac, budget, next-app-router, react]

# Dependency graph
requires:
  - phase: 06-actuals-entry-and-enterprise-budget-view
    provides: BudgetTab.tsx with seed rows and ActualCell, confirm route without auth, BUDGET_CATEGORIES constant
provides:
  - Auth-guarded POST /api/import-plan/confirm with 401/403 responses and populated audit log
  - Seed Actual column displaying cost per acre (actualPricePerUnit * rate) — unit-consistent with Projected
  - BUDGET_CATEGORIES aligned to ALLOWED_CATEGORIES in unplanned-cost route (11 items, no Equipment)
affects:
  - 07-all-enterprise-sync
  - 08-farm-wide-budget-summary

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Auth guard: getAuthContext() + hasPermission(role, 'budget:write') at top of POST handler, before body parsing"
    - "logAudit population: use user.id and user.name from getAuthContext() return object"
    - "Seed per-acre display: actualPricePerUnit * rate ($/unit * units/ac = $/ac)"

key-files:
  created: []
  modified:
    - src/app/api/import-plan/confirm/route.ts
    - src/components/budget/BudgetTab.tsx

key-decisions:
  - "Auth guard placed before request.json() — avoids parsing untrusted body from unauthenticated callers"
  - "logAudit now uses user.id and user.name — getAuthContext() returns { id, email, name, role, farmId, farmName }"
  - "Seed actualPerAcre = actualPricePerUnit * rate (client display formula only) — API storage unchanged"
  - "BUDGET_CATEGORIES replaced with exact copy of ALLOWED_CATEGORIES — static copy is correct pattern (no categories endpoint)"

patterns-established:
  - "Auth guard pattern: all write routes use getAuthContext + hasPermission(role, 'budget:write') as first statements in try block"
  - "Category dropdown = static copy of API ALLOWED_CATEGORIES — keeps UI/API contract visible in one place"

requirements-completed:
  - ACT-02
  - VIEW-01
  - VIEW-02

# Metrics
duration: 8min
completed: 2026-03-21
---

# Phase 6.1 Plan 01: Phase 6 Defect Fixes Summary

**Auth guard added to confirm route (401/403), seed Actual column fixed to show $/ac via actualPricePerUnit * rate, and category dropdown aligned to 11 API-accepted values**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-03-21T05:19:39Z
- **Completed:** 2026-03-21T05:27:00Z
- **Tasks:** 1 of 2 (Task 2 is checkpoint:human-verify — awaiting review)
- **Files modified:** 2

## Accomplishments

- Closed security gap: POST /api/import-plan/confirm was the only Phase 6 write route with no auth guard — now returns 401 for unauthenticated and 403 for users missing budget:write
- Audit log now records real userId and userName from session instead of undefined
- Seed variance is now meaningful: both projected and actual columns show $/ac with the same formula basis
- Silent API failures from "Equipment" category eliminated — dropdown matches ALLOWED_CATEGORIES exactly

## Task Commits

1. **Task 1: Add auth guard to confirm route and fix BudgetTab defects** - `96ca9c1` (fix)

## Files Created/Modified

- `src/app/api/import-plan/confirm/route.ts` — Added getAuthContext + hasPermission(budget:write) guard; populated logAudit userId/userName
- `src/components/budget/BudgetTab.tsx` — Fixed seed actualPerAcre formula (* sc.rate), changed sub-label to /ac, replaced BUDGET_CATEGORIES with 11-item aligned list

## Decisions Made

- Auth guard placed before `request.json()` — avoids parsing untrusted request body from unauthenticated callers (follows canonical pattern from other Phase 6 routes)
- `getAuthContext()` confirmed to return `{ id, email, name, role, farmId, farmName }` — used `user.id` for userId and `user.name` for userName in logAudit
- Seed formula is client-display only: `actualPricePerUnit * rate` — API storage of actualPricePerUnit unchanged
- BUDGET_CATEGORIES replaced wholesale with a static copy of ALLOWED_CATEGORIES — no categories endpoint exists, static copy is the right approach

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. TypeScript compiled cleanly after all three fixes.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- All three Phase 6.1 defect fixes applied and compiled
- Task 2 is a code review checkpoint — human verifier should confirm the three fixes in both files before this plan is marked complete
- After human verification, phase 06.1 is complete and phase 07 (All-Enterprise Sync) can proceed

## Self-Check: PASSED

- FOUND: src/app/api/import-plan/confirm/route.ts
- FOUND: src/components/budget/BudgetTab.tsx
- FOUND: commit 96ca9c1

---
*Phase: 06.1-phase-6-defect-fixes*
*Completed: 2026-03-21*
