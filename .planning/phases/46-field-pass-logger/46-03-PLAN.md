---
phase: 46-field-pass-logger
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - organic-cert/src/app/api/field-enterprises/[id]/operations/route.ts
autonomous: true
requirements: [FPL-01, FPL-02, FPL-04]
gap_closure: true

must_haves:
  truths:
    - "A confirmed pass written via portal proxy has plannedSource 'mobile-logger' in the organic-cert FieldOperation record"
    - "A confirmed pass written via portal proxy has budgetImplementId saved in the organic-cert FieldOperation record"
    - "After page reload, crop-plans detail merges confirmed ops back to planned passes via budgetImplementId match"
  artifacts:
    - path: "organic-cert/src/app/api/field-enterprises/[id]/operations/route.ts"
      provides: "POST handler that persists plannedSource and budgetImplementId from request body"
      contains: "plannedSource"
  key_links:
    - from: "glomalin-portal/src/app/api/mobile/passes/confirm/route.ts"
      to: "organic-cert/src/app/api/field-enterprises/[id]/operations/route.ts"
      via: "fetchCertService POST with plannedSource and budgetImplementId in body"
      pattern: "plannedSource.*budgetImplementId"
    - from: "organic-cert/src/app/api/field-enterprises/[id]/operations/route.ts"
      to: "prisma.fieldOperation.create"
      via: "Prisma create data object includes plannedSource and budgetImplementId"
      pattern: "plannedSource.*budgetImplementId"
---

<objective>
Fix the organic-cert POST handler to persist plannedSource and budgetImplementId fields that the portal proxy already sends correctly.

Purpose: Without this fix, confirmed passes are indistinguishable from manual entries (no "mobile-logger" tag) and the crop-plans detail page cannot match confirmed ops back to planned passes after reload (budgetImplementId is null). This is the single remaining gap blocking Phase 46 completion.

Output: Updated organic-cert operations route that persists both fields.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-field-pass-logger/46-VERIFICATION.md
@.planning/phases/46-field-pass-logger/46-01-SUMMARY.md

Key file to modify:
@organic-cert/src/app/api/field-enterprises/[id]/operations/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add plannedSource and budgetImplementId to organic-cert POST handler</name>
  <files>organic-cert/src/app/api/field-enterprises/[id]/operations/route.ts</files>
  <action>
Two targeted changes to the POST handler in this file:

1. **Line 14 — expand destructuring:** Add `plannedSource` and `budgetImplementId` to the destructured fields from `body`:
   ```
   const { type, operationDate, equipmentId, operatorId, acresWorked, description, notes, plannedSource, budgetImplementId } = body;
   ```

2. **Lines 25-38 — add to Prisma create data object:** Include both fields in the `prisma.fieldOperation.create` data:
   ```
   plannedSource,
   budgetImplementId,
   ```
   Add these two lines after `notes,` (line 37) inside the data object. Both fields already exist in the Prisma schema and accept optional string values.

Do NOT modify the PUT handler — it already uses spread (`...safeUpdateData`) which passes through any schema-valid field including these two.

Do NOT change the `passStatus: "CONFIRMED"` or `dataSource: "MANUAL"` hardcoded values — the portal sends matching values so there is no conflict, and existing organic-cert manual entry UI relies on these defaults.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Read the file and confirm plannedSource and budgetImplementId appear in both the destructuring (line 14) and the Prisma create data object</manual>
  </verify>
  <done>
    - POST handler destructures plannedSource and budgetImplementId from request body
    - Prisma create call includes both fields in the data object
    - Portal proxy writes with plannedSource "mobile-logger" are now persisted
    - Portal proxy writes with budgetImplementId are now persisted, enabling crop-plans detail merge after reload
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd organic-cert && npx tsc --noEmit` passes
- The word `plannedSource` appears in both the destructuring line and the Prisma create data object
- The word `budgetImplementId` appears in both the destructuring line and the Prisma create data object
- PUT handler is unchanged (already handles both fields via spread)
- No other files modified
</verification>

<success_criteria>
- plannedSource field persisted to FieldOperation table on POST (closes gap for FPL-01, FPL-02, FPL-04)
- budgetImplementId field persisted to FieldOperation table on POST (enables crop-plans merge logic)
- Existing organic-cert manual entry behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/46-field-pass-logger/46-03-SUMMARY.md`
</output>
