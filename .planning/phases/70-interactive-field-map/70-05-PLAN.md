---
phase: 70-interactive-field-map
plan: 05
type: execute
wave: 4
depends_on:
  - 70-03
  - 70-04
files_modified:
  - glomalin-portal/src/lib/modules.ts
autonomous: false
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
  - MAP-05

must_haves:
  truths:
    - "Field Map appears in the portal dashboard module grid"
    - "/app/maps is navigable from the dashboard card"
    - "The map renders all fields as colored polygons on satellite imagery"
    - "On first load the map auto-zooms to fit all farm fields (fitBounds)"
    - "Organic fields have dashed border overlay"
    - "Clicking a field opens the detail panel with name, crop, organic status, acres, and timeline link"
    - "A compact legend is visible at the bottom-left corner of the map"
    - "The admin Field Boundaries section on /admin accepts a .zip shapefile and returns a summary"
  artifacts:
    - path: "glomalin-portal/src/lib/modules.ts"
      provides: "MODULES array with maps entry"
      contains: "id: 'maps'"
  key_links:
    - from: "glomalin-portal/src/lib/modules.ts"
      to: "/app/maps"
      via: "route: '/app/maps' in MODULES entry"
      pattern: "route.*app/maps"
---

<objective>
Add the Field Map to the portal MODULES nav list, then run a human verification checkpoint to confirm the full map experience works end-to-end in production.

Purpose: Without the MODULES entry, the map is unreachable from the dashboard. The checkpoint gates deployment to confirm all Phase 70 success criteria are met.
Output: Maps module in nav + verified working interactive field map.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@glomalin-portal/src/lib/modules.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Field Map to MODULES + deploy</name>
  <files>glomalin-portal/src/lib/modules.ts</files>
  <action>
Read `glomalin-portal/src/lib/modules.ts` (already loaded in context above).

Add a new native module entry to the MODULES array. Insert it after the `field-ops` entry (field-ops was added in Phase 69 after compliance):

```typescript
{
  id: 'maps',
  label: 'Field Map',
  sublabel: 'Polygon Map & Field Detail',
  route: '/app/maps',
  status: 'live',
  type: 'native',
},
```

After editing, verify TypeScript compiles:
```bash
cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npx tsc --noEmit --skipLibCheck
```

Then build and deploy to production:
```bash
cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && npm run build 2>&1 | tail -20
```

If build succeeds, deploy via PM2 restart on the droplet:
```bash
ssh root@portal.whughesfarms.com "cd /var/www/glomalin-portal && git pull && npm ci --production=false && npm run build && pm2 restart glomalin-portal" 2>/dev/null || echo "Manual deploy needed — build succeeded locally"
```

If the SSH deploy fails (expected in local dev), document: "Build passed. Run manual deploy on droplet: cd /var/www/glomalin-portal && git pull && npm ci --production=false && npm run build && pm2 restart glomalin-portal"

Note: Set `NEXT_PUBLIC_MAPTILER_KEY` in the production .env for high-quality satellite tiles. Without it, the map falls back to demotiles (functional but lower quality). No Mapbox token is needed.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/glomalin-portal && grep -q "id: 'maps'" src/lib/modules.ts && echo "maps module found" && npx tsc --noEmit --skipLibCheck 2>&1 | grep -c "error" | xargs -I{} test {} -eq 0 && echo "No TS errors"</automated>
    <manual>Confirm 'maps' entry appears in modules.ts with correct route and type:native</manual>
  </verify>
  <done>MODULES array contains { id: 'maps', label: 'Field Map', sublabel: 'Polygon Map & Field Detail', route: '/app/maps', status: 'live', type: 'native' }. Build succeeds. Deployed or documented for manual deploy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — full field map end-to-end</name>
  <action>Pause for human verification of the complete Phase 70 deliverable. Claude has built: /app/maps with MapLibre GL satellite polygon rendering, crop colors, organic dashed borders, hover tooltips, fitBounds auto-zoom, compact legend, right-side detail panel with name/crop/organic/acres/timeline link, and a Field Boundaries section on /admin with drag-drop .zip shapefile import.</action>
  <verify>
    <automated>echo "Checkpoint — human verification required"</automated>
    <manual>
    1. Go to https://portal.whughesfarms.com — confirm "Field Map" card appears in module grid under "Polygon Map & Field Detail"
    2. Click Field Map — confirm /app/maps loads with a MapLibre satellite basemap (not blank/gray screen)
    3. If boundaries imported: colored polygons visible and map auto-zoomed to fit all fields; if not: empty map at default center (expected)
    4. Hover a field polygon — confirm field name tooltip appears
    5. Click a field polygon — confirm right slide-in detail panel shows name, crop badge, organic status, and reporting acres
    6. Confirm detail panel has "View Field Activity Timeline →" link at the bottom
    7. Confirm detail panel has NO weather section, NO GDD data, NO temperature display
    8. Click X or outside panel — panel dismisses
    9. Confirm compact legend is visible at bottom-left of the map (crop color swatches + organic dashed indicator)
    10. Confirm there is NO layer switcher or basemap toggle button on the map
    11. Go to /admin — scroll to "Field Boundaries" section — confirm it is embedded in the admin page (not a separate route)
    12. Drop a .zip shapefile in the upload zone — confirm summary report shows matched/updated counts and "Previous Boundaries Replaced" confirmation
    13. Return to /app/maps — confirm polygons appear for imported fields and map fitBounds to all of them

    If NEXT_PUBLIC_MAPTILER_KEY not set: map shows demotiles (lower quality) — functional but not satellite. Fix: add key to droplet .env.local and restart PM2.
    </manual>
  </verify>
  <done>Human confirms: (1) /app/maps renders MapLibre satellite polygon map, (2) crop colors and organic dashed borders visible, (3) map auto-zooms to fit all fields on load, (4) detail panel opens with name/crop/organic/acres/timeline link and no weather data, (5) compact legend always visible, (6) no layer switcher present, (7) /admin Field Boundaries section with .zip import works end-to-end</done>
</task>

</tasks>

<verification>
- Field Map module card visible on portal dashboard
- /app/maps loads without 404 or build errors
- At least satellite basemap renders (even before boundary import)
- No layer switcher visible on the map
- Detail panel has "View Field Activity Timeline" link
- Compact legend visible at bottom-left
- /admin has Field Boundaries section with shapefile upload (not a standalone route)
</verification>

<success_criteria>
All Phase 70 success criteria confirmed by human verify:
1. /app/maps loads and renders field polygons (after boundary import) with auto-zoom fitBounds
2. Fields color-coded by crop, organic fields have dashed border, compact legend always visible
3. Clicking opens detail panel with name/crop/organic/acres and timeline link — no weather/GDD
4. Boundaries stored in Supabase, loaded via API (not hardcoded)
5. Admin Field Boundaries section on /admin accepts .zip shapefile with full-replace import
</success_criteria>

<output>
After completion, create `.planning/phases/70-interactive-field-map/70-05-SUMMARY.md`
</output>
