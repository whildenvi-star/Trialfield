---
phase: 72-acreage-reconciliation-tool
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/app/api/fsa/fieldview/sync/route.ts
  - glomalin-portal/vercel.json
autonomous: false
requirements: [ACR-01]
user_setup:
  - service: climate-fieldview
    why: "Live FieldView OAuth as-planted sync (D-07) — code is complete, credentials are the blocker"
    env_vars:
      - name: FIELDVIEW_CLIENT_ID
        source: "Climate/Bayer FieldView developer portal → registered application"
      - name: FIELDVIEW_CLIENT_SECRET
        source: "Climate/Bayer FieldView developer portal → registered application"
      - name: FIELDVIEW_API_KEY
        source: "Climate/Bayer FieldView developer portal → registered application"
      - name: CRON_SECRET
        source: "Self-generated random secret; set in Vercel env + used by the cron header check"
    dashboard_config:
      - task: "Register a developer application with Climate FieldView / Bayer to obtain the three credentials"
        location: "Climate FieldView developer portal"
      - task: "Confirm redirect URI matches FIELDVIEW_REDIRECT_URI (already set to https://portal.whughesfarms.com/api/fsa/fieldview/callback)"
        location: "Climate FieldView developer portal"
must_haves:
  truths:
    - "A scheduled Vercel cron route can pull FieldView as-planted events and write them to coverage_events via the existing import_coverage_event RPC"
    - "The cron route is protected by a CRON_SECRET header check, not user session auth"
    - "Live OAuth activation is gated on external credential registration (human-verify) and nothing else in the phase depends on it (D-15)"
  artifacts:
    - path: "glomalin-portal/src/app/api/fsa/fieldview/sync/route.ts"
      provides: "scheduler-invoked as-planted sync, cron-secret protected"
      exports: ["GET"]
    - path: "glomalin-portal/vercel.json"
      provides: "crons[] entry hitting the sync route nightly"
      contains: "crons"
  key_links:
    - from: "vercel.json crons[]"
      to: "/api/fsa/fieldview/sync"
      via: "scheduled GET"
      pattern: "fieldview/sync"
    - from: "src/app/api/fsa/fieldview/sync/route.ts"
      to: "import_coverage_event RPC"
      via: "adapter fetchEvents → loop insert"
      pattern: "import_coverage_event"
---

<objective>
Activate the FieldView as-planted sync (ACR-01 source #3, D-07). The OAuth routes, adapter, and token table are already fully coded (RESEARCH Open Q4) — the code side is essentially done; the real blocker is external credential registration with Climate/Bayer (Pitfall 5), which is NOT a coding task. n8n is confirmed absent from the droplet (RESEARCH Open Q2), so scheduling uses the Vercel Cron fallback (D-04) hitting a new portal route.

Purpose: Complete the third ingest path. Because D-15 makes zero-automated-input a hard requirement, this plan is the ONLY one gated on external credentials and no other plan depends on it — the manual DAT ZIP import already covers the fallback.
Output: a cron-secret-protected sync route, a `vercel.json` crons entry, and a human-verify checkpoint for credential registration + n8n/SSH confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72-acreage-reconciliation-tool/72-RESEARCH.md
@.planning/phases/72-acreage-reconciliation-tool/72-PATTERNS.md

<interfaces>
CoverageAdapter interface (already implemented for FieldView, do not change signatures): `{ id; name; isConfigured: () => boolean; fetchEvents: (cropYear: number) => Promise<NormalizedCoverageEvent[]> }`. `createFieldViewAdapter()` calls `/v4/asPlanted` etc. against `https://platform.climate.com`.
Manual DAT import path (`fieldview/dat-import/route.ts`) already loops adapter output into the `import_coverage_event` RPC — mirror that write loop.
Vercel cron auth (standard pattern): `if (request.headers.get('authorization') !== 'Bearer ' + process.env.CRON_SECRET) return 401`.
FIELDVIEW_REDIRECT_URI is already set correctly in env; FIELDVIEW_CLIENT_ID/SECRET/API_KEY are currently empty.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FieldView scheduled sync route + vercel.json cron entry</name>
  <files>glomalin-portal/src/app/api/fsa/fieldview/sync/route.ts, glomalin-portal/vercel.json</files>
  <read_first>
    - glomalin-portal/src/app/api/fsa/fieldview/dat-import/route.ts (adapter invocation + import_coverage_event RPC write loop to mirror)
    - glomalin-portal/src/lib/fsa/adapters/fieldview.ts (createFieldViewAdapter, isConfigured, fetchEvents, NormalizedCoverageEvent)
    - glomalin-portal/src/app/api/fsa/coverage-import/route.ts (import_coverage_event RPC call shape)
  </read_first>
  <action>Create `fieldview/sync/route.ts` exporting `GET` (Vercel cron invokes GET). First line: cron-secret check — `if (request.headers.get('authorization') !== \`Bearer ${process.env.CRON_SECRET}\`) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })`. Then: build `createFieldViewAdapter()`; if `!adapter.isConfigured()` return `{ skipped: true, reason: 'FieldView credentials not configured' }` with 200 (so the cron does not error-spam while credentials are pending — respects D-15/D-17 that the automated path is optional). Otherwise call `adapter.fetchEvents(CURRENT_CROP_YEAR)` and loop-insert each event via the `import_coverage_event` RPC exactly as `dat-import` does, tallying inserted/skipped/errors; return the tally. Keep all FieldView tokens server-side — never return token material in the response. Create `vercel.json` (or add to it if present) with a `crons` array entry: `{ "path": "/api/fsa/fieldview/sync", "schedule": "0 7 * * *" }` (nightly ~2am US-Central ≈ 07:00 UTC). If `vercel.json` already exists, merge the crons entry without disturbing existing config.</action>
  <verify>
    <automated>cd glomalin-portal && npx tsc --noEmit 2>&1 | grep -c "fieldview/sync" | awk '{if($1==0)print "ok-ts"; else {print "FAIL"; exit 1}}' && grep -c "fieldview/sync" vercel.json</automated>
  </verify>
  <acceptance_criteria>
    - `sync/route.ts` exports `GET` and rejects requests without the correct `Bearer ${CRON_SECRET}` header (401)
    - When `isConfigured()` is false, returns 200 `{ skipped: true }` (no error spam while credentials pending)
    - When configured, loops adapter events into `import_coverage_event` (source assertion: `grep -c import_coverage_event sync/route.ts` >= 1)
    - No token material in any response body
    - `vercel.json` has a `crons` entry pointing at `/api/fsa/fieldview/sync`
    - `npx tsc --noEmit` no errors in the route
  </acceptance_criteria>
  <done>A cron-secret-protected sync route exists, degrades gracefully when unconfigured, and is scheduled via vercel.json.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Register FieldView developer app + confirm n8n absence + end-to-end connect</name>
  <what-built>A complete FieldView as-planted sync pipeline (OAuth routes, adapter, token table already existed; this phase added the scheduled sync route). The only remaining step is external credential registration, which no agent can perform.</what-built>
  <read_first>
    - glomalin-portal/src/app/api/fsa/fieldview/status/route.ts (connection-status endpoint to verify against)
    - glomalin-portal/src/app/api/fsa/fieldview/connect/route.ts (OAuth authorize redirect + CSRF state cookie)
    - glomalin-portal/.env.local (confirm FIELDVIEW_CLIENT_ID/SECRET/API_KEY presence — do not print values)
  </read_first>
  <how-to-verify>
    1. Register a developer application with Climate FieldView / Bayer; obtain FIELDVIEW_CLIENT_ID, FIELDVIEW_CLIENT_SECRET, FIELDVIEW_API_KEY. Confirm the app's redirect URI matches FIELDVIEW_REDIRECT_URI (https://portal.whughesfarms.com/api/fsa/fieldview/callback).
    2. Set the three FieldView env vars + a generated CRON_SECRET in the Vercel project env (and .env.local for local test).
    3. SSH to the droplet and confirm whether n8n is running (`pm2 list`, `docker ps`); record the result. If n8n IS unexpectedly present, note it — the Vercel Cron approach still works but the team may prefer n8n (RESEARCH Assumption A5 / Open Q2).
    4. Visit the portal, trigger the FieldView "Connect" flow, complete OAuth, and confirm `/api/fsa/fieldview/status` reports connected.
    5. Manually invoke the sync route with the CRON_SECRET header and confirm as-planted events land in coverage_events (or that it returns `skipped` cleanly if no as-planted data exists yet for the crop year).
  </how-to-verify>
  <acceptance_criteria>
    - The three FieldView credentials + CRON_SECRET are set in Vercel env
    - `/api/fsa/fieldview/status` reports a connected state after the OAuth flow
    - n8n presence/absence on the droplet is explicitly recorded in the SUMMARY
    - A manual sync invocation returns a non-error response (inserted tally or clean skip)
  </acceptance_criteria>
  <resume-signal>Type "approved" once credentials are registered and the connect + sync round-trip succeeds, or describe the blocker (e.g. Bayer approval pending — in which case the manual DAT import remains the operating path per D-17).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel scheduler → /api/fsa/fieldview/sync | Unauthenticated-by-session cron invocation must be authorized by a shared secret |
| sync route → FieldView API + fieldview_tokens | Per-user OAuth tokens must never reach the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-06-01 | Spoofing | cron sync route | mitigate | `Bearer ${CRON_SECRET}` header check rejects any caller without the shared secret (401) |
| T-72-06-02 | Information Disclosure | FieldView OAuth tokens | mitigate | Tokens stay in `fieldview_tokens` (RLS-scoped) and server-side fetch calls; sync route returns no token material |
| T-72-06-03 | Tampering | supply-chain (external credential) | mitigate | T-72-SC: credential registration is a blocking-human checkpoint; no new npm packages installed in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` no errors in the sync route
- `grep -c "fieldview/sync" glomalin-portal/vercel.json` >= 1
- Human-verify checkpoint approved (or blocker recorded; manual DAT import remains the D-17 fallback)
</verification>

<success_criteria>
The FieldView automated ingest path is code-complete and scheduled; live activation is correctly gated behind the external credential-registration checkpoint, and no other plan depends on it (D-15 preserved).
</success_criteria>

<output>
Create `.planning/phases/72-acreage-reconciliation-tool/72-06-SUMMARY.md` when done, recording the n8n droplet finding and credential status.
</output>
