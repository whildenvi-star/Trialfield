---
phase: 72-acreage-reconciliation-tool
plan: 07
type: execute
wave: 3
depends_on: ["72-01", "72-02", "72-03", "72-04", "72-05", "72-06"]
files_modified:
  - glomalin-portal/supabase/migrations/
autonomous: false
requirements: [ACR-01, ACR-02, ACR-03, ACR-04, ACR-05, ACR-06]
must_haves:
  truths:
    - "All five new migrations (037-041) are applied to the live Supabase database"
    - "The new RPCs (merge_reporting_units, insert_manual_clu, extended get_farm_reconciliation, extended export_clu_shapefile_rows) exist and run against real data without error"
    - "The full vitest suite is green and tsc has no new errors after the whole phase"
  artifacts:
    - path: "glomalin-portal/supabase/migrations/037_acreage_thresholds.sql"
      provides: "confirmation all phase migrations are present and applied"
      contains: "acreage_thresholds"
  key_links:
    - from: "live Supabase database"
      to: "phase migrations 037-041"
      via: "apply_migration / supabase db push"
      pattern: "037|038|039|040|041"
---

<objective>
[BLOCKING] Apply every schema change this phase produced to the live Supabase database, then run whole-phase verification. Build and type checks pass WITHOUT the push because the Supabase types come from config, not the live DB — so skipping this creates a false-positive green state where the RPCs and columns don't actually exist in production (schema_push_requirement).

Purpose: Migrations 037 (acreage_thresholds), 038 (merge/draw RPCs + geometry widen), 039 (clu_boundaries MultiPolygon widen), 040 (RMA columns + export RPC), and 041 (coverage-folding reconciliation RPC) are inert files until applied. This plan applies them in order and smoke-tests the new RPCs against real data.
Output: applied migrations, RPC smoke-test results, and a green full test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72-acreage-reconciliation-tool/72-RESEARCH.md
@.planning/phases/72-acreage-reconciliation-tool/72-01-SUMMARY.md
@.planning/phases/72-acreage-reconciliation-tool/72-02-SUMMARY.md
@.planning/phases/72-acreage-reconciliation-tool/72-03-SUMMARY.md
@.planning/phases/72-acreage-reconciliation-tool/72-04-SUMMARY.md
@.planning/phases/72-acreage-reconciliation-tool/72-05-SUMMARY.md
@.planning/phases/72-acreage-reconciliation-tool/72-06-SUMMARY.md

<interfaces>
Repo convention (RESEARCH Open Q5): sequential SQL files in `glomalin-portal/supabase/migrations/`, applied via Supabase MCP `apply_migration` (project convention — Supabase MCP is read-only for queries; schema writes go through apply_migration) OR `supabase db push` with SUPABASE_ACCESS_TOKEN set. Apply in ascending numeric order: 037 → 038 → 039 → 040 → 041.
Migrations are idempotent (IF NOT EXISTS / CREATE OR REPLACE / guarded ALTERs) so re-application is safe.
Widen ordering note: 038 widens clu_records.split_geometry; 039 widens clu_boundaries.geometry — both must precede any insert that produces MultiPolygon geometry.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Apply migrations 037-041 to live Supabase</name>
  <what-built>Five migrations produced across plans 72-01..72-05, all idempotent, defining the acreage_thresholds table, merge/draw RPCs, two geometry widenings, the RMA schema + export RPC, and the coverage-folding reconciliation RPC.</what-built>
  <read_first>
    - glomalin-portal/supabase/migrations/037_acreage_thresholds.sql
    - glomalin-portal/supabase/migrations/038_manual_spatial_editing.sql
    - glomalin-portal/supabase/migrations/039_widen_clu_boundaries_geometry.sql
    - glomalin-portal/supabase/migrations/040_clu_records_rma_schema.sql
    - glomalin-portal/supabase/migrations/041_farm_reconciliation_coverage.sql
  </read_first>
  <how-to-verify>
    1. Apply each migration 037→041 in ascending order via Supabase MCP `apply_migration` (one call per file, named after the file) OR `supabase db push` from `glomalin-portal/` with SUPABASE_ACCESS_TOKEN set. If the tool requires interactive prompts/credentials not available, stop and request them (this is why the task is non-autonomous).
    2. Confirm each applied with no error. Migrations are idempotent — a re-run must not error.
    3. Verify presence via read-only queries: `select count(*) from acreage_thresholds` (>=1 global row); `select proname from pg_proc where proname in ('merge_reporting_units','insert_manual_clu','get_farm_reconciliation','export_clu_shapefile_rows')` (all four present); confirm `clu_boundaries.geometry` and `clu_records.split_geometry` are both `MultiPolygon` via `geometry_columns`; confirm `clu_records` has columns `unit_number, grower, share_pct, policy_number, failed_acre, source_flag`.
  </how-to-verify>
  <acceptance_criteria>
    - All five migrations applied to the live database without error
    - `acreage_thresholds` has exactly one seeded global row
    - `merge_reporting_units`, `insert_manual_clu`, `get_farm_reconciliation` (coverage-folding), `export_clu_shapefile_rows` (RMA-extended) all exist in `pg_proc`
    - `clu_boundaries.geometry` and `clu_records.split_geometry` are both `geometry(MultiPolygon,4326)`
    - `clu_records` has all six RMA columns
  </acceptance_criteria>
  <resume-signal>Type "applied" once all five migrations are live and the presence queries pass, or describe the blocker (missing credentials / apply error).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Whole-phase verification — RPC smoke tests + full suite</name>
  <files>glomalin-portal/supabase/migrations/</files>
  <read_first>
    - glomalin-portal/src/app/api/fsa/clu-records/merge/route.ts (merge RPC contract to smoke-test)
    - glomalin-portal/src/app/api/fsa/export-shapefile/route.ts (RMA export to smoke-test)
    - glomalin-portal/src/app/api/fsa/reconciliation/route.ts (coverage-folding output to smoke-test)
  </read_first>
  <action>With migrations live: (1) Run the full test suite `npm run test` (vitest run) in `glomalin-portal/` — must be green (thresholds.test.ts, merge/route.test.ts, clu-boundaries/import/route.test.ts, export-shapefile/route.test.ts all pass). (2) Run `npx tsc --noEmit` — no new errors across the phase. (3) Smoke-test the new RPCs against real data via read-only queries or a scratch call: call `get_farm_reconciliation` for a real farm/year and confirm coverage rows appear when as-planted coverage exists; hit `/api/fsa/export-shapefile` for a farm and confirm the returned .zip's DBF includes the RMA fields (or verify the RPC returns the six new columns); call `merge_reporting_units` in a transaction on two throwaway/superseded-safe test CLU rows and roll back (or verify via the merge route against a disposable pair), confirming ST_Union acreage is computed. Record all results in the SUMMARY. If any smoke test fails, this is a gap to route back — do not mark the phase complete.</action>
  <verify>
    <automated>cd glomalin-portal && npm run test 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | grep -c "error TS" | awk '{if($1==0)print "tsc-ok"; else {print "FAIL tsc errors"; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` full suite green (all four new test files pass)
    - `npx tsc --noEmit` reports zero `error TS` lines
    - `get_farm_reconciliation` returns coverage rows for a farm with as-planted data (smoke)
    - Export path includes RMA fields; merge RPC computes ST_Union acreage (smoke)
    - Results recorded in SUMMARY; any failure flagged as a gap, not silently passed
  </acceptance_criteria>
  <done>All migrations are live, the full suite is green, tsc is clean, and the new RPCs are confirmed working against real data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → live Supabase (apply_migration) | Schema DDL applied to production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-07-01 | Tampering | production migration apply | mitigate | Migrations are idempotent + reviewed; applied in ascending order under a blocking-human checkpoint; presence queries confirm expected state before proceeding |
| T-72-07-02 | Denial of Service | geometry-widen ALTER on populated tables | accept | 015/019 precedent shows the same widen ran safely on these tables; clu_boundaries/clu_records row counts are small (56 fields) — no lock-duration concern |
</threat_model>

<verification>
- All five migrations applied and confirmed via `pg_proc` / `geometry_columns` / column presence queries
- `npm run test` green; `npx tsc --noEmit` clean
- RPC smoke tests pass against real data
</verification>

<success_criteria>
The phase's schema is live in production, every new RPC and column exists and works against real data, and the full automated suite plus type check are green — no false-positive verification state.
</success_criteria>

<output>
Create `.planning/phases/72-acreage-reconciliation-tool/72-07-SUMMARY.md` when done, recording applied-migration list and smoke-test results.
</output>
