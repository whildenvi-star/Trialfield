---
phase: 72-acreage-reconciliation-tool
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - glomalin-portal/supabase/migrations/041_farm_reconciliation_coverage.sql
  - glomalin-portal/src/lib/fsa/reconciliation.ts
  - glomalin-portal/src/app/api/fsa/reconciliation/route.ts
  - glomalin-portal/src/components/fsa/reconciliation-view.tsx
  - glomalin-portal/src/components/compliance/acreage-tab.tsx
autonomous: true
requirements: [ACR-02, ACR-04, ACR-05]
must_haves:
  truths:
    - "The farm-level reconciliation folds in coverage_events (as-planted), not just management_zones, so CLU x as-planted discrepancies appear farm-wide"
    - "The orphaned ReconciliationView is wired into the Acreage tab as a 6th view and renders live"
    - "Discrepancy Green/Yellow/Red coloring comes from the configurable threshold engine (plan 72-01), not the retired hardcoded cutoffs"
  artifacts:
    - path: "glomalin-portal/supabase/migrations/041_farm_reconciliation_coverage.sql"
      provides: "get_farm_reconciliation extended (or v2) folding coverage_events"
      contains: "coverage_events"
    - path: "glomalin-portal/src/components/compliance/acreage-tab.tsx"
      provides: "reconciliation as 6th view in the tab toggle"
      contains: "reconciliation"
  key_links:
    - from: "src/components/compliance/acreage-tab.tsx"
      to: "src/components/fsa/reconciliation-view.tsx"
      via: "dynamic import + view render"
      pattern: "reconciliation-view"
    - from: "src/lib/fsa/reconciliation.ts"
      to: "src/lib/fsa/thresholds.ts"
      via: "evaluateThreshold with fetched ThresholdConfig"
      pattern: "evaluateThreshold"
    - from: "src/app/api/fsa/reconciliation/route.ts"
      to: "acreage_thresholds"
      via: "config fetched and included in response"
      pattern: "acreage_thresholds"
---

<objective>
Deliver the reconciliation OUTPUT surface (ACR-04 map view + ACR-05 tabular report) by (a) extending the farm-level PostGIS reconciliation to fold in `coverage_events` (as-planted) — today `get_farm_reconciliation` only compares CLU x zone; the true CLU x as-planted comparison only exists at single-CLU level — and (b) promoting the fully-built-but-orphaned `ReconciliationView` into the Acreage tab as a 6th view, wired to the configurable threshold engine from plan 72-01. RESEARCH is explicit: PROMOTE the orphaned components, do not rebuild them.

Purpose: ACR-02 requires the overlay to produce per-CLU/crop reconciliation records folding as-planted; ACR-04/ACR-05 require the map + tabular report — all of which the orphaned ReconciliationView already renders, just unwired and using retired thresholds.
Output: migration 041 (coverage-folding RPC), reconciliation.ts extended with a coverage array + threshold wiring, reconciliation route returning threshold config, and the 6th Acreage-tab view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72-acreage-reconciliation-tool/72-RESEARCH.md
@.planning/phases/72-acreage-reconciliation-tool/72-PATTERNS.md
@.planning/phases/72-acreage-reconciliation-tool/72-01-SUMMARY.md

<interfaces>
From plan 72-01 (already merged): `src/lib/fsa/thresholds.ts` exports `evaluateThreshold(delta, fsaAcres, config)`, `ThresholdConfig`, `DEFAULT_THRESHOLDS`, `toReconciliationStatus`. `acreage_thresholds` table holds one global config row.
reconciliation.ts today: `ReconciliationRpcRow` (from get_farm_reconciliation), `ReconciliationRow { ...; zones: ZoneIntersection[]; status; cause; delta; fsa_acres }`, `ZoneIntersection`, `groupRpcRows()` (calls deltaStatus/deltaCause), `cluFeatureCollection`/`zoneFeatureCollection` (each feature already carries a `status` property for map coloring — the existing D-20 hook).
reconciliation route today: `supabase.rpc('get_farm_reconciliation', { p_farm, p_year })`.
acreage-tab.tsx today (126 lines): `const [view, setView] = useState<'clu'|'map'|'zones'|'overlay'|'coverage'>('map')`; labels map at line 77; view render blocks lines 91-123; `wrapClass` ternary lines 53-55; dynamic-import pattern lines 12-21.
ReconciliationView already accepts `cropYear` (optional, defaults CURRENT_CROP_YEAR) and internally lazy-loads ReconciliationMap; its `SummaryBar` reads `{ total, ok, flagged, unresolved }`.
PITFALL 3: keep discrepancy-status coloring (D-11-14) semantically SEPARATE from reporting-map's workflow-completion coloring — do not merge the two.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: migration 041 — fold coverage_events into farm reconciliation</name>
  <files>glomalin-portal/supabase/migrations/041_farm_reconciliation_coverage.sql</files>
  <read_first>
    - glomalin-portal/supabase/migrations/012_reconciliation_rpc.sql (get_farm_reconciliation — the CLU x zone intersection to extend)
    - glomalin-portal/supabase/migrations/033_clu_overlay_intersections.sql (get_clu_overlay_intersections — already does CLU-zone-coverage at single-CLU level; the coverage-join shape to copy)
  </read_first>
  <action>Create migration `041_farm_reconciliation_coverage.sql`. `CREATE OR REPLACE FUNCTION get_farm_reconciliation(...)` copying migration 012's body, then add a join/intersection against `coverage_events` the same way it currently intersects `management_zones` — using `ST_MakeValid()` on BOTH sides of every intersection (Pitfall 4) and the exact `ROUND(CAST(ST_Area(...::geography)/4046.856422 AS numeric),2)` acreage divisor. Return an additional coverage row-set analogous to the existing zone columns: `coverage_id`, `coverage_geojson`, `coverage_crop`, `coverage_acres` (nullable, present when a CLU intersects an as-planted coverage polygon). Preserve every existing returned column and the existing zone intersection output unchanged (additive). If altering the RETURNS TABLE signature in place is awkward, create a sibling `get_farm_reconciliation_v2` with the superset columns and have the route call the v2 — either is acceptable; document the choice in the migration header.</action>
  <verify>
    <automated>grep -v '^--' glomalin-portal/supabase/migrations/041_farm_reconciliation_coverage.sql | grep -c -E "coverage_events|ST_MakeValid|4046.856422" | awk '{if($1>=3)print "ok"; else {print "FAIL"; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - RPC folds `coverage_events` into the farm-level reconciliation (source assertion: `coverage_events` referenced in a join/intersection)
    - Every intersection wraps both geometries in `ST_MakeValid()`
    - Acreage uses the `/ 4046.856422` divisor
    - Existing zone-intersection output columns preserved (additive change)
  </acceptance_criteria>
  <done>Farm-level reconciliation now compares CLU against as-planted coverage, not just zones.</done>
</task>

<task type="auto">
  <name>Task 2: reconciliation.ts coverage array + threshold-engine wiring; route returns config</name>
  <files>glomalin-portal/src/lib/fsa/reconciliation.ts, glomalin-portal/src/app/api/fsa/reconciliation/route.ts</files>
  <read_first>
    - glomalin-portal/src/lib/fsa/reconciliation.ts (ReconciliationRpcRow, ReconciliationRow, ZoneIntersection, groupRpcRows lines 82-135, cluFeatureCollection/zoneFeatureCollection)
    - glomalin-portal/src/lib/fsa/thresholds.ts (evaluateThreshold, ThresholdConfig, toReconciliationStatus — from plan 72-01)
    - glomalin-portal/src/app/api/fsa/reconciliation/route.ts (existing get_farm_reconciliation rpc call + response shape)
  </read_first>
  <action>In `reconciliation.ts`: add interface `CoverageIntersection` mirroring `ZoneIntersection` (coverage_id, coverage_geojson, coverage_crop, coverage_acres); extend `ReconciliationRpcRow` with the new coverage columns and `ReconciliationRow` with `coverage: CoverageIntersection[]`; in `groupRpcRows()` push coverage rows into the new array exactly like zones are pushed. Change `groupRpcRows` to accept a `config: ThresholdConfig` parameter (default `DEFAULT_THRESHOLDS` for back-compat) and compute each row's status via `toReconciliationStatus(evaluateThreshold(entry.delta, entry.fsa_acres, config))` instead of the retired `deltaStatus` — the per-feature `status` used by `cluFeatureCollection`/`zoneFeatureCollection` for D-20 map coloring now flows from the configurable engine. Keep the `'ok'|'flagged'|'unresolved'` vocabulary so downstream code and the SummaryBar (`{total,ok,flagged,unresolved}`) are unchanged. In `reconciliation/route.ts`: after calling the (now coverage-folding) RPC, also fetch the global `acreage_thresholds` row (fall back to `DEFAULT_THRESHOLDS`), pass it into `groupRpcRows(rows, config)`, and include the config in the JSON response so the client can render the same thresholds if it recomputes.</action>
  <verify>
    <automated>cd glomalin-portal && grep -c "evaluateThreshold" src/lib/fsa/reconciliation.ts | awk '{if($1>=1)print "ok"; else {print "FAIL"; exit 1}}' && npx tsc --noEmit 2>&1 | grep -c -E "reconciliation.ts|reconciliation/route" | awk '{if($1==0)print "tsc-ok"; else {print "FAIL"; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - `CoverageIntersection` type added and `ReconciliationRow.coverage[]` populated by `groupRpcRows`
    - `groupRpcRows` computes status via `evaluateThreshold` (source assertion: grep match) — not the retired `deltaStatus`
    - reconciliation route fetches `acreage_thresholds`, passes config to `groupRpcRows`, and includes it in the response body
    - Status vocabulary stays `'ok'|'flagged'|'unresolved'` (SummaryBar unchanged)
    - `npx tsc --noEmit` no errors in these two files
  </acceptance_criteria>
  <done>Reconciliation rows carry as-planted coverage intersections and Green/Yellow/Red status is computed from the configurable engine.</done>
</task>

<task type="auto">
  <name>Task 3: wire ReconciliationView as the 6th Acreage-tab view + threshold coloring</name>
  <files>glomalin-portal/src/components/compliance/acreage-tab.tsx, glomalin-portal/src/components/fsa/reconciliation-view.tsx</files>
  <read_first>
    - glomalin-portal/src/components/compliance/acreage-tab.tsx (view-state union line 32, dynamic-import lines 12-21, labels line 77, view render blocks 91-123, wrapClass ternary 53-55)
    - glomalin-portal/src/components/fsa/reconciliation-view.tsx (already-built structure: farm picker, parallel fetch, three-panel layout, SummaryBar, export buttons — the component being promoted)
    - glomalin-portal/src/lib/fsa/reconciliation.ts (updated coverage array + status source from Task 2)
  </read_first>
  <action>In `acreage-tab.tsx`: add `'reconciliation'` to the `view` union type and the toggle array literal; add a `reconciliation: 'Reconciliation'` entry to the `labels` map; add a render block `{view === 'reconciliation' && <ReconciliationView cropYear={CURRENT_CROP_YEAR} />}`; add `ReconciliationView` as a `dynamic(() => import('@/components/fsa/reconciliation-view').then(m => m.ReconciliationView), { ssr: false })`; extend the `wrapClass` ternary so `'reconciliation'` also gets the full-viewport-height branch alongside `'overlay'`/`'map'` (ReconciliationView has an internal three-panel flex layout). Do NOT add a new top-level nav item (D-01) — this is only an addition to the tab-internal toggle. In `reconciliation-view.tsx`: ensure the fetched reconciliation response's threshold config (from Task 2) drives the Green/Yellow/Red map ring/fill and table cell coloring via the existing per-feature `status` property (the plumbing already exists — only confirm the values now originate from `evaluateThreshold`); if the component currently displays as-planted vs CLU deltas, surface the new `coverage` array so as-planted appears alongside zones; surface the `source_flag` per row if present (added in plan 72-05) so ACR-05's manual/overridden marking is honored (defensive: render only if the field exists). Keep the reporting-map workflow-status coloring semantically separate (Pitfall 3) — do not repurpose it for discrepancy status.</action>
  <verify>
    <automated>cd glomalin-portal && grep -c "reconciliation" src/components/compliance/acreage-tab.tsx | awk '{if($1>=2)print "ok"; else {print "FAIL"; exit 1}}' && npx tsc --noEmit 2>&1 | grep -c -E "acreage-tab|reconciliation-view" | awk '{if($1==0)print "tsc-ok"; else {print "FAIL"; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - `acreage-tab.tsx` view union includes `'reconciliation'`, labels has `reconciliation: 'Reconciliation'`, and a render block mounts `<ReconciliationView>` (source assertions)
    - `ReconciliationView` is dynamically imported with `ssr: false`
    - `wrapClass` grants `'reconciliation'` the full-viewport-height branch
    - No new top-level nav entry added (D-01 preserved)
    - `npx tsc --noEmit` no errors in either file
  </acceptance_criteria>
  <done>The Acreage tab exposes a working Reconciliation view rendering the CLU/as-planted map + tabular report with configurable Green/Yellow/Red status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/fsa/reconciliation | Farm/year params cross into the SECURITY DEFINER reconciliation RPC |
| reconciliation route → acreage_thresholds | Config read influences which discrepancies are flagged vs hidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-02-01 | Elevation of Privilege | reconciliation route | mitigate | Existing `requireModuleAccess('fsa-578')` guard on the route preserved; RPC is SECURITY DEFINER scoped to reads |
| T-72-02-02 | Tampering | malformed coverage/zone geometry | mitigate | `ST_MakeValid()` wraps both sides of every intersection in the RPC (Pitfall 4) — no TopologyException crash on real parcels |
| T-72-02-03 | Information Disclosure | reconciliation response | accept | Response returns farm acreage/crop data already visible to any fsa-578 module user; no new sensitive fields exposed |
</threat_model>

<verification>
- `grep -c "evaluateThreshold" glomalin-portal/src/lib/fsa/reconciliation.ts` >= 1
- `grep -c "reconciliation" glomalin-portal/src/components/compliance/acreage-tab.tsx` >= 2 (view + render)
- `npx tsc --noEmit` no new errors across the modified files
- Migration 041 applied in plan 72-07; farm reconciliation smoke query confirms coverage rows appear
</verification>

<success_criteria>
The Acreage tab has a live Reconciliation view (promoted from the orphaned components) that shows CLU vs as-planted discrepancies farm-wide with configurable Green/Yellow/Red coloring and a tabular report — satisfying ACR-02 (folded overlay), ACR-04 (map), and ACR-05 (table).
</success_criteria>

<output>
Create `.planning/phases/72-acreage-reconciliation-tool/72-02-SUMMARY.md` when done.
</output>
