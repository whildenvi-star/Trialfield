---
phase: 72-acreage-reconciliation-tool
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/supabase/migrations/040_clu_records_rma_schema.sql
  - glomalin-portal/src/app/api/fsa/export-shapefile/route.ts
  - glomalin-portal/src/app/api/fsa/export-shapefile/route.test.ts
  - glomalin-portal/src/app/api/fsa/clu-records/route.ts
  - glomalin-portal/src/components/fsa/clu-workspace.tsx
autonomous: true
requirements: [ACR-05, ACR-06]
must_haves:
  truths:
    - "Each reporting unit carries the full RMA attribute set: grower/policyholder, RMA unit number, policy number, share %, prevent-plant vs failed-acre, and a source flag (auto/manual/overridden)"
    - "The exported .shp DBF contains all RMA fields with ≤10-char names and no truncation collisions"
    - "A user can enter/edit RMA unit number, grower, share %, and policy number per reporting unit before export"
  artifacts:
    - path: "glomalin-portal/supabase/migrations/040_clu_records_rma_schema.sql"
      provides: "RMA columns on clu_records + extended export_clu_shapefile_rows RPC"
      contains: "source_flag"
    - path: "glomalin-portal/src/app/api/fsa/export-shapefile/route.ts"
      provides: "DBF_FIELDS extended with RMA_UNIT/GROWER/SHARE_PCT/POLICY_NO/FAILED_AC/SRC_FLAG"
      contains: "RMA_UNIT"
  key_links:
    - from: "src/app/api/fsa/export-shapefile/route.ts"
      to: "clu_records RMA columns"
      via: "export_clu_shapefile_rows RPC → dbfRows"
      pattern: "RMA_UNIT"
    - from: "src/components/fsa/clu-workspace.tsx"
      to: "/api/fsa/clu-records"
      via: "RMA field edit → PUT"
      pattern: "unit_number|share_pct"
---

<objective>
Extend the reporting-unit schema and the shapefile export to carry the full RMA attribute set (ACR-06 / D-18), and give the user an editable per-unit RMA field surface (ACR-05, D-19: RMA unit number is user-entered). The hand-rolled shapefile writer already works and is production-tested — EXTEND its `DBF_FIELDS` array in place; do NOT introduce a new shapefile library (RESEARCH Don't-Hand-Roll).

Purpose: ACR-05 tabular report and ACR-06 output .shp both require fields that don't exist yet: grower/policyholder, share %, RMA unit number, policy number, a genuine prevent-plant vs failed-acre distinction, and the auto/manual/overridden source flag.
Output: migration 040 (RMA columns + extended export RPC), extended DBF export, extended clu-records write route, and RMA edit fields in the CLU Records workspace.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72-acreage-reconciliation-tool/72-RESEARCH.md
@.planning/phases/72-acreage-reconciliation-tool/72-PATTERNS.md

<interfaces>
D-18 RMA output schema (full set): grower/policyholder, FSA farm#, tract#, CLU/field#, RMA unit number (user-entered), crop code & name, type/variety/practice, intended use, reported acres (0.1ac), plant date, share (grower %), prevent-plant/failed-acre flag, source flag (auto/manual/overridden).
Existing clu_records already has: farm_number, tract_number, clu, field_name, crop, use (intended use), fsa_acres, irrigated, organic, prevented_planting (boolean), prevented_planting_acres. MISSING (confirmed via grep — no migration defines them): unit_number, policy_number, grower, share_pct, failed_acre distinction, source_flag. Add all missing with ADD COLUMN IF NOT EXISTS (idempotent, safe even if some already exist in prod).
DBF Pitfall 6: dBASE III field names ≤10 chars; `buildDbf()` truncates via `substring(0,10)` and does NOT error on collision. Existing names: FARM_NBR, TRACT_NBR, CLU_NBR, FIELD_NAME, CROP, USE, FSA_ACRES, CALC_ACRES, IRRIGATED, ORGANIC, COVER_CROP, PREV_PLANT, PLANT_DATE, STATUS, GLM_ZONE, GLM_CROP, GLM_IRR, GLM_ORG. New (all ≤10): RMA_UNIT, GROWER, SHARE_PCT, POLICY_NO, FAILED_AC, SRC_FLAG.
export_clu_shapefile_rows RPC is defined in migration 016 and updated in 033/034 — it feeds `dbfRows` in export-shapefile/route.ts (line ~236 rpc call, ~267 dbfRows.push).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: migration 040 — RMA columns + extended export_clu_shapefile_rows RPC</name>
  <files>glomalin-portal/supabase/migrations/040_clu_records_rma_schema.sql</files>
  <read_first>
    - glomalin-portal/supabase/migrations/024_clu_records_schema.sql (clu_records column conventions, prevented_planting/prevented_planting_acres)
    - glomalin-portal/supabase/migrations/016_export_shapefile_rpc.sql (export_clu_shapefile_rows definition — the RETURNS TABLE column set)
    - glomalin-portal/supabase/migrations/034_clu_intended_use.sql (latest CREATE OR REPLACE of export_clu_shapefile_rows — the version to extend)
  </read_first>
  <action>Create migration `040_clu_records_rma_schema.sql`. (a) `ALTER TABLE clu_records ADD COLUMN IF NOT EXISTS` for each missing RMA field: `unit_number text` (RMA unit #, user-entered), `policy_number text`, `grower text` (grower/policyholder name), `share_pct numeric(5,2)` (grower share %), `failed_acre boolean NOT NULL DEFAULT false` (distinct from prevented_planting — failed acreage vs prevent-plant), `source_flag text NOT NULL DEFAULT 'auto' CHECK (source_flag IN ('auto','manual','overridden'))`. (b) `CREATE OR REPLACE FUNCTION export_clu_shapefile_rows(...)` copying the latest version (from migration 034) verbatim, then extend its `RETURNS TABLE(...)` and SELECT to also emit `unit_number`, `grower`, `share_pct`, `policy_number`, `failed_acre`, `source_flag`. Preserve every existing returned column and the existing acreage/geometry logic unchanged — additive only.</action>
  <verify>
    <automated>grep -v '^--' glomalin-portal/supabase/migrations/040_clu_records_rma_schema.sql | grep -c -E "unit_number|source_flag|share_pct|export_clu_shapefile_rows" | awk '{if($1>=4)print "ok"; else {print "FAIL"; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - All six RMA columns added with `ADD COLUMN IF NOT EXISTS` (idempotent)
    - `source_flag` has a CHECK constraint limiting values to auto/manual/overridden, default 'auto'
    - `failed_acre` is a separate boolean from the existing `prevented_planting`
    - `export_clu_shapefile_rows` is CREATE OR REPLACE'd emitting the six new columns while preserving all existing returned columns
  </acceptance_criteria>
  <done>clu_records carries the full RMA schema and the export RPC returns it, additively.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extend DBF_FIELDS + export rows with RMA fields</name>
  <files>glomalin-portal/src/app/api/fsa/export-shapefile/route.ts, glomalin-portal/src/app/api/fsa/export-shapefile/route.test.ts</files>
  <read_first>
    - glomalin-portal/src/app/api/fsa/export-shapefile/route.ts (DBF_FIELDS array lines 291-310, dbfRows.push mapping lines 255-279, rpc call line 236, buildDbf truncation)
    - glomalin-portal/src/lib/fsa/shapefile.ts (buildDbf/buildShpAndShx helpers, DbfField type)
  </read_first>
  <behavior>
    - DBF_FIELDS contains RMA_UNIT, GROWER, SHARE_PCT, POLICY_NO, FAILED_AC, SRC_FLAG
    - No two DBF field names collide after `substring(0,10)` truncation (assert unique truncated set)
    - dbfRows map each new column from the RPC row (RMA_UNIT ← row.unit_number, GROWER ← row.grower, SHARE_PCT ← numeric row.share_pct, POLICY_NO ← row.policy_number, FAILED_AC ← row.failed_acre ? 'Y':'N', SRC_FLAG ← row.source_flag)
  </behavior>
  <action>Add the six new entries to `DBF_FIELDS` (all ≤10 chars): `{ name: 'RMA_UNIT', type: 'C', length: 15 }`, `{ name: 'GROWER', type: 'C', length: 30 }`, `{ name: 'SHARE_PCT', type: 'N', length: 6, decimals: 2 }`, `{ name: 'POLICY_NO', type: 'C', length: 20 }`, `{ name: 'FAILED_AC', type: 'C', length: 1 }`, `{ name: 'SRC_FLAG', type: 'C', length: 12 }`. Extend the `dbfRows.push({...})` mapping to populate each from the corresponding RPC row column (per the behavior mapping). Write `export-shapefile/route.test.ts` (vitest) that imports/exposes the DBF_FIELDS list (extract it to a small exported const if needed to make it testable) and asserts: (1) all six RMA field names are present; (2) the set of `name.substring(0,10)` values across ALL DBF_FIELDS has no duplicates (ACR-06 Pitfall 6 collision guard).</action>
  <verify>
    <automated>cd glomalin-portal && npx vitest run src/app/api/fsa/export-shapefile/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RMA_UNIT" export-shapefile/route.ts` >= 1 and all six new fields present
    - `route.test.ts` passes, including the no-truncation-collision assertion across all DBF field names
    - dbfRows maps every new column from the RPC row (source assertion for each new key)
    - `npx tsc --noEmit` no errors in this file
  </acceptance_criteria>
  <done>Exported shapefile carries the full RMA attribute schema with collision-free ≤10-char DBF names, unit-tested.</done>
</task>

<task type="auto">
  <name>Task 3: RMA field editing in CLU Records workspace + write route</name>
  <files>glomalin-portal/src/app/api/fsa/clu-records/route.ts, glomalin-portal/src/components/fsa/clu-workspace.tsx</files>
  <read_first>
    - glomalin-portal/src/app/api/fsa/clu-records/route.ts (existing GET/PUT/POST field allowlist and write shape)
    - glomalin-portal/src/components/fsa/clu-workspace.tsx (accordion CRUD, existing per-CLU editable fields, escapeCell CSV export)
    - glomalin-portal/src/lib/fsa/calc.ts (CluRecord type — add the new RMA fields)
  </read_first>
  <action>In `clu-records/route.ts`, extend the accepted-field allowlist for writes to include `unit_number`, `policy_number`, `grower`, `share_pct`, `failed_acre`, `source_flag`. Validate `share_pct` is 0–100 when present; validate `source_flag` ∈ {auto,manual,overridden}. When a user manually edits acreage or any geometry-derived value, set `source_flag='overridden'` server-side (mirror the implicit-lock pattern used elsewhere, e.g. Phase 53 price manual-override auto-set); when a row originated from manual draw (plan 72-03) it is already 'manual'. In `clu-workspace.tsx`, add editable inputs for RMA unit number, grower, share %, and policy number to the per-CLU edit surface (reuse the existing input/row styling in that component), and display the `source_flag` as a small badge (auto/manual/overridden) so ACR-05's "manual/overridden values clearly marked" is satisfied. Add the six fields to the `CluRecord` interface in calc.ts. Also harden the existing `escapeCell()` in clu-workspace against leading formula characters (`= + - @`) — prefix such cells with a `'` — since RMA export adds new user-entered string fields (RESEARCH Security: CSV/DBF injection).</action>
  <verify>
    <automated>cd glomalin-portal && npx tsc --noEmit 2>&1 | grep -c -E "clu-workspace|clu-records/route|calc.ts" | awk '{if($1==0)print "ok"; else {print "FAIL"; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - clu-records write route accepts and validates the six RMA fields; `share_pct` bounded 0–100; `source_flag` enum-checked
    - Manual acreage/geometry override sets `source_flag='overridden'` server-side
    - clu-workspace exposes editable RMA unit #, grower, share %, policy # inputs and a source-flag badge
    - `CluRecord` type in calc.ts includes the six new fields
    - `escapeCell` neutralizes leading `= + - @` in exported cells
    - `npx tsc --noEmit` no errors in the three files
  </acceptance_criteria>
  <done>User can enter every user-supplied RMA field per reporting unit, source flags are shown and auto-set on override, and CSV export is injection-hardened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/fsa/clu-records (PUT) | User-entered RMA strings (grower, unit#, policy#) and share % cross into clu_records |
| clu_records → exported .shp/.csv | User-entered strings flow into DBF/CSV files opened in Excel/QGIS by the insurance agent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-05-01 | Tampering | CSV/DBF formula injection | mitigate | `escapeCell` prefixes leading `= + - @` with `'` for the new user-entered RMA string fields |
| T-72-05-02 | Tampering | share_pct / source_flag values | mitigate | Server-side validation: share_pct 0–100, source_flag enum CHECK at both route and DB layer |
| T-72-05-03 | Elevation of Privilege | clu-records write route | mitigate | `requireModuleAccess('fsa-578')` guard (existing on this route) + field allowlist (reject unknown keys) |
| T-72-05-04 | Repudiation | manual override provenance | mitigate | `source_flag` auto-set to 'overridden' server-side records that a value was hand-edited, surfaced in report |
</threat_model>

<verification>
- `npx vitest run src/app/api/fsa/export-shapefile/route.test.ts` green (RMA fields + collision guard)
- `grep -c "RMA_UNIT" glomalin-portal/src/app/api/fsa/export-shapefile/route.ts` >= 1
- `npx tsc --noEmit` no new errors
- Migration 040 applied in plan 72-07 before an export smoke test
</verification>

<success_criteria>
Reporting units carry the full D-18 RMA schema including source flag; the exported .shp/.csv (ACR-06) contains every RMA field with collision-free DBF names; and the user can enter the user-supplied fields (RMA unit #, grower, share %, policy #) per unit (ACR-05).
</success_criteria>

<output>
Create `.planning/phases/72-acreage-reconciliation-tool/72-05-SUMMARY.md` when done.
</output>
