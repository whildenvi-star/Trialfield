---
phase: 72-acreage-reconciliation-tool
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/supabase/migrations/038_manual_spatial_editing.sql
  - glomalin-portal/src/app/api/fsa/clu-records/merge/route.ts
  - glomalin-portal/src/app/api/fsa/clu-records/merge/route.test.ts
  - glomalin-portal/src/app/api/fsa/clu-records/draw/route.ts
  - glomalin-portal/src/components/fsa/merge-panel.tsx
  - glomalin-portal/src/components/fsa/overlay-map.tsx
autonomous: true
requirements: [ACR-03]
must_haves:
  truths:
    - "A user can select 2+ adjacent CLU reporting units and merge them into one via ST_Union, with acreage recomputed server-side"
    - "A user can draw a brand-new boundary from scratch (no parent CLU) and it becomes a manual reporting unit"
    - "Merge and draw-from-scratch work with zero automated data loaded (D-15) and never delete source rows (superseded, not deleted)"
  artifacts:
    - path: "glomalin-portal/supabase/migrations/038_manual_spatial_editing.sql"
      provides: "merge_reporting_units() + insert_manual_clu() SECURITY DEFINER RPCs + merged_from_ids column"
      contains: "merge_reporting_units"
    - path: "glomalin-portal/src/app/api/fsa/clu-records/merge/route.ts"
      provides: "guarded merge endpoint with parent validation + rollback"
      exports: ["POST"]
    - path: "glomalin-portal/src/components/fsa/merge-panel.tsx"
      provides: "merge drawer UI mirroring split-panel"
      exports: ["MergePanel"]
  key_links:
    - from: "src/app/api/fsa/clu-records/merge/route.ts"
      to: "merge_reporting_units RPC"
      via: "supabase.rpc"
      pattern: "merge_reporting_units"
    - from: "src/components/fsa/overlay-map.tsx"
      to: "/api/fsa/clu-records/merge"
      via: "MergePanel submit"
      pattern: "clu-records/merge"
---

<objective>
Fill the two genuine ACR-03 manual-editing gaps that do NOT already exist in the codebase: (1) CLU **merge** (`ST_Union`) — split exists, merge does not exist anywhere (confirmed: `grep -rln ST_Union supabase/migrations/` returns nothing); (2) **draw-from-scratch** — the current split/snip tools require an existing `clu_boundaries` row to snip against, so a CLU with no boundary at all cannot be digitized. Split, snip-preview, and manual acreage override already ship (migrations 032/033/036) and are out of scope.

Purpose: ACR-03 + D-16 require merge, draw-from-scratch, split, and manual override to all work with zero automated inputs. This plan adds the two missing operations.
Output: migration 038 (two RPCs + `merged_from_ids` column), merge + draw API routes, `MergePanel` component, and two new interaction modes on `overlay-map.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/72-acreage-reconciliation-tool/72-RESEARCH.md
@.planning/phases/72-acreage-reconciliation-tool/72-PATTERNS.md

<interfaces>
Project-wide acreage formula (copy exactly): `ROUND(CAST(ST_Area(geom::geography) / 4046.856422 AS numeric), 2)`.
`ST_MakeValid()` MUST wrap every geometry sourced from user draw / import before ST_Union / intersection (Pitfall 4 — non-negotiable in this codebase).
Draw-tool wiring (reuse verbatim from overlay-map.tsx lines 260-314): dynamic-import `@mapbox/mapbox-gl-draw` + its CSS, register as IControl, `draw.changeMode('draw_polygon')`, listen `map.on('draw.create', handler)`, keep the fallback-alert-on-attach-failure branch.
clu_records split columns already exist (migration 032): sub_label, split_geometry geometry(Polygon,4326), parent_clu_id uuid, superseded boolean.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: migration 038 — merge_reporting_units + insert_manual_clu RPCs</name>
  <files>glomalin-portal/supabase/migrations/038_manual_spatial_editing.sql</files>
  <read_first>
    - glomalin-portal/supabase/migrations/032_clu_split_support.sql (create_clu_split SECURITY DEFINER shape, column additions, partial unique indexes, GiST index on split_geometry)
    - glomalin-portal/supabase/migrations/033_clu_overlay_intersections.sql (ST_MakeValid usage before intersection/union)
    - glomalin-portal/supabase/migrations/035_insert_zone_rpc.sql (insert RPC that returns jsonb, geometry-from-geojson pattern)
  </read_first>
  <action>Create migration `038_manual_spatial_editing.sql` with a header comment. (a) `ALTER TABLE clu_records ADD COLUMN IF NOT EXISTS merged_from_ids uuid[]` (array = many→one, mirrors 032's `parent_clu_id uuid` which is one→many). (b) `CREATE OR REPLACE FUNCTION merge_reporting_units(p_parent_ids uuid[], p_merged_label text, p_crop text, p_irrigated boolean, p_organic boolean, p_intended_use text) RETURNS uuid LANGUAGE plpgsql SECURITY DEFINER`: validate `COUNT(*) WHERE id = ANY(p_parent_ids) AND NOT superseded = array_length(p_parent_ids,1)` else RAISE EXCEPTION; SELECT first parent row for inherited farm/tract/crop_year/field_name/farm_name; compute `merged_geom` as `ST_Multi(ST_Union(ST_MakeValid(COALESCE(split_geometry, (SELECT cb.geometry FROM clu_boundaries cb WHERE cb.farm_number=cr.farm_number AND cb.tract_number=cr.tract_number AND cb.clu_label=cr.clu AND cb.crop_year=cr.crop_year)))))` across `clu_records cr WHERE cr.id = ANY(p_parent_ids)`; `merged_ac := ROUND(CAST(ST_Area(merged_geom::geography)/4046.856422 AS numeric),2)`; INSERT one child row (crop_year, farm_number, tract_number, clu=p_merged_label, field_name, farm_name, fsa_acres=merged_ac, sub_label=NULL, split_geometry=merged_geom, merged_from_ids=p_parent_ids, crop, irrigated, organic, use=p_intended_use, superseded=false) RETURNING id. Do NOT mark parents superseded here — the API route does that after success (mirrors split route discipline). (c) `CREATE OR REPLACE FUNCTION insert_manual_clu(p_crop_year int, p_farm_number text, p_tract_number text, p_label text, p_geojson text, p_crop text, p_irrigated boolean, p_organic boolean, p_intended_use text, p_manual_acres numeric) RETURNS uuid ... SECURITY DEFINER`: `geom := ST_Multi(ST_MakeValid(ST_GeomFromGeoJSON(p_geojson)))`; if `p_manual_acres` is not null use it else `ROUND(CAST(ST_Area(geom::geography)/4046.856422 AS numeric),2)`; INSERT a clu_records row with split_geometry=geom, parent_clu_id=NULL, merged_from_ids=NULL, sub_label=NULL, superseded=false RETURNING id. Note the merged/manual geometry is MultiPolygon — if `clu_records.split_geometry` is declared `geometry(Polygon,4326)` (it is, per 032), widen it in THIS migration with `ALTER TABLE clu_records ALTER COLUMN split_geometry TYPE geometry(MultiPolygon,4326) USING ST_Multi(split_geometry)` (mirrors 015/019 widen precedent) so ST_Multi output inserts cleanly. GiST index already exists on split_geometry; recreate if the widen drops it.</action>
  <verify>
    <automated>grep -v '^--' glomalin-portal/supabase/migrations/038_manual_spatial_editing.sql | grep -c -E "merge_reporting_units|insert_manual_clu|ST_MakeValid|merged_from_ids" | awk '{if($1>=4)print "ok"; else {print "FAIL"; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - `merge_reporting_units` and `insert_manual_clu` are both `SECURITY DEFINER` functions in the file
    - Every geometry input is wrapped in `ST_MakeValid()` before `ST_Union`/insert (source assertion: no bare `ST_Union(split_geometry` or `ST_GeomFromGeoJSON(p_geojson))` insert without ST_MakeValid)
    - `merged_from_ids uuid[]` column added with `IF NOT EXISTS`
    - `split_geometry` widened to `geometry(MultiPolygon,4326)` so ST_Multi output inserts without a type-mismatch error
    - Acreage uses the exact `/ 4046.856422` divisor
  </acceptance_criteria>
  <done>Two SECURITY DEFINER RPCs exist following the create_clu_split pattern; merge does ST_Union, draw-from-scratch inserts a parentless manual CLU; both defend geometry with ST_MakeValid.</done>
</task>

<task type="auto">
  <name>Task 2: merge + draw API routes with parent validation and rollback</name>
  <files>glomalin-portal/src/app/api/fsa/clu-records/merge/route.ts, glomalin-portal/src/app/api/fsa/clu-records/merge/route.test.ts, glomalin-portal/src/app/api/fsa/clu-records/draw/route.ts</files>
  <read_first>
    - glomalin-portal/src/app/api/fsa/clu-records/[id]/split/route.ts (validate parents, call RPC, supersede, rollback-on-partial-failure discipline — full shape to mirror)
    - glomalin-portal/src/app/api/fsa/export-shapefile/route.ts (requireModuleAccess guard)
    - glomalin-portal/src/components/marketing/delivery-progress-bar.test.tsx (vitest mocking convention for a route/unit test)
  </read_first>
  <action>Create `clu-records/merge/route.ts` exporting `POST`: guard with `requireModuleAccess('fsa-578')`; parse body `{ parent_ids: string[]; merged_label; crop; irrigated; organic; intended_use }`; reject `parent_ids.length < 2` with 400; select the parents (`id, superseded, farm_number, tract_number, crop_year`), 404 if count mismatch, 409 if any `superseded`; call `supabase.rpc('merge_reporting_units', { p_parent_ids, p_merged_label, p_crop, p_irrigated, p_organic, p_intended_use })`; on success `update({superseded:true}).in('id', parent_ids)` and if THAT fails delete the new merged row and return 500 (rollback, never orphan); return `{ merged_id, superseded_ids }`. Create `clu-records/draw/route.ts` exporting `POST`: guard; parse `{ crop_year, farm_number, tract_number, label, geojson, crop, irrigated, organic, intended_use, manual_acres }`; validate `geojson` is present and parseable and `crop_year` is a number; call `supabase.rpc('insert_manual_clu', {...})`; return `{ clu_record_id }`. Write `merge/route.test.ts` (vitest) that mocks the supabase client and asserts: <2 parent_ids → 400; a superseded parent → 409; happy path calls `rpc('merge_reporting_units')` then supersedes parents and returns `merged_id`. (Direct PostGIS RPC correctness is verified by the smoke query in plan 72-07 — no local PostGIS test harness exists in this repo, per RESEARCH Validation.)</action>
  <verify>
    <automated>cd glomalin-portal && npx vitest run src/app/api/fsa/clu-records/merge/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both routes export `POST` and call `requireModuleAccess('fsa-578')` before DB access
    - merge route returns 400 (<2 ids), 404 (missing), 409 (superseded), and rolls back the merged row if superseding parents fails
    - `merge/route.test.ts` passes and asserts the 400/409/happy-path behaviors
    - draw route validates `geojson` present and calls `insert_manual_clu`
    - `npx tsc --noEmit` no errors in these files
  </acceptance_criteria>
  <done>Merge and draw endpoints exist, are guarded, validate inputs, and the merge route is unit-tested with rollback discipline.</done>
</task>

<task type="auto">
  <name>Task 3: MergePanel component + overlay-map merge-select & draw-from-scratch modes</name>
  <files>glomalin-portal/src/components/fsa/merge-panel.tsx, glomalin-portal/src/components/fsa/overlay-map.tsx</files>
  <read_first>
    - glomalin-portal/src/components/fsa/split-panel.tsx (Drawer + form-row + acres-mismatch-warning + submit-handler + footer/undo-note pattern to mirror)
    - glomalin-portal/src/components/fsa/overlay-map.tsx (existing "Split by zones/coverage" buttons, draw-mode flow lines ~260-314, IControl registration, fallback alert)
    - glomalin-portal/src/components/fsa/crop-typeahead.tsx (CropTypeahead used in split-panel)
  </read_first>
  <action>Create `merge-panel.tsx` exporting `MergePanel`, mirroring `split-panel.tsx`'s Drawer + CropTypeahead + intended-use select + footer/error/undo-note structure. Props: `{ open, onClose, parentIds: string[], onMergeComplete }`. Interface `MergeProposal { parent_ids; merged_label; acres; crop; irrigated; organic; intended_use }`. Invert split-panel's acres-mismatch warning: warn when the union acreage differs significantly from the sum of the selected parcels' `fsa_acres` (sliver/gap detection). Submit handler POSTs to `/api/fsa/clu-records/merge` with the exact payload shape from Task 2, then calls `onMergeComplete()` + `onClose()`; copy the "original records preserved" undo note verbatim. In `overlay-map.tsx`, add two new interaction modes alongside the existing split flow, reusing the SAME dynamic-import + IControl + `draw.create` pattern already present (do NOT add a new draw integration): (1) "Select CLUs to merge" multi-select mode — click adjacent CLUs to build a `parentIds` array, then open `MergePanel`; (2) "Draw new boundary" mode — `changeMode('draw_polygon')`, on `draw.create` POST the drawn GeoJSON to `/api/fsa/clu-records/draw` (open a lightweight attribute prompt for crop/label/manual_acres, or reuse MergePanel's field rows). Keep the existing fallback-alert-on-attach-failure branch.</action>
  <verify>
    <automated>cd glomalin-portal && npx tsc --noEmit 2>&1 | grep -c -E "merge-panel|overlay-map" | awk '{if($1==0)print "ok"; else {print "FAIL"; exit 1}}'</automated>
  </verify>
  <acceptance_criteria>
    - `MergePanel` exported from `merge-panel.tsx`, POSTs to `/api/fsa/clu-records/merge` (source assertion: `grep -c "clu-records/merge" merge-panel.tsx` >= 1)
    - `overlay-map.tsx` has a merge-select mode that assembles ≥2 parentIds and a draw-new-boundary mode that POSTs to `/api/fsa/clu-records/draw`
    - The draw wiring reuses the existing `@mapbox/mapbox-gl-draw` dynamic-import/IControl pattern (no new draw library import: `grep -c "mapbox-gl-draw" overlay-map.tsx` reflects reuse, `grep -rc "leaflet\|terra-draw" overlay-map.tsx` == 0)
    - `npx tsc --noEmit` no errors in either file
  </acceptance_criteria>
  <done>User can multi-select CLUs and merge them, and draw a new boundary from scratch, both from the existing overlay map, using the existing draw tooling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/fsa/clu-records/merge, /draw | User-drawn GeoJSON and CLU id arrays cross into SECURITY DEFINER PostGIS RPCs |
| API route → SECURITY DEFINER RPC | RPC runs with elevated rights; malformed geometry or ids could corrupt reporting state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-03-01 | Elevation of Privilege | merge/draw routes | mitigate | `requireModuleAccess('fsa-578')` guard before any RPC call on both routes |
| T-72-03-02 | Tampering | merge_reporting_units inputs | mitigate | RPC validates all parent ids exist and are not superseded (RAISE EXCEPTION otherwise); route re-validates and rolls back on partial failure |
| T-72-03-03 | Denial of Service | insert_manual_clu geojson | mitigate | `ST_MakeValid` + `ST_GeomFromGeoJSON` reject malformed geometry server-side; route validates geojson presence/parse before RPC |
| T-72-03-04 | Tampering | superseded-row reuse | mitigate | 409 on any already-superseded parent; parents superseded (never deleted) so merge/split history is preserved and reversible |
</threat_model>

<verification>
- `npx vitest run src/app/api/fsa/clu-records/merge/route.test.ts` green
- `grep -rln "ST_Union" glomalin-portal/supabase/migrations/038_manual_spatial_editing.sql` matches (merge gap closed)
- `npx tsc --noEmit` no new errors
- RPC runtime correctness verified by smoke query in plan 72-07 after migration apply
</verification>

<success_criteria>
Merge (`ST_Union`) and draw-from-scratch — the two ACR-03 operations that did not previously exist — are implemented end to end (RPC → guarded route → overlay-map UI), work with zero automated data, and never delete source rows.
</success_criteria>

<output>
Create `.planning/phases/72-acreage-reconciliation-tool/72-03-SUMMARY.md` when done.
</output>
