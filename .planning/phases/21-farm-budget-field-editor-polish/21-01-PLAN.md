---
phase: 21-farm-budget-field-editor-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - farm-budget/public/field-editor.js
  - farm-budget/public/style.css
  - farm-budget/public/dashboard.js
  - farm-budget/public/enterprise.js
  - farm-budget/public/pdf-report.js
  - farm-budget/public/reports.js
autonomous: true
requirements:
  - BUD-01
  - BUD-02

must_haves:
  truths:
    - "Field editor preview shows group subtotal rows at the bottom of Land, Inputs, Operations, and Other groups with combined /ac and total values"
    - "Negative Profit/AC and Profit (w/ Payments) values display in red across field editor preview, dashboard crop rows, enterprise summary table, and collapsed preview summary"
    - "Zero profit uses default text color (no red or green)"
    - "COP is colored red when COP > pricePerUnit and green when COP < pricePerUnit"
    - "Print reports show negatives in red AND accounting parentheses format (123.45)"
    - "Field-Level Input Plan print report includes group subtotals matching field editor preview layout"
  artifacts:
    - path: "farm-budget/public/field-editor.js"
      provides: "updatePreview with group subtotals and COP coloring"
      contains: "prev-group-subtotal"
    - path: "farm-budget/public/style.css"
      provides: "Subtotal row styling and print CSS for red/parentheses negatives"
      contains: "prev-group-subtotal"
    - path: "farm-budget/public/pdf-report.js"
      provides: "PDF reports with group subtotals and red negative formatting"
      contains: "profitColor"
  key_links:
    - from: "farm-budget/public/field-editor.js"
      to: "farm-budget/public/calc.js"
      via: "Calc.computeFieldBudget returns budget object with all category totals"
      pattern: "Calc\\.computeFieldBudget"
    - from: "farm-budget/public/field-editor.js"
      to: "farm-budget/public/app.js"
      via: "util.profitClass returns profit-neg/profit-pos CSS class"
      pattern: "util\\.profitClass"
    - from: "farm-budget/public/style.css"
      to: "farm-budget/public/field-editor.js"
      via: "CSS classes prev-group-subtotal and cop-neg/cop-pos styled in CSS, applied in JS"
      pattern: "prev-group-subtotal|cop-neg|cop-pos"
---

<objective>
Add group subtotal rows to the field editor budget preview and ensure consistent red/green profit and COP coloring across all views including print reports.

Purpose: Users need to see at-a-glance category totals (Land, Inputs, Operations, Other) in the field editor preview, and negative profit values must be immediately visible in red across every view. Print reports must use both red color AND accounting parentheses for maximum clarity.

Output: Updated field-editor.js with subtotal rows, consistent profit/COP coloring across dashboard.js and enterprise.js, print-ready formatting in pdf-report.js and reports.js, and supporting CSS in style.css.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-farm-budget-field-editor-polish/21-CONTEXT.md

@farm-budget/public/field-editor.js
@farm-budget/public/calc.js
@farm-budget/public/style.css
@farm-budget/public/app.js
@farm-budget/public/dashboard.js
@farm-budget/public/enterprise.js
@farm-budget/public/pdf-report.js
@farm-budget/public/reports.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add group subtotal rows to field editor preview and COP coloring</name>
  <files>farm-budget/public/field-editor.js, farm-budget/public/style.css</files>
  <action>
In field-editor.js, modify the `updatePreview()` function (around line 809):

1. **Group subtotals**: After defining the `groups` array (lines 849-868), add a subtotal computation for each group. For each group, sum the /ac and total values of its items:
   - Land subtotal: rentPerCropAcre, rentTotal
   - Inputs subtotal: springFertPerAcre + fallFertPerAcre + seedCostPerAcre, springFertTotal + fallFertTotal + seedTotal
   - Operations subtotal: machineryPerAcre + laborPerAcre + overheadPerAcre + fuelPerAcre, machineryTotal + laborTotal + overheadTotal + fuelTotal
   - Other subtotal: dryingPerAcre + interestPerAcre + cropInsurancePerAcre, dryingTotal + interestTotal + cropInsuranceTotal

2. **Render subtotal rows**: In the `groups.map()` rendering (line 872-874), after each group's items, append a subtotal row using a new `renderSubtotal` helper:
   ```javascript
   function renderSubtotal(perAcre, total) {
     return '<div class="prev-item prev-group-subtotal">' +
       '<span class="label">Subtotal</span>' +
       '<span class="val">' + util.formatMoney(perAcre) + '</span>' +
       '<span class="val val-total">' + util.formatMoney(total) + '</span>' +
       '</div>';
   }
   ```
   Add subtotal data to each group object: `{ name: 'Land', items: [...], subtotalPerAcre: budget.rentPerCropAcre, subtotalTotal: budget.rentTotal }` etc.

3. **COP coloring**: In the Totals section (line 884), change the COP `renderItem` call to include a profit-style class based on COP vs pricePerUnit:
   ```javascript
   var copClass = '';
   if (budget.cop > 0 && budget.pricePerUnit > 0) {
     copClass = budget.cop > budget.pricePerUnit ? 'profit-neg' : 'profit-pos';
   }
   renderItem('COP', budget.cop, budget.cop, copClass)
   ```

4. **Verify profitClass usage**: Confirm that `util.profitClass` is already used for:
   - Profit row (line 882) -- already has `util.profitClass(budget.profitPerAcre)` -- CORRECT
   - Profit (w/ Pay) row (line 883) -- already has `util.profitClass(budget.profitFarmWithPayments)` -- CORRECT
   - Collapsed summary (line 890) -- already has `util.profitClass(budget.profitPerAcre)` -- CORRECT

In style.css, add subtotal row styling after the existing `.prev-item` rules (around line 1425):

```css
.prev-group-subtotal {
  border-top: 1px solid var(--border);
  margin-top: 0.1rem;
  padding-top: 0.1rem;
}
.prev-group-subtotal .label {
  font-weight: 700;
  font-size: 0.68rem;
  text-transform: uppercase;
  letter-spacing: 0.02em;
}
.prev-group-subtotal .val {
  font-weight: 700;
}
```

Also add light-theme overrides in the `body.light` section for consistency.

5. **Profit coloring in dashboard.js**: Read dashboard.js and verify that `util.profitClass(row.profitPerAcre)` is already used on crop row profit values (line ~166). Verify COP display — if COP is shown in dashboard crop rows, add COP coloring there too (compare `row.cop` vs resolved pricePerUnit). The dashboard may need a small addition for COP coloring.

6. **Profit coloring in enterprise.js**: Read enterprise.js and verify that `util.profitClass` is already used on the enterprise summary table profit rows (lines ~401, 409, 429, 437). Verify COP — enterprise.js line 554 shows COP in module view. Add COP coloring there (compare budget.cop vs budget.pricePerUnit).
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && node -e "
      var fs = require('fs');
      var fe = fs.readFileSync('farm-budget/public/field-editor.js','utf8');
      var css = fs.readFileSync('farm-budget/public/style.css','utf8');
      var checks = [
        ['subtotal in field-editor', fe.includes('prev-group-subtotal')],
        ['renderSubtotal or subtotal render', fe.includes('Subtotal')],
        ['COP coloring logic', fe.includes('cop') && fe.includes('pricePerUnit')],
        ['subtotal CSS class', css.includes('prev-group-subtotal')],
        ['profit-neg exists', css.includes('profit-neg')],
        ['profit-pos exists', css.includes('profit-pos')]
      ];
      var pass = checks.every(function(c){ return c[1]; });
      checks.forEach(function(c){ console.log((c[1]?'PASS':'FAIL') + ': ' + c[0]); });
      process.exit(pass ? 0 : 1);
    "</automated>
    <manual>Open farm-budget at http://localhost:3001, click a field to open editor, verify: (1) each budget group shows subtotal row at bottom, (2) negative profit shows red, (3) zero profit shows default color, (4) COP shows red when > selling price</manual>
  </verify>
  <done>Field editor preview shows group subtotals for Land, Inputs, Operations, Other. Negative profit is red, zero is default, COP is red when > price and green when below. Dashboard and enterprise views also show COP coloring consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Add group subtotals and red/parentheses negatives to print reports</name>
  <files>farm-budget/public/pdf-report.js, farm-budget/public/reports.js, farm-budget/public/style.css</files>
  <action>
1. **Print CSS for negatives** — In style.css, add a `@media print` block (or update existing one) that ensures:
   - `.profit-neg` text is red in print (use `color: #cc0000 !important; -webkit-print-color-adjust: exact; print-color-adjust: exact;`)
   - Add a `.print-neg` class that formats values with accounting parentheses — this will be applied by the JS rendering functions

2. **reports.js — Field-Level Input Plan** — Read the `buildFieldPlanReport` function. This report shows field-level budget data. If it currently shows individual budget line items, add group subtotal rows matching the field editor preview layout (Land, Inputs, Operations, Other groups with subtotals). The categories object in the report should include subtotal rows after each group.

   Also update the HTML report rendering to use red + parentheses for negative values:
   - Create a helper `formatPrintMoney(val)` that returns `<span class="profit-neg">(${Math.abs(val).toFixed(2)})</span>` for negative values, or normal `$val.toFixed(2)` for positive
   - Apply this to profit and COP values in the Field-Level Input Plan report

3. **reports.js — Order Status and Delivery Receipt Log** — Verify these reports work correctly (they were built in Phase 19 and should be functional). No subtotal changes needed for these reports since they show order/delivery data, not budget categories. Just verify they render without errors.

4. **pdf-report.js — PDF subtotals and profit coloring** — In the jsPDF-based reports:
   - The `profitColor(val)` helper already returns red/green (line 33-35). Verify it's used on profit values.
   - For negative values in PDF output, use accounting parentheses: modify the `money()` helper to return `($X.XX)` for negatives instead of `-$X.XX`
   - Add group subtotal rows to the "Cost Category Rollup" section if it currently shows individual categories. The PDF should show Land/Inputs/Operations/Other subtotals.

5. **Consistency check** — Verify that the Field-Level Input Plan HTML report matches the field editor preview layout with the same group structure and subtotal placement.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && node -e "
      var fs = require('fs');
      var css = fs.readFileSync('farm-budget/public/style.css','utf8');
      var reports = fs.readFileSync('farm-budget/public/reports.js','utf8');
      var pdf = fs.readFileSync('farm-budget/public/pdf-report.js','utf8');
      var checks = [
        ['print color-adjust in CSS', css.includes('print-color-adjust')],
        ['print media or profit-neg print', css.includes('@media print') || css.includes('print-color-adjust')],
        ['reports.js has parentheses formatting', reports.includes('(') && (reports.includes('formatPrintMoney') || reports.includes('accounting') || reports.includes('toFixed'))],
        ['pdf-report.js profitColor exists', pdf.includes('profitColor')]
      ];
      var pass = checks.every(function(c){ return c[1]; });
      checks.forEach(function(c){ console.log((c[1]?'PASS':'FAIL') + ': ' + c[0]); });
      process.exit(pass ? 0 : 1);
    "</automated>
    <manual>Open farm-budget, navigate to any enterprise, click Print Reports, verify Field-Level Input Plan shows group subtotals. Check that negative profit values show in red with parentheses (123.45) format.</manual>
  </verify>
  <done>Print reports show red + accounting parentheses for negative values. Field-Level Input Plan report includes group subtotals matching field editor preview. PDF reports use same formatting. Order Status and Delivery Receipt Log reports verified functional.</done>
</task>

</tasks>

<verification>
1. Start farm-budget server: `cd farm-budget && node server.js`
2. Open http://localhost:3001 in browser
3. Click any field to open field editor — verify group subtotals appear under each category (Land, Inputs, Operations, Other)
4. Check a field with negative profit — verify red text on Profit/AC and Profit (w/ Payments)
5. Check a field with zero profit — verify default text color (not red or green)
6. Check COP value — verify red when COP > price per unit, green when COP < price per unit
7. Check collapsed preview summary — verify profit coloring matches
8. Navigate to Dashboard — verify crop row profit coloring is consistent
9. Navigate to any enterprise — verify profit coloring in table and module views
10. Print Field-Level Input Plan — verify group subtotals and red/parentheses negatives
11. Generate PDF report — verify subtotals and profit coloring
</verification>

<success_criteria>
- All 4 budget groups (Land, Inputs, Operations, Other) show subtotal rows with /ac and total columns
- profitClass (red/green) is consistent across field editor preview, dashboard crop rows, enterprise summary, and collapsed preview
- Zero profit displays in default text color
- COP shows red when losing money (COP > price), green when profitable
- Print reports use red text AND accounting parentheses (123.45) for negative values
- Field-Level Input Plan report layout matches field editor preview group structure
</success_criteria>

<output>
After completion, create `.planning/phases/21-farm-budget-field-editor-polish/21-01-SUMMARY.md`
</output>
