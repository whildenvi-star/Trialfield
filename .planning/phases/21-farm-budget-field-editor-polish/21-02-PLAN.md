---
phase: 21-farm-budget-field-editor-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - farm-budget/public/index.html
  - farm-budget/public/orders.js
  - farm-budget/public/deliveries.js
autonomous: false
requirements:
  - BUD-03
  - BUD-04

must_haves:
  truths:
    - "Orders tab button is visible in navigation bar and navigates to a working Orders tab"
    - "Deliveries tab button is visible in navigation bar and navigates to a working Deliveries tab"
    - "Tab order matches user decision: Dashboard, Enterprises, Forecasts, Seeds, Reference, Programs, Sales, Orders, Deliveries, Map"
    - "Orders tab shows empty state with column headers and a prominent Create Order button when no orders exist"
    - "Deliveries tab shows empty state with column headers and a prominent New Delivery button when no deliveries exist"
    - "User can create a PO in Orders tab, assign a supplier, and track order status"
    - "User can log a delivery receipt in Deliveries tab, link it to an order, and track items received"
    - "Order Status and Delivery Receipt Log print reports render correctly with data from the active tabs"
  artifacts:
    - path: "farm-budget/public/index.html"
      provides: "Visible Orders and Deliveries tab buttons in correct tab order"
      contains: "data-tab=\"orders\""
    - path: "farm-budget/public/orders.js"
      provides: "Full Orders tab CRUD with empty state, create PO, status tracking"
      contains: "loadOrders"
    - path: "farm-budget/public/deliveries.js"
      provides: "Full Deliveries tab CRUD with delivery receipt logging and order linking"
      contains: "loadDeliveries"
  key_links:
    - from: "farm-budget/public/index.html"
      to: "farm-budget/public/orders.js"
      via: "tab-btn data-tab=orders triggers tab-activate event, orders.js listens"
      pattern: "data-tab=\"orders\""
    - from: "farm-budget/public/index.html"
      to: "farm-budget/public/deliveries.js"
      via: "tab-btn data-tab=deliveries triggers tab-activate event, deliveries.js listens"
      pattern: "data-tab=\"deliveries\""
    - from: "farm-budget/public/orders.js"
      to: "farm-budget/server.js"
      via: "api.get('/api/orders') and api.post('/api/orders') HTTP calls"
      pattern: "api\\.(get|post|put|delete).*orders"
    - from: "farm-budget/public/deliveries.js"
      to: "farm-budget/server.js"
      via: "api.get('/api/deliveries') and api.post('/api/deliveries') HTTP calls"
      pattern: "api\\.(get|post|put|delete).*deliveries"
---

<objective>
Activate the hidden Orders and Deliveries tabs by removing display:none, reordering tabs per user decision, and verifying all existing CRUD functionality works correctly.

Purpose: Orders and Deliveries code was built in Phase 19 but hidden (display:none). Users need these tabs visible and functional to track purchase orders and delivery receipts as part of the procurement pipeline.

Output: Visible, functional Orders and Deliveries tabs in the correct navigation position with verified CRUD operations and working print reports.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-farm-budget-field-editor-polish/21-CONTEXT.md

@farm-budget/public/index.html
@farm-budget/public/orders.js
@farm-budget/public/deliveries.js
@farm-budget/server.js
@farm-budget/public/reports.js
@farm-budget/public/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unhide Orders and Deliveries tabs and reorder navigation</name>
  <files>farm-budget/public/index.html, farm-budget/public/orders.js, farm-budget/public/deliveries.js</files>
  <action>
1. **Unhide tab buttons** -- In index.html lines 36-37, remove `style="display:none"` from both the Orders and Deliveries tab buttons:
   - `<button class="tab-btn" data-tab="orders" style="display:none">Orders</button>` becomes `<button class="tab-btn" data-tab="orders">Orders</button>`
   - `<button class="tab-btn" data-tab="deliveries" style="display:none">Deliveries</button>` becomes `<button class="tab-btn" data-tab="deliveries">Deliveries</button>`

2. **Reorder tabs** -- Per user decision, the tab order should be: Dashboard, Enterprises (dropdown), Forecasts, Seeds, Reference, Programs, Sales, **Orders, Deliveries**, Map.
   The current order (lines 28-42) is: Dashboard, Enterprises, Forecasts, Orders (hidden), Deliveries (hidden), Seeds, Reference, Programs, Sales, Map.
   Move the Orders and Deliveries buttons to appear AFTER Sales and BEFORE Map:
   ```html
   <button class="tab-btn active" data-tab="dashboard">Dashboard</button>
   <!-- Enterprises dropdown stays -->
   <button class="tab-btn" data-tab="forecasts">Forecasts</button>
   <button class="tab-btn" data-tab="seeds">Seeds</button>
   <button class="tab-btn" data-tab="reference">Reference</button>
   <button class="tab-btn" data-tab="programs">Programs</button>
   <button class="tab-btn" data-tab="sales">Sales</button>
   <button class="tab-btn" data-tab="orders">Orders</button>
   <button class="tab-btn" data-tab="deliveries">Deliveries</button>
   <button class="tab-btn" data-tab="map">Map</button>
   ```

3. **Verify empty state** -- Read orders.js to confirm it already shows an empty state when no orders exist (line ~79 uses `util.emptyState`). Check that the empty state includes a "Create Order" button or link. If it says "Select products on the Forecasts tab and click Create Order", update the empty state to also include a standalone "Create Order" button that opens the order form directly from the Orders tab. Same for deliveries.js (line ~78 uses `util.emptyState`).

4. **Test CRUD endpoints** -- Start the farm-budget server and verify:
   - GET /api/orders returns 200 (even if empty array)
   - GET /api/deliveries returns 200 (even if empty array)
   - POST /api/orders creates an order successfully
   - POST /api/deliveries creates a delivery successfully
   If any CRUD operation fails, fix the issue. The routes were built in Phase 19 and should work, but verify.

5. **Visual consistency** -- Ensure the Orders and Deliveries tab buttons match the styling of other tab buttons (same font, color, hover state). Since they use the same `tab-btn` class, this should be automatic. Check that the tab-content sections (#tab-orders and #tab-deliveries at lines 307-331 in index.html) are properly structured.

6. **Order Status and Delivery Receipt Log reports** -- Verify that the print reports (Report 4 and Report 5 in reports.js) can be triggered and render correctly when Orders/Deliveries tabs have data. The reports read from the same API endpoints. No code changes expected here -- just verification.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && node -e "
      var fs = require('fs');
      var html = fs.readFileSync('farm-budget/public/index.html','utf8');
      var checks = [
        ['orders tab visible (no display:none)', !html.match(/data-tab=\"orders\"[^>]*display:\s*none/)],
        ['deliveries tab visible (no display:none)', !html.match(/data-tab=\"deliveries\"[^>]*display:\s*none/)],
        ['orders before map', html.indexOf('data-tab=\"orders\"') < html.indexOf('data-tab=\"map\"')],
        ['deliveries before map', html.indexOf('data-tab=\"deliveries\"') < html.indexOf('data-tab=\"map\"')],
        ['orders after sales', html.indexOf('data-tab=\"orders\"') > html.indexOf('data-tab=\"sales\"')],
        ['deliveries after orders', html.indexOf('data-tab=\"deliveries\"') > html.indexOf('data-tab=\"orders\"')]
      ];
      var pass = checks.every(function(c){ return c[1]; });
      checks.forEach(function(c){ console.log((c[1]?'PASS':'FAIL') + ': ' + c[0]); });
      process.exit(pass ? 0 : 1);
    "</automated>
    <manual>Open http://localhost:3001, verify Orders and Deliveries tabs are visible in navigation. Click each tab, verify content loads. If orders exist, verify list renders. If empty, verify empty state with create button.</manual>
  </verify>
  <done>Orders and Deliveries tab buttons are visible in the navigation bar in the correct position (after Sales, before Map). Both tabs load their content when clicked. Empty state shows column headers and create button. CRUD operations work for both orders and deliveries.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Orders and Deliveries tabs are functional</name>
  <files>farm-budget/public/index.html</files>
  <action>
Human verification checkpoint. Claude has activated the hidden Orders and Deliveries tabs by removing display:none, reordered tabs in the navigation bar, and verified CRUD operations work with the existing API endpoints. User needs to confirm the tabs are visible, functional, and the print reports work correctly.
  </action>
  <verify>
    <manual>
    1. Open http://localhost:3001 in your browser
    2. Verify the tab bar shows: Dashboard, Enterprises, Forecasts, Seeds, Reference, Programs, Sales, Orders, Deliveries, Map
    3. Click the Orders tab:
       - If no orders exist: verify empty state with create button
       - If orders exist: verify list renders with status, supplier, and items
       - Try creating a new order: click Create Order, fill in details, save
       - Verify order appears in the list with correct status
    4. Click the Deliveries tab:
       - If no deliveries exist: verify empty state
       - If deliveries exist: verify list renders with order reference, items received
       - Try logging a new delivery: link to an existing order, add items, save
       - Verify delivery appears and order status updates
    5. Test the print reports: from the Forecasts tab, click Print Reports dropdown, verify Order Status and Delivery Receipt Log reports generate correctly
    Resume: Type "approved" or describe any issues found
    </manual>
  </verify>
  <done>User has visually confirmed that Orders and Deliveries tabs are visible, correctly positioned, and fully functional with working CRUD and print reports.</done>
</task>

</tasks>

<verification>
1. Start farm-budget server: `cd farm-budget && node server.js`
2. Open http://localhost:3001
3. Verify Orders and Deliveries tabs are visible and in correct position
4. Click Orders tab -- verify it loads and shows content
5. Click Deliveries tab -- verify it loads and shows content
6. Create an order -- verify it saves and appears in list
7. Create a delivery linked to an order -- verify it saves and order status updates
8. Print Order Status report -- verify it renders
9. Print Delivery Receipt Log -- verify it renders
</verification>

<success_criteria>
- Orders tab visible in navigation at position after Sales
- Deliveries tab visible in navigation at position after Orders
- Both tabs load their existing content without errors
- Order CRUD works: create, read, update status
- Delivery CRUD works: create, read, link to order
- Order Status and Delivery Receipt Log print reports generate correctly
- Tab styling matches other navigation tabs
</success_criteria>

<output>
After completion, create `.planning/phases/21-farm-budget-field-editor-polish/21-02-SUMMARY.md`
</output>
