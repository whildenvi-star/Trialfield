---
phase: 34-insurance-claims-ui-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/components/insurance/policy-drawer.tsx
  - glomalin-portal/src/components/insurance/insurance-workspace.tsx
autonomous: true
requirements:
  - INS-05
  - INS-06
  - CLM-07

must_haves:
  truths:
    - "When PolicyDrawer opens with a policy, APH yield info auto-fetches from CLU records and displays as a read-only info box"
    - "User can click Sync Yield on a policy row and see actual yield updated from grain-tickets (or an inline error if offline/no match)"
    - "User can click File Claim on a policy row, fill date_of_loss in a modal, and be navigated to /app/claims after successful claim creation"
  artifacts:
    - path: "glomalin-portal/src/components/insurance/policy-drawer.tsx"
      provides: "APH auto-fetch useEffect + display section in Acres & Yields area"
      contains: "aph-lookup"
    - path: "glomalin-portal/src/components/insurance/insurance-workspace.tsx"
      provides: "Sync Yield button, File Claim button, File Claim modal, handleSyncYield, handleFileClaim"
      contains: "yield-sync"
  key_links:
    - from: "glomalin-portal/src/components/insurance/policy-drawer.tsx"
      to: "/api/insurance/aph-lookup"
      via: "fetch in useEffect on [open, policy?.crop, policy?.farm_name]"
      pattern: "fetch.*aph-lookup"
    - from: "glomalin-portal/src/components/insurance/insurance-workspace.tsx"
      to: "/api/insurance/yield-sync"
      via: "fetch POST in handleSyncYield"
      pattern: "fetch.*yield-sync"
    - from: "glomalin-portal/src/components/insurance/insurance-workspace.tsx"
      to: "/api/claims"
      via: "fetch POST in handleFileClaim + router.push('/app/claims')"
      pattern: "fetch.*api/claims"
---

<objective>
Wire three existing backend APIs into the Insurance module UI — APH auto-populate in PolicyDrawer, Sync Yield button on policy rows, and File Claim button with modal for claim creation from a policy.

Purpose: Close 3 partial requirements (INS-05, INS-06, CLM-07) that have working backend APIs but no UI integration. All three are "button -> fetch -> update state" patterns with no new API routes, schema changes, or packages.

Output: Updated `policy-drawer.tsx` with APH auto-fetch display, updated `insurance-workspace.tsx` with Sync Yield button, File Claim button, and File Claim modal.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-insurance-claims-ui-wiring/34-RESEARCH.md

@glomalin-portal/src/components/insurance/policy-drawer.tsx
@glomalin-portal/src/components/insurance/insurance-workspace.tsx
@glomalin-portal/src/app/api/insurance/aph-lookup/route.ts
@glomalin-portal/src/app/api/insurance/yield-sync/route.ts
@glomalin-portal/src/app/api/claims/route.ts
@glomalin-portal/src/lib/fsa/calc.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: APH auto-populate in PolicyDrawer</name>
  <files>glomalin-portal/src/components/insurance/policy-drawer.tsx</files>
  <action>
Add APH auto-fetch to PolicyDrawer. This is read-only informational display, NOT a form field.

1. Add state for APH data and loading:
   - `aphData: { avgAph: number; count: number; totalRecords: number } | null` — init `null`
   - `aphLoading: boolean` — init `false`

2. Add a `useEffect` that fires on `[open, policy?.crop, policy?.farm_name]`:
   - Guard: if `!open || !policy?.crop`, reset `aphData` to `null` and return
   - Set `aphLoading = true`
   - Build URLSearchParams with `crop` (required) and `farmName` (optional, only if `policy.farm_name` is truthy)
   - `fetch('/api/insurance/aph-lookup?' + params)` → `.json()` → `setAphData(data)`
   - `.catch(() => setAphData(null))` — swallow errors silently (APH is informational)
   - `.finally(() => setAphLoading(false))`

3. Add APH display section in the form body, placed immediately AFTER the "Actual (bu/ac)" field div and BEFORE the "Other" section header. Render as a read-only info box (not a form field):
   - Container: `div` with classes `mb-3 rounded border border-soil-border bg-soil-bg px-3 py-2 text-xs font-mono`
   - Title: `<p className="text-soil-accent font-semibold mb-1">APH from CLU Records</p>`
   - Only render the content block when `open && policy` is truthy (don't show on create mode)
   - Three display states (check `count` and `totalRecords` separately — do NOT just check `avgAph > 0`):
     a. `aphLoading === true` → show "Loading..." in `text-soil-muted`
     b. `aphData.count > 0` → show `"{avgAph} bu/ac (avg of {count} records)"` in `text-soil-text` with count in `text-soil-muted`
     c. `aphData.totalRecords > 0 && aphData.count === 0` → show "CLU records found — no APH values entered yet" in `text-soil-muted`
     d. `aphData.totalRecords === 0` (or `aphData` is null after fetch completes) → show "No matching CLU records found" in `text-soil-muted`
   - IMPORTANT: This is purely display. Do NOT add `avgAph` to `PolicyFormData` or include it in `onSave`. APH is informational context for the user.
  </action>
  <verify>
    <automated>cd glomalin-portal && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Open an existing policy in the PolicyDrawer — the APH info box should appear below the Actual field, showing one of the three states based on CLU data availability</manual>
  </verify>
  <done>PolicyDrawer auto-fetches APH from /api/insurance/aph-lookup when opened in edit mode and displays the result as a read-only info box with correct three-state rendering (value, no-APH, no-match). No APH data leaks into the form submission.</done>
</task>

<task type="auto">
  <name>Task 2: Sync Yield button + File Claim button and modal in InsuranceWorkspace</name>
  <files>glomalin-portal/src/components/insurance/insurance-workspace.tsx</files>
  <action>
Add Sync Yield and File Claim features to InsuranceWorkspace. Both use existing API endpoints.

**New imports:**
- Add `useRouter` from `'next/navigation'` (App Router, NOT `next/router`)

**New state variables (add after existing state declarations):**
- `syncingId: string | null` — tracks which policy row is currently syncing (init `null`)
- `syncFeedback: { id: string; message: string; type: 'success' | 'error' } | null` — inline feedback after sync (init `null`)
- `filingPolicy: InsurancePolicy | null` — which policy the File Claim modal is open for (init `null`)
- `claimDate: string` — date_of_loss input value (init `''`)
- `claimDesc: string` — description textarea value (init `''`)
- `claimSubmitting: boolean` — loading state for claim creation (init `false`)

**handleSyncYield function:**
```
async function handleSyncYield(policyId: string) {
  setSyncingId(policyId)
  setSyncFeedback(null)
  try {
    const res = await fetch('/api/insurance/yield-sync', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ policyId }),
    })
    const data = await res.json()
    if (data.matched && data.policy) {
      // Full row replacement — data.policy includes actual_synced_from_grain and recomputed claim_alert
      setPolicies((prev) => prev.map((p) => (p.id === policyId ? data.policy : p)))
      setSyncFeedback({ id: policyId, message: 'Yield synced successfully', type: 'success' })
    } else {
      const msg = data.error || 'No grain ticket match found'
      setSyncFeedback({ id: policyId, message: msg, type: 'error' })
    }
  } catch {
    setSyncFeedback({ id: policyId, message: 'Network error', type: 'error' })
  } finally {
    setSyncingId(null)
    // Auto-clear feedback after 5 seconds
    setTimeout(() => setSyncFeedback((prev) => prev?.id === policyId ? null : prev), 5000)
  }
}
```
Key: Replace the full policy object from the API response (not a partial merge). Check for `data.error` even on HTTP 200 — grain-tickets offline returns 200 with error string.

**handleFileClaim function:**
```
async function handleFileClaim() {
  if (!filingPolicy || !claimDate) return
  setClaimSubmitting(true)
  try {
    const res = await fetch('/api/claims', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        policy_id: filingPolicy.id,
        date_of_loss: claimDate,
        description: claimDesc || null,
      }),
    })
    if (res.ok) {
      setFilingPolicy(null)
      setClaimDate('')
      setClaimDesc('')
      router.push('/app/claims')
    } else {
      const err = await res.json().catch(() => ({}))
      console.error('Failed to create claim:', err)
    }
  } catch (err) {
    console.error('Network error filing claim:', err)
  } finally {
    setClaimSubmitting(false)
  }
}
```
Key: Navigate AFTER confirming `res.ok`. POST body uses `policy_id` (not `policyId`). `description` sends `null` if empty string.

**Update policy table Actions column:**
In the existing `<td>` Actions cell (the one with `onClick={(e) => e.stopPropagation()}`), replace the inner `<div>` to add Sync Yield and File Claim buttons between Edit and Delete:

```
<div className="flex items-center justify-center gap-2">
  <button onClick={() => openEditDrawer(policy)} className="text-xs text-soil-muted hover:text-soil-accent transition-colors font-mono">Edit</button>
  <span className="text-soil-border">|</span>
  <button onClick={() => handleSyncYield(policy.id)} disabled={syncingId === policy.id} className="text-xs text-soil-muted hover:text-soil-green transition-colors font-mono disabled:opacity-50">
    {syncingId === policy.id ? 'Syncing...' : 'Sync Yield'}
  </button>
  <span className="text-soil-border">|</span>
  <button onClick={() => setFilingPolicy(policy)} className="text-xs text-soil-muted hover:text-soil-accent transition-colors font-mono">File Claim</button>
  <span className="text-soil-border">|</span>
  <button onClick={() => handleDelete(policy.id)} className="text-xs text-soil-muted hover:text-red-400 transition-colors font-mono">Delete</button>
</div>
```

**Add sync feedback inline display:**
After the Actions `<td>` closing tag but still inside the `<tr>`, this won't work — instead, add a conditional row after each policy row when syncFeedback matches that policy id. Alternatively (simpler): add the feedback as a small text below the actions div within the same `<td>`:

Actually, the cleanest approach: inside the Actions `<td>`, after the button div, add a conditional feedback line:
```
{syncFeedback?.id === policy.id && (
  <p className={`text-[10px] mt-1 ${syncFeedback.type === 'success' ? 'text-soil-green' : 'text-red-400'}`}>
    {syncFeedback.message}
  </p>
)}
```

**Add File Claim modal (render after PolicyDrawer):**
Add the modal as a sibling to `<PolicyDrawer>` at the bottom of the return JSX. Structure:
- Backdrop: `fixed inset-0 z-40 bg-black/40` with `onClick={() => setFilingPolicy(null)}`
- Modal card: `fixed inset-0 z-50 flex items-center justify-center p-4` → inner `div` with `w-full max-w-md bg-soil-surface border border-soil-border rounded-lg p-6 font-mono`
- Header: "File a Claim" in `text-soil-accent font-semibold text-base`
- Policy context line: `{filingPolicy.farm_name ?? '(no farm)'} — {filingPolicy.crop ?? 'no crop'} — {filingPolicy.plan_type ?? '—'}`
- Date of Loss field: `<input type="date" required value={claimDate} onChange={...} />` with label, using `inputClass`-equivalent styling
- Description field: `<textarea rows={3} value={claimDesc} onChange={...} />` with label, optional
- Button row: Cancel (ghost button, calls `setFilingPolicy(null)`) and Submit (`disabled={!claimDate || claimSubmitting}`, calls `handleFileClaim()`)
- Only render the entire modal block when `filingPolicy !== null`

**Add router declaration:**
At the top of the `InsuranceWorkspace` function body: `const router = useRouter()`
  </action>
  <verify>
    <automated>cd glomalin-portal && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>In the insurance module: (1) Click "Sync Yield" on a policy row — should show feedback. (2) Click "File Claim" on a policy row — modal should open with policy context, require date_of_loss, and navigate to /app/claims on submit.</manual>
  </verify>
  <done>Policy table rows show Sync Yield and File Claim buttons. Sync Yield calls yield-sync API and updates the policy row in-place with full replacement (including claim_alert recompute). File Claim opens a minimal modal with date_of_loss (required) and description (optional), POSTs to /api/claims, and navigates to /app/claims on success.</done>
</task>

</tasks>

<verification>
1. `cd glomalin-portal && npx tsc --noEmit` — zero type errors
2. PolicyDrawer in edit mode: APH info box renders below Actual field with correct three-state display
3. Policy table: Sync Yield and File Claim buttons appear in Actions column alongside Edit and Delete
4. Sync Yield: clicking updates the policy row; shows inline feedback on success or error
5. File Claim: modal opens with policy context, requires date_of_loss, submits to /api/claims, navigates to /app/claims
6. No new API routes, no new packages, no schema changes
</verification>

<success_criteria>
- INS-05: APH auto-fetches from CLU records when PolicyDrawer opens in edit mode and displays as read-only info
- INS-06: Sync Yield button on each policy row calls yield-sync and updates actual yield with full policy replacement
- CLM-07: File Claim button opens modal, creates claim via POST /api/claims with policy pre-fill, navigates to claims page
- TypeScript compiles without errors
- All three features use existing API endpoints with no modifications
</success_criteria>

<output>
After completion, create `.planning/phases/34-insurance-claims-ui-wiring/34-01-SUMMARY.md`
</output>
