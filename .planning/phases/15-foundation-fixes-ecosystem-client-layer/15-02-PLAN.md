---
phase: 15-foundation-fixes-ecosystem-client-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - organic-cert/src/lib/ecosystem/types.ts
  - organic-cert/src/lib/ecosystem/budget-client.ts
  - organic-cert/src/lib/ecosystem/registry-client.ts
  - organic-cert/src/lib/ecosystem/tickets-client.ts
  - organic-cert/src/lib/ecosystem/index.ts
  - organic-cert/src/app/api/compile/sources/route.ts
  - organic-cert/src/app/api/compile/fields-preview/route.ts
  - organic-cert/src/components/compile/source-status-bar.tsx
  - organic-cert/src/app/(app)/compile/page.tsx
  - organic-cert/src/components/layout/sidebar.tsx
autonomous: false
requirements:
  - ECO-01
  - ECO-02
  - ECO-05

must_haves:
  truths:
    - "The compile page shows live connection status for farm-budget, farm-registry, and grain-tickets as colored dots with app name and port"
    - "Each source shows available (green) or unavailable (red) independently — one down source does not affect the others"
    - "Killing a source app while the compile page is open, then clicking Refresh, shows that source as unavailable without crashing organic-cert"
    - "The compile page shows a simple table preview of fields and acres pulled from farm-budget and farm-registry"
    - "When farm-budget or farm-registry is down, the corresponding section shows source unavailable with a description of what data is missing and a suggested fix action"
    - "Compile appears as a navigation item in the sidebar"
  artifacts:
    - path: "organic-cert/src/lib/ecosystem/types.ts"
      provides: "Shared SourceStatus, EcosystemError, SourceName types"
      contains: "EcosystemError"
    - path: "organic-cert/src/lib/ecosystem/budget-client.ts"
      provides: "farm-budget HTTP client with 3s AbortController timeout"
      exports: ["pingBudget", "getBudgetOrganicFields"]
    - path: "organic-cert/src/lib/ecosystem/registry-client.ts"
      provides: "farm-registry HTTP client with 3s AbortController timeout"
      exports: ["pingRegistry", "getRegistryFields"]
    - path: "organic-cert/src/lib/ecosystem/tickets-client.ts"
      provides: "grain-tickets HTTP client with 3s AbortController timeout"
      exports: ["pingTickets"]
    - path: "organic-cert/src/lib/ecosystem/index.ts"
      provides: "checkAllSources() using Promise.allSettled, re-exports all clients"
      exports: ["checkAllSources"]
    - path: "organic-cert/src/app/api/compile/sources/route.ts"
      provides: "GET route returning source availability array"
      exports: ["GET"]
    - path: "organic-cert/src/app/api/compile/fields-preview/route.ts"
      provides: "GET route returning field/acre data from budget + registry with partial failure handling"
      exports: ["GET"]
    - path: "organic-cert/src/components/compile/source-status-bar.tsx"
      provides: "Horizontal status bar component with colored dots, app names, ports, refresh button"
      exports: ["SourceStatusBar"]
    - path: "organic-cert/src/app/(app)/compile/page.tsx"
      provides: "Compile page with status bar, field/acre preview table, placeholder NOP sections"
      contains: "CompilePage"
    - path: "organic-cert/src/components/layout/sidebar.tsx"
      provides: "Navigation with Compile item added"
      contains: "compile"
  key_links:
    - from: "organic-cert/src/app/(app)/compile/page.tsx"
      to: "/api/compile/sources"
      via: "fetch GET on page load and manual refresh"
      pattern: "fetch.*api/compile/sources"
    - from: "organic-cert/src/app/(app)/compile/page.tsx"
      to: "/api/compile/fields-preview"
      via: "fetch GET on page load"
      pattern: "fetch.*api/compile/fields-preview"
    - from: "organic-cert/src/app/api/compile/sources/route.ts"
      to: "organic-cert/src/lib/ecosystem/index.ts"
      via: "import checkAllSources"
      pattern: "import.*checkAllSources.*ecosystem"
    - from: "organic-cert/src/app/api/compile/fields-preview/route.ts"
      to: "organic-cert/src/lib/ecosystem/budget-client.ts"
      via: "import getBudgetOrganicFields"
      pattern: "import.*getBudgetOrganicFields"
    - from: "organic-cert/src/lib/ecosystem/budget-client.ts"
      to: "http://localhost:3001"
      via: "native fetch with AbortController 3s timeout"
      pattern: "AbortController"
---

<objective>
Build the typed ecosystem client layer that connects organic-cert to farm-budget, farm-registry, and grain-tickets via HTTP, and create the Compile page with source availability status bar and a field/acre preview table. Each source has independent failure handling — one unavailable source never blocks the others.

Purpose: This is the data pipeline foundation for the entire v3.0 compilation engine. Phases 16-18 call these clients to pull enterprise, input, seed, rotation, and harvest data.
Output: `src/lib/ecosystem/` client module with 3 typed clients, 2 new API routes, a compile page with status bar and field preview, and a Compile nav item in the sidebar.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-foundation-fixes-ecosystem-client-layer/15-CONTEXT.md
@.planning/phases/15-foundation-fixes-ecosystem-client-layer/15-RESEARCH.md
@organic-cert/src/components/layout/sidebar.tsx
@organic-cert/src/app/api/fields/sync-registry/route.ts
@organic-cert/src/app/api/fields/sync-macro/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ecosystem client layer with typed HTTP clients and fault tolerance</name>
  <files>
    organic-cert/src/lib/ecosystem/types.ts
    organic-cert/src/lib/ecosystem/budget-client.ts
    organic-cert/src/lib/ecosystem/registry-client.ts
    organic-cert/src/lib/ecosystem/tickets-client.ts
    organic-cert/src/lib/ecosystem/index.ts
  </files>
  <action>
Create `organic-cert/src/lib/ecosystem/` directory with 5 files.

**types.ts** — Shared types:
- `SourceName = "farm-budget" | "farm-registry" | "grain-tickets"` literal union
- `SourceStatus = { name: SourceName; port: number; available: boolean; technicalDetail?: string; friendlyMessage?: string; suggestedFix?: string }`
- `EcosystemError` class extending Error with `source: SourceName`, `technicalDetail: string` properties. Constructor takes `(source, technicalDetail, message)`.
- `BudgetEnterprise` interface: `{ id: string; name: string; category: string; systemCodes: string[]; fieldNames: string[]; acres: number }`
- `BudgetField` interface: `{ id: string; name: string; crop: string; acres: number; enterpriseId: string }`
- `RegistryField` interface: `{ id: string; name: string; aliases: string[]; reportingAcres: number; organicAcres: number; ownership: string }`

**Shared fetchWithTimeout helper** (in types.ts or a shared util):
```typescript
const TIMEOUT_MS = 3000; // per CONTEXT.md: 3-second timeout

export async function fetchWithTimeout(url: string, source: SourceName): Promise<Response> {
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), TIMEOUT_MS);
  try {
    const res = await fetch(url, { signal: controller.signal, cache: "no-store" });
    return res;
  } catch (err) {
    if (err instanceof Error && err.name === "AbortError") {
      throw new EcosystemError(source, "Request timed out after 3s",
        `${source} is not responding`);
    }
    const detail = err instanceof Error ? err.message : String(err);
    throw new EcosystemError(source, detail, `${source} is not responding`);
  } finally {
    clearTimeout(timer);
  }
}
```

**budget-client.ts** — farm-budget client (port 3001):
- `const BUDGET_URL = process.env.BUDGET_API_URL || "http://localhost:3001"` (matches existing env var pattern from sync-macro)
- `pingBudget()`: fetch `${BUDGET_URL}/api/settings` — lightweight health probe, check `res.ok`
- `getBudgetEnterprises()`: fetch `${BUDGET_URL}/api/enterprises`, return all enterprises
- `getBudgetFields()`: fetch `${BUDGET_URL}/api/fields?all=true`, return all fields
- `getBudgetOrganicFields()`: Calls getBudgetEnterprises() and getBudgetFields() via Promise.all. Filter enterprises to `category === "organic"`. Build a Set of organic field names from the organic enterprises' fieldNames arrays. Filter fields to only those whose names are in the organic field name set. Return `{ enterprises: BudgetEnterprise[], fields: BudgetField[] }`.
- All functions throw EcosystemError with source "farm-budget", friendly message "farm-budget (:3001) is not responding", suggested fix "Start it with `node server.js` in farm-budget/"

**registry-client.ts** — farm-registry client (port 3005):
- `const REGISTRY_URL = process.env.FARM_REGISTRY_URL || "http://localhost:3005"` (matches existing env var in sync-registry route)
- `pingRegistry()`: fetch `${REGISTRY_URL}/api/fields` — check `res.ok` only, don't consume body
- `getRegistryFields()`: fetch `${REGISTRY_URL}/api/fields`, parse JSON, return `RegistryField[]`
- Friendly message: "farm-registry (:3005) is not responding", suggested fix: "Start it with `node server.js` in farm-registry/"

**tickets-client.ts** — grain-tickets client (port 3000):
- `const TICKETS_URL = process.env.GRAIN_TICKETS_URL || "http://localhost:3000"`
- `pingTickets()`: fetch `${TICKETS_URL}/api/stats` — health probe, check `res.ok`
- Friendly message: "grain-tickets (:3000) is not responding", suggested fix: "Start it with `node server.js` in grain-tickets/"
- Note: Data-pull functions for grain-tickets will be added in Phase 18. This phase only needs the ping/health check for the status bar.

**index.ts** — Re-exports and checkAllSources():
- Re-export all types from types.ts
- Re-export all client functions from budget-client, registry-client, tickets-client
- `checkAllSources()`: Uses `Promise.allSettled([pingBudget(), pingRegistry(), pingTickets()])`. Maps results to `SourceStatus[]` array with:
  - `name` and `port` from a static definitions array
  - `available: result.status === "fulfilled"`
  - `technicalDetail`: extracted from EcosystemError.technicalDetail if rejected
  - `friendlyMessage`: e.g., "farm-budget (:3001) is not responding"
  - `suggestedFix`: e.g., "Start it with `node server.js` in farm-budget/"

**Environment variables:** Check `organic-cert/.env.example` — add `GRAIN_TICKETS_URL=http://localhost:3000` if not already present (BUDGET_API_URL and FARM_REGISTRY_URL should already exist from sync-macro and sync-registry routes).

Zero new npm packages — native fetch, AbortController, Promise.allSettled are all built-in.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Import checkAllSources in a test script and call it with farm-budget running and grain-tickets stopped — verify mixed available/unavailable results.</manual>
  </verify>
  <done>Five files in src/lib/ecosystem/ compile cleanly. Each client uses AbortController with 3s timeout. checkAllSources() returns independent availability per source via Promise.allSettled. EcosystemError carries source name, technical detail, and friendly message.</done>
</task>

<task type="auto">
  <name>Task 2: Build compile page with status bar, field/acre preview, API routes, and nav item</name>
  <files>
    organic-cert/src/app/api/compile/sources/route.ts
    organic-cert/src/app/api/compile/fields-preview/route.ts
    organic-cert/src/components/compile/source-status-bar.tsx
    organic-cert/src/app/(app)/compile/page.tsx
    organic-cert/src/components/layout/sidebar.tsx
  </files>
  <action>
**API Route: GET /api/compile/sources**

Create `organic-cert/src/app/api/compile/sources/route.ts`:
- Import `checkAllSources` from `@/lib/ecosystem`
- `export async function GET()` calls `checkAllSources()` and returns `NextResponse.json(statuses)`
- No auth check needed (single-user farm app)

**API Route: GET /api/compile/fields-preview**

Create `organic-cert/src/app/api/compile/fields-preview/route.ts`:
- Import `getBudgetOrganicFields` from `@/lib/ecosystem/budget-client`
- Import `getRegistryFields` from `@/lib/ecosystem/registry-client`
- Use `Promise.allSettled` to call both in parallel
- Return JSON with partial results:
  ```typescript
  {
    budgetFields: budgetResult.status === "fulfilled" ? budgetResult.value : null,
    budgetError: budgetResult.status === "rejected" ? extractError(budgetResult.reason) : null,
    registryFields: registryResult.status === "fulfilled" ? registryResult.value : null,
    registryError: registryResult.status === "rejected" ? extractError(registryResult.reason) : null,
  }
  ```
- `extractError` helper pulls `friendlyMessage`, `technicalDetail`, `suggestedFix` from EcosystemError, or falls back to generic error string

**Component: SourceStatusBar**

Create `organic-cert/src/components/compile/source-status-bar.tsx`:
- Per CONTEXT.md locked decision: horizontal status bar, always visible, never collapses
- Format: `● farm-budget (:3001)  ● farm-registry (:3005)  ○ grain-tickets (:3000)`
- Green dot (`text-green-500`) = available, red dot (`text-red-500`) = unavailable
- Status bar stays same size whether all green or some red — consistent layout
- Single "Refresh" button (RefreshCw icon from lucide-react) at right end
- Refresh button shows spinner (`animate-spin`) while checking; dots stay in previous state until new results arrive
- Below the status bar, show an error detail area: when a source is unavailable, display friendly message (e.g., "farm-budget (:3001) is not responding") with a click-to-expand section showing technical detail (ECONNREFUSED, timeout, etc.) and suggested fix action
- Use `<details>` HTML element for the expandable technical detail (Claude's discretion per CONTEXT.md)
- Props: `{ sources: SourceStatus[]; onRefresh: () => void; refreshing: boolean }`
- Style with Tailwind — match existing dark theme from sidebar (bg-stone-900, text-stone-300, border-stone-700, font-mono)

**Compile Page**

Create `organic-cert/src/app/(app)/compile/page.tsx`:
- "use client" — React client component
- State: `sources` (SourceStatus[]), `refreshing` (boolean), `preview` (field/acre data or null), `previewLoading` (boolean)
- On mount (`useEffect`): call `loadSources()` and `loadPreview()` in parallel
- `loadSources()`: fetch `/api/compile/sources`, update `sources` state
- `loadPreview()`: fetch `/api/compile/fields-preview`, update `preview` state
- Refresh button calls `loadSources()` only (per CONTEXT.md: no cross-reload caching, each page load fetches fresh; refresh button rechecks sources)

Page layout per CONTEXT.md locked decisions:
1. **SourceStatusBar** at top — always visible
2. **Fields & Acres section** (ECO-01 + ECO-02 proof-of-concept):
   - If both budgetFields and registryFields available: render a table showing each organic field with columns: Field Name, Crop (from budget), Budget Acres (from budget), Registry Acres (from registry), Organic Acres (from registry), Ownership (from registry)
   - If budget unavailable: show "farm-budget is not available — enterprise data and input records cannot be pulled" (per CONTEXT.md decision on section-level messages)
   - If registry unavailable: show "farm-registry is not available — field identities and certified acres cannot be pulled"
   - Table matching: join budget fields to registry fields by name (case-insensitive). Budget fields with no registry match get a "No registry match" note. Registry fields with no budget organic enterprise get listed separately as "registry-only" rows.
   - Loading state: show a simple "Loading field data..." text or skeleton while preview loads

3. **Placeholder NOP sections** — remaining sections show placeholder headers with phase badges:
   - "Enterprises" — Coming in Phase 16
   - "Inputs" — Coming in Phase 17
   - "Seeds" — Coming in Phase 17
   - "Rotation" — Coming in Phase 18
   - "Harvest" — Coming in Phase 18

   Each placeholder section: `<h2>` with section name + a small muted badge "Phase NN". Styled as muted/disabled to indicate future work.

**Sidebar Navigation**

Edit `organic-cert/src/components/layout/sidebar.tsx`:
- Add a new nav item for the compile page. Insert it between "reports" and "c2.0 crop overview" in the navItems array:
  ```typescript
  { href: "/compile", label: "compile", icon: Layers },  // or GitMerge, Package2, etc.
  ```
- Import the chosen icon from lucide-react. Use `Layers` (represents data compilation from multiple sources) or a similar icon — Claude's discretion per CONTEXT.md.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one/organic-cert && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Start organic-cert on port 3004. Navigate to /compile. Verify status bar shows colored dots for all 3 sources. Start/stop farm-budget and click Refresh to see status change. Check field/acre preview table populates when farm-budget and farm-registry are running.</manual>
  </verify>
  <done>Compile page renders at /compile with horizontal status bar showing independent source availability. Field/acre preview table shows organic field data from farm-budget matched with registry acres. Unavailable sources show friendly error messages with expandable tech detail. Sidebar has "compile" navigation item. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify compile page, status bar, and field preview</name>
  <files>organic-cert/src/app/(app)/compile/page.tsx</files>
  <action>
Human verification of the ecosystem client layer and compile page. What was built:
- Ecosystem client layer (src/lib/ecosystem/) with 3 typed HTTP clients using AbortController 3-second timeouts and Promise.allSettled for independent failure handling
- Compile page at /compile with horizontal status bar, field/acre preview table, error messages, placeholder sections, and sidebar nav item

Verification steps:
1. Start organic-cert: `cd organic-cert && npm run dev` (port 3004)
2. Start farm-budget: `cd farm-budget && node server.js` (port 3001)
3. Start farm-registry: `cd farm-registry && node server.js` (port 3005)
4. Start grain-tickets: `cd grain-tickets && node server.js` (port 3000)
5. Navigate to http://localhost:3004/compile
6. Check status bar: All 3 sources should show green dots with names and ports
7. Check field/acre table: Should show organic fields with budget acres and registry acres columns
8. Stop farm-budget (Ctrl+C in its terminal)
9. Click "Refresh" button on the compile page
10. Check degradation: farm-budget dot should turn red, friendly error message should appear below the bar with a suggested fix action, field preview table should show "source unavailable" for budget data
11. Check sidebar: "compile" should appear as a navigation item
12. Check placeholder sections: Enterprises, Inputs, Seeds, Rotation, Harvest should show with phase badges
  </action>
  <verify>Human visual verification — type "approved" or describe issues</verify>
  <done>User confirms: status bar shows correct availability, field/acre preview populates from live sources, graceful degradation works when a source is stopped, sidebar has compile nav item, placeholder sections visible.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles cleanly: `cd organic-cert && npx tsc --noEmit` exits 0
2. GET /api/compile/sources returns array of 3 source statuses
3. GET /api/compile/fields-preview returns partial data when one source is down
4. Status bar shows independent availability per source (green/red dots)
5. Killing farm-budget shows it as unavailable without crashing organic-cert or blocking registry/tickets
6. Field/acre preview table populates from farm-budget + farm-registry data
7. Compile page has sidebar navigation item
</verification>

<success_criteria>
- ECO-01: Field/acre preview table shows organic field data pulled from farm-budget
- ECO-02: Field/acre preview table shows field identities and acres pulled from farm-registry
- ECO-05: Killing any single source shows it as unavailable without affecting others; friendly error messages with suggested fixes shown
- All source checks use 3-second AbortController timeout
- Promise.allSettled ensures independent failure handling
- Zero new npm packages installed
</success_criteria>

<output>
After completion, create `.planning/phases/15-foundation-fixes-ecosystem-client-layer/15-02-SUMMARY.md`
</output>
