---
phase: 33-cross-module-integration-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/app/(protected)/dashboard/page.tsx
  - glomalin-portal/src/components/dashboard/summary-cards.tsx
autonomous: true
requirements:
  - INT-04

must_haves:
  truths:
    - "User sees FSA reporting progress card on dashboard showing reported/total CLU count"
    - "User sees Insurance card showing potential claim alert count"
    - "User sees Claims card showing open claims count"
    - "Dashboard loads without error even if one module's table query fails"
  artifacts:
    - path: "glomalin-portal/src/components/dashboard/summary-cards.tsx"
      provides: "Three summary card components with live data display"
      contains: "SummaryCards"
    - path: "glomalin-portal/src/app/(protected)/dashboard/page.tsx"
      provides: "Server-side Promise.allSettled queries feeding summary cards"
      contains: "Promise.allSettled"
  key_links:
    - from: "glomalin-portal/src/app/(protected)/dashboard/page.tsx"
      to: "supabase.from('clu_records')"
      via: "Promise.allSettled server query"
      pattern: "clu_records.*reported"
    - from: "glomalin-portal/src/app/(protected)/dashboard/page.tsx"
      to: "supabase.from('insurance_policies')"
      via: "Promise.allSettled server query"
      pattern: "insurance_policies.*claim_alert"
    - from: "glomalin-portal/src/app/(protected)/dashboard/page.tsx"
      to: "supabase.from('claims')"
      via: "Promise.allSettled server query"
      pattern: "claims.*stage"
---

<objective>
Add live summary cards to the portal dashboard showing FSA reporting progress, Insurance claim alerts, and Claims pipeline count.

Purpose: Give users an at-a-glance overview of all three modules from the dashboard, enabling quick identification of items needing attention without navigating into each module separately.
Output: Three summary cards above the existing module grid on the dashboard page, driven by real-time Supabase queries.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-cross-module-integration-dashboard/33-RESEARCH.md
@glomalin-portal/src/app/(protected)/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SummaryCards component and wire into dashboard with Promise.allSettled queries</name>
  <files>
    glomalin-portal/src/components/dashboard/summary-cards.tsx
    glomalin-portal/src/app/(protected)/dashboard/page.tsx
  </files>
  <action>
**SummaryCards component (new file: `glomalin-portal/src/components/dashboard/summary-cards.tsx`):**

Create a server-renderable component (no 'use client') that accepts summary data as props and renders three cards in a 3-column grid. Props interface:

```typescript
interface SummaryCardsProps {
  fsa: { reported: number; total: number } | null  // null = query failed
  insurance: { claimAlerts: number } | null
  claims: { openCount: number } | null
}
```

Card layout (3 cards in `grid grid-cols-1 sm:grid-cols-3 gap-4 mb-6`):

1. **FSA Reporting** card — Links to `/app/fsa-578`
   - Label: "FSA Reporting" (uppercase tracking-wider, text-xs, text-soil-muted)
   - Value: `{reported} / {total}` with reported in text-soil-text text-2xl font-bold and total in text-soil-muted text-base
   - Sublabel: "CLUs reported"
   - If data is null, show dash character for value
   - Use soil-surface bg, soil-border border, hover:border-soil-accent

2. **Insurance** card — Links to `/app/insurance`
   - Label: "Insurance"
   - Value: `{claimAlerts}` — if > 0, use text-yellow-400 and border-yellow-700, hover:border-yellow-500
   - Sublabel: "potential claim alerts"
   - If data is null, show dash
   - Conditional border color based on alert count (amber/yellow if alerts > 0)

3. **Claims** card — Links to `/app/claims`
   - Label: "Claims"
   - Value: `{openCount}`
   - Sublabel: "open claims"
   - If data is null, show dash

Each card is wrapped in a Next.js `Link`. All use `font-mono` throughout, matching the dark soil aesthetic (bg-soil-surface, border-soil-border, text-soil-text, text-soil-muted color tokens). Pattern matches existing module cards in dashboard/page.tsx but with smaller padding (p-5 not p-6) and no arrow/lock icons.

**Dashboard page.tsx modification:**

1. Add `Promise.allSettled` Supabase queries AFTER the existing `module_access` query but BEFORE the return statement. Use the existing `supabase` client (already created at top of function). Three parallel queries:

```typescript
const results = await Promise.allSettled([
  supabase.from('clu_records').select('id, reported').eq('crop_year', 2026),
  supabase.from('insurance_policies').select('id').eq('policy_year', 2026).eq('claim_alert', 'potential'),
  supabase.from('claims').select('id').neq('stage', 'closed'),
])
```

2. Destructure results with null fallbacks for failed queries:
   - FSA: count total rows and filter for reported=true rows. If settled as rejected or data has error, set to null.
   - Insurance: count rows returned (these are all claim_alert='potential'). If failed, null.
   - Claims: count rows returned (these are all open claims). If failed, null.

3. Import and render `<SummaryCards>` ABOVE the existing "Farm Modules" heading and module grid. Pass the computed summary data as props.

4. Use `Promise.allSettled` (NOT `Promise.all`) so a single table query failure does not crash the entire dashboard (per Research pitfall 5). This matches the established cross-app fetch pattern documented in STATE.md.

5. Show all three summary cards to all authenticated users regardless of module access. Cards are informational — clicking navigates to the module which will enforce RBAC via middleware if the user lacks access. This keeps the dashboard useful for admins who always have full access.
  </action>
  <verify>
    <automated>cd glomalin-portal && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Visit /dashboard in the portal. Three summary cards should appear above the module grid showing live counts from Supabase.</manual>
  </verify>
  <done>Dashboard shows three summary cards (FSA reported/total, Insurance alert count, Claims open count) above the module grid, all driven by real Supabase queries with graceful null fallback for failed queries</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `cd glomalin-portal && npx tsc --noEmit`
2. Dev server starts and dashboard page renders without error
3. Summary cards display live data from clu_records, insurance_policies, and claims tables
4. If any one table is empty or query fails, the other cards still render normally (Promise.allSettled resilience)
5. Clicking a card navigates to the corresponding module page
</verification>

<success_criteria>
- Three summary cards visible on /dashboard above the module grid
- FSA card shows "X / Y CLUs reported" with accurate counts from clu_records table
- Insurance card shows claim alert count, highlighted in yellow/amber when > 0
- Claims card shows open claims count (stage != closed)
- Failed queries display a dash instead of crashing the page
- Cards link to their respective modules
</success_criteria>

<output>
After completion, create `.planning/phases/33-cross-module-integration-dashboard/33-02-SUMMARY.md`
</output>
