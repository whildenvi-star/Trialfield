---
phase: 57.1-marketing-yield-summaries-production-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/src/app/(protected)/app/marketing/page.tsx
autonomous: true
requirements:
  - MKT-01
  - MKT-02
gap_closure: true

must_haves:
  truths:
    - "Marketing position page fetches yield summaries through fetchGrainService proxy — not hardcoded localhost"
    - "Est. Production column shows correct bushel totals in both local dev and VPS production"
  artifacts:
    - path: "glomalin-portal/src/app/(protected)/app/marketing/page.tsx"
      provides: "Server-side yield-summaries fetch via proxy"
      contains: "fetchGrainService"
  key_links:
    - from: "glomalin-portal/src/app/(protected)/app/marketing/page.tsx"
      to: "glomalin-portal/src/app/api/mobile/_lib/proxy.ts"
      via: "import fetchGrainService"
      pattern: "fetchGrainService.*yield-summaries"
---

<objective>
Replace the hardcoded `http://localhost:3007` yield-summaries fetch in the marketing position page with the established `fetchGrainService` proxy helper — fixing production where Est. Production shows 0 for all crops because the VPS grain-tickets service is not on localhost.

Purpose: The marketing page correctly uses `appUrl` for CBOT prices (line 145) and Supabase for contracts, but the yield-summaries fetch on line 149 bypasses the proxy pattern and hardcodes `http://localhost:3007`. This works in local dev but breaks in production where grain-tickets may be on a different host/port.

Output: Updated page.tsx using fetchGrainService for yield data
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@glomalin-portal/src/app/(protected)/app/marketing/page.tsx
@glomalin-portal/src/app/api/mobile/_lib/proxy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded localhost yield-summaries fetch with fetchGrainService proxy</name>
  <files>glomalin-portal/src/app/(protected)/app/marketing/page.tsx</files>
  <action>
In `glomalin-portal/src/app/(protected)/app/marketing/page.tsx`:

1. Add import at the top of the file:
   ```
   import { fetchGrainService } from '@/app/api/mobile/_lib/proxy'
   ```

2. Replace the hardcoded yield-summaries fetch (line ~149):
   FROM:
   ```ts
   fetch(`http://localhost:3007/api/yield-summaries?cropYear=${CURRENT_CROP_YEAR}`, {
     next: { revalidate: 0 },
     signal: AbortSignal.timeout(5000),
   }).then((r) => r.json() as Promise<YieldSummary[]>),
   ```
   TO:
   ```ts
   fetchGrainService(`/api/yield-summaries?cropYear=${CURRENT_CROP_YEAR}`)
     .then((r) => r.json() as Promise<YieldSummary[]>),
   ```

   Note: `fetchGrainService` already sets `signal: AbortSignal.timeout(8000)`, `next: { revalidate: 0 }`, and the embed_session Cookie header via `defaultOptions` in proxy.ts. The 8s timeout (vs previous 5s) is consistent with the CBOT fetch on line 145 and is acceptable — grain-tickets responses are fast.

3. Do NOT modify the CBOT prices fetch (line 145) or the contracts Supabase query — those are already correct.
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -n 'localhost:3007' glomalin-portal/src/app/\(protected\)/app/marketing/page.tsx | wc -l | tr -d ' ' | grep '^0$'</automated>
    <manual>Confirm fetchGrainService is imported and used for yield-summaries fetch</manual>
  </verify>
  <done>
    - No `localhost:3007` references remain in marketing/page.tsx
    - fetchGrainService imported from proxy module
    - Yield-summaries fetch uses fetchGrainService with the same query parameter
    - CBOT and contracts fetches unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify TypeScript compilation and no other hardcoded localhost:3007 in portal</name>
  <files>glomalin-portal/src/app/(protected)/app/marketing/page.tsx</files>
  <action>
1. Run TypeScript type-check on the portal to confirm no import or type errors:
   ```bash
   cd glomalin-portal && npx tsc --noEmit --pretty 2>&1 | head -30
   ```
   The file should compile clean — fetchGrainService returns Promise<Response> and .json() returns the expected YieldSummary[] type.

2. Scan for any other hardcoded `localhost:3007` references in the portal src/ directory:
   ```bash
   grep -rn 'localhost:3007' glomalin-portal/src/
   ```
   Expected: zero results. If any found, replace with fetchGrainService following the same pattern.

3. Confirm proxy.ts GT_BASE is still `http://localhost:3007` (this is correct — the proxy centralizes the base URL so production can override via env var or reverse proxy config).
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Desktop/my-project-one && grep -rn 'localhost:3007' glomalin-portal/src/app/ glomalin-portal/src/components/ glomalin-portal/src/lib/ 2>/dev/null | grep -v '_lib/proxy.ts' | wc -l | tr -d ' ' | grep '^0$'</automated>
    <manual>TypeScript compiles without errors related to the changed file</manual>
  </verify>
  <done>
    - TypeScript compilation passes (or pre-existing errors only — no new errors from this change)
    - No hardcoded localhost:3007 in portal app/components/lib directories (proxy.ts is the single source of truth for GT_BASE)
  </done>
</task>

</tasks>

<verification>
1. `grep -n 'localhost:3007' glomalin-portal/src/app/(protected)/app/marketing/page.tsx` returns nothing
2. `grep -n 'fetchGrainService' glomalin-portal/src/app/(protected)/app/marketing/page.tsx` shows import and usage
3. No new TypeScript errors introduced
</verification>

<success_criteria>
- The marketing position page fetches yield summaries using fetchGrainService proxy — not hardcoded localhost
- Est. Production will show correct values in production (grain-tickets reachable via proxy.ts GT_BASE)
- Single source of truth for grain-tickets base URL remains in proxy.ts
</success_criteria>

<output>
After completion, create `.planning/phases/57.1-marketing-yield-summaries-production-fix/57.1-01-SUMMARY.md`
</output>
