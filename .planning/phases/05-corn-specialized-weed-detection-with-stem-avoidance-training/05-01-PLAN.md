---
phase: 05-corn-specialized-weed-detection-with-stem-avoidance-training
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Projects/owl-orin/training/train_corn.py
  - Projects/owl-orin/training/download_dataset.py
  - Projects/owl-orin/training/export_engine.py
  - Projects/owl-orin/training/README.md
autonomous: true
requirements: [CORN-01, CORN-07]

must_haves:
  truths:
    - "Training script runs on a GPU host and produces a corn_detector.pt weights file"
    - "Dataset download script fetches YOLOv8-formatted data from Roboflow and fixes data.yaml paths"
    - "TensorRT export script runs on Orin and produces a .engine file from .pt weights"
  artifacts:
    - path: "Projects/owl-orin/training/train_corn.py"
      provides: "YOLOv8n training with corn-specific augmentation"
      contains: "model.train"
    - path: "Projects/owl-orin/training/download_dataset.py"
      provides: "Roboflow dataset download with data.yaml path fix"
      contains: "rf.workspace"
    - path: "Projects/owl-orin/training/export_engine.py"
      provides: "TensorRT FP16 export for Orin"
      contains: "model.export"
  key_links:
    - from: "training/download_dataset.py"
      to: "training/train_corn.py"
      via: "data.yaml path produced by download, consumed by train"
      pattern: "data.yaml"
    - from: "training/train_corn.py"
      to: "training/export_engine.py"
      via: "best.pt output from training, input to TRT export"
      pattern: "corn_detector.pt"
---

<objective>
Create the model training pipeline: dataset download from Roboflow, YOLOv8n training script with corn-specific augmentation, and TensorRT FP16 export script for Orin deployment.

Purpose: These scripts form the repeatable pipeline for training and deploying corn detection models. They run on separate machines (GPU host for training, Orin for export) and produce the model artifacts consumed by the CornDetector module.

Output: Three scripts in `training/` directory plus a brief README documenting the workflow.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-corn-specialized-weed-detection-with-stem-avoidance-training/05-CONTEXT.md
@.planning/phases/05-corn-specialized-weed-detection-with-stem-avoidance-training/05-RESEARCH.md
@Projects/owl-orin/utils/greenongreen.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Roboflow dataset download and YOLOv8n training scripts</name>
  <files>
    Projects/owl-orin/training/download_dataset.py
    Projects/owl-orin/training/train_corn.py
  </files>
  <action>
Create `training/` directory in owl-orin.

**download_dataset.py:**
- Accept `--api-key` arg (or read from `ROBOFLOW_API_KEY` env var — never hardcode)
- Accept `--workspace`, `--project`, `--version` args with defaults for whughesfarms/corn-weed-detection/1
- Use `roboflow` SDK: `rf.workspace().project().version().download("yolov8")`
- After download, fix `data.yaml` paths to relative (see Pitfall 2 from RESEARCH.md): rewrite `train:`, `val:`, `test:` values to relative paths (`images/train`, `images/val`, `images/test`)
- Set `path: .` in data.yaml
- Verify the data.yaml has `nc: 2` and `names: [corn, weed]` (class 0 = corn per locked decision)
- Output directory: `training/dataset/`

**train_corn.py:**
- Accept `--data` arg (default: `training/dataset/data.yaml`)
- Accept `--epochs` (default: 100), `--batch` (default: 16), `--imgsz` (default: 640)
- Accept `--device` (default: 0 for CUDA GPU)
- Load `yolov8n.pt` pretrained weights via `YOLO("yolov8n.pt")`
- Call `model.train()` with these augmentation overrides per RESEARCH.md discretion recommendations:
  - `hsv_h=0.015, hsv_s=0.7, hsv_v=0.5` (hsv_v increased from default 0.4 for dawn/dusk)
  - `degrees=10.0` (pitch variation from row mount)
  - `scale=0.5, fliplr=0.5, flipud=0.0` (NO vertical flip — sky/ground orientation matters)
  - `mosaic=1.0, mixup=0.1, blur=0.01`
  - `project="runs/corn_detect"`, `name="yolov8n_corn_v1"`
- After training, copy `runs/corn_detect/yolov8n_corn_v1/weights/best.pt` to `models/corn_detector.pt`
- Print summary: best mAP50, best mAP50-95, path to weights file

Both scripts: add `if __name__ == "__main__":` with argparse. Include docstring explaining which machine to run on (GPU host, NOT Orin).
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Projects/owl-orin && python -c "import ast; ast.parse(open('training/train_corn.py').read()); ast.parse(open('training/download_dataset.py').read()); print('syntax ok')"</automated>
    <manual>Review that train_corn.py uses augmentation overrides matching RESEARCH.md recommendations</manual>
  </verify>
  <done>
    - download_dataset.py parses args, calls Roboflow SDK, fixes data.yaml paths
    - train_corn.py loads yolov8n.pt, trains with corn-specific augmentation params, copies best.pt to models/
    - Both scripts have argparse CLI and docstrings stating which machine they run on
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TensorRT export script and training README</name>
  <files>
    Projects/owl-orin/training/export_engine.py
    Projects/owl-orin/training/README.md
  </files>
  <action>
**export_engine.py:**
- Accept `--model` arg (default: `models/corn_detector.pt`)
- Accept `--imgsz` (default: 640)
- Accept `--device` (default: 0)
- Load model via `YOLO(model_path)`
- Call `model.export(format="engine", half=True, imgsz=imgsz, device=device)` — FP16 per RESEARCH.md recommendation (NOT INT8, safer for crop protection)
- Print output path of generated `.engine` file
- Include docstring: "Run this script ON the Jetson Orin. TensorRT engines are device-specific and cannot be cross-compiled."
- Add `if __name__ == "__main__":` with argparse

**training/README.md:**
- Document the 3-step workflow:
  1. Download dataset (GPU host): `python training/download_dataset.py --api-key $ROBOFLOW_API_KEY`
  2. Train model (GPU host): `python training/train_corn.py --epochs 100`
  3. Export to TensorRT (ON ORIN): copy `models/corn_detector.pt` to Orin, then `python training/export_engine.py`
- Note: `.engine` files are device-specific, never copy between machines
- Note: Add `*.engine` to `.gitignore`

**Also update `.gitignore`** in owl-orin root (create if not exists):
- Add `*.engine` entry
- Add `training/dataset/` entry (downloaded data, large)
- Add `runs/` entry (training output, large)
  </action>
  <verify>
    <automated>cd /Users/glomalinguild/Projects/owl-orin && python -c "import ast; ast.parse(open('training/export_engine.py').read()); print('syntax ok')" && test -f training/README.md && echo "README exists"</automated>
    <manual>Verify export_engine.py uses half=True (FP16) not INT8</manual>
  </verify>
  <done>
    - export_engine.py exports .pt to TensorRT .engine with FP16 on Orin
    - README documents the full 3-step training-to-deployment workflow
    - .gitignore excludes .engine files, dataset downloads, and training runs
  </done>
</task>

</tasks>

<verification>
- All three training scripts parse without syntax errors
- Each script has argparse CLI and can show --help
- export_engine.py uses half=True (FP16), not INT8
- .gitignore includes *.engine, training/dataset/, runs/
</verification>

<success_criteria>
- Training pipeline is a 3-script workflow: download -> train -> export
- Scripts are self-documenting with argparse and docstrings
- No hardcoded API keys or absolute paths
- Augmentation parameters match RESEARCH.md discretion recommendations
- TRT export is FP16 per safety-critical crop protection rationale
</success_criteria>

<output>
After completion, create `.planning/phases/05-corn-specialized-weed-detection-with-stem-avoidance-training/05-01-SUMMARY.md`
</output>
