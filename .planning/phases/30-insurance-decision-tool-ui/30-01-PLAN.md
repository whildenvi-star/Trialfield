---
phase: 30-insurance-decision-tool-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - glomalin-portal/scripts/migrate-30.ts
  - glomalin-portal/src/app/api/insurance/policies/route.ts
  - glomalin-portal/src/app/api/insurance/policies/[id]/route.ts
  - glomalin-portal/src/app/(protected)/app/insurance/page.tsx
  - glomalin-portal/src/components/insurance/insurance-workspace.tsx
  - glomalin-portal/src/components/insurance/policy-card.tsx
  - glomalin-portal/src/components/insurance/policy-drawer.tsx
  - glomalin-portal/src/components/insurance/coverage-matrix.tsx
  - glomalin-portal/src/lib/fsa/calc.ts
autonomous: true
requirements: [INS-02, INS-03]

must_haves:
  truths:
    - "User can create a new insurance policy via the slide-out drawer and see it appear in the policy list"
    - "User can edit an existing policy's fields (farm, crop, plan type, coverage, guarantee, actual, acres) and save changes"
    - "User can delete a policy with confirmation and see it removed from the list"
    - "User can select a policy and see a coverage matrix comparing RP, RP-HPE, and YP at 50-85% coverage levels with heat-map coloring"
  artifacts:
    - path: "glomalin-portal/scripts/migrate-30.ts"
      provides: "plan_type column addition to insurance_policies"
      contains: "ALTER TABLE insurance_policies ADD COLUMN IF NOT EXISTS plan_type"
    - path: "glomalin-portal/src/app/api/insurance/policies/route.ts"
      provides: "POST handler for policy creation"
      exports: ["GET", "POST"]
    - path: "glomalin-portal/src/app/api/insurance/policies/[id]/route.ts"
      provides: "DELETE handler for policy removal"
      exports: ["GET", "PATCH", "DELETE"]
    - path: "glomalin-portal/src/components/insurance/insurance-workspace.tsx"
      provides: "Client orchestrator owning policy state and CRUD operations"
      contains: "use client"
    - path: "glomalin-portal/src/components/insurance/policy-drawer.tsx"
      provides: "Slide-out right panel for create/edit"
      contains: "translate-x"
    - path: "glomalin-portal/src/components/insurance/coverage-matrix.tsx"
      provides: "8x3 CSS grid with heat-map coloring"
      contains: "computeInsurancePolicy"
  key_links:
    - from: "glomalin-portal/src/components/insurance/insurance-workspace.tsx"
      to: "/api/insurance/policies"
      via: "fetch POST/PATCH/DELETE for CRUD"
      pattern: "fetch.*api/insurance/policies"
    - from: "glomalin-portal/src/components/insurance/coverage-matrix.tsx"
      to: "glomalin-portal/src/lib/fsa/calc.ts"
      via: "computeInsurancePolicy() called 24 times client-side"
      pattern: "computeInsurancePolicy"
    - from: "glomalin-portal/src/app/(protected)/app/insurance/page.tsx"
      to: "glomalin-portal/src/components/insurance/insurance-workspace.tsx"
      via: "Server component passes initialPolicies and initialPricing as props"
      pattern: "InsuranceWorkspace"
---

<objective>
Build the insurance policy CRUD workflow and coverage comparison matrix. Adds the plan_type schema column, POST/DELETE API routes, then replaces the static insurance page with an interactive InsuranceWorkspace that supports policy create/edit/delete via a slide-out drawer, a selectable policy table, and a CSS grid coverage matrix showing RP/RP-HPE/YP at 8 coverage levels with heat-map coloring.

Purpose: Enables INS-02 (policy CRUD with slide-out editor) and INS-03 (coverage level comparison matrix) — the two primary interactive features of the insurance decision tool.
Output: migrate-30.ts, POST + DELETE API routes, InsuranceWorkspace, PolicyCard row, PolicyDrawer, CoverageMatrix components, refactored page.tsx as thin server wrapper.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-insurance-decision-tool-ui/30-RESEARCH.md
@.planning/phases/29-insurance-tables-calculation-engine/29-01-SUMMARY.md
@.planning/phases/29-insurance-tables-calculation-engine/29-02-SUMMARY.md
@.planning/phases/28-fsa-planting-workflow-ui/28-02-SUMMARY.md
@glomalin-portal/src/app/(protected)/app/insurance/page.tsx
@glomalin-portal/src/app/api/insurance/policies/route.ts
@glomalin-portal/src/app/api/insurance/policies/[id]/route.ts
@glomalin-portal/src/lib/fsa/calc.ts
@glomalin-portal/src/components/fsa/clu-workspace.tsx
@glomalin-portal/src/components/fsa/confirm-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration + POST/DELETE API routes + InsurancePolicy type update</name>
  <files>
    glomalin-portal/scripts/migrate-30.ts
    glomalin-portal/src/app/api/insurance/policies/route.ts
    glomalin-portal/src/app/api/insurance/policies/[id]/route.ts
    glomalin-portal/src/lib/fsa/calc.ts
  </files>
  <action>
    1. Create `glomalin-portal/scripts/migrate-30.ts` following the exact pattern from migrate-29.ts:
       - Use `createClient` from `@supabase/supabase-js` with `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`
       - Run: `ALTER TABLE insurance_policies ADD COLUMN IF NOT EXISTS plan_type TEXT;`
       - Console.log the raw SQL for manual execution in Supabase SQL editor (same pattern as migrate-29.ts)
       - Print success message

    2. Update the `InsurancePolicy` interface in `glomalin-portal/src/lib/fsa/calc.ts`:
       - Add `plan_type?: string | null` field after `premium_per_acre`
       - Add `agent_name?: string | null` field if not present
       - Add `notes?: string | null` field if not present
       - Add Phase 29 fields if not present in this interface: `aph_computed`, `aph_clu_count`, `actual_synced_from_grain`, `claim_alert`
       - These fields are optional (?) since they may not exist on all records and the calc engine doesn't need them

    3. Add POST handler to `glomalin-portal/src/app/api/insurance/policies/route.ts` (keep existing GET):
       - Auth check (same pattern as GET)
       - Parse JSON body with try/catch
       - Validate planted_acres is a positive number (required)
       - Build insert object with all policy fields, defaulting: policy_year=2026, guarantee=0, actual=0, coverage_level=75, claim_alert='none', actual_synced_from_grain=false, prevented_planting=false
       - Generate synthetic legacy_id: `ins_manual_${Date.now()}`
       - Insert via supabase.from('insurance_policies').insert().select().single()
       - Return 201 with { policy }

    4. Add DELETE handler to `glomalin-portal/src/app/api/insurance/policies/[id]/route.ts` (keep existing GET + PATCH):
       - Auth check (same pattern)
       - Await params (Next.js 15+ async params pattern): `const { id } = await params`
       - Delete via supabase.from('insurance_policies').delete().eq('id', id)
       - Return { deleted: id }

    5. Expand the PATCH handler's PolicyPatch interface to accept all editable fields:
       - Add: farm_name, farm_number, crop, planted_acres, unit_type, premium_per_acre, agent_name, plan_type, notes, prevented_planting, prevented_planting_acres
       - Extract each from body with type checking (same pattern as existing fields)
       - Keep existing claim_alert recompute logic untouched
  </action>
  <verify>
    <automated>cd glomalin-portal && npx tsc --noEmit 2>&1 | tail -5</automated>
    <manual>Verify migrate-30.ts exists with ALTER TABLE, POST in policies/route.ts returns 201, DELETE in [id]/route.ts returns JSON</manual>
  </verify>
  <done>POST /api/insurance/policies creates a policy and returns 201. DELETE /api/insurance/policies/[id] removes a policy. PATCH accepts all editable fields. InsurancePolicy type in calc.ts includes plan_type. migrate-30.ts prints ALTER TABLE SQL.</done>
</task>

<task type="auto">
  <name>Task 2: InsuranceWorkspace + PolicyDrawer + CoverageMatrix + refactored page.tsx</name>
  <files>
    glomalin-portal/src/app/(protected)/app/insurance/page.tsx
    glomalin-portal/src/components/insurance/insurance-workspace.tsx
    glomalin-portal/src/components/insurance/policy-drawer.tsx
    glomalin-portal/src/components/insurance/coverage-matrix.tsx
  </files>
  <action>
    1. **Refactor page.tsx** to thin server component:
       - Keep as async Server Component
       - Fetch both `insurance_policies` (year=2026, order by farm_name) and `insurance_pricing` (year=2026) via Promise.all
       - Import and render `InsuranceWorkspace` passing `initialPolicies` and `initialPricing` as props
       - Remove all existing stat cards, table, and inline UI (moves to InsuranceWorkspace)

    2. **Create `insurance-workspace.tsx`** — the client orchestrator ('use client'):
       - Props: `initialPolicies: InsurancePolicy[]`, `initialPricing: PricingEntry[]`
       - Local state: `policies` (initialized from props), `selectedPolicyId`, `drawerOpen`, `drawerMode` ('create' | 'edit'), `editingPolicy`
       - Layout structure (all in one scrollable page, not tabs):
         a. **Page header row**: "Crop Insurance" title + "Add Policy" button (top right) + placeholder div for PDF button (Plan 30-02 will wire it)
         b. **Stat cards row**: 3 cards — Policies count, Crops Insured (unique crop set size), Claim Alerts (policies with claim_alert='potential'). Reuse exact styling from current page.tsx
         c. **Disclaimer banner**: "This is a decision-support tool..." italic text (same as current page.tsx)
         d. **Policy table**: Compact table with columns: Farm, Crop, Plan Type, Coverage %, Guarantee (bu/ac), Actual (bu/ac), Alert, Actions (Edit/Delete). Each row is clickable to select. Selected row gets highlighted border-l-2 border-soil-accent. VERIFY rows keep orange styling.
         e. **Coverage Matrix section**: Shown only when a policy is selected. Header "Coverage Comparison" with selected policy name. Renders CoverageMatrix component.
       - CRUD handlers:
         - `handleCreate(formData)`: POST to /api/insurance/policies, append to policies state, close drawer
         - `handleUpdate(id, formData)`: PATCH to /api/insurance/policies/[id], update in policies state, close drawer
         - `handleDelete(id)`: Show browser confirm() ("Delete policy for {farm} / {crop}?"), DELETE to /api/insurance/policies/[id], remove from policies state, clear selection if deleted policy was selected
       - Import types from `@/lib/fsa/calc` (InsurancePolicy, PricingEntry)

    3. **Create `policy-drawer.tsx`** — slide-out right panel ('use client'):
       - Props: `open`, `policy` (null = create mode), `onClose`, `onSave(data)`
       - Fixed position right panel, 480px wide, z-50, bg-soil-surface, border-l border-soil-border
       - Backdrop: fixed inset-0 z-40 bg-black/40, onClick closes
       - Slide animation: `translate-x-full` (closed) → `translate-x-0` (open) with `transition-transform duration-200`
       - Header: "New Policy" or "Edit Policy" with X close button
       - Form fields (grouped into two sections):
         **Policy Details:**
         - Farm Name (text input)
         - Farm Number (text input)
         - Crop (text input)
         - Plan Type (select: RP, RP-HPE, YP, or empty — with hint "RP is most common")
         - Coverage Level (select: 50, 55, 60, 65, 70, 75, 80, 85 — default 75)
         - Unit Type (text input)
         **Acres & Yields:**
         - Planted Acres (number input, required)
         - Guarantee bu/ac (number input)
         - Actual bu/ac (number input)
         - Premium per Acre (number input)
         **Other:**
         - Agent Name (text input)
         - Notes (textarea)
       - All inputs use soil design tokens: bg-soil-bg border-soil-border text-soil-text focus:border-soil-accent font-mono text-sm
       - Submit button: bg-soil-accent text-soil-bg font-bold — "Create Policy" or "Save Changes"
       - In edit mode, pre-fill all fields from policy prop
       - On submit, call onSave with form data object

    4. **Create `coverage-matrix.tsx`** — CSS grid heat-map ('use client'):
       - Props: `policy: InsurancePolicy`, `pricing: PricingEntry[]`
       - Constants: `COVERAGE_LEVELS = [50, 55, 60, 65, 70, 75, 80, 85]`, `PLAN_TYPES = ['RP', 'RP-HPE', 'YP'] as const`
       - Helper function `computeCell(policy, pricing, coverage, planType)`:
         - For RP: pass pricing as-is (computeInsurancePolicy uses max(spring, fall))
         - For RP-HPE and YP: create adjusted pricing with `fall_price = spring_price` (excludes harvest price premium — simplification noted in code comment)
         - Call `computeInsurancePolicy({ ...policy, coverage_level: coverage }, adjustedPricing)`
       - Compute all 24 cells via nested map (COVERAGE_LEVELS × PLAN_TYPES)
       - Find max indemnity across all cells for heat-map normalization (floor at 1 to avoid div/0)
       - Render CSS grid with `grid-cols-4` (label column + 3 plan type columns), `gap-px bg-soil-border`
       - Header row: "Coverage" label + RP / RP-HPE / YP column headers in soil-accent
       - Data rows: coverage level label (e.g., "75%") + 3 cells
       - Cell content: `$X,XXX` for indemnity > 0, "—" for zero
       - Heat-map coloring: `rgba(200, 134, 10, 0.1 + intensity * 0.5)` where intensity = cell.indemnity / maxIndemnity (soil-accent amber tint, higher indemnity = more opaque)
       - Zero indemnity cells: no background override (uses default soil-surface via alternating row)
       - Show the policy's current coverage level row with a subtle left border highlight (border-l-2 border-soil-accent)
       - If policy has no plan_type set: show a note above the matrix "Select a plan type via Edit to highlight the active column"
       - Wrap computations in useMemo keyed on [policy, pricing] for performance (though 24 arithmetic operations are trivial)
       - Add comment explaining RP-HPE/YP simplification: "RP-HPE excludes harvest price increase from guarantee; YP is yield-only. Both use spring_price for decision-support comparison. Results are illustrative — verify with your agent."
  </action>
  <verify>
    <automated>cd glomalin-portal && npx tsc --noEmit 2>&1 | tail -5 && npx next build 2>&1 | grep -E "(error|Error|✓|Route)" | tail -20</automated>
    <manual>Visit /app/insurance — see policy table, click Add Policy to see drawer slide in, select a policy to see coverage matrix</manual>
  </verify>
  <done>Insurance page renders InsuranceWorkspace with interactive policy table. Add Policy opens PolicyDrawer slide-out. Edit/Delete work via API. Selecting a policy shows CoverageMatrix with 24 heat-mapped cells comparing RP/RP-HPE/YP at 50-85% coverage. All TypeScript compiles, Next.js builds.</done>
</task>

</tasks>

<verification>
1. `cd glomalin-portal && npx tsc --noEmit` — zero TypeScript errors
2. `cd glomalin-portal && npx next build` — build succeeds, all insurance routes appear
3. Insurance page at /app/insurance shows policy table with stat cards
4. "Add Policy" button opens slide-out drawer with form fields including Plan Type
5. Saving a new policy via drawer adds it to the table
6. Edit button on a policy row opens drawer pre-filled with policy data
7. Delete button removes a policy after confirmation
8. Clicking a policy row selects it and shows CoverageMatrix below
9. Coverage matrix shows 8 rows × 3 columns with heat-map coloring on indemnity values
</verification>

<success_criteria>
- POST /api/insurance/policies creates a policy (201 response)
- DELETE /api/insurance/policies/[id] removes a policy
- PATCH /api/insurance/policies/[id] accepts all editable fields including plan_type
- InsuranceWorkspace manages policy list state and CRUD operations
- PolicyDrawer slides in from right with form fields for create/edit
- CoverageMatrix renders 24 cells (8 coverage levels × 3 plan types) with heat-map coloring
- All components use soil design tokens consistently
- TypeScript strict mode passes, Next.js build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/30-insurance-decision-tool-ui/30-01-SUMMARY.md`
</output>
