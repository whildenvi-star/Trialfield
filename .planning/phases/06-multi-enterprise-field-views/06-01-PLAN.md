---
phase: 06-multi-enterprise-field-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - organic-cert/src/app/(app)/fields/page.tsx
  - organic-cert/src/app/(app)/fields/[id]/history/page.tsx
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-04]

must_haves:
  truths:
    - "Field index cards show enterprise count badge (e.g., '3 enterprises') when a field has 2+ enterprises for the current crop year"
    - "Field index cards show acre utilization (e.g., '120.0 of 160.0 ac') instead of bare totalAcres when acreUtilization is non-null"
    - "Field index cards show over-allocated warning badge when acreUtilization.isOverAllocated is true"
    - "Field history season cards display multiple enterprise rows when a field has 2+ enterprises for that crop year"
    - "Single-enterprise season cards render exactly as before (backward compatible -- no visual regression)"
    - "Each enterprise row in a multi-enterprise season card shows crop, label, acres, operation count, organic status, and a drill-down link to /field-enterprises/[id]"
    - "Field history page has a 'New Enterprise' link that navigates to the enterprise creation page with the field pre-selected"
  artifacts:
    - path: "organic-cert/src/app/(app)/fields/page.tsx"
      provides: "Enterprise count badge and acre utilization display on field cards"
      contains: "acreUtilization"
    - path: "organic-cert/src/app/(app)/fields/[id]/history/page.tsx"
      provides: "Multi-enterprise season cards with enterprise rows and drill-down links"
      contains: "enterprisesByYear"
  key_links:
    - from: "organic-cert/src/app/(app)/fields/page.tsx"
      to: "GET /api/fields"
      via: "Field interface consuming acreUtilization from API response"
      pattern: "acreUtilization"
    - from: "organic-cert/src/app/(app)/fields/[id]/history/page.tsx"
      to: "/field-enterprises/[id]"
      via: "drill-down link from EnterpriseRow onClick to enterprise detail page"
      pattern: "field-enterprises"
    - from: "organic-cert/src/app/(app)/fields/[id]/history/page.tsx"
      to: "enterprisesByYear Map"
      via: "Map<number, Enterprise[]> replacing Map<number, Enterprise>"
      pattern: "enterprisesByYear"
---

<objective>
Add enterprise count badges and acre utilization to field index cards, refactor field history season cards to display multiple enterprise rows per year with drill-down navigation.

Purpose: Users need to see split-field status at a glance on the field index and navigate multi-enterprise seasons on the history page -- the core consolidated view for Phase 6.
Output: Updated field index cards with enterprise badges, multi-enterprise season cards with enterprise rows and drill-down links.
</objective>

<execution_context>
@/Users/glomalinguild/.claude/get-shit-done/workflows/execute-plan.md
@/Users/glomalinguild/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-multi-enterprise-field-views/06-RESEARCH.md
@.planning/phases/05-split-field-schema-acre-reconciliation/05-01-SUMMARY.md
@.planning/phases/05-split-field-schema-acre-reconciliation/05-02-SUMMARY.md
@organic-cert/src/app/(app)/fields/page.tsx
@organic-cert/src/app/(app)/fields/[id]/history/page.tsx
@organic-cert/src/app/api/fields/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add enterprise count badge and acre utilization to field index cards</name>
  <files>organic-cert/src/app/(app)/fields/page.tsx</files>
  <action>
Modify the Field interface (approx line 37-49) to include the Phase 5 fields that the API already returns:

```typescript
acreUtilization?: {
  planted: number;
  total: number;
  fallow: number;
  isOverAllocated: boolean;
} | null;
```

Also ensure the interface includes enterprise count access. The API already returns `enterprises` array and/or `_count` -- check what the GET /api/fields route actually returns and match the interface accordingly.

In the field card rendering (approx line 297-365), make these changes:

1. **Acre utilization display:** When `f.acreUtilization` is non-null, show "X.X of Y.Y ac" instead of "Z.Z acres". When null (single enterprise), show the existing totalAcres display unchanged.

2. **Enterprise count badge:** When the field has more than 1 enterprise for the current crop year, add a Badge with variant="outline" showing "N enterprises". Use Sprout icon from lucide-react (size 10) inline. Only show this badge when count > 1 -- single-enterprise fields show no badge.

3. **Over-allocated warning:** When `acreUtilization?.isOverAllocated` is true, add a Badge with yellow styling: `className="text-xs text-yellow-700 border-yellow-300 bg-yellow-50"` showing "Over-allocated".

Use existing Badge component from `@/components/ui/badge`. Import Sprout from lucide-react if not already imported.

Do NOT change the sort/search/filter logic, card layout structure, or Link navigation. Only ADD the badges and utilization display within the existing card structure.
  </action>
  <verify>
    <automated>cd organic-cert && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit /fields -- field cards should show enterprise count badge and acre utilization for multi-enterprise fields; single-enterprise fields should look unchanged</manual>
  </verify>
  <done>Field index cards show enterprise count badge when 2+ enterprises exist, acre utilization replaces bare totalAcres when acreUtilization is non-null, and over-allocated warning badge appears when isOverAllocated is true. Single-enterprise fields render unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor history page for multi-enterprise season cards with enterprise rows</name>
  <files>organic-cert/src/app/(app)/fields/[id]/history/page.tsx</files>
  <action>
This is the critical task for Phase 6. The existing history page has a one-enterprise-per-year assumption that must be changed.

**Step 1: Update Enterprise interface (approx line 116-127)**

Add the Phase 5 fields to the Enterprise interface:
```typescript
label: string | null;
isFallow: boolean;
fallowCostAmount: number | null;
fallowCostCategory: string | null;
```

**Step 2: Refactor enterpriseByYear to enterprisesByYear (approx line 1880-1887)**

Change from `Map<number, Enterprise>` to `Map<number, Enterprise[]>`:

```typescript
const enterprisesByYear = useMemo(() => {
  const map = new Map<number, Enterprise[]>();
  for (const e of data.field.enterprises) {
    const existing = map.get(e.cropYear) || [];
    existing.push(e);
    map.set(e.cropYear, existing);
  }
  return map;
}, [data]);
```

Rename ALL references from `enterpriseByYear` to `enterprisesByYear` throughout the file. Every `.get(year)` call now returns `Enterprise[] | undefined` instead of `Enterprise | undefined`.

**Step 3: Update season card rendering (approx line 2110-2199)**

Replace the single-enterprise season card rendering with branching logic:

1. Get `const yearEnterprises = enterprisesByYear.get(year) || [];`
2. If `yearEnterprises.length === 0`: render the existing empty season card (link to create enterprise)
3. If `yearEnterprises.length === 1`: render EXACTLY as today -- same single-enterprise card with timeline, operation details, etc. Extract `yearEnterprises[0]` and use it where the old `enterprise` variable was used.
4. If `yearEnterprises.length > 1`: render a CONSOLIDATED season card:
   - Card header: "Growing Season {year}" with subtitle showing enterprise count and total planted acres (sum of all enterprise plantedAcres) vs field totalAcres
   - Card content: iterate over `yearEnterprises` rendering an `EnterpriseRow` for each

**Step 4: Create EnterpriseRow inline component**

Define within the same file (not a separate component file -- this is specific to the history page):

```tsx
function EnterpriseRow({ enterprise, onDrillDown }: { enterprise: Enterprise; onDrillDown: () => void }) {
```

Each row shows:
- Crop name (or "Fallow" if `isFallow`), with label in parentheses if present: `"Corn (North 40)"`
- Planted acres, operation count (use existing `buildTimelineItems()` for count), variety if present
- Organic status badge with existing status coloring
- ChevronRight icon indicating drill-down
- `onClick={onDrillDown}` navigates to `/field-enterprises/${enterprise.id}`
- Styling: `p-3 border border-stone-200 rounded-lg hover:border-[#2d5a27]/30 cursor-pointer transition-colors`

For fallow enterprise rows, use muted styling: italic text, "Fallow" as crop name, stone-400 text color.

**Step 5: Add "New Enterprise" link**

Add a small link/button near the season cards (e.g., below the year header or at the top of the page) that navigates to `/field-enterprises` page. If possible, pre-populate the field selection via query parameter: `/field-enterprises?fieldId={fieldId}`. This satisfies the part of VIEW-05 that says "without leaving the page" by providing a quick navigation shortcut from the history view.

**Step 6: Fix all references to the old enterpriseByYear**

Search the entire file for `enterpriseByYear` and update every reference:
- Where it was used to get a single enterprise: now get the array and either use `[0]` (for single-enterprise paths) or iterate
- The `preselectedEnterpriseId` logic for the Add Record form -- in multi-enterprise context, each enterprise row's "Add Record" button should pass that specific enterprise ID
- Any timeline building that used the singular enterprise -- adapt to handle the array

Import ChevronRight from lucide-react if not already imported.
  </action>
  <verify>
    <automated>cd organic-cert && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Visit /fields/{id}/history for a field with multiple enterprises in the same year -- should see consolidated season card with multiple enterprise rows. For a field with single enterprise per year, should look unchanged.</manual>
  </verify>
  <done>Field history page shows multi-enterprise season cards with enterprise rows for split fields, single-enterprise seasons render as before, enterprise rows link to drill-down detail page, "New Enterprise" link available on history page.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd organic-cert && npx tsc --noEmit` passes without errors related to fields/page.tsx or fields/[id]/history/page.tsx
2. Field index page: cards for multi-enterprise fields show enterprise count badge and acre utilization
3. Field index page: cards for single-enterprise fields show no enterprise badge and show totalAcres as before
4. Field history page: season with 1 enterprise renders same as before (backward compatible)
5. Field history page: season with 2+ enterprises shows consolidated card with enterprise rows
6. Enterprise rows show crop, label, acres, operation count, status, and ChevronRight icon
7. Clicking an enterprise row navigates to /field-enterprises/{id}
8. "New Enterprise" link is visible on the history page
</verification>

<success_criteria>
- Field index cards display enterprise count badge and acre utilization for multi-enterprise fields
- Field history season cards display multiple enterprise rows when 2+ enterprises exist for that year
- Single-enterprise fields and seasons render unchanged (no visual regression)
- Enterprise rows in multi-enterprise season cards link to /field-enterprises/[id] for drill-down
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-multi-enterprise-field-views/06-01-SUMMARY.md`
</output>
